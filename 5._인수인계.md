# 인수인계서 — 메모 편집 개편 (2026-09-08 기준)

다른 대화창에서 이어서 일할 사람을 위한 문서다. 이 문서 하나만 읽고도 바로 손댈 수 있게 썼다.
자세한 경위는 `2._WORK_LOG.md`, 남은 목록의 원본은 `3._TODO.md` 에 있다.

---

## 1. 지금 상태

TomaDesk (PyQt6, 한국어 데스크톱 앱). 작업 폴더는

```
D:\251210-백업\백업_251121\폴더 모음\이현주\155._codex\35_단축키 프로그램
```

메모 편집 탭 개편이 **1~8단계까지 모두 끝났다.** 2026-09-08 일정·D-Day
추가 업데이트도 **1~5단계를 모두 완료**했고, 현재 전체 검사는 **660개 통과·실패 0개**다.
아래 620/621개 내용은 일정 업데이트 전의 과거 기준선으로만 본다. 기존 기록상 검사는
**27개 파일 621개가 전부 통과**했다. 다만 2026-09-08 새 대화에서 다시 돌렸을 때는
**620개 통과, 실패 0개, 1개는 테스트 격리 문제로 실행 완료하지 못했다.** 자세한 내용은
아래 `1-1. 새 대화에서 다시 확인한 현재 상태`에 적었다.

최근에 끝낸 것만 추리면:

- 목록을 접히는 트리로, 메모 안에 페이지, `/` 삽입 메뉴, 강조 상자·구분선, 줄 끌어 옮기기
- 메모 링크, 그림 크기 끌어 조절, F11 전체 화면, 목록 접기(`Ctrl+\`)
- 제목은 위에 두고 서식 도구를 오른쪽 아래로 (편집창이 900px 이상일 때 두 칸)
- 넣을 수 있는 열 가지 모두에 단축키 + 줄 앞 입력, ＋ 메뉴·`/` 메뉴 설명 띄우기
- 빠르기(바뀐 자리만 다시 보기), 최근 본 메모 칩, 본문 찾기, 인용문·코드 줄, 표

결과 페이지 두 개:

- 배치 변경 — https://claude.ai/code/artifact/9d9ff8a4-1baf-40f7-8d55-2ac2d309b881
- 명령어와 새 블록 — https://claude.ai/code/artifact/34658e4a-d5eb-469b-8fa4-211ecff7053e

### 1-1. 새 대화에서 다시 확인한 현재 상태 (2026-09-08)

폴더 전체 파일 목록, 현재 관리 문서, 핵심 메모 코드와 27개 테스트 파일을 다시 확인했다.
이 절은 다음 대화에서 같은 조사를 반복하지 않도록 남긴 최신 기준선이다.

#### 현재 기준 문서

- 앞으로의 계획: 루트 `1._PROJECT_PLAN.md`
- 실제 처리 기록: 루트 `2._WORK_LOG.md`
- 남은 작업: 루트 `3._TODO.md`
- 이 문서: 새 대화용 시작점
- 이전 기록이던 `PROJECT_PLAN.md`·`docs/WORK_LOG.md`·`docs/TODO.md`·`docs/DECISIONS.md`·`docs/PROJECT_INDEX.md`는 2026-09-18에 지웠다. 내용은 루트의 번호 문서로 옮겨졌고 예전 내용이 필요하면 git 이력에서 본다.
- `.git` 폴더는 비어 있고 현재 경로는 Git 저장소로 인식되지 않는다. `git diff`에 기대지 말고
  수정 전후 파일 직접 비교, `rg`, 테스트와 렌더 결과로 변경 범위를 확인한다.

#### 실제 코드와 맞는 부분

- 실행 진입점은 `A_shortcut_launcher.py`, 메인 UI는 `main_window.py`다.
- 메모 편집 중심은 `alert_notes/rich_memo_edit.py`이고 코드 지도에 적은 클래스·상수·메서드가
  실제로 존재한다.
- `MemoEditor.TWO_COLUMN_WIDTH=900`, `BOTTOM_LEFT_WIDTH=440`,
  `ACTIONS_ROW_WIDTH=605`, `RichMemoTextEdit.VISIBLE_MARGIN=40`,
  `MemoListPanel.RECENT_CHIPS=3`, `MemoFindBar.TYPING_DELAY=120`을 확인했다.
- `move_line`, 표 넣기·줄/칸 추가, 메모 링크, 페이지 제목 동기화, 그림 크기 조절도 존재한다.
- `alert_notes/insert_menu.py`의 `INSERT_ITEMS`가 ＋ 메뉴·`/` 메뉴·단축키의 단일 목록이라는
  설명과 실제 구현이 일치한다.

#### 문서에서 바로잡아야 할 부분

1. `1._PROJECT_PLAN.md`는 마지막 수정일이 2026-08-31이라 9월의 메모 개편 1~8단계가
   반영되지 않았다. 다음 개발 계획을 세울 때 최신 상태로 갱신해야 한다.
2. `2._WORK_LOG.md`에는 이 문서에 적힌 `2026-09-08 / 621개 통과` 재검사 결과가 없다.
   다음 작업 기록을 쓸 때 아래의 최신 검사 결과도 함께 정리한다.
3. `3._TODO.md` 27번은 빠르기와 Ctrl+F를 미완료로 묶고 있지만, 같은 문서 34번과
   36번에서는 둘 다 완료로 표시한다. 27번은 오래된 묶음 항목이므로 정리해야 한다.
4. 그림 용량 줄이기는 완전 미구현이 아니다. 현재 `insert_image`는 긴 변을
   `MAX_IMAGE_EDGE`로 줄이고 PNG 또는 JPEG로 인코딩하며 8MB 상한도 검사한다.
   남은 것은 **PNG와 JPEG를 둘 다 만들어 더 작은 쪽을 선택하는 보완**이다.
5. 큰 모듈이 많다. `main_window.py`는 약 3,400줄, `rich_memo_edit.py`는 약 2,100줄이다.
   새 기능을 넣을 때는 관련 부분만 좁혀 읽고, 가능하면 새 책임을 작은 모듈로 분리한다.

#### 최신 자동 검사 결과

2026-09-08에 27개 `test_*.py`를 Qt 안정성을 위해 파일별 독립 프로세스로 다시 실행했다.

- 정상 통과: **620개**
- 실패: **0개**
- 실행 완료하지 못한 검사: **1개**
- 해당 검사:
  `test_settings_dday_ux.py::SettingsWindowTest::test_saving_applies_the_choice_without_waiting`

이 한 검사는 기능 실패가 확인된 것이 아니라 **테스트 격리 문제**다. 테스트의 임시 DB를 쓰면서도
`save_storage_paths()`를 가짜로 바꾸지 않아 실제
`%LOCALAPPDATA%\CodexShortcutLauncher\storage_paths.json`에 임시 데이터 폴더 경로를 쓰려고 한다.
보호 환경에서는 이 쓰기가 `OSError`로 막히고, 뒤이어 뜨는 `QMessageBox.warning`이 사용자 입력을
기다려 검사가 멈춘다. `faulthandler_timeout=10`으로 확인한 정지 위치는
`main_window.py`의 `_apply_settings()` 안 `save_storage_paths()` 예외 처리 부분이었다.

**중요:** 621개 숫자를 맞추려고 보호를 풀어 이 검사를 그대로 실행하지 말 것. 실제 사용자
`storage_paths.json`이 삭제될 임시 폴더를 가리키게 될 수 있다. 먼저 테스트에서
`main_window.save_storage_paths`를 임시 파일용 가짜 함수로 패치한 뒤 실행해야 한다.
이 테스트 보완은 소스 기능 수정과 분리해 계획에 넣고 승인받는다.

---

## 2. 일하는 방식 (이게 제일 중요하다)

사용자가 되풀이해 요구한 것들이다. 지키지 않으면 되돌리는 일이 생긴다.

1. **코딩 전에 시안부터.** 화면이 달라지는 일은 먼저 이미지를 만들어 보여 주고, 고르신 뒤에 손댄다.
   "코딩 전에 먼저 시안을 줘"라는 말이 여러 번 나왔다.
2. **검사만으로는 부족하다. 반드시 렌더해서 눈으로 본다.**
   지금까지 큰 결함 여러 개가 검사가 아니라 화면을 보다가 나왔다
   (강조 상자 배경이 다음 줄로 번진 것, 900px 에서 휴지통 단추가 잘린 것, 토글 화살표가 사라진 것).
3. **끝나면 `2._WORK_LOG.md` 와 `3._TODO.md` 를 갱신한다.**
   작업 일지는 요청·처리·검증을 나눠 쓰고, 겪은 함정을 반드시 남긴다.
4. **결과는 artifact 페이지로 전달한다.** 실제 화면을 넣고, 결론을 먼저 쓰고, 대화하듯 쉬운 한국어로.
   `plan-brief` 스킬이 이 형식을 규정한다. 계획·제안을 낼 때 이 스킬을 부른다.
5. **말은 쉽게.** 코드 주석까지 한국어이고, 전문 용어보다 쉬운 말을 쓴다.
   예: "문서 전체를 훑는다", "칸이 좁으면 두 줄로 접힌다".
6. **묻기 전에 재 본다.** 폭·높이·시간은 짐작하지 말고 위젯에 물어보거나 실제로 잰다.

---

## 3. 환경과 실행 방법

### 검사

파일별로 **독립 프로세스**로 돌린다. 한 프로세스에서 다 돌리면 Qt 가 불안정하다.

```bash
python -m pytest test_memo_stage8.py -q -p no:unraisableexception
```

전부 돌릴 때는 반복문으로:

```bash
for f in test_*.py; do echo "$f: $(python -m pytest $f -q -p no:unraisableexception 2>&1 | tail -1)"; done
```

### 화면 찍기 (렌더)

- `QT_QPA_PLATFORM=offscreen` 은 **한글이 두부(□)로 나온다.** 화면을 찍을 때는 쓰지 말 것.
- 대신 `QT_QPA_PLATFORM=windows` + 위젯에 `WA_DontShowOnScreen` 속성을 준다.
  창이 실제로 뜨지 않으면서 글자는 제대로 나온다.
- 검사에서는 `offscreen` 이어도 된다 (글자 모양을 보지 않으므로).

```python
os.environ["QT_QPA_PLATFORM"] = "windows"
widget.setAttribute(Qt.WidgetAttribute.WA_DontShowOnScreen, True)
widget.resize(1080, 620); widget.show()
app.processEvents(); app.processEvents()   # 자리 잡을 시간을 두 번 준다
widget.grab().save("out.png")
```

`processEvents()` 를 한 번만 부르면 크기가 아직 예전 값인 채로 찍힌다. 실제로 그렇게 잘못 잰 적이 있다.

### 콘솔 글자 깨짐

Windows 콘솔이 cp949 라서 `—`(em dash) 같은 글자를 찍으면 스크립트가 죽는다.
스크립트를 돌릴 때 `PYTHONIOENCODING=utf-8` 을 앞에 붙인다.

### 임시 파일

렌더 스크립트·확인용 스크립트는 프로젝트가 아니라 세션 스크래치패드에 둔다.

---

## 4. 코드 지도 (메모 편집 쪽)

| 파일 | 하는 일 |
|---|---|
| `alert_notes/rich_memo_edit.py` | 본문 편집기. 가장 크고 중심이다. 토글·체크리스트·강조 상자·구분선·인용문·코드·표·페이지·링크·그림·찾기가 모두 여기 있다 |
| `alert_notes/insert_menu.py` | **넣을 수 있는 것들의 단일 목록.** 표시·이름·메서드·검색어·설명·단축키·줄 앞 입력을 한 줄에 적는다. 기능을 늘리면 ＋ 메뉴·`/` 메뉴·포스트잇 세 곳에 저절로 나타나고 단축키도 저절로 걸린다 |
| `alert_notes/editor.py` | 메모 편집 왼쪽 판 전체 (제목·본문·찾기 줄·알림 예약·단축키 카드·색상 줄). 두 칸 배치 계산이 여기 있다 |
| `alert_notes/find_bar.py` | 본문 찾기 줄 (Ctrl+F) |
| `alert_notes/memo_list.py` | 메모 목록 트리, 최근 본 메모 칩 |
| `alert_notes/panel.py` | 세 탭을 묶는 판. 메모 열기·되돌아가기·전체 화면·최근 목록 기억 |
| `alert_notes/line_gutter.py` | 본문 오른쪽 끝 줄 손잡이 |
| `alert_notes/sqlite_store.py` | 저장소. `parent_id`·`sort_order`·`embedded`·`pinned`, 설정 값 |
| `alert_notes/text_format_toolbar.py` | 서식 도구 줄 |
| `ui_theme.py` | 전체 QSS |

### 알아 둘 값

| 이름 | 값 | 뜻 |
|---|---|---|
| `MemoEditor.TWO_COLUMN_WIDTH` | 900 | 이보다 넓으면 아래 보조 줄이 두 칸으로 갈라진다 |
| `MemoEditor.BOTTOM_LEFT_WIDTH` | 440 | 왼쪽 칸 바닥. 이보다 좁으면 알림 요약이 네 줄로 접혀 흉하다 |
| `MemoEditor.ACTIONS_ROW_WIDTH` | 605 | 색상·휴지통 줄이 한 줄로 들어가는 폭. 그보다 좁으면 두 줄로 접힌다 |
| `RichMemoTextEdit.VISIBLE_MARGIN` | 40 | 화면에 보이는 줄 앞뒤로 이만큼만 훑는다 |
| `MemoListPanel.RECENT_CHIPS` | 3 | 최근 본 메모 칩 개수 |
| `MemoFindBar.TYPING_DELAY` | 120ms | 손을 멈추면 찾는다 |

---

## 5. Qt 에서 겪은 함정 (되풀이하지 말 것)

이 목록은 실제로 시간을 크게 잡아먹었던 것들이다.

1. **QSS 의 `min-height`·`min-width` 가 `setFixedSize`·`setFixedHeight` 를 이긴다.**
   단추가 안 줄어들면 QSS 를 봐라.
2. **`QTreeWidget::branch` 를 꾸미면 펼침 화살표가 사라진다.** 절대 손대지 말 것.
   검사로는 안 잡히고 화면을 봐야 보인다.
3. **`Qt.WindowType.Popup` 창은 키를 먼저 가져간다.** 부모 위젯의 `keyPressEvent` 까지 오지 않는다.
   `/` 메뉴가 그래서 `eventFilter` 를 쓴다.
4. **`QMenu` 는 설명(툴팁)을 기본으로 감춘다.** `setToolTipsVisible(True)` 를 켜야 뜬다.
5. **표 칸 바탕은 `QTextTableCellFormat` 으로만 붙는다.** 보통 `QTextCharFormat` 으로 넣으면
   오류 없이 조용히 사라진다. 게다가 **칸 안쪽 글자 서식을 나중에 건드리면 그 바탕까지 지워진다.**
   칸 서식은 반드시 맨 마지막에 건다.
6. **`Qt.Key.Key_Any` 는 `Key_Space` 와 값이 같다.** 검사에서 글자를 보낼 때 `Key_A` 를 쓰고
   실제 글자는 `event.text()` 에 실어 보낸다.
7. **지워진 `QTextBlockItem`·`QTextDocument` 를 건드리면 `RuntimeError`.**
   `paintEvent` 안에서 나면 프로세스가 그대로 죽는다. 문서를 바꿔 달 때 이전 신호를 끊되
   `(RuntimeError, TypeError)` 를 함께 잡는다.
8. **줄 서식이 다음 줄로 번진다.** 새 줄을 열 때는 반드시 `_open_clean_block` 을 쓴다.
   그러지 않으면 강조 상자의 노란 바탕이 뒤따르는 줄·구분선·그림까지 물든다.
9. **`QColor.isValid()` 는 `NoBrush` 여도 True 다.** 색이 실제로 붙었는지 보려면 브러시 스타일을 봐라.
10. **나눔선(QSplitter)은 감춘 칸의 폭을 스스로 나눠 주지 않는다.** `setSizes` 로 직접 넘겨야 한다.

---

## 6. 남은 일

### A. 결정만 기다리는 것 (사용자에게 물어야 함)

| # | 무엇 | 상태 |
|---|---|---|
| A-1 | **메모 목록 머리글 눌러 정렬하기** | 3단계에서 껐다. 되살릴지 결정 대기. 아래에 권장안 있음 |
| A-2 | **서식 첫 줄(글씨체·크기·색상)까지 A안으로 넓힐지** | 둘째 줄만 A안으로 바꿔 둔 상태 |
| A-3 | **두 칸 기준값 900px / 왼쪽 칸 바닥 440px** | 재서 정했지만 실제로 써 보고 조절할 수 있음 |

**A-1 권장안** (계층 목록에서 일반적으로 쓰는 방식이라고 이미 사용자에게 제안해 둔 내용):

1. 형제끼리만 정렬한다. 하위 메모는 제 부모 아래에서만 자리를 바꾼다.
2. 고정(⭐)은 정렬과 무관하게 늘 맨 위.
3. 머리글을 세 번 누르면 원래 순서로 (오름 → 내림 → 해제).
4. 정렬이 켜진 채로 끌어 옮기면 자동으로 "직접 정한 순서"로 바뀌고 상태줄에 알린다.

예상 반나절. 손댈 곳은 `alert_notes/memo_list.py` 의 `MemoTree`, 순서 저장은
`sqlite_store.reorder_notes` 와 `sort_order` 열.

### B. 채택됐지만 아직 안 만든 기능

| 순서 | 무엇 | 예상 | 착수 지점 |
|---|---|---|---|
| B-1 | **Alt+↑↓ 로 줄 옮기기** | 1시간 | 가장 쉽다. `move_line(from_pos, to_pos)` 가 끌어 옮기기용으로 **이미 있다.** `rich_memo_edit.keyPressEvent` 에 Alt+Up/Down 을 붙여 위/아래 줄과 바꾸면 끝. `test_memo_drag_stage6.py` 의 검사를 참고 |
| B-2 | **되돌리기 묶기** | 0.5일 | `Ctrl+Z` 가 잘게 쪼개진다. `beginEditBlock`/`endEditBlock` 을 안 쓰는 곳을 찾아 묶는다. 특히 토글 접기, `_ensure_toggle_children` 의 자동 줄 넣기, `_run_typing_rule` 의 "친 글 지우고 기능 적용"이 두 단계로 되돌려진다 |
| B-3 | **그림 용량 줄이기** | 1일 | `insert_image` 가 QImage 를 그대로 문서에 넣어 메모가 무거워진다. 넣을 때 긴 변 기준으로 줄이고 JPEG/PNG 중 작은 쪽으로 담는다. `QByteArray`·`QBuffer` 는 이미 import 되어 있다 |
| B-4 | **백링크** | 1.5일 | "이 메모를 가리키는 메모들"을 편집창 아래에 보여 준다. 지금 메모 링크는 본문 HTML 의 앵커(`toma-note://<id>`)로만 있어서 역방향을 찾으려면 전부 훑어야 한다. 저장할 때 링크 관계를 표 하나에 같이 적는 편이 낫다. `sync_page_titles` 와 `_known_pages` 가 "본문에 지금 무엇이 들어 있는지"를 추적하는 방식이니 그대로 본떠 쓰면 된다 |

### C. 표 보완 (이번에 안 넣은 것)

- 줄·칸 **지우기**
- 칸 **너비 조절**

지금은 넣기(`insert_table`), 줄·칸 더하기(`add_table_row`·`add_table_column`),
`Tab`/`Shift+Tab` 이동만 된다. 오른쪽 클릭 메뉴로 넣는 것이 자연스럽다.
`rich_memo_edit.contextMenuEvent` 에 이미 오른쪽 클릭 메뉴가 있으니 거기에 붙인다.

### D. 실제 기기로 확인할 것 (코드가 아니라 손으로)

- 실제 키보드에서 전역 단축키가 다른 프로그램보다 먼저 도는지
  (낮은 수준 갈고리는 넣어 둔 상태)
- 실제 마우스로 일정 만들기·옮기기 조작감, 가장자리로 시간 조절을 넣을지 결정
- 실제 배율·다중 모니터에서 Compact 배지 위치와 가장자리 끌기
- 주요 매크로가 실제 대상 프로그램에서 도는지 기록 정리
- 런타임 데이터·백업 경로 문서 최신화
- 자연어 일정 입력 / 일정 템플릿 / 주간 통계 중 무엇을 먼저 할지 결정

### E. 메모편집 강화 작업 명세 (2026-09Fill? no)

원본은 `메모편집_강화_작업명세.md`, 상세 구현 계획은
`1._PROJECT_PLAN.md`의 `24. 2026-09-08 메모편집 강화`에 있다. 아직 코딩하지 않았다.

- [ ] **제목1~4 스타일 추가** — 모두 굵게, 단계별 크기·자간·위아래 여백을 상수로 분리하고
  현재 줄 또는 선택한 여러 줄에 적용
- [ ] **기능 항목 순서 드래그 변경** — 길게 눌러 이동, 놓을 자리 표시, 순서 영구 저장,
  기본 순서 복원
- [ ] **제목1~4 줄앞 입력** — 지정 조합을 줄 맨 앞에서 치면 문자를 지우고 제목 스타일 적용,
  되돌리기 1회로 원상 복구
- [ ] **기능 설정 화면** — 제목을 포함한 줄앞 입력 조합 편집, 중복 차단, 기본값 복원,
  열린 편집기에 즉시 반영, 영구 저장
- [ ] 기존 `＋` 메뉴·`/` 메뉴·포스트잇이 같은 항목 순서와 줄앞 입력 설정을 쓰도록
  단일 목록 계약 유지
- [ ] 시안 승인 후 구현하고, 좁은 독립창·메인창·포스트잇에서 실제 렌더와 마우스 드래그 확인

#### 구현 전에 사용자에게 확인할 것

1. 작업명세의 `0. 이전 채팅 반영 항목` 1~4가 비어 있다. 빠진 요구가 있으면 원문을 받아
   이 계획에 먼저 합친다.
2. 현재 실제 UI에는 별도 기능 탭이 없고 `＋ 기능 넣기` 메뉴만 있다. 계획은 이를
   `기능 / 설정` 두 탭을 가진 팝업으로 확장하는 안이다. 사용자가 말한 기능 탭이 다른 곳이면
   위치를 다시 확인한다.
3. 제목 기본 크기 제안은 **22 / 18 / 15 / 13pt**, 기본 줄앞 입력 제안은
   **`# ` / `## ` / `### ` / `#### `**이다. 확정 전에는 구현값으로 고정하지 않는다.
4. 현재 글머리표(`-`, `*`), 체크리스트(`[]`, `[ ]`), 인용문(`"`, `|`)처럼 한 기능에
   별칭이 둘인 규칙을 설정 화면에서도 둘 다 편집할지, 대표 조합 하나만 편집하고 나머지는
   기존 별칭으로 보존할지 선택이 필요하다. 보존 우선 권장안은 후자다.

### F. 인수인계2 일정·D-Day 추가 업데이트 (2026-09-08 추가, 전체 권장안 승인)

원본은 `5._인수인계_2.md`, 전체 구현 계획은 `1._PROJECT_PLAN.md`의
`25. 2026-09-08 일정·D-Day 추가 업데이트`에 있다. 중앙 미완료 목록은 `3._TODO.md`에 반영했다.

#### 이미 있으므로 다시 만들지 않을 것

- 캘린더 날짜 제목 축약·월간 두 줄 제목·좁은 화면 빠른 메모·현재 범위 선택 표시
- 지난 일정만 흐리기와 `calendar_dim_past`
- 반복 입력 줄 숨김·반복 간격 문구·미저장 일정의 월간 날짜 이동
- 메모 📌·Delete 삭제·열 최소 너비·수정시간 축약·저장 상태 위치·알림 예약 문구
- D-Day 칩 위치·나머지 개수·셈법 설정, 요약 패널 자동 높이, 날짜 단축키, 구형 백업 복원
- 빠른 입력 팝오버 `alert_notes/schedule_popover.py`

#### 추가할 작업의 순서

1. **표시 구조 확정과 시안** — 일정 포스트잇, 오늘 요약, 지난 D-Day 묶음이 같은 정보를
   중복 표시하지 않도록 역할을 먼저 정한다.
2. **일정 표시 포스트잇** — 날짜 이동·완료·보기 전환·최대 8줄 자동 높이와 5개 설정을 만든다.
3. **전체 편집 폼 분리** — `＋ 새 일정`과 `＋ 새 할 일`, 할 일 전용 마감 입력을 나눈다.
4. **연계 보완** — 지난 D-Day 묶음·목표 시각·D-Day 필터와 편집 중 시간 겹침 정보를 붙인다.
5. **회귀와 실제 화면 검증** — 기존 빠른 팝오버는 유지하고 실제 마우스·좁은 창·배율별 화면을 확인한다.

#### 구현 전에 사용자에게 확인할 것

- 할 일 폼에 `D-Day로 세기`를 넣을지 결정. 권장안은 **넣기**다.
- 일정 포스트잇 기본값은 `당일 목록`, 최대 `8줄`, 완료 시 `삭선`, 단축키는 `비움`으로 제안한다.
- 지난 D-Day 묶음을 오늘 요약의 위쪽과 아래쪽 중 어디에 둘지 시안으로 선택한다.
- 설정창은 스크롤을 넣지 않는 기존 계약을 유지해야 하므로, 새 설정 배치는 시안에서 높이를 먼저 잰다.

#### 1단계 진행 결과

- 역할표·선택 문서: `7._일정_DDay_1단계_시안.md`
- 화면 시안: `output/schedule_dday_stage1_mockup.png`
- 추천 구조: 일정 포스트잇=실행·완료, 오늘 요약=오늘 전체 상황·이동,
  지난 D-Day=밀린 마감 정리
- 추천 위치: 지난 D-Day를 오늘 요약 맨 위에 접이식으로 배치
- 설정창 격리 렌더: 100%·125%는 요약 한 줄 추가 가능, 150%는 기존 단축키 2열이 잘림
- 단순 3열은 가로폭 부족으로 부적합. 150% 완료 기준을 위해 설정 범주 탭 분리를 권장
- 340px 좁은 포스트잇 시안은 보기 2×2, 목표 시각 보조 줄, 가로 스크롤 없음으로 렌더 확인
- 설정 범주 탭 A안은 150% 크기 1380×972에서 일정·D-Day 설정과 저장·취소가 내부 스크롤 없이 표시됨
- 일정 포스트잇 스타일은 사용자 붙임 기준으로 확정: 흰 카드, 파란 날짜, 작은 문자 배지,
  우측 시각, 완료 삭선, 하단 완료 방식 안내. 네 가지 보기는 날짜 아래 얇은 탭으로 유지
- 2단계 완료: 일정 포스트잇의 날짜 이동·네 보기·표시 대상·완료 체크·최대 8줄과
  초과 스크롤, 설정 복원, 빈 단축키와 선택 등록을 구현했다.
- 설정창은 `단축키 / 일정·D-Day / 프로그램 / 데이터` 네 탭으로 나눴고 값 선택 UI의
  포커스 중 휠 변경을 막았다.
- 임시 DB와 실제 클릭 경로 중심 집중·회귀 검사 122개가 통과했다. 별도 기능 회귀 60개 중
  새 탭 배치를 전제로 갱신해야 했던 옛 2열 위치 검사 1개도 수정 후 통과했다.
- 실제 위젯을 100%·125%·150%로 렌더해 9번째 줄부터 세로 스크롤, 가로 스크롤 없음,
  설정 탭 내부 스크롤 없음을 확인했다.
- 3단계 완료: 캘린더 머리말에 `＋ 새 일정`과 `＋ 새 할 일`을 분리하고 두 단추는
  정해진 종류의 전체 편집기를 연다. 시간 칸·끌기·기존 항목에는 빠른 팝오버를 그대로 유지했다.
- 할 일 폼은 `마감` 하나만 보이고 종류·종료 줄을 숨긴다. 일정 폼은 시작·종료를 유지한다.
  빠른 팝오버에서 전체 편집으로 넘길 때와 저장 항목 재열기 모두 저장된 종류를 따른다.
- 할 일의 `D-Day로 세기`를 추가했다. 새 `count_as_dday` 열은 추가 전 자동 DB 백업을 만들고,
  구형 전체 백업에는 기본 0을 적용한다. 비정상 종류는 자동 변환하지 않고 안내만 한다.
- 3단계 집중·일정·백업·빠른 입력 회귀 83개가 통과했고, 실제 Windows 글꼴 렌더에서
  400px 일반 폼과 680px 좁은 폼의 가로 스크롤 0, 종료 줄 분리를 확인했다.
- 4단계 완료: 오늘 요약 맨 위에 지난 D-Day 접이식 묶음과 `전체 완료`를 추가하고,
  모든 D-Day 줄에 `오늘 18:00` 또는 `09/05 16:00` 형식의 목표 날짜·시각을 함께 표시했다.
- 캘린더는 일정·할 일·D-Day 필터와 `D-Day만` 단추를 제공하고 상태를 저장한다.
  메모 D-Day는 현재 범위 띠와 목록·월간에, `D-Day로 세기` 할 일은 일정 블록에 표시한다.
- 전체 편집기 아래에 같은 날 다른 일정을 표시하고 실제 시간 범위가 겹치는 줄만
  `겹침` 글자와 붉은 배경으로 강조한다.
- 오늘 요약의 일정은 대표 3건과 나머지 개수만 보여 긴 실행 목록은 일정 포스트잇이 맡도록 조정했다.
- 4단계 집중·일정·캘린더·설정·포스트잇 회귀 106개가 통과했고,
  실제 Windows 렌더에서 세 화면의 한글·접기·필터·겹침과 가로 스크롤 0을 확인했다.
- 5단계 완료: 자정 경계·연속 날짜 이동·오늘 복귀·단축키 해제·재실행 복원 검사를 보강했다.
- 전체 `test_*.py` 660개와 `compileall`이 통과했다.
- Windows 100%·125%·150% 렌더와 680px 좁은 화면에서 포스트잇·설정창·일정/할 일 폼의
  한글, 종료 줄 분리, D-Day 선택, 스크롤 조건과 가로 스크롤 0을 확인했다.
- 마우스 검증은 셋터 직접 호출 대신 `QTest.mouseClick`의 위젯 이벤트·신호 경로로 수행했다.
  Computer Use는 현재 Windows 앱 표면을 제공하지 않아 OS 포인터 자동화는 실행하지 않았다.
- 실제 사용자 DB·LOCALAPPDATA·기존 EXE는 건드리지 않았고 EXE를 생성하지 않았다.
- 상태: **일정·D-Day 추가 업데이트 1~5단계 전부 완료**

---

## 7. 처음 이어받았을 때 순서

1. 이 문서와 `3._TODO.md` 를 읽는다.
2. 검사를 한 번 돌려 기준선을 잡는다. 단,
   `test_saving_applies_the_choice_without_waiting`은 위의 테스트 격리 문제를 먼저 고치기 전에는
   보호를 풀어 실행하지 않는다. 현재 안전한 기준선은 620개 통과·실패 0개다.
3. 사용자에게 **A 항목 세 가지 중 무엇을 정할지**, 그리고 **B 항목 중 무엇부터 할지** 묻는다.
   가장 값싸고 체감이 큰 것은 B-1(Alt+↑↓, 한 시간)이다.
4. 화면이 달라지는 일이면 시안부터 만든다.
5. 끝나면 렌더해서 눈으로 보고, 일지와 목록을 갱신하고, artifact 페이지로 전달한다.
