# TomaDesk 인수인계서

작성 2026-09-08 · 다음 작업자를 위한 문서

---

## 0. 먼저 알아야 할 것

**이 폴더는 git 저장소가 아닙니다.** 되돌리기가 안 되니 큰 수정 전에는 폴더를 복사해 두세요.

**여러 채팅창이 동시에 이 코드를 고치고 있습니다.** 파일이 언제든 바뀌어 있을 수 있습니다. 작업 전에 건드릴 파일을 먼저 읽어 현재 모습을 확인하세요.

### 실행과 검사

```bash
python A_shortcut_launcher.py
```

```bash
QT_QPA_PLATFORM=offscreen python -m pytest -q
```

테스트는 프로젝트 루트의 `test_*.py` **26개 파일 555건**입니다. 전부 통과 상태로 넘깁니다.

세션용 검사 스크립트는 임시 폴더에 있습니다(`scratchpad/verify*.py`). 그중 `verify_mine.py` 한 개만 챙기면 됩니다 — 이 세션에서 넣은 기능 27가지가 살아 있는지 한 번에 확인합니다.

---

## 1. 확정된 설계 결정

바꾸기 전에 반드시 사용자에게 확인해야 하는 것들입니다. 이유가 있어 정해졌습니다.

| 결정 | 내용 | 이유 |
|---|---|---|
| **지난 D-Day는 사라지지 않는다** | `D+2` 처럼 계속 셉니다. 사용자가 체크로 직접 끝냅니다 | 업무 마감 관리가 주 용도. 날짜가 지났다고 지우면 **처리 안 한 일이 조용히 없어집니다** |
| **애매하면 설정으로** | 사람마다 갈릴 판단은 프로그램이 정하지 않습니다 | 사용자가 명시적으로 요구한 원칙 |
| **입력 경로 둘** | 빠른 입력(팝오버)은 **종류 토글**, 전체 편집은 **종류별 분리** | 캘린더에서 끌 때는 토글이 빠르고, 마감을 제대로 적을 땐 나뉜 폼이 명확 |
| **D-Day 셈법** | 기본은 `D-2 / D-DAY / D+2`(마감용). 설정을 켜면 당일이 `D-1`(기념일용) | 한국 D-Day 앱이 전부 이 옵션을 둡니다 |
| **포스트잇 단축키** | **기본값 없이** 시작. 설정에서 직접 지정 | 상주 프로그램이 조합을 선점하면 다른 프로그램에서 그 키가 안 먹는 이유를 사용자가 영영 모릅니다 |
| **완료 표시** | 즉시 삭제하지 않습니다. 기본은 **삭선**, 휴지통은 설정에서 선택 | 체크 직후 사라지면 "잘못 눌렀나" 불안이 생깁니다 |
| **추천은 하되 자동 생성하지 않는다** | 반복 패턴을 찾아 제안만 하고, 거절하면 다시 묻지 않습니다 | 프로그램이 멋대로 만든 것은 사용자가 이유를 알 수 없습니다 |

---

## 2. 이 세션에서 넣은 것

전부 동작 확인 완료. 파일과 위치를 함께 적습니다.

### 캘린더

- **날짜 제목 축약** — 일간 `9월 2일`, 주간 `8/31 – 9/6` · `alert_notes/calendar.py` `refresh()`
- **월간은 두 줄** — 연도 위, 월 아래, 크기 **정확히 1:3**(`11px` : `33px`) · `_set_stacked_period()`
  - 크기를 상수로 계산했더니 글꼴에 따라 칸을 넘겼습니다. 지금은 **라벨에게 직접 높이를 물어** 한계(50px) 안에 드는 가장 큰 크기를 고릅니다
- **헤더 빠른 메모 버튼** — 사이드바가 숨는 **1180px 미만에서만** 표시 · `update_responsive_layout()`
  - 처음엔 중복이라 지우려 했으나, 사이드바가 통째로 숨어 좁은 창에서 접근이 사라지는 문제가 있었습니다
- **사이드바 선택 상태** — `오늘`·`이번 주`가 지금 보는 범위면 파랗게 · `_sync_nav_range()`
- **지난 일정 흐리게** — 지난 *일정*만 흐려지고 지난 *D-Day*는 그대로 · `calendar_month_view.py` `set_dim_past()`
  - 지나간 사실과 아직 안 끝난 빚은 반대로 다뤄야 합니다. 설정 `calendar_dim_past`(기본 켬)

### 일정 편집

- **반복 항목 숨김** — `반복 안 함`이면 관련 5줄이 **0픽셀** · `schedule_editor.py` `_sync_repeat_controls()`
- **반복 간격 문구** — `1 간격` → `2주마다` · `REPEAT_UNITS`
- **월간 날짜 클릭이 작성 중인 일정을 따라감** · `calendar.py` `_month_selection_changed()`
  - **새 일정(미저장)만** 옮깁니다. 저장된 일정은 건드리지 않습니다
  - `schedule_popover.move_to_date()` 와 `schedule_editor.move_to_date()` 둘 다 지원

### 메모

- **📌 표식** — 포스트잇 메모는 제목 앞에 · `memo_list.py` `POSTIT_MARK`
- **Delete 키 삭제** — 체크한 것 우선, 없으면 선택한 행 · `deletion_ids()`. **확인 창을 띄웁니다**
- **열 최소 너비가 구분선 폭을 따라감** · `column_minimums()` · `MIN_SHARE_OF_DEFAULT = 0.55`
- **수정시간 축약** — 올해면 `09-07 15:30`, 다른 해면 `24-12-31 00:00` · `list_datetime()`
- **저장 상태 위치** — 옵션 줄에서 빼 휴지통 줄 왼쪽으로 · `editor.py` `_apply_responsive()`
  - `포스트잇 모드` 글자가 잘리던 문제. 줄을 늘리지 않고 자리를 만들었습니다
- **알림 예약 문구** — 저장 전 `예약하면 …`, 저장 후 `… 예약됨` · `datetime_input.py` `set_scheduled()`
  - **메모 편집 화면에만** 적용됩니다. 독립 알림 창은 이미 맥락이 분명해 그대로 둡니다

### D-Day

- **상단 칩이 탭 줄 밖으로** — 도구 버튼 앞 · `main_window.py:558`
  - 탭 사이에 있어 네 번째 탭처럼 보였습니다
- **칩에 나머지 개수** — `D-1  예산안  +2`
- **셈법 전환** — `deadline.py` `set_count_today_as_one()`. 앱 전체가 같은 셈법을 씁니다

### 요약 패널

- **여백 축소** — 바깥 `16·16·18·16` → `12·12·12·10`, 구역 간격 `10px` → `4px`
- **줄 수만큼만 자리 차지** — `today_summary.py` `_rebalance()`
  - 비어 있으면 안내 한 줄(43px), D-Day 5건이면 148px. 공간이 모자랄 때만 줄 수에 비례해 나눠 갖습니다
  - 줄 높이를 26px 상수로 뒀더니 다 들어가는데도 스크롤막대가 생겼습니다. **위젯에게 직접 물어** 계산합니다
- **예정 알림만 빼는 옵션** — `TodaySummaryPanel(store, show_reminders=False)`

### 그 밖

- **날짜 프리셋 단축키** — `Ctrl+1` 오늘 · `Ctrl+2` 내일 · `Ctrl+3` 다음 주 · `datetime_input.py` `DATE_PRESETS`
  - 툴팁으로 안내합니다. `Ctrl+0`(지우기)은 우리 날짜 칸이 빈 값을 가질 수 없어 넣지 않았습니다
- **구형 백업 복원** — 없는 컬럼은 스키마 기본값으로 · `alert_notes/database_bundle.py` `_schema_defaults()`
  - 기본값이 하드코딩된 표라 컬럼이 늘 때마다 깨지는 구조였습니다

---

## 3. 되돌린 것 — 단축키 탭 요약 패널

작업 편집 옆에 요약 패널을 붙이는 3단 구성을 만들었다가 **되돌렸습니다.**

작업 편집 화면은 폭에 따라 세 번 접힙니다 — **900px**(매크로 단축키 줄), **650px**(입력 헤더), **620px**(아래 버튼 줄). 이 값들은 **2단을 전제로** 맞춰진 것입니다.

요약 패널(최소 280px)을 넣으면:

| 창 폭 | 목록 | 편집 | 요약 | 결과 |
|---|---|---|---|---|
| 1484px | 520 | 660 | 280 | 900px 미달 |
| 2000px | 837 | 837 | 320 | **2000px에서도 900px 미달** |

목록을 최소(520px)까지 밀어도 편집은 660px이 한계입니다. **세 임계값을 다시 잡는 것은 단축키 화면 전체의 재설계**라 보류했습니다.

`TodaySummaryPanel(show_reminders=False)` 옵션은 남겨 뒀습니다 — 재개할 때 그대로 씁니다.

---

## 4. 다음에 할 일

### 1단계 — 일정 표시 포스트잇 (아직 없음)

그날의 마감을 보여주는 **새 쪽지**입니다. 지금 있는 포스트잇(메모 쪽지, 매달 반복 체크리스트)과 별개입니다.

- **`‹ 오늘 ›` 날짜 이동** — 연달아 누를 수 있고, 가운데를 누르면 오늘로. 오늘이 아니면 색이 빠짐
- **체크박스 완료** — 삭선 / 휴지통을 설정에서 선택 (기본 삭선)
- **보기 전환** — 당일 · 이번 주 · 임박순 · 우선도순
- **자동 높이** — 목록 길이에 맞춰 늘고 줄되 **최대 8줄**(설정에서 변경). 그 위로는 스크롤
- **설정 5개** — 단축키(기본 비움) · 완료 처리 · 표시 대상 · 기본 보기 · 최대 높이

> 자동 높이는 `today_summary.py` `_rebalance()` 와 같은 방식으로 하면 됩니다. **줄 높이를 상수로 두지 마세요** — 같은 함정을 두 번 밟습니다.

### 2단계 — 전체 편집 폼 나누기

- `＋ 새 일정` / `＋ 새 할 일` 두 버튼으로 분리
- 할 일 폼에서 **종료 줄 없애고 마감 하나만**
- 종류 드롭다운 제거

> 저장된 일정을 열 때 어느 폼으로 보낼지 규칙이 필요합니다. 저장된 종류를 보고 고르면 되지만, 종류를 잘못 넣어둔 자료가 있으면 그때 드러납니다.

### 빠른 입력 팝오버 — 이미 다른 창에서 완료

`alert_notes/schedule_popover.py`. `＋ 새 일정`이 팝오버를 엽니다. 제 계획의 3단계에 해당하던 것으로, **다시 만들지 마세요.**

---

## 5. 답을 못 받은 질문

**할 일 폼에 `D-Day로 세기` 체크를 넣을까요?**

넣으면 할 일을 만들면서 바로 D-Day가 됩니다. 지금은 D-Day가 **메모에만** 붙어서, 캘린더에 마감 일정을 만들어도 메모를 따로 만들고 날짜를 다시 넣어야 합니다.

넣는 쪽을 권합니다 — 마감 관리가 주 용도라면 할 일이 곧 D-Day 후보이고, 폼을 나누는 김에 붙이면 나중에 따로 손대지 않아도 됩니다. 빼도 1·2단계는 그대로 진행됩니다.

기본값으로 두기로 한 것(사용자가 달리 말하지 않으면):
- 포스트잇 최대 높이 → **8줄**
- 포스트잇 기본 보기 → **당일 목록**

---

## 6. 아직 정리되지 않은 것

시중 앱 조사에서 답이 나왔지만 아직 손대지 않은 항목입니다.

| | 내용 | 조사 결과 |
|---|---|---|
| **D2** | 지난 D-Day가 쌓이면 오늘 것이 묻힘 | **별도 섹션으로 묶기** + 헤더에 일괄 처리 버튼 + 위/아래 위치를 설정으로 |
| **D3** | D-Day에 목표 시각이 안 보임 | 같은 줄 오른쪽에 `9/4 18:00`. 당일이면 시간 단위 (관습 없음 — 우리가 정하면 됨) |
| **D5** | 캘린더에서 D-Day만 골라 보기 | **체크박스 목록**이 표준 (색 범례 클릭 방식은 확인 안 됨) |
| **U8** | 일정 편집 패널 아래 빈 공간 | 그날의 다른 일정을 띄워 시간 겹침 확인 |
| **S5** | 편집 패널이 캘린더를 좁힘 | 캘린더 격자를 고정 단으로 미는 앱이 **없었음** → 겹침 팝오버 (이미 반영됨) |

**D2·D3은 1단계 포스트잇과 같은 문제입니다.** 포스트잇이 그날 마감을 띄우면 요약 패널에 무엇을 남길지 다시 정해야 합니다. **셋을 함께 설계하세요.**

---

## 7. 함정 — 겪은 것들

### 설정 창은 스크롤을 넣으면 안 됩니다

`test_feature_updates.py` 가 `dialog.findChildren(QScrollArea) == []` 를 명시적으로 검사합니다. 한 화면에 다 보이게 하려는 의도적 설계입니다.

설정이 늘어 높이 한계를 넘으면 **기본 높이를 올리세요**(`settings_dialog.py:115`, 현재 850px). **지금 여유가 28px뿐**이라 설정 두어 개만 더 늘면 또 걸립니다. 그때는 카드 배치를 다시 잡아야 합니다.

### 실제 앱 클릭 자동화가 자주 실패합니다

다른 창에 가리면 합성 클릭이 전달되지 않습니다. 화면 확인은 **오프스크린 렌더**가 훨씬 안정적입니다.

```python
os.environ["QT_QPA_PLATFORM"] = "offscreen"
widget.grab().save("shot.png")
```

오프스크린에는 한글 글꼴이 없어 글자가 네모로 보입니다. **배치를 볼 때만 쓰고 글자 확인은 실제 앱 캡처로** 하세요.

### 셸 heredoc 안에서 `\n` 이 깨집니다

`python - <<'PY'` 안에 `\n` 을 쓰면 실제 줄바꿈으로 바뀌어 SyntaxError가 납니다. 여러 번 겪었습니다. **긴 코드는 Write 도구로 파일에 직접 쓰세요.**

### 프로그램적 setter는 신호를 보내지 않습니다

`CalendarMonthView.setSelectedDate()` 는 값만 바꿉니다. 실제 클릭 경로는 `_select_cell()` 입니다. 테스트에서 setter를 쓰면 **아무 일도 안 일어나는데 통과하는 가짜 통과**가 납니다.

### `set_deadlines()` 는 그리지 않습니다

`CalendarMonthView.set_deadlines()` 는 값만 담습니다. 실제 코드처럼 **`set_deadlines()` 를 먼저, `set_month()` 를 나중에** 부르세요.

---

## 8. 참고 문서

| 문서 | 내용 |
|---|---|
| [TomaDesk 화면 진단](https://claude.ai/code/artifact/3242de15-14ce-4ad7-bbb8-320c7e0b4afd) | 캡처에서 찾은 문제 19개 + 코드 원인 |
| [TomaDesk 보류 항목](https://claude.ai/code/artifact/82723431-8c45-4972-b09c-083a27c92faf) | 보류 12건을 기다리는 결정별로 묶음 |
| [일정 앱 조사 지침](https://claude.ai/code/artifact/107e6afd-d155-4c4e-9aea-a7f4792f2f13) | 조사 관찰 항목 19개 |
| [정해진 것 만들기](https://claude.ai/code/artifact/0a4b7c3d-534f-4d49-b1bb-c3eb751e4bed) | 이 인수인계의 그림 버전 |
| [개선안 화면 예시](https://claude.ai/code/artifact/fbe719da-53f2-44f1-b83d-7a42b4215600) | 폼 분리·포스트잇 목업 |
| `PROGRAM_OVERVIEW.md` | 프로그램 전체 설명서 (파일 접근 없는 대화용) |

사용자가 만든 조사 결과 두 건도 있습니다 — **일정 입력 다시 짜기**(캘린더 개편 로드맵), **세 앱이 다르게 한 것**(TickTick·Outlook·Rainlendar 비교). 캘린더를 손댈 때 앞의 것을 먼저 읽으세요.

---

## 9. 사용자에 대해

- **터미널에 길고 빽빽한 설명을 쓰면 이해하기 어려워합니다.** 계획·비교·로드맵은 **아티팩트 페이지로 만들고 그림을 넣으세요.** 터미널에는 결론 3~5줄과 링크만 남깁니다 (`plan-brief` 스킬에 규칙이 있습니다)
- 선택지를 줄 때는 **각 선택지의 결과를 그림으로** 보여주세요. 글로만 쓰면 판단하지 못합니다
- 아티팩트에 **코멘트로 답합니다.** 작업 전에 `Artifact action:"comments"` 로 확인하세요
- 결정을 미루고 싶어 할 때는 "지금 확정 가능한 것부터" 진행하기를 원합니다
