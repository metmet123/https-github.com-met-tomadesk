# 설계 결정

## 2026-09-15 모바일 연동 대비 메모 식별·변경·백업 계약

- 로컬 정수 PK/FK는 유지하고 외부 관계에는 UUIDv4 `sync_id`를 사용한다. 동기화 대상 변경은 `revision`, 밀리초 UTC 시각, 설치 UUID를 함께 갱신하며 설치 UUID는 백업 교체 복원으로 덮어쓰지 않는다.
- 독립 복제는 메모·첨부·주석·block에 새 UUID를 만들고 명시적 참조 링크만 원래 대상을 유지한다. 백업 교체는 UUID를 유지하며, 병합에서 같은 메모 UUID가 이미 있으면 덮어쓰지 않고 `conflict_of_sync_id`로 연결된 새 UUID 사본을 만든다.
- 영구 삭제는 `sync_tombstones`에 종류·UUID·revision·UTC 시각을 남긴다. 휴지통 이동과 복원은 일반 revision 변경이다.
- 새 메모·블록 링크는 v2 UUID 주소를 만들고 숫자/v1 주소도 계속 해석한다. `.tomamemo` v2는 UUID 관계·카테고리·주석·템플릿·버전·tombstone을 포함하며 v1 읽기를 유지한다.
- UI는 `MemoDataService` 경계를 통해 백링크·주석·템플릿·버전 트랜잭션을 수행한다. 네트워크 전송·계정·서버 충돌 UI는 이번 결정 범위에 포함하지 않는다.

## 2026-09-09 메모 전용 백업 형식과 복원 경계

- 메모 전용 백업은 ZIP 기반 `.tomamemo` 형식 버전 1을 사용한다.
- `manifest.json`에는 메모 관계 데이터와 첨부 경로·크기·SHA-256을 기록하고 첨부 원본은 별도 항목으로 둔다.
- 병합은 모든 관계 ID와 본문 내부 `toma-note://`, `toma-note-image://` 참조를 새 ID로 치환한다.
- 전체 교체는 메모 관련 표만 바꾸고 사용자 일정·단축키 작업·앱 설정은 보존한다.
  메모와 연결되었던 사용자 일정은 삭제하지 않고 연결만 해제한다.
- 알림에서 만든 일정 미러는 백업 대상에서 제외하고 복원 트랜잭션에서 다시 생성한다.
- 기존 앱 전체 JSON 백업과 `legacy_name="hotkeys"` 구형 복원 계약은 변경하지 않는다.

## 2026-09-14 메모 Phase 4 블록 식별자 저장

- Qt의 임의 블록 UserProperty와 HTML `id`는 `QTextDocument.toHtml()` 왕복에서 보존되지 않는다. 이름 앵커는 보존되지만 링크 서식·연동 해제와 충돌하므로 블록 ID로 쓰지 않는다.
- UUIDv4 블록 ID와 본문 고정 목록을 버전형 HTML `<meta>`에 보관한다. 보이는 글자·링크에는 식별자를 심지 않고 DB schema도 추가하지 않는다. 구형 메모는 열 때 메모리에서 ID를 부여하되 수정 상태를 바꾸지 않으며 실제 편집·저장 시 직렬화한다.
- 링크 대상은 `memo_id + block_id`로 판별한다. `toma-note://id`는 그대로 유지하고 블록 링크는 `toma-block://v1/<memo_id>/<uuid>`를 사용한다. 독립 복제는 새 블록 ID를 만들고 기존 참조 링크는 원래 대상을 유지한다.
- 목차에서 블록 링크를 복사할 때 대상 ID가 저장되어 있어야 한다. 편집되지 않은 구형 메모에는 ID 메타데이터만 기록하고, 미저장 수동 편집 내용은 임의로 저장하지 않고 저장을 안내한다. 메모 백업 병합 복원은 블록 UUID를 유지하되 새 메모 ID로 링크 주소를 재매핑한다.
