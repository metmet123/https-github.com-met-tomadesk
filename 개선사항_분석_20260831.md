# 토마 데스크 개선사항 분석

- 작성일: 2026-08-31
- 대상: `35_단축키 프로그램` (TomaDesk / PyQt6)
- 범위: 소스 코드, SQLite 데이터 구조, UI 구성, 테스트, 프로젝트 문서 검토
- 성격: **분석 전용**. 파일 수정, 코드 작성, EXE 빌드, 데이터 변경을 수행하지 않았다.
- 검증 방법: 코드 정독 + 임시 DB / 오프스크린 Qt에서 주요 의심 항목 실제 재현

---

## 1. 전체 평가

### 1.1 현재 강점

- **데이터 계층이 견고하다.** `store.py` / `alert_notes/sqlite_store.py` 모두 소프트 삭제(`deleted_at`) + 7일 휴지통 + 스키마 업그레이드 전 자동 DB 백업(`_backup_before_upgrade`)을 갖췄다. `_rebuild_reminders_table()`은 트랜잭션·롤백까지 처리한다.
- **백업/복원이 두 DB를 하나의 번들로 묶는다.** `alert_notes/database_bundle.py`가 `hotkeys.db` + `alert_notes.db`를 원자적으로 교체하고, 없는 컬럼은 `_column_default()`로 채워 구버전 백업도 복원된다. 복원 직전 `before_restore_*.json` 안전 백업까지 만든다.
- **저장 유실 방어가 여러 겹이다.** 메모 자동저장(400ms 디바운스), 수동 모드의 7일 초안(`memo_draft_*` settings), `flush_pending_save()`가 창 닫기·전환·shutdown 경로마다 호출된다. 포스트잇은 저장 실패 시 재시도 버튼(`postitSaveError`)을 띄운다.
- **전경 앱별 단축키 해제**(`_sync_action_hotkeys`)는 상용 런처에도 드문 기능이고, 매크로 재생 중 긴급 중지(`StopHotkeyMonitor`)도 폴링이 아닌 실시간 체크다.
- **반응형 대응이 실제로 코드에 있다.** `_update_responsive_layout`, `AlertNotesPanel.update_responsive_layout`(1080px), `CalendarPanel.update_responsive_layout`(900/1180px), 스플리터 비율 영속화.
- **테스트 168개**가 존재하고 매크로 재생·반복 규칙·백업 왕복 같은 핵심 로직을 덮는다.

### 1.2 가장 큰 문제 5개

| # | 문제 | 근거 (검증됨) |
|---|---|---|
| 1 | **기본 "새 메모" 단축키가 파서에서 거부되어 영구 등록 실패 + 설정 저장 전체 차단** | `NEW_MEMO_HOTKEY = "Ctrl+Alt+Shift+N"` (main_window.py:86) 인데 `hotkey_parser._validate_modifiers`는 수식키 2개까지만 허용 → `parse_hotkey("Ctrl+Alt+Shift+N")` 예외. 실행 확인: `수식키는 최대 2개까지만 사용할 수 있습니다.` 또한 `HotkeyBuilder.setText`가 `modifiers[:2]`로 Shift를 버려 설정 창에서 `Ctrl+Alt+N`으로 표시 → 빠른 메모와 중복 → `_validate_and_accept`가 **설정 저장 자체를 거부**. 오프스크린 재현 완료 |
| 2 | **Excel 가져오기가 확인 없이 단축키 전체를 영구 교체** | `import_excel()` (main_window.py:2932)는 파일 선택 직후 `store.replace_actions()` 호출 → `DELETE FROM hotkey_actions` (store.py). 휴지통에 있던 항목까지 함께 사라지고 확인 대화상자·미리보기가 없다 |
| 3 | **D-Day 알림이 "알림 해제" 한 번에 조용히 사라진다** | `set_deadline(alert=True)`가 `occurrence_kind='deadline'` reminders 행을 만드는데, `clear_reminder(note_id)`는 `DELETE FROM reminders WHERE note_id=? AND status='pending'`로 그 행까지 지운다. 재현 결과: `pending: []` / `d_day_at: 202609031353` / `d_day_alert: 1` → 배지는 D-3, 대화상자는 "알림 켜짐", 실제 알림은 영원히 안 옴 |
| 4 | **캘린더 일간/주간이 00:00~07:59 일정을 표시도 생성도 못 한다** | `CalendarTimelineView.render_range`는 `range(8, 24)` 16행 고정, `row = due.hour - 8`, `if not (0 <= row < 16): continue`. `_slot_datetime`도 `time(hour=row + 8)`. 06:30 일정 재현 결과 `row=-2 rendered=False` — 저장은 되어 있으나 주 화면에서 사라짐 |
| 5 | **알림에서 파생된 캘린더 항목의 사용자 편집이 소리 없이 폐기된다** | `sync_legacy_reminder()`는 `DELETE ... WHERE source_reminder_id=?` 후 `migrate_legacy_reminders()`로 재삽입. 재현 결과 사용자가 넣은 제목·상세·분류·미리 알림이 전부 초기화되고 새 id로 대체(`notifications kept: []`) |

### 1.3 기능 간 공통으로 개선해야 할 기반 요소

1. **알림 체계가 두 개로 갈라져 있다.** `reminders`(메모 알림 + D-Day) 와 `schedule_notifications`(일정 미리 알림). 저장·반복 규칙·목록·Excel·처리 이력이 전부 따로다. `AlertService.check_now()`만 두 소스를 합친다.
2. **반복 규칙 어휘가 두 벌이다.** `alert_notes/recurrence.py`(daily/weekdays/weekly/monthly, end_type) vs `alert_notes/schedule_recurrence.py`(none/daily/weekly/monthly/yearly + interval). 메모 알림에는 "평일"이 있는데 일정에는 없고, 일정에는 "매년/간격"이 있는데 메모 알림에는 없다.
3. **`register_hotkeys()`가 전체 재등록만 지원한다.** 메모 자동저장 400ms마다 `shortcuts_changed` → 전역 단축키 전체 unregister/register. 부분 갱신 API가 필요하다.
4. **날짜 표기 유틸이 통일되어 있지 않다.** `excel_export.display_datetime`, `deadline.reminder_display_text`, `reminder_choice_dialog.display_due`가 따로 있고, 휴지통은 아예 원시 `202608311532`를 그대로 출력한다(main_window.py:275).
5. **오류 메시지가 파이썬 예외를 그대로 노출한다.** `ScheduleEditor._save`, `_macro_document`, `save_editor_values` 모두 `str(exc)`를 그대로 `QMessageBox`에 넣는다.
6. **UI 배율(`_ui_scale`)이 저장되지 않는다.** `main_window.py:421`에서 항상 1.0으로 시작하고 어떤 setting에도 기록되지 않는다.

### 1.4 지금 손대면 안 되는 보존 대상

- **SQLite 스키마 전체** — 특히 `notes.d_day_*`, `reminders.occurrence_kind/series_id/scheduled_at`, `schedule_items.source_reminder_id UNIQUE`. `_backup_before_upgrade`가 이 컬럼 존재 여부로 업그레이드를 판단하므로 컬럼을 추가/제거하면 기존 사용자 DB가 매 실행마다 백업 파일을 만든다.
- **`settings` 테이블 키 이름** — `postit_geometry_*`, `postit_expanded_geometry_*`, `memo_draft_*`, `main_splitter_*`, `memo_editor_splitter_horizontal_ratios`, `standalone_memo_editor_*`. 이름을 바꾸면 사용자 창 배치·미저장 초안이 날아간다.
- **`database_bundle` JSON 포맷과 `legacy_name="hotkeys"` 경로** — `data/backup_2026072*.json`, `backup/auto_backup_*.json` 같은 실제 사용자 백업이 이 포맷이다.
- **저장된 단축키 문자열 표기법** (`Ctrl+Alt+F9`, `Numpad0`, 백틱) — `hotkey_parser._display_key`의 출력이 그대로 DB에 들어있다.
- **`hotkey_actions.payload`의 매크로 JSON 구조**(`steps` / `timing_mode` / `playback_speed` / `repeat_count` / `excluded_apps`) — `action_runner`가 레거시 `VK_xx` 스텝까지 호환 처리 중이다.
- **포스트잇 3단 표시 모드**(`normal` / `title` / `badge`)와 접힘 상태 저장 — 최근 작업기록의 완료 항목이다.

---

## 2. 기능별 분석

### 2.1 단축키

**현재 제공되는 주요 기능**: 문구 입력 / 사이트 열기 / 프로그램·폴더 열기 / 반복작업(매크로) 4종, 전역 등록, ON/OFF 토글 스위치, 상태·유형 필터 + 텍스트 검색, 다중 선택 체크박스 + 전체선택 헤더, 7일 휴지통, 매크로 녹화·단계 편집·타이밍 편집·Undo/Redo(50단계), Excel 왕복, 프로그램별 제외, 긴급 중지.

| 우선순위 | 개선사항 | 현재 문제와 근거 | 사용자 효과 | 구현 방향 | 난이도 | 회귀 위험 | 관련 파일 |
|---|---|---|---|---|---|---|---|
| **P0** | 3-수식키 단축키 처리 정합화 | `NEW_MEMO_HOTKEY="Ctrl+Alt+Shift+N"`(main_window.py:86, settings_dialog.py:45)를 `parse_hotkey`가 거부(`_validate_modifiers`, 최대 2개). 실행 확인 완료. `_hotkey_from_store`는 예외 시 같은 잘못된 fallback을 반환해 `register_hotkeys`에서 매 실행 "실패 1개" 발생 | 새 메모 단축키가 실제로 동작하고, 설정 창에서 저장이 가능해짐 | ① 파서를 3개 수식키까지 허용하고 `HotkeyBuilder`에 세 번째 수식키 슬롯 추가 **또는** ② 기본값을 2-수식키(예: `Ctrl+Shift+N`)로 낮추고 기존 저장값 마이그레이션. 어느 쪽이든 저장된 사용자 값은 그대로 읽히도록 유지 | 중 | 중 (파서는 모든 단축키 경로가 공유) | `hotkey_parser.py`, `hotkey_builder.py`, `main_window.py:86,1996`, `settings_dialog.py:45` |
| **P0** | 설정 창이 기본값만으로 저장 차단되는 문제 | `SettingsDialog._validate_and_accept`가 중복 판정 → "빠른 메모와 새 메모 단축키가 같습니다"로 **모든 설정 저장 거부**. 오프스크린 재현 완료 | 시작 위치·자동저장·토마펫·데이터 폴더 설정을 저장할 수 있게 됨 | 위 항목 수정으로 근본 해소. 추가로 중복 검사를 단축키 필드에만 국한하고, 나머지 설정은 저장 후 해당 필드만 되돌리는 방식 검토 | 하 | 하 | `settings_dialog.py:334-352` |
| **P0** | Excel 가져오기 전 확인·미리보기 | `import_excel()`이 파일 선택 직후 `replace_actions()` 실행. `DELETE FROM hotkey_actions`로 휴지통 항목까지 영구 삭제. 확인 대화상자 없음 | 실수로 잘못된 파일을 열어도 기존 단축키를 잃지 않음 | "N개 교체 / 현재 M개 삭제" 요약 + Yes/No 확인, 백업 경로를 **사전**에 안내. `replace_actions`는 그대로 두고 호출부만 감싼다 | 하 | 하 | `main_window.py:2932`, `store.py:replace_actions` |
| **P1** | 일괄 활성/비활성·실행 테스트 버튼 노출 | `_build_table_panel`에서 `bulk_action_toolbar`를 만들고 `setParent(panel)` 후 **레이아웃에 넣지 않고 `hide()`**(main_window.py:783-787). `set_selected_active()`(2651)·`test_action()`(2662)·`selection_summary` 모두 진입점 없음. 우클릭 메뉴도 없음(표 위젯 컨텍스트 메뉴 0건) | 체크박스로 여러 개 골라 놓고 실제로 할 수 있는 게 삭제뿐인 상태 해소 | 툴바를 `layout`에 추가하거나, 선택이 1개 이상일 때만 나타나는 선택 액션 바로 노출. 로직은 그대로 재사용 | 하 | 하 (테스트 `test_ui_responsive.py:103,259`가 `isHidden()`을 단정하므로 함께 수정) | `main_window.py:766-787,2244-2259` |
| **P1** | 단축키 등록 실패 표시와 재시도 | `register_hotkeys`는 실패를 상태 표시줄 문자열로만 알린다(`등록 완료 N개 / 실패 M개`). 실패한 작업도 목록에서 ON 스위치가 켜진 채라 사용자는 왜 안 되는지 모른다. 재시도 버튼 없음 | "단축키를 눌러도 아무 일이 없다"는 최다 문의 유형 해소 | 표에 등록 상태 아이콘(등록됨/충돌/제외중) 추가 + 실패 목록 대화상자 + "다시 등록" 버튼. `_registered_action_hotkey_ids`를 이미 갖고 있어 데이터는 준비됨 | 중 | 하 | `main_window.py:2672-2742,2104` |
| **P1** | 자동저장이 전역 단축키를 전면 재등록하는 문제 | `alert_panel.shortcuts_changed.connect(lambda: self.register_hotkeys(False))`(main_window.py:531) ← `save_editor_values`가 매 자동저장마다 emit. 400ms 타이핑 정지마다 전체 `unregister_all()` + 재등록, 그 사이 전역 단축키 공백, 상태 표시줄 문구도 계속 덮어씀 | 메모 입력 중 다른 단축키가 씹히는 현상 제거, CPU/Win32 호출 급감 | 단축키 문자열 집합이 실제로 바뀐 경우에만 재등록(해시 비교), 또는 변경된 항목만 부분 등록 | 중 | 중 (등록 경로 전체가 영향) | `main_window.py:531,2672`, `alert_notes/panel.py:save_editor_values` |
| **P1** | 녹화·재생 중 알림 팝업 차단 | `AlertService`는 20초 폴링을 녹화·재생 중에도 계속하며 `WindowStaysOnTopHint` 대화상자를 띄운다. 매크로는 절대 좌표 클릭이라 팝업이 클릭을 가로챌 수 있다. `start_recording`은 전역 단축키만 정리하고 알림은 건드리지 않는다 | 매크로 오작동·녹화 오염 방지 | `_recording` / 재생 중에는 `AlertService`를 일시 중지하고 종료 후 `check_now()`로 밀린 알림 표시. 큐 구조가 이미 있어 손실 없음 | 하 | 하 | `main_window.py:1905,2803`, `alert_notes/service.py` |
| **P2** | 전경 앱 폴링 조건화 | `_foreground_hotkey_timer` 120ms 고정(`FOREGROUND_HOTKEY_SYNC_MS`). 제외 프로그램이 하나도 없어도 초당 8회 `OpenProcess` + `QueryFullProcessImageNameW` | 유휴 시 CPU·핸들 사용 감소 | 제외 목록이 있는 활성 작업이 0개면 타이머 정지, 생기면 재시작 | 하 | 하 | `main_window.py:409,2757`, `foreground_app.py` |
| **P2** | 매크로 JSON 오류 메시지 한국어화 | `_macro_document()`가 `json.loads` 예외를 그대로 올려 `Expecting value: line 1 column 1 (char 0)`가 사용자에게 노출 | 반복작업 저장 실패 원인 파악 가능 | JSONDecodeError를 잡아 줄·열 위치를 포함한 한국어 안내로 변환 | 하 | 하 | `main_window.py:2513,2361` |
| **P2** | 휴지통 복원 결과 안내 | `store.restore_action`은 `active=0`으로 되돌리는데(store.py) 사용자에게 안내가 없어 "복원했는데 안 된다"가 발생. 삭제 시각은 `202608311532` 원시 문자열 표시(main_window.py:275) | 복원 후 혼란 제거 | 복원 후 "N개 복원 · 모두 비활성 상태입니다" 안내 + `display_datetime()` 재사용 | 하 | 하 | `main_window.py:241-305,705`, `store.py:restore_action` |
| **P2** | Excel 왕복이 생성 시각·휴지통을 보존하지 않음 | `replace_actions`가 `created_at`을 현재 시각으로 덮어쓰고 `deleted_at` 컬럼을 채우지 않음 | 백업 대체 용도로 Excel을 쓸 때 이력 보존 | INSERT에 `created_at` / `deleted_at` 유지 옵션 추가(스키마 변경 없음) | 하 | 중 | `store.py:replace_actions`, `excel_io.py` |
| **P3** | 도움말의 하드코딩 단축키 | `HelpDialog` 카드에 `Ctrl+Alt+F12로 종료` 고정(main_window.py:175). 사용자가 녹화 종료 단축키를 바꿔도 그대로 | 안내 정확성 | `self.record_stop_hotkey` 주입 (`_update_stop_hotkey_labels`와 동일 패턴) | 하 | 하 | `main_window.py:150-200` |

**추천 사용자 흐름**

`＋ 새 작업 → 이름 입력 → 유형 선택 → 단축키 캡처(즉시 사용 가능 여부 표시) → 실행 테스트 → 저장 → 목록에서 등록 상태 확인`

현재는 "실행 테스트"가 화면에 없어 테스트 없이 저장할 수밖에 없고, "등록 상태 확인"도 상태 표시줄 한 줄뿐이다.

**Quick Win (반나절 이내)**

- `bulk_action_toolbar`를 레이아웃에 추가 → 일괄 활성/비활성·실행 테스트·선택 요약 즉시 복구
- Excel 가져오기 앞에 확인 대화상자 1개 추가
- 휴지통 삭제 시각에 `display_datetime()` 적용
- 도움말 단축키 동적 치환

**중장기 (구조 변경)**

- 단축키 등록 상태를 `_action_hotkey_rows` / `_registered_action_hotkey_ids`에서 표 모델로 승격 → 등록 상태 열
- `register_hotkeys` 부분 갱신 API 분리(전체 재등록 제거)
- 수식키 3개 지원(파서 + 빌더 UI 동시)

**추가 테스트 필요**

- `parse_hotkey`를 실제로 통과시키는 등록 테스트 — 현재 `_TrackingHotkeys.register`(test_feature_updates.py:33)는 문자열만 기록하고 파서를 호출하지 않아 P0 버그를 못 잡았다
- 모든 `HOTKEY_DEFAULTS` 값이 `parse_hotkey`를 통과하는지 검사하는 회귀 테스트
- `SettingsDialog`를 기본값으로 열어 `_validate_and_accept`가 성공하는지 검사
- `import_excel` 취소 시 기존 데이터 무변경 검증

---

### 2.2 메모

**현재 제공되는 주요 기능**: 목록(검색·체크박스·전체선택·Excel 내보내기·선택 삭제), 리치 텍스트 편집(B/I/U/S, 글머리표, 체크리스트, 이미지 첨부 → `note_attachments`), 색상 5종·배경 투명도, 포스트잇(항상 위·입력 잠금·시작 시 표시·normal/title/badge 3단 압축), 메모별 전역 단축키(열기 / 포스트잇 토글), 독립 편집창, 빠른 메모 대화상자, 자동/수동 저장 + 7일 초안, 7일 휴지통.

| 우선순위 | 개선사항 | 현재 문제와 근거 | 사용자 효과 | 구현 방향 | 난이도 | 회귀 위험 | 관련 파일 |
|---|---|---|---|---|---|---|---|
| **P1** | 자동저장 시 전체 화면 재조회 제거 | `save_editor_values`가 매 저장마다 `calendar.refresh()` + `reminder_history.refresh()` 호출. 캘린더는 `items_for_range`에서 항목마다 최대 20000회 반복 전개(`expand_occurrences`). 포스트잇 타이핑도 `_save_postit_content` → `self.refresh()`로 전체 목록 재조회 | 메모가 많아질수록 심해지는 타이핑 끊김 제거 | 자동저장은 목록 행 1개만 갱신, 캘린더·알림내역은 탭 진입 시 또는 알림 변경 시에만 refresh | 중 | 중 | `alert_notes/panel.py:refresh, save_editor_values, _save_postit_content` |
| **P1** | 수동 저장 초안이 단축키 설정을 복원하지 않음 | `_save_draft()`는 `values()` 전체를 저장하지만 `_restore_draft()`는 title/content/postit/always_on_top/lock/color/opacity만 복원. `hotkey`, `hotkey_action`은 복원 대상에서 빠짐(editor.py:525-553) | 수동 저장 모드에서 단축키 지정 중 앱이 종료돼도 복구됨 | `_restore_draft`에 두 필드 추가(설정 키·저장 형식 변경 없음) | 하 | 하 | `alert_notes/editor.py:507-553` |
| **P1** | 새 메모 단축키가 빈 메모를 남김 | `show_new_memo_editor()`가 무조건 `create_note("새 메모","")` 후 독립창을 연다. 아무것도 입력하지 않고 닫아도 목록에 빈 "새 메모"가 영구히 남는다. 인라인 편집기는 `has_meaningful_content()`로 막고 있어 동작이 불일치 | 목록에 빈 메모가 쌓이지 않음 | 창을 닫을 때 내용이 없으면 삭제, 또는 인라인과 동일한 지연 생성 방식 적용 | 중 | 중 (`test_new_memo_hotkey_opens_only_the_standalone_editor`와 상충 확인 필요) | `main_window.py:595`, `alert_notes/standalone_editor.py:closeEvent` |
| **P2** | 메모 목록에 알림·D-Day 열이 없음 | `MemoListPanel.HEADERS = ["", "번호", "표시", "제목", "수정 시간", "내용 미리보기"]`. 알림이 걸린 메모인지, D-Day가 있는지 목록에서 알 수 없다. `_NOTE_SELECT`는 이미 `reminder_due_at`를 함께 조회하고 있어 데이터는 있음 | 목록만 보고 "언제 알림 오는 메모인지" 파악 | 열 추가 또는 제목 옆 배지. 정렬은 `display_datetime` 문자열이라 사전식 정렬이 그대로 통함 | 하 | 하 | `alert_notes/memo_list.py:18-19,131`, `alert_notes/sqlite_store.py:_NOTE_SELECT` |
| **P2** | 편집기에 그 메모의 알림 목록이 없음 | `set_note()`는 `reminder_due_at`(가장 빠른 1건)만 표시하고 `editing_reminder_id=None`으로 초기화. 알림 3건이 걸려 있어도 편집기에는 1건만 보이고, 어느 것을 수정하는지 알 수 없어 `ExistingReminderDialog`가 매번 뜬다 | "이 메모에 몇 개 알림이 있나"를 그 자리에서 확인 | 알림 카드 안에 `pending_reminders_for_note()` 목록(시간·반복 요약·삭제)을 표시. 저장소 API는 이미 존재 | 중 | 중 | `alert_notes/editor.py:420-474`, `alert_notes/panel_reminder_actions.py` |
| **P2** | 통합 검색 결과에 HTML 원문 노출 | `MemoSearchDialog.refresh`가 `str(note['content'])[:90]`을 그대로 출력. 리치 텍스트 메모는 `<!DOCTYPE HTML PUBLIC ...`이 미리보기로 보인다. 같은 파일의 다른 경로는 `plain_text_from_content()`를 쓴다 | 검색 결과 가독성 | `plain_text_from_content()` 한 줄 적용 | 하 | 하 | `alert_notes/quick_capture.py:120` |
| **P2** | 목록 검색이 본문 HTML을 대상으로 함 | `NoteReminderStore.notes()`가 `content LIKE ?`로 검색. 리치 텍스트 메모의 본문은 HTML이라 `color`, `span`, `font-weight` 같은 태그 문자열이 검색에 걸린다 | 잘못된 검색 결과 제거 | 결과를 `plain_text_from_content`로 2차 필터하거나, 검색어에 꺾쇠가 없으면 태그 구간을 제외 (스키마 변경 없이 가능) | 중 | 중 | `alert_notes/sqlite_store.py:notes()` |
| **P2** | 메모 편집기의 단축키가 전역 단축키와 충돌해도 경고 없음 | `EditorShortcutSettingsDialog._save`는 자기들끼리의 중복만 검사. 포스트잇의 하드코딩 단축키(`Ctrl+N/W/D/B/I/U/T/Shift+L`, postit.py:548-566)나 전역 단축키(`Ctrl+Alt+N` 등)와의 충돌은 검사하지 않는다. 전역이 이기면 편집기 단축키가 조용히 죽는다 | 단축키를 바꿨는데 안 먹는 혼란 제거 | 저장 시 포스트잇 고정 목록 + `_content_hotkeys()`와 교차 검사 후 경고 | 중 | 하 | `alert_notes/editor_shortcut_settings.py:47`, `alert_notes/postit.py:548` |
| **P2** | 포스트잇 배지 배치가 다중 모니터에서 흔들림 | `_arrange_compact_badges()`가 `window.screen().availableGeometry()` 기준으로 우하단에 세로 정렬. 배지가 화면 높이를 넘으면 `max(area.top()+10, y)`로 전부 같은 y에 겹친다 | 배지 다수 사용 시 겹침 해소 | 열 넘김(다단) 배치로 확장 | 하 | 하 | `alert_notes/panel.py:_arrange_compact_badges` |
| **P3** | 메모 목록에 일괄 색상/포스트잇 전환 없음 | 체크박스는 있으나 일괄 동작은 "선택 삭제"와 "Excel 내보내기"뿐 | 정리 작업 시간 단축 | 선택 항목 일괄 포스트잇 해제/색상 변경 추가 | 중 | 하 | `alert_notes/memo_list.py`, `alert_notes/panel.py` |

**추천 사용자 흐름**

`＋ 새 메모 → 제목·본문 입력(자동저장) → 필요 시 알림 예약 / D-Day / 포스트잇 고정 → 목록에서 알림·D-Day 배지로 상태 확인`

**Quick Win**

- `MemoSearchDialog` 미리보기에 `plain_text_from_content()` 적용
- `_restore_draft`에 `hotkey`, `hotkey_action` 복원 추가
- 메모 목록에 알림/D-Day 배지 열 추가 (데이터는 이미 조회 중)

**중장기**

- 편집기 안 다중 알림 목록 UI
- 자동저장 경로에서 캘린더·알림내역 재조회 분리
- 본문 검색을 평문 기준으로 정규화

**추가 테스트 필요**

- 리치 텍스트 메모에서 태그 문자열이 검색되지 않는지
- 수동 저장 모드에서 단축키 지정 후 강제 종료 → 초안 복원에 단축키가 포함되는지
- 빈 새 메모 창을 닫았을 때 목록에 남지 않는지
- 포스트잇 20개 열린 상태에서 타이핑 시 프레임 드랍(성능 회귀)

---

### 2.3 알림

**현재 제공되는 주요 기능**: 메모별 다중 알림, 반복(매일/평일/매주/매월 + 종료일·횟수), 20초 폴링 + 단일 대화상자 큐(`AlertService`), 토마펫 알림 표면 + 기본 대화상자 폴백, 확인/미루기/건너뛰기, 처리 이력(`reminder_history`)과 재예약, 예정·이력 검색과 Excel 내보내기, 일정 미리 알림(분 단위 최대 5개).

| 우선순위 | 개선사항 | 현재 문제와 근거 | 사용자 효과 | 구현 방향 | 난이도 | 회귀 위험 | 관련 파일 |
|---|---|---|---|---|---|---|---|
| **P0** | D-Day 알림이 "알림 해제"로 사라지는 문제 | `clear_reminder(note_id)`가 `occurrence_kind='deadline'` 행을 구분하지 않고 `DELETE`. 재현: 해제 후 `pending: []`, `d_day_at` 유지, `d_day_alert=1` 유지 → 배지·설정은 켜져 있는데 알림은 죽음. `delete_reminder()`(알림내역 선택 삭제), `complete_reminder()`도 같은 구조 | 사용자가 설정한 D-Day 알림이 조용히 사라지지 않음 | ① `clear_reminder`가 deadline 행을 제외하거나 ② 삭제 시 `d_day_alert=0`을 함께 갱신. 스키마 변경 불필요 | 하 | 중 (D-Day·알림 양쪽 경로) | `alert_notes/reminder_store.py:clear_reminder,delete_reminder,complete_reminder`, `alert_notes/sqlite_store.py:set_deadline` |
| **P1** | 일정 미리 알림이 "예정 알림" 목록에 없음 | `ReminderHistoryPanel.refresh_pending`은 `store.pending_reminders()`만 조회 → `reminders` 테이블 전용. `schedule_notifications`로 예약된 일정 알림은 목록·검색·Excel 어디에도 없다. `AlertService.check_now()`만 두 소스를 합친다 | "예약된 알림 전부"를 한 화면에서 확인 | 예정 목록에 일정 알림을 별도 종류(`종류` 열)로 병합 표시. `schedules.due_notifications()`와 `schedule_notifications` 조회를 합치는 어댑터 추가, 스키마 유지 | 중 | 중 | `alert_notes/reminder_history.py:refresh_pending`, `alert_notes/schedule_store.py` |
| **P1** | 처리 이력도 두 갈래 | 메모 알림은 `reminder_history`에 완료/미룸/건너뜀이 기록되지만, 일정 알림은 `schedule_notification_log`에 상태만 남고 이력 탭에 안 나온다 | 일정 알림을 처리했는지 나중에 확인 가능 | 이력 탭에 일정 알림 로그를 읽기 전용으로 병합(테이블 추가 없음) | 중 | 중 | `alert_notes/reminder_history.py:refresh_history`, `alert_notes/schedule_store.py:complete_notification` |
| **P1** | 체크 없이 현재 행에 일괄 작업이 적용됨 | `_selected_ids()`는 체크가 없으면 `table.currentRow()`를 대신 반환. "선택 삭제"를 눌렀을 때 실제로는 강조된 행이 지워진다. 확인 문구는 "선택한 알림 1건" | 의도치 않은 삭제 방지 | 삭제 등 파괴적 동작은 체크 항목만 대상으로 하고, 체크가 없으면 안내 후 중단 (열기/수정 등 비파괴 동작만 현재 행 폴백 유지) | 하 | 하 | `alert_notes/reminder_history.py:_selected_ids,_delete_pending` |
| **P2** | D-Day 알림이 예정 목록에서 구분되지 않음 | 예정 목록에 `occurrence_kind='deadline'` 행이 일반 알림과 똑같이 "반복 없음 · 예정"으로 표시됨(`REMINDER_SELECT`의 `repeat_summary` CASE에 deadline 분기 없음) | 실수로 D-Day 알림을 지우는 일 감소 | `repeat_summary` CASE에 `deadline → 'D-Day'` 분기 추가, 상태 열에 D-Day 표시 | 하 | 하 | `alert_notes/reminder_store.py:REMINDER_SELECT` |
| **P2** | 예정/이력 표에 정렬 기능 없음 | `ReminderHistoryPanel._table()`이 `setSortingEnabled`를 호출하지 않는다. 단축키 목록과 메모 목록은 정렬이 되는데 여기만 안 된다 | 화면 간 조작 일관성 | `setSortingEnabled(True)` + 정렬 인디케이터 | 하 | 하 | `alert_notes/reminder_history.py:_table` |
| **P2** | 알림 대화상자의 미루기 옵션 불일치 | `ReminderAlertDialog`는 10분 고정 1개, `ScheduleAlertDialog`는 10/30/60분 3개, 토마펫 알림은 9개(`test_alert_has_nine_snooze_buttons`). 같은 "미루기"인데 화면마다 다름 | 학습 비용 감소 | 표면 3종의 미루기 옵션을 공통 상수로 통일 | 하 | 중 (토마펫 레이아웃 테스트 존재) | `alert_notes/alert_dialog.py`, `alert_notes/toma_pet_alert.py` |
| **P2** | 반복 알림은 "다음 1건"만 존재 | `generate_next_occurrence`가 완료 시점에 다음 1건만 만든다(`_has_regular_pending` 가드). 그래서 "앞으로 3주 일정"을 미리 볼 수 없고, 캘린더에도 다음 1건만 투영된다 | 반복 알림의 향후 일정 미리 보기 | 저장 방식은 유지하고, 표시용으로만 `next_occurrence`를 N회 전개해 미리보기 목록 제공 | 중 | 중 | `alert_notes/reminder_recurrence_store.py`, `alert_notes/schedule_store.py:migrate_legacy_reminders` |
| **P2** | 앱이 꺼져 있는 동안의 알림 처리 안내 없음 | `due_reminders()`는 `due_at<=now`인 모든 미처리 알림을 반환하므로, 3일 만에 켜면 밀린 알림이 큐에 전부 쌓여 순차로 뜬다(일정 알림은 `notify_at < now - 7일` 컷오프가 있으나 메모 알림은 무제한) | 재실행 시 팝업 폭탄 방지 | "놓친 알림 N건" 요약 화면 1개로 묶고 개별 처리/일괄 확인 제공 | 중 | 중 | `alert_notes/service.py:check_now`, `alert_notes/reminder_store.py:due_reminders` |
| **P3** | 알림 소리·중요도 없음 | 코드 전체에 사운드 재생 경로 없음. 항상 위 대화상자만 | 자리를 비웠을 때 인지 | 선택형 알림음(설정 항목) | 하 | 하 | `alert_notes/service.py`, `settings_dialog.py` |

**추천 사용자 흐름**

`메모 열기 → 알림 예약 펼치기 → 시간·반복 지정 → 알림 저장 → 알림내역 "예정 알림"에서 메모·일정 알림을 한 목록으로 확인 → 알림 뜨면 확인/미루기/건너뛰기 → "처리 내역"에서 재예약`

**Quick Win**

- `clear_reminder`의 deadline 예외 처리 (P0)
- `_selected_ids`의 파괴적 동작 폴백 제거
- `REMINDER_SELECT`에 D-Day 구분 추가
- 예정/이력 표 정렬 활성화

**중장기**

- 예정/이력에 일정 알림 병합 (알림 도메인 통합의 첫 단계)
- 밀린 알림 요약 화면
- 반복 알림 미리보기

**추가 테스트 필요**

- D-Day 알림이 걸린 메모에서 "알림 해제"·"선택 삭제"·"완료" 후 `d_day_alert` 정합성
- 앱을 3일 정지 후 재시작했을 때 큐 동작
- 일정 알림이 예정 목록·Excel에 포함되는지
- 미루기 후 재알림 시각 정확도(반복 시리즈 있는 경우 / 없는 경우 분기)

---

### 2.4 D-DAY

**현재 제공되는 주요 기능**: 메모당 1개(`notes.d_day_at` / `d_day_label` / `d_day_alert`), `DeadlineDialog`로 설정·해제, 편집기·독립창·포스트잇에 D-n 배지(60초 갱신), 알림 체크 시 `occurrence_kind='deadline'` 알림 생성, 토마펫 알림에 "D-Day 알림" 배지.

| 우선순위 | 개선사항 | 현재 문제와 근거 | 사용자 효과 | 구현 방향 | 난이도 | 회귀 위험 | 관련 파일 |
|---|---|---|---|---|---|---|---|
| **P0** | D-Day 알림 유실 (2.3 P0과 동일 원인) | `clear_deadline`은 알림을 정확히 지우지만, 반대 방향(알림 쪽에서 삭제)은 `d_day_alert`를 되돌리지 않는다. 재현 완료 | 설정과 실제 동작 일치 | 위 항목과 같은 수정으로 해소 | 하 | 중 | `alert_notes/sqlite_store.py:312-362`, `alert_notes/reminder_store.py` |
| **P1** | **D-DAY 전용 구역 신설** (요청 사항) | 현재 D-Day는 메모에 종속된 배지일 뿐, 목록·요약 화면이 전혀 없다. 조회 경로는 `note()` / `notes()`뿐이고 D-Day만 뽑는 쿼리도 없다. 여러 D-Day가 있어도 각 메모를 열어야만 확인된다 | "남은 날짜"를 한눈에 확인 — 이 기능의 본래 목적 | `notes WHERE d_day_at != '' AND deleted_at=''` 조회 1개 추가(스키마 변경 없음). 표시 위치는 `CalendarPanel`의 `navigation` 프레임(현재 정적 라벨만 있는 죽은 공간)에 붙이면 신규 레이아웃 없이 가능 | 중 | 하 (읽기 전용 추가) | `alert_notes/sqlite_store.py`, `alert_notes/calendar.py:47-80`, `alert_notes/deadline.py` |
| **P1** | 캘린더에 D-Day가 표시되지 않음 | 알림을 켠 D-Day만 `reminders` → `schedule_items` 미러를 통해 **메모 제목의 30분짜리 일반 일정**으로 나타난다. 알림을 끄면 캘린더에 흔적조차 없다. D-Day인지 알 수 없고 종일 표시도 아니다 | 마감일을 캘린더에서 인지 | D-Day 전용 구역과 함께, 월간 뷰에 D-Day 마커를 읽기 전용으로 표시 | 중 | 중 | `alert_notes/calendar_month_view.py`, `alert_notes/schedule_store.py:migrate_legacy_reminders` |
| **P2** | 지난 D-Day가 영구히 D+n으로 남음 | `countdown_text()`는 과거 날짜에 `D+n`을 계속 반환하고 자동 정리 로직이 없다. 알림은 한 번 소비되지만 배지는 계속 남는다 | 포스트잇·목록의 노이즈 제거 | 목록에서 지난 D-Day를 분리 표시하고 "정리" 동작 제공. 자동 삭제는 하지 않음 | 하 | 하 | `alert_notes/deadline.py:20-45` |
| **P2** | D-Day 진입점이 메모 편집기에만 있음 | `deadline_requested` 연결은 편집기·독립창·포스트잇 옵션 메뉴 3곳뿐. 캘린더·알림내역·메모 목록에서는 D-Day를 만들 수 없다 | 접근성 | 전용 구역에 "+ D-Day" 추가(내부적으로 메모 생성 후 `set_deadline`), 메모 목록 우클릭 메뉴 | 중 | 하 | `alert_notes/panel.py:459`, `alert_notes/calendar.py` |
| **P2** | 메모당 D-Day 1개 제한이 UI에 설명되지 않음 | `set_deadline`은 항상 기존 값을 덮어쓴다. 두 번째 D-Day를 만들려면 메모를 새로 만들어야 하는데 안내가 없다 | 기대 불일치 해소 | 대화상자에 "메모당 하나" 안내 문구 (스키마는 그대로) | 하 | 하 | `alert_notes/deadline.py:64-100` |
| **P3** | D-Day 반복(매년) 없음 | `set_deadline`에 반복 개념이 없어 생일·기념일에 매년 다시 설정해야 한다 | 반복 마감 관리 | `schedule_items`의 `yearly` 규칙을 활용하는 별도 흐름 검토 (구조 변경 필요, 후순위) | 상 | 중 | `alert_notes/schedule_recurrence.py`, `alert_notes/sqlite_store.py` |
| **P3** | D-Day가 Excel 내보내기에 없음 | `export_memos()` 컬럼은 `번호/표시/제목/수정 시간/내용`뿐 | 외부 공유 | 메모 Excel에 D-Day·라벨 열 추가 | 하 | 하 | `alert_notes/panel.py:export_memos` |

**추천 사용자 흐름**

`D-DAY 구역에서 + 추가 → 이름·목표 시각·알림 여부 → 구역에 D-n 정렬 표시(임박순) → 클릭하면 연결된 메모 열기 → 지난 항목은 접힌 영역으로`

**Quick Win**

- 전용 구역 1차 버전(읽기 전용 목록 + 클릭 시 메모 열기) — 새 쿼리 1개 + 위젯 1개
- 알림 목록에서 D-Day 행 구분 표시
- D-Day 대화상자에 "메모당 1개" 안내

**중장기**

- 캘린더 월간 뷰의 D-Day 마커
- D-Day 반복

**추가 테스트 필요**

- D-Day 목록 쿼리가 삭제된 메모를 제외하는지
- `d_day_at`이 과거/오늘/미래일 때 `countdown_text` 경계값 (기존 `test_countdown_and_badge_cover_before_today_and_after`가 일부 커버)
- 알림 켠 D-Day를 해제했을 때 캘린더 미러 항목까지 함께 사라지는지

---

### 2.5 일정

**현재 제공되는 주요 기능**: 일간/주간/월간/목록 4뷰, 슬롯 클릭·드래그 생성, 드래그 이동 + 1회 되돌리기, 일정/할 일 종류, 5분류·우선순위·완료 상태, 반복(매일/매주/매월/매년 + 간격/요일/종료일/횟수), 반복 예외(이번 건만 이동/건너뛰기), 미리 알림(분 단위 5개), 메모 연결, 일정별 전역 단축키, 전체 화면 대화상자, 7일 휴지통.

| 우선순위 | 개선사항 | 현재 문제와 근거 | 사용자 효과 | 구현 방향 | 난이도 | 회귀 위험 | 관련 파일 |
|---|---|---|---|---|---|---|---|
| **P1** | 00:00~07:59 일정이 사라짐 | `render_range`가 `setRowCount(16)` + `range(8,24)` 고정, `row = due.hour - 8`, 범위를 벗어나면 `continue`. `_slot_datetime`도 `time(hour=row+8)`. 06:30 일정 재현 결과 `rendered=False`. 저장은 되지만 일간/주간에서 보이지도, 그 시간대에 새로 만들 수도 없다 | 새벽/이른 아침 일정 사용 가능 | 24시간 렌더로 확장하고 기본 스크롤 위치를 08:00에 맞춤. 범위를 벗어난 항목이 있으면 "위쪽에 N건" 안내 | 중 | 중 (셀 좌표 계산·스팬·드래그가 모두 이 오프셋 사용) | `alert_notes/calendar_timeline_view.py:44-79`, `alert_notes/calendar.py:_slot_datetime,_finish_drag` |
| **P1** | 알림 미러 항목의 편집이 소리 없이 폐기됨 | `sync_legacy_reminder`가 `DELETE` 후 `migrate_legacy_reminders()`로 재삽입. 재현 결과 제목·상세·분류·미리 알림 전부 소실, id도 변경. 게다가 `migrate_legacy_reminders`는 **모든** pending 알림을 매번 순회해 알림 1건 변경마다 O(N) 비용 | 캘린더에서 다듬은 내용이 사라지지 않음 | 미러 항목을 편집 불가로 명시(읽기 전용 표시 + 안내)하거나, 해당 `source_reminder_id` 1건만 갱신하도록 부분 동기화. 어느 쪽이든 `source_reminder_id UNIQUE` 스키마 유지 | 중 | 중 | `alert_notes/schedule_store.py:migrate_legacy_reminders,sync_legacy_reminder` |
| **P1** | 미러 항목의 이동·삭제가 이유 없이 무시됨 | `_finish_drag`는 `item["source_reminder_id"] is not None`이면 `return`(아무 메시지 없음). `delete_item`은 SQL 자체가 `AND source_reminder_id IS NULL`이라 조용히 0행 갱신. `ScheduleEditor`는 삭제 버튼만 비활성화하고 설명이 없다 | "왜 안 되지"가 사라짐 | 미러 항목 선택 시 "메모 알림에서 만들어진 일정입니다. 메모에서 수정하세요" 안내 + 메모로 이동 버튼 | 하 | 하 | `alert_notes/calendar.py:_finish_drag`, `alert_notes/schedule_store.py:delete_item`, `alert_notes/schedule_editor.py:load_item` |
| **P1** | 캘린더에 검색·필터가 전혀 없음 | `CalendarPanel`에 검색 입력이 없다. 좌측 카테고리 목록(`"●  업무"` 등)은 `QLabel`이라 클릭 불가 — 필터처럼 생겼지만 아무 동작도 안 한다. 검색은 전역 단축키(`Ctrl+Alt+M`)의 `MemoSearchDialog`로만 가능 | 일정이 쌓였을 때 찾기 | 카테고리 라벨을 체크 가능한 필터로 전환하고 검색창 추가. 저장소에는 `schedules.search()`가 이미 있다 | 중 | 중 | `alert_notes/calendar.py:70-77,refresh`, `alert_notes/schedule_store.py:search` |
| **P2** | 일정 Excel 내보내기가 없음 | 메모(`export_memos`)·예정 알림·처리 내역에는 있는데 일정만 없다 | 기능 일관성 | `export_table_xlsx()` 재사용 (헤더: 제목/종류/시작/종료/분류/우선순위/반복/상태/연결 메모) | 하 | 하 | `alert_notes/calendar.py`, `alert_notes/excel_export.py` |
| **P2** | 미리 알림 입력 오류 메시지가 파이썬 예외 | `ScheduleEditor.values()`가 `int(value)`를 그대로 호출. "10분"이라고 입력하면 `_save`의 `except`가 `invalid literal for int() with base 10: '10분'`을 그대로 보여준다 | 입력 오류 복구 | 파싱 실패 시 한국어 안내 + 해당 필드 포커스 | 하 | 하 | `alert_notes/schedule_editor.py:values,_save` |
| **P2** | 반복 요일 오타가 조용히 무시됨 | `_parse_weekdays()`가 인식 못 하는 토큰을 그냥 버린다. "월,수,금요일"에서 오타가 나면 요일 목록이 비고, `_period_candidates`가 앵커 요일로 폴백해 사용자가 의도한 것과 다른 반복이 만들어진다 | 잘못된 반복 방지 | 자유 입력 대신 요일 토글 버튼(값 형식은 `[0..6]` 그대로 유지) | 중 | 중 | `alert_notes/schedule_editor.py:_parse_weekdays,_recurrence_values` |
| **P2** | "종일"과 "우선순위"가 저장만 되고 쓰이지 않음 | `all_day`는 `_normalized_item`에서 저장되지만 `render_range`·`_fill_agenda`·월간 뷰 어디서도 참조하지 않는다. `priority`도 저장 전용 | 설정한 값이 화면에 반영됨 | 종일 일정은 상단 고정 행, 우선순위는 색 강도/아이콘. 스키마·저장값 그대로 사용 | 중 | 중 | `alert_notes/calendar_timeline_view.py`, `alert_notes/calendar_month_view.py` |
| **P2** | 되돌리기가 1회·5초 제한 | `_last_move`가 값 1개만 보관하고 `QTimer.singleShot(5000, self.move_status.hide)`로 상태 바가 사라진다. 버튼은 비활성만 되고 이력은 남지 않는다 | 실수 복구 | 최근 이동 이력 N개 스택 또는 Ctrl+Z 지원 | 중 | 중 | `alert_notes/calendar.py:_finish_drag,_undo_last_move` |
| **P2** | 목록 뷰가 anchor+60일 고정 | `_range()`의 `list` 분기가 `anchor ~ anchor+60일`. 지난 일정을 목록으로 볼 수 없고 기간 선택도 없다 | 지난 일정 확인 | 기간 선택(이번 주/이번 달/지난 30일/전체) 추가 | 하 | 하 | `alert_notes/calendar.py:_range` |
| **P3** | 시간대 겹침 표시가 텍스트 누적 | 같은 셀에 겹치면 `cell.setText(cell.text() + 개행 + line)`으로 한 칸에 쌓고, 클릭은 `ids[0]` 즉 첫 항목만 연다 | 겹친 일정 선택 가능 | 셀 안 항목 선택 팝업 또는 좌우 분할 렌더 | 상 | 중 | `alert_notes/calendar_timeline_view.py:60-79`, `alert_notes/calendar.py:_cell_clicked` |

**추천 사용자 흐름**

`캘린더 탭 → 기간 이동 → 빈 슬롯 드래그로 시간 지정 → 제목 입력 → (추가 설정에서 반복·미리 알림·메모 연결) → 저장 → 드래그로 조정, 필요하면 되돌리기`

**Quick Win**

- 미리 알림 / 요일 입력 오류 메시지 한국어화
- 미러 항목에 "메모에서 만든 일정" 안내 배지
- 일정 Excel 내보내기 추가
- 목록 뷰 기간 선택

**중장기**

- 타임라인 24시간화 (좌표 계산 전반 검토 필요)
- 캘린더 검색·카테고리 필터
- 미러 동기화 구조 정리(부분 갱신 또는 읽기 전용 확정)

**추가 테스트 필요**

- 00:00~07:59 일정의 렌더·생성·드래그 (현재 테스트 0건)
- 미러 항목을 편집한 뒤 원본 알림을 변경했을 때의 데이터 보존
- 종일 일정과 자정을 넘는 일정의 렌더링
- 반복 + 예외(이동/건너뛰기)가 걸린 항목의 미리 알림 발화 시각
- 일정 200건 이상에서 `items_for_range` 응답 시간

---

## 3. 기능 간 통합 개선

### 3.1 단축키 → 메모 열기 및 포스트잇 제어

`_run_note_hotkey`(main_window.py:2744)가 `hotkey_action`에 따라 `open_standalone_note` / `toggle_note_postit`으로 분기하며, 이 부분은 **잘 동작한다**.

문제는 세 가지다.

1. 독립 편집창은 **단 하나**(`self.standalone_window`)라 메모 3개에 "메모 열기" 단축키를 걸어도 같은 창의 내용이 바뀔 뿐이다.
2. 등록 시점의 `row`를 람다에 캡처하므로 `shortcuts_changed` 재등록이 누락되면 옛 동작으로 실행된다.
3. 메모 자동저장마다 전체 재등록이 일어나 그 짧은 순간 단축키가 먹지 않는다(2.1 P1).

**권장**: 재등록을 변경 감지 기반으로 바꾸고, "메모 열기"는 다중 창 또는 명시적 단일 창 안내 중 하나로 확정.

### 3.2 메모 → 알림 예약 및 반복 설정

편집기 안에서 시간·반복을 지정해 저장하는 흐름 자체는 완결되어 있다(`_save_reminder` → `save_reminder_for`). 다만

1. 이미 알림이 있으면 `ExistingReminderDialog`가 매번 떠서 "추가/변경"을 물어보는데, 편집기에는 기존 알림 목록이 없어 무엇을 변경하는지 모른 채 선택해야 한다.
2. `set_note()`가 `editing_reminder_id`를 항상 `None`으로 리셋해 메모를 다시 열면 "변경" 문맥이 사라진다.

**권장**: 편집기 알림 카드에 그 메모의 예정 알림 목록을 넣고, 목록 항목을 고르면 `set_reminder()`로 편집 문맥이 잡히게 한다.

### 3.3 메모 → 일정·캘린더 연결

연결 경로가 **두 개인데 서로 모른다.**

- 명시적 연결: `ScheduleEditor`의 "연결 메모" 콤보 → `schedule_items.note_id`. 캘린더에서 Enter/더블클릭하면 `_activate_cell`이 메모를 연다.
- 암묵적 미러: 메모 알림 1건 = 캘린더의 30분 일정(`source_reminder_id`).

미러는 사용자가 만든 적이 없는 일정처럼 보이고, 편집하면 지워지며(2.5 P1), 삭제도 안 된다. 게다가 **반복 알림은 다음 1건만 미러링**되므로 "매주 회의" 메모 알림이 캘린더에는 한 칸만 나온다. 반대로 메모 편집기에서는 연결된 일정을 볼 수 없다.

**권장**: 미러 항목을 시각적으로 구분(자물쇠 배지 + "메모에서 열기")하고, 편집기 쪽에도 "연결된 일정" 표시를 추가. 미러 생성 로직 자체는 데이터 보존을 위해 유지.

### 3.4 알림 → 메모 또는 일정 다시 열기

`ReminderAlertDialog`는 "메모 열기"만, `ScheduleAlertDialog`는 `note_id`가 있으면 메모, 없으면 일정을 여는 "열기" 버튼 하나다. 토마펫 알림은 또 다른 버튼 구성이다. 세 표면의 동작 어휘가 다르다. 알림내역에서는 "메모 열기"는 있으나 **일정 열기가 없다**(`_open_note`만 존재).

**권장**: 세 표면의 버튼 세트(확인/미루기/건너뛰기/열기)를 공통 정의로 통일하고, 알림내역에 "일정 열기"를 추가.

### 3.5 알림 → D-DAY 생성 (전용 구역 분리)

현재 D-Day는 알림의 특수 케이스(`occurrence_kind='deadline'`)로 구현되어 있어 **알림 쪽 조작이 D-Day 상태를 깨뜨린다**(2.3 / 2.4 P0, 재현 완료). 알림내역에서는 D-Day 행이 일반 알림과 구분되지 않고, D-Day를 만드는 경로도 알림 화면에는 없다.

요청하신 **"D-DAY를 별개의 조그만 구역으로 분리"** 는 다음과 같이 스키마 변경 없이 구현 가능하다.

1. `notes WHERE d_day_at != '' AND deleted_at = ''` 조회 1개 추가 (`alert_notes/sqlite_store.py`)
2. `CalendarPanel`의 `navigation` 프레임 — 현재 클릭도 안 되는 정적 카테고리 라벨만 있는 죽은 공간(calendar.py:70-77) — 하단에 D-Day 카드 배치. 1180px 미만에서 숨겨지므로 좁은 화면 대응도 기존 규칙을 그대로 탄다
3. 항목: `D-n · 이름` 임박순 정렬, 클릭 → 연결 메모 열기, 지난 항목은 접힘
4. `+ D-Day` → 메모 생성 후 `DeadlineDialog` 재사용
5. 알림 목록에서 D-Day 행은 종류 열로 구분하고, 파괴적 동작 시 D-Day 해제임을 명시

### 3.6 휴지통·복원 → 단축키 / 메모 / 알림 / 일정 데이터 일관성

`show_trash()`는 세 저장소의 삭제 항목을 하나의 표로 모아 잘 처리한다. 다만 일관성 문제가 있다.

| 항목 | 삭제 방식 | 복원 결과 | 문제 |
|---|---|---|---|
| 단축키 작업 | 소프트 삭제 | `active=0`으로 복원 | 사용자에게 안내 없음 → "복원했는데 안 됨" |
| 메모 | 소프트 삭제 + 연결 일정 동반 삭제 | 같은 `deleted_at` 스탬프의 일정도 함께 복원 | 정상. 알림은 `notes.deleted_at=''` 필터 덕분에 복원 시 되살아남(설계상 OK) |
| 일정 | 소프트 삭제 | 정상 | **미러 항목은 애초에 삭제 불가**라 휴지통에 안 나타남 |
| 알림 | **하드 삭제** (`DELETE FROM reminders`) | 복원 불가 | 휴지통에 없음 — 3개 도메인 중 알림만 복구 수단이 없다 |
| 처리 이력 | **하드 삭제** ("전체 삭제" 포함) | 복원 불가 | 확인 대화상자 1개뿐 |

**권장**: ① 복원 결과(비활성 여부 포함)를 명시 ② 삭제 시각을 `display_datetime()`으로 표시 ③ 휴지통에 검색·종류 필터·"지금 영구 삭제" 추가 ④ 알림 삭제는 최소한 되돌리기 1회 제공. `purge_expired_trash`가 시작 시점에만 도는 것도 장기 실행 시 문제이므로 주기 실행 검토.

### 3.7 검색 · 필터 · Excel 내보내기 · 백업의 일관성

| 화면 | 검색 | 필터 | 정렬 | Excel 내보내기 | Excel 가져오기 |
|---|---|---|---|---|---|
| 단축키 작업 | O | O (상태·유형) | O | O | O (무확인 전체 교체) |
| 메모 목록 | O (HTML 포함 검색) | X | O | O (D-Day·알림 열 없음) | X |
| 캘린더 | X | X (라벨만 있음) | — | X | X |
| 예정 알림 | O | X | X | O (일정 알림 누락) | X |
| 처리 내역 | O | X | X | O | X |
| 휴지통 | X | X | X | X | X |

백업 쪽도 어긋난다.

- `_rotate_backups`는 `*_backup_*.json` 글롭을 쓰는데 수동 전체 백업 파일명은 `backup_{now}.json`(앞에 언더스코어 없음)이라 **한 번도 회전되지 않고 무한 누적**된다. `before_restore_*.json`도 마찬가지다. 실제 `data/` 폴더에 `backup_20260727105848.json` 등이 남아 있는 것이 그 증거다.
- `import_backup`의 확인 문구는 *"현재 데이터를 교체합니다. 복원 전 현재 상태를 자동 백업할까요?"* 인데, **"아니오"를 누르면 백업을 건너뛰는 게 아니라 복원 전체가 취소**된다. 질문과 동작이 다르다.

**권장**: 검색·정렬을 최소 공통선으로 맞추고, `_rotate_backups` 글롭을 `*backup_*.json`으로 정정, 복원 확인 문구를 "복원하시겠습니까?"로 바꾸고 안전 백업은 무조건 수행.

---

## 4. 최종 우선순위 목록

| 전체 순위 | 우선순위 | 기능 | 개선사항 | 추천 이유 | 예상 난이도 | 선행 작업 |
|---|---|---|---|---|---|---|
| 1 | P0 | 단축키 | 3-수식키 파서/빌더 정합화 (`Ctrl+Alt+Shift+N`) | 기본 단축키가 영구 등록 실패 + **설정 저장 전체가 차단됨**. 재현 완료. 다른 모든 설정 변경의 선행 조건 | 중 | 없음 |
| 2 | P0 | 알림 / D-DAY | `clear_reminder`·`delete_reminder`가 D-Day 알림을 지우고 `d_day_alert`를 방치 | 사용자가 설정한 알림이 **조용히** 사라진다. 재현 완료. D-Day 전용 구역의 선행 조건 | 하 | 없음 |
| 3 | P0 | 단축키 | Excel 가져오기 확인·미리보기 | 확인 없는 전체 교체 + 휴지통 항목까지 영구 삭제 | 하 | 없음 |
| 4 | P1 | 일정 | 알림 미러 항목 편집 소실 차단 (읽기 전용화 또는 부분 동기화) | 사용자가 캘린더에서 넣은 제목·분류·미리 알림이 통째로 폐기. 재현 완료 | 중 | 없음 |
| 5 | P1 | 단축키 | `bulk_action_toolbar` 노출 (일괄 활성/비활성·실행 테스트) | 이미 구현된 기능이 레이아웃 미추가로 접근 불가. 체크박스 UI가 절반만 동작 | 하 | 없음 (테스트 2건 동시 수정) |
| 6 | P1 | 단축키 | 자동저장 시 전역 단축키 전면 재등록 제거 | 메모 입력 중 단축키가 씹히고 Win32 호출이 폭주 | 중 | 없음 |
| 7 | P1 | 단축키 | 등록 실패 표시 + 재시도 | 실패해도 스위치는 ON이라 원인 파악 불가. 1번 수정 후 남은 실패를 사용자가 볼 수 있어야 함 | 중 | 1번 |
| 8 | P1 | 일정 | 캘린더 타임라인 24시간 확장 | 00:00~07:59 일정이 저장은 되나 표시·생성 불가. 재현 완료 | 중 | 없음 |
| 9 | P1 | 알림 | 일정 미리 알림을 "예정 알림" 목록에 병합 | 예약한 알림의 절반이 어느 목록에도 없다 | 중 | 없음 |
| 10 | P1 | D-DAY | **D-DAY 전용 구역 신설** (요청 사항) | 기능의 본래 목적인 "남은 날짜 한눈에"가 현재 불가능 | 중 | 2번 |
| 11 | P1 | 일정 | 미러 항목 이동·삭제 불가 시 안내 | 아무 반응 없이 무시되어 고장으로 인식됨 | 하 | 4번 |
| 12 | P1 | 알림 | 파괴적 동작의 "현재 행 폴백" 제거 | 체크 없이 삭제를 누르면 강조된 행이 지워짐 | 하 | 없음 |
| 13 | P1 | 메모 | 자동저장 경로에서 캘린더·알림내역 재조회 분리 | 메모가 늘수록 타이핑이 끊김 | 중 | 없음 |
| 14 | P1 | 전체 | 녹화·재생 중 알림 폴링 일시 중지 | 항상 위 알림이 매크로 클릭을 가로챌 수 있음 | 하 | 없음 |
| 15 | P2 | 전체 | 전체 복원 확인 문구 / 백업 회전 글롭 정정 | "아니오"가 백업 거절이 아니라 복원 취소로 동작. `backup_*.json`이 무한 누적 | 하 | 없음 |
| 16 | P2 | 일정 | 캘린더 검색 + 카테고리 필터 활성화 | 좌측 라벨이 필터처럼 생겼지만 죽은 UI. `schedules.search()`는 이미 있음 | 중 | 없음 |
| 17 | P2 | 메모 | 목록에 알림·D-Day 열 추가 | 데이터는 이미 조회 중(`_NOTE_SELECT`) | 하 | 10번 |
| 18 | P2 | 메모 | 수동 저장 초안에 `hotkey` / `hotkey_action` 복원 추가 | 복원 누락으로 단축키 설정이 유실됨 | 하 | 없음 |
| 19 | P2 | 전체 | UI 배율 영속화 + 적용 범위 확대 | 매 실행 100%로 초기화되고 포스트잇에는 적용 안 됨 | 중 | 없음 |
| 20 | P2 | 메모 | 통합 검색 결과의 HTML 원문 노출 수정 | 한 줄 수정, 즉시 체감 | 하 | 없음 |
| 21 | P2 | 알림 | 예정/이력 표 정렬 활성화 + D-Day 행 구분 | 다른 화면과의 조작 일관성 | 하 | 2번 |
| 22 | P2 | 일정 | 미리 알림·반복 요일 입력 오류 한국어화 | 파이썬 예외가 그대로 노출 | 하 | 없음 |
| 23 | P2 | 전체 | 휴지통 시각 포맷·복원 결과 안내·영구 삭제 | 원시 `202608311532` 표시, 복원 후 비활성 미안내 | 하 | 없음 |
| 24 | P2 | 일정 | 일정 Excel 내보내기 추가 | 세 도메인 중 일정만 빠져 있음 | 하 | 없음 |
| 25 | P2 | 메모 | 편집기 안 다중 알림 목록 | 알림 3건이 있어도 1건만 보임 | 중 | 9번 |
| 26 | P3 | 단축키 | 도움말의 하드코딩 단축키 동적 치환 | 안내가 실제 설정과 어긋남 | 하 | 없음 |
| 27 | P3 | 일정 | `all_day` · `priority` 화면 반영 | 저장만 되고 표시되지 않는 설정 | 중 | 8번 |

---

## 5. 단계별 개선 로드맵

### 1단계 — 오류 · 데이터 보호 · 회귀 위험 (전체 순위 1~4, 12, 14, 15)

**추천 작업**: 3-수식키 정합화 / D-Day 알림 유실 차단 / Excel 가져오기 확인 / 미러 편집 소실 차단 / 파괴적 동작 폴백 제거 / 녹화·재생 중 알림 중지 / 복원 문구·백업 회전 정정

**작업 순서**

1. `parse_hotkey` + `HotkeyBuilder`의 수식키 정책을 하나로 맞춘다 → 이 수정 없이는 설정을 저장할 수 없어 뒤 작업의 검증이 불가능하다
2. 같은 커밋에서 `HOTKEY_DEFAULTS` 전 항목이 파서를 통과하는지 단정하는 테스트를 추가한다
3. `clear_reminder` / `delete_reminder` / `complete_reminder`에 deadline 분기를 넣는다
4. `import_excel` · `import_backup`의 확인 흐름을 정리한다
5. `sync_legacy_reminder`의 삭제-재삽입을 손본다
6. `AlertService`에 일시 중지 API를 추가하고 `start_recording` / `run_saved_action`에서 호출

**함께 구현해야 하는 항목**

- 1~2는 반드시 한 묶음 (테스트 더블이 파서를 우회하는 구조를 그대로 두면 같은 버그가 재발한다)
- 3은 D-Day 대화상자의 상태 표시와 함께 (한쪽만 고치면 UI가 여전히 거짓말을 한다)
- 5는 미러 항목 안내 문구(순위 11)와 함께

**검증 방법**

- `python -m pytest -q test_feature_updates.py test_ui_responsive.py test_alert_notes.py test_memo_management.py test_schedule_management.py`
- 신규: 기본값 파서 통과 / 설정 무변경 저장 성공 / D-Day 알림 해제 후 `d_day_alert` 정합성 / Excel 취소 시 무변경
- 실제 실행: 설정 열고 저장 → 성공 확인, `Ctrl+Alt+Shift+N` 실제 키보드 입력
- **기존 사용자 DB 복사본**으로 실행해 기동 시 "실패 N개" 메시지가 사라졌는지 확인

### 2단계 — 자주 사용하는 핵심 흐름 (5~8, 13)

**추천 작업**: 일괄 액션 툴바 노출 / 재등록 최소화 / 등록 실패 표시·재시도 / 타임라인 24시간 / 자동저장 재조회 분리

**작업 순서**

1. `bulk_action_toolbar` 레이아웃 편입 + 관련 테스트 2건 수정 (가장 빠른 체감)
2. `register_hotkeys`를 "변경 감지 후 부분 갱신"으로 분리
3. 그 위에 등록 상태 열·실패 목록·재시도 추가
4. `save_editor_values`의 refresh 범위를 축소
5. 타임라인 24시간화 — `render_range`, `_slot_datetime`, `_finish_drag`, `CurrentTimeDelegate`를 한 번에

**함께 구현해야 하는 항목**

- 2와 3은 같은 리팩터링 (상태를 알려면 부분 등록 결과가 필요하다)
- 4와 2는 같은 병목의 앞뒤 (자동저장 → 재등록 → 재조회가 한 체인이다)
- 5는 드래그 생성/이동/현재 시각 델리게이트가 모두 `hour-8` 오프셋을 공유하므로 분할 불가

**검증 방법**

- 메모 200건 + 일정 200건 시드 후 타이핑 시 프레임 드랍 / DB 쿼리 수 측정
- 00:00 / 07:59 / 08:00 / 23:59 경계 일정의 렌더·클릭 생성·드래그 이동
- 중복 단축키를 일부러 만들어 실패 표시와 재시도가 동작하는지
- 기존 UI 스냅샷(`output/` 하위 PNG)과 비교해 레이아웃 회귀 확인

### 3단계 — 기능 간 연결과 일괄 작업 (9~11, 16, 17, 21, 23, 24)

**추천 작업**: 알림 목록 통합 / D-DAY 전용 구역 / 미러 항목 안내 / 캘린더 검색·필터 / 메모 목록 배지 / 표 정렬 / 휴지통 보강 / 일정 Excel

**작업 순서**

1. 알림 도메인을 읽기 전용으로 통합 (예정 목록 → 이력 → Excel 순)
2. D-DAY 전용 구역 (조회 → 카드 위젯 → `navigation` 프레임 배치 → 추가/열기 동작)
3. 메모 목록 배지 (D-Day 조회를 2단계에서 재사용)
4. 캘린더 검색·필터
5. 휴지통 보강 + 일정 Excel + 표 정렬

**함께 구현해야 하는 항목**

- 1과 5의 Excel은 같은 열 정의를 쓰므로 함께 (예정 알림 Excel에 일정 알림이 빠진 채 남으면 안 된다)
- 2와 3은 같은 조회를 공유
- 4의 카테고리 필터는 `_category_name` 매핑(timeline / editor / nav 3곳에 흩어져 있음)을 하나로 모으는 작업을 포함

**검증 방법**

- D-Day 3건(과거/오늘/미래) + 알림 켠 것 / 끈 것 조합으로 구역 표시·정렬 확인
- 예정 알림 목록에 메모 알림과 일정 알림이 모두 나오고 각각의 처리 동작이 올바른 테이블에 기록되는지
- 1180px / 1080px / 900px / 820px에서 D-Day 구역 표시·숨김 (`test_ui_responsive.py` 확장)
- 기존 백업 JSON 복원 후 새 화면들이 정상 표시되는지

### 4단계 — 편의 기능과 장기 개선 (18~20, 22, 25~27)

**추천 작업**: 초안 복원 보강 / UI 배율 영속화 / 검색 HTML 수정 / 오류 메시지 한국어화 / 편집기 다중 알림 / 도움말 동적화 / `all_day` · `priority` 표시

**작업 순서**

1. 한 줄~수십 줄짜리 정리 묶음 (18, 20, 22, 26)
2. UI 배율 영속화 + 독립창·포스트잇에 스타일시트 전파 (19)
3. 편집기 다중 알림 목록 (25)
4. 종일 / 우선순위 렌더 (27)

**함께 구현해야 하는 항목**

- 19는 `settings_dialog`가 `parent._ui_scale`을 읽는 기존 경로와 함께 검토
- 25는 3단계의 알림 통합 이후에 (통합된 조회를 재사용)

**검증 방법**

- Windows 배율 100% / 125% / 150% / 175%에서 설정·메모·캘린더·포스트잇 렌더 (기존 `output/settings_no_scroll_100.png`, `settings_no_scroll_150.png` 기준선 활용)
- 배율 변경 후 재시작 시 값 유지
- 알림 3건이 걸린 메모에서 목록·수정·해제 각각의 동작

---

## 6. 추가 확인이 필요한 사항

### 6.1 실제 화면 확인 필요

- **`bulk_action_toolbar` 은닉이 의도인지 잔재인지** — `test_ui_responsive.py:103,259`가 `isHidden()`을 단정하고 있어 "의도적으로 숨긴 것"일 가능성이 있다. 다만 `set_selected_active` / `test_action` / `selection_summary`가 살아 있고 다른 진입점이 없어 잔재로 보인다. 원래 의도한 UI 확인 필요.
- **캘린더 좌측 카테고리 라벨의 의도** — 필터로 만들 계획이었는지, 단순 범례인지.
- **1180px 미만에서 `navigation`이 숨겨지므로**, D-DAY 구역을 여기 두면 좁은 창에서 사라진다. 다른 위치가 필요한지 확인 필요.
- **포스트잇 배지 다수(6개 이상) 배치**가 실제 화면에서 겹치는지.
- **`_sync_drawer_placement`의 900~1180px 오버레이 구간**에서 캘린더 서랍이 잘리지 않는지.

### 6.2 실제 키보드 · 마우스 테스트 필요

- **`Ctrl+Alt+Shift+N` 등록 실패**는 코드·파서 수준에서 재현했지만, 실제 Windows `RegisterHotKey` 반환값까지 확인한 것은 아니다. 실 키보드 확인 권장 (기존 `3._TODO.md` 항목과 동일 맥락).
- **`Ctrl+Alt+Esc`(긴급 중지)** — Windows가 이 조합을 시스템용으로 잡는 경우가 있어 실제 등록 성공 여부 확인 필요.
- **일정 드래그 생성·이동의 조작감**, 특히 24시간 확장 후 스크롤 동작 (`3._TODO.md`의 기존 항목).
- **`Ctrl+마우스휠` UI 배율**이 표·캘린더·포스트잇에서 어떻게 보이는지.
- **`Ctrl+Tab` 작업공간 전환**이 포스트잇의 `Ctrl+Tab`(포스트잇 순환)과 충돌하는지 — 두 곳 모두 `WidgetWithChildrenShortcut`이라 포커스에 따라 갈린다.
- **매크로 재생 중 알림 팝업이 실제로 클릭을 가로채는지**.

### 6.3 사용자 선택 필요

- **3-수식키 지원 vs 기본값 하향** (순위 1) — 파서를 확장할지, `Ctrl+Shift+N` 같은 2-수식키 기본값으로 바꿀지. 후자가 훨씬 안전하지만 기존에 3-수식키를 쓰려던 의도가 있었다면 전자.
- **알림 미러 항목의 성격** (순위 4) — ① 캘린더에서 읽기 전용으로 확정 ② 편집 가능하게 만들고 메모 알림과 양방향 동기화 ③ 아예 캘린더에 표시하지 않음. ①이 가장 안전하다.
- **D-DAY 구역의 위치와 크기** — 캘린더 좌측 사이드 / 메모 목록 상단 / 별도 하위 탭.
- **캘린더 타임라인 기본 범위** — 24시간 전체 vs 사용자 지정 업무 시간대.
- **"새 메모" 단축키의 빈 메모 처리** — 즉시 생성 유지 vs 내용 입력 시 생성.
- **밀린 알림 처리 방식** — 순차 팝업 유지 vs 요약 화면.

### 6.4 기존 데이터로 검증 필요

- **현재 `data/alert_notes.db`에 08시 이전 일정이 있는지** — 있다면 사용자가 이미 "사라진 일정"을 겪고 있을 가능성이 있다.
- **`d_day_alert=1`인데 대응하는 deadline 알림이 없는 메모가 있는지** — 이미 유실이 발생했는지 확인.
- **`schedule_items`에 `source_reminder_id IS NOT NULL`인 항목이 몇 건인지**, 그중 사용자가 편집한 흔적이 있는지.
- **`settings`의 `memo_draft_*` 잔여 초안**이 있는지 (수동 저장 모드 사용 이력).
- **`backup/` · `data/`에 누적된 백업 JSON 개수** — 회전이 안 되고 있으므로 용량 확인 필요.
- **저장된 단축키 문자열 중 현재 파서가 거부하는 값이 있는지** — 수식키 3개 이상, 미지원 키 등.

---

## 부록: 검증 로그 요약

이 문서의 P0 / P1 항목 중 다음은 임시 DB 또는 오프스크린 Qt에서 실제로 재현했다. (사용자 데이터 미사용)

| 항목 | 재현 결과 |
|---|---|
| 3-수식키 파싱 거부 | `parse_hotkey("Ctrl+Alt+Shift+N")` → `수식키는 최대 2개까지만 사용할 수 있습니다.` |
| 빌더의 수식키 절삭 | `split_hotkey_text("Ctrl+Alt+Shift+N")` → `(['Ctrl','Alt'], 'N')` |
| 설정 기본값 충돌 | `SettingsDialog` 기본값 → `new_memo_hotkey`가 `Ctrl+Alt+N`으로 표시, `quick_memo_hotkey`와 중복 판정 |
| D-Day 알림 유실 | `clear_reminder` 후 `pending: []`, `d_day_at` 유지, `d_day_alert=1` 유지 |
| 캘린더 08시 이전 누락 | 06:30 일정 → `items_for_range`에는 존재, 타임라인 `row=-2 rendered=False` |
| 미러 항목 편집 소실 | 편집 후 알림 시간 변경 → 제목·상세·분류 초기화, 새 id, `notifications kept: []` |
| 일괄 액션 툴바 은닉 | `bulk_action_toolbar`가 레이아웃 미포함 + `hide()`, 표 위젯 컨텍스트 메뉴 0건 |
| UI 배율 미저장 | `_ui_scale`이 어떤 `set_setting` 경로에도 기록되지 않음 |
