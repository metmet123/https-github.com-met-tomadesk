# 프로젝트 계획

## 현재 상태와 적용 기준 (2026-09-19)

이 표와 `docs/PLAN_REVIEW_2026-09-19.md`를 현재 기준으로 사용한다. 아래 날짜별 승인·검증 수치와 하단 요약은 당시의 이력이며, 오래된 승인 대기 문구를 현재 상태로 해석하지 않는다. 확인하지 않은 구현기간은 제시하지 않는다.

| 작업 | 구현 상태 | 자동 검사 | 실제 환경 확인 | 다음 작업 |
| --- | --- | --- | --- | --- |
| 파일 이름 변경 | 1~4단계 코드 구현, 확인 후 실제 실행 | 결과는 `docs/FILE_RENAME_STAGE4.md` | 탐색기·엑셀·물리 포인터·배율 확인 대기 | 실제 조작 확인과 잔존 TODO |
| 창 고정·단축키 안내 | 구현됨 | 이전 실행 기록 참조 | TODO 대기 | 실창 확인 |
| 창 배치 | 4단계 코드 구현, 전체 보류 | 이전 실행 기록 참조 | 다중 모니터 등 대기 | 보류 해제 후 확인 |
| 메모 Phase 4~7·보완 A~F | 코드 구현됨 | 이전 실행 기록 참조 | 일부 조건부·TODO 잔존 | 제목 필터 후속 |
| 제목 필터 | G-A~G-D 구현됨 | `docs/TITLE_FILTER_STAGE_D.md` | 실창·IME 등 대기 | G-E 카테고리 고정 |
| 화면 글자 따기(OCR) | 3-1·3-2 코드 구현 | 결과는 `docs/SCREEN_OCR_STAGE2.md` | 실제 PDF·혼합 배율·저장 조작 확인 대기 | 실사용 확인 |
| 모바일 조회 | 계획 단계 | 미실행 | 미실행 | OCR 다음 단계와 별도 우선순위 결정 |

## 진행 중인 계획

### 2026-09-19 제목 필터 G-D — 더보기 메뉴 일원화

- 사용자 승인: 다음 단계 진행. 검색 줄의 보기·정렬 콤보는 숨긴 설정 모델로 유지하고, 모든 폭에서 더보기 메뉴로 접근한다.
- 더보기는 항상 칩 줄 오른쪽 끝에 표시한다. 숨은 카테고리와 미지정, 보기 2종, 정렬 5종, 기존 카테고리 설정 진입을 제공한다. 숨은 선택 카테고리 표시가 우선이고, 그 외 보기/정렬이 기본값이 아니면 파란 점을 표시한다.
- 설정 저장/복원 및 기존 목록 전용 갱신 경로를 유지한다. 초기화로 보기/정렬을 바꾸지 않는다. 카테고리 설정은 기존 편집기의 관리 동작에 연결한다.
- 구현/검사는 `docs/TITLE_FILTER_STAGE_D.md`. G-E 고정 카테고리와 G-F 자동완성은 이번 범위가 아니다. 실창 조작과 EXE 갱신은 미완료로 유지한다.

### 2026-09-19 제목 필터 G-C — 버튼·머리말 메뉴·필터 초기화

- 사용자 승인: 다음 단계 진행. G-A/B의 추출·집계·교차 필터를 재사용하고, 버튼 세 상태·빈 상태 도움말·머리말 검색 메뉴·네 가지 필터 초기화를 구현한다.
- 카테고리·검색·기호·머리말만 초기화한다. 보기·정렬·고정·메모는 유지하고, 필터 변경은 목록만 갱신하여 편집 중인 미저장 본문을 다시 읽지 않는다.
- 기존 반응형 보기/정렬 콤보는 공간이 부족할 때 더보기로 이동하는 방식을 유지하되 새 버튼에 맞게 필요 폭을 계산한다. 항상 더보기로 이동시키는 G-D, 카테고리 고정 G-E, 자동완성 G-F는 다음 단계다.
- 자동 검사 및 실제 환경 미확인 항목은 `docs/TITLE_FILTER_STAGE_C.md`에 구분해 기록한다. EXE는 이번 작업에서 갱신하지 않는다.

### 2026-09-19 화면 글자 따기 3-2 — 확인 후 새 메모·D-Day 생성

- 사용자 승인: 다음 단계 진행. 3-1 실사용 미확인 항목을 확인해 유지하고, 결과창에 `메모로 만들기`와 날짜가 있을 때 `D-Day로 만들기`를 추가했다.
- 수정한 OCR 본문을 기준으로 별도 확인창에서 제목과 저장 본문을 검토한다. D-Day는 날짜 후보를 고르고 날짜·시간을 수정한 뒤 승인해야 생성한다. 기존 메모와 편집 중인 미저장 내용은 바꾸지 않는다.
- 기존 한국어 일정 파서를 줄 시작에 적용한다. `마감일:`·`마감:`·`날짜:`·`일시:` 뒤의 날짜도 지원한다. 시간만/반복 표현/잘못된 날짜는 제안하지 않는다. 여러 날짜는 직접 선택하며, 날짜만 있으면 00:00 초기값임을 명시한다. 상대 날짜의 기준은 인식 결과가 표시된 시점에 고정한다.
- 저장은 새 메모와 선택한 D-Day를 같은 INSERT/트랜잭션으로 처리한다. 개별 알림/알림 레코드는 만들지 않는다. 앱의 기존 당일 요약 알림 설정은 유지한다. 취소/제안 거절은 DB 쓰기 없음, 성공한 같은 인식 결과는 중복 저장하지 않는다.
- 확인 범위와 결과는 `docs/SCREEN_OCR_STAGE2.md`. 실제 PDF 캡처·혼합 DPI·물리 조작, EXE 빌드는 이번에도 미완료로 유지한다. 모바일·제목 필터 구현과 보류된 창 배치는 이번 범위가 아니다.

### 2026-09-19 화면 글자 따기 3-1 — 영역 인식·클립보드

- 사용자 승인: 추천 순서(파일 이름 변경 확인 → OCR 3-1)대로 진행. 파일 변경의 임시 파일 자동 검사 123개를 재실행했고, 물리 마우스·탐색기·엑셀 검증은 완료로 표시하지 않았다.
- 구현: `screen_ocr.py`의 모니터별 영역 선택, Windows 내장 한국어 우선/영어 대체 인식, 편집 가능한 결과창·복사·줄바꿈 정리·원문 복원·다시 인식. 기본 단축키 `Ctrl+Alt+O`, 트레이 진입, 설정 상태 안내.
- 보호: 캡처/인식은 외부 전송·디스크 저장 없이 메모리에서 처리한다. 인식은 별도 작업 스레드, 취소·종료 후 늦은 결과 폐기, 빈 결과·오류 때 기존 클립보드 보존. 패키지·언어팩 없음은 OCR만 제한한다. 기존 메모/일정/사용자 DB는 변경하지 않는다.
- 확인 범위: 생성한 한글·영문·숫자 이미지의 실제 Windows OCR, 가짜 다중 모니터 픽셀 매핑, Qt offscreen 조작·배율 검사. 실제 PDF 화면 선택·물리 포인터·혼합 DPI 장비 시험과 EXE 배포는 별도다. 정확한 검사 결과는 `docs/SCREEN_OCR_STAGE1.md`에 기록한다.
- 경계: 메모 만들기·D-Day 제안(3-2), 모바일·제목 필터 후속, 보류된 창 배치는 이번에 구현하지 않는다. 아래 날짜별 'OCR 미착수' 문구는 과거 이력이다.

### 2026-09-18 모바일 연동 0단계 — PC 메모를 휴대폰에서 읽는 개인용 조회 실험판 (계획 확정, 착수 대기)

- 목표: PC에서 작성한 메모를 Google Drive로 보내고, Android에서 검색·읽기·오프라인 조회한다. 실제로 며칠 써 본 뒤 모바일 편집·양방향 동기화로 확장할지 판단한다.
- 이번 단계의 경계: **휴대폰에서 메모를 수정·삭제하지 않고 PC로 데이터를 돌려보내지 않는다.** 한 방향(PC → Drive → 휴대폰) 조회만 만든다.
- 전체 구도: ① 표시 확인 → ② Drive 전송 → ③ Android 조회 앱 → ④ 오프라인·실사용 보완 → (사용 후 판단) → ⑤ 모바일 편집 → ⑥ 양방향 동기화 → ⑦ 자동화·안정화. 0단계는 ①~④다.
- 채택 근거: 2026-09-15 `DECISIONS.md`의 UUIDv4 `sync_id`, `revision`+밀리초 UTC+설치 UUID, `sync_tombstones`, `.tomamemo` v2(UUID 관계·카테고리·주석·템플릿·버전·첨부 경로·SHA-256) 계약을 재사용한다. 단, 전체 백업과 모바일 조회 전송의 포함 범위는 다르므로 기존 v2 백업을 바꾸지 않고 조회용 내보내기/모바일 봉투를 분리한다. 후속 ⑥ 양방향의 충돌 보존(`conflict_of_sync_id`) 계약은 유지한다.
- 기간은 검증되지 않아 제시하지 않는다. 단계별 완료 조건과 확인 결과로 진행 상황을 보고한다.

#### 단계별 범위와 승인 게이트

| 단계 | 범위 | 단계 완료 기준 | 현재 상태 |
| --- | --- | --- | --- |
| 0-1 | 메모 구조 확인과 모바일 표시 실험 | 대표 메모를 휴대폰에서 읽을 수 있고 표시 불가 요소를 식별 | 대기 |
| 0-2 | PC에 `모바일로 보내기` 추가 | 전송 실패해도 직전 성공본을 계속 받을 수 있음 | 대기 |
| 0-3 | 최소 Android 조회 앱 | PC에서 보낸 메모를 휴대폰에서 검색하고 열 수 있음 | 대기 |
| 0-4 | 오프라인 조회와 실사용 보완 | `보내기 → 새로고침 → 오프라인 읽기`가 반복해서 정상 동작 | 대기 |

#### 0-1 메모 구조 확인과 모바일 표시 실험

- 본문 저장 형식, `sync_id`, 메모 계층(`parent_id`), 내부 링크, 첨부 참조 방식을 확인한다.
- 일반 문장·표·토글·이미지·내부 링크가 섞인 대표 메모 5~10개를 고른다.
- 원본 리치 데이터와 **모바일 표시용 HTML**을 함께 내보낸다.
- 전용 주소 변환만으로 표시가 완성되지는 않는다. 정수 ID/v1과 UUID/v2 메모·블록 링크, `toma-note-image://`를 함께 처리하고 원본 대응표를 유지한다. Qt HTML의 블록 ID·접기 상태·제목 끝 경계 메타데이터도 해석한다. 표·이미지·접힌 제목·중첩 토글·내부 링크·누락 첨부의 PC/모바일 비교 결과와 표시 제한을 기록한다.
- 휴대폰에서 글자 크기, 표 가로 스크롤, 토글, 한글 표시를 확인한다.
- 지원하지 못하는 요소는 **숨기거나 지우지 않고 제한 사항으로 표시한다.**

#### 0-2 PC에 `모바일로 보내기` 추가

| 항목 | 동작 |
| --- | --- |
| Google Drive 연결 | 사용할 계정을 고르고 접근을 승인한다 |
| 모바일로 보내기 | 현재 메모 전체를 내보내 업로드한다 |
| 전송 상태 | 진행 중·성공·실패와 마지막 성공 시각을 표시한다 |

- 첫 버전은 변경분 계산 없이 **휴지통을 제외한 현재 메모 전체**를 전송한다. 관련 계층·카테고리·주석·첨부와 링크 식별자는 유지한다. 휴지통·과거 버전·템플릿·알림/일정·실행 이력·동기화 tombstone은 조회용 전송에서 제외한다. 전송 전 포함/제외 항목과 메모·첨부 개수/크기를 보여준다.
- 기존 `.tomamemo` v2 백업은 전체 보존 계약을 유지한다. 현재 백업 함수를 그대로 업로드하면 제외 대상까지 포함되므로 조회용 별도 내보내기 경계를 만든다. 형식 버전·고유 묶음 ID·생성 시각·검증 정보는 모바일 봉투(manifest)에 두고 기존 v2 형식을 임의 변경하지 않는다. DB 읽기 스냅샷과 첨부 무결성 검사로 일관성을 보장한다.
- 기존 `sync_id`와 부모·카테고리·내부 링크 참조를 유지한다.
- 첨부는 정한 크기 한도 안에서 포함하고, 제외한 파일은 목록으로 보여 준다.
- 업로드를 **마친** 새 묶음만 최신본으로 지정하고 직전 성공본을 남긴다.
- 계정 연결을 끊어도 PC의 메모와 첨부는 그대로 둔다.

#### 0-3 최소 Android 조회 앱

| 화면 | 구성 |
| --- | --- |
| 첫 실행 | `Google Drive 연결` → `메모 가져오기` |
| 메모 목록 | 검색창·목록·새로고침·마지막 수신 시각 |
| 메모 본문 | 뒤로가기·제목·상위 메모 경로·리치 본문·첨부 |
| 간단 설정 | 연결 계정·다시 가져오기·기기 저장공간 |

- HTML 표시 화면을 넣어 0-1의 표시 결과를 그대로 쓴다.
- 검색은 우선 제목과 본문 텍스트를 대상으로 한다.
- 내부 링크는 가져온 메모의 `sync_id`로 연결한다.
- 첨부는 내려받은 파일을 열거나 아직 쓸 수 없는 상태를 표시한다.
- 새 묶음은 **다운로드와 검증이 끝난 뒤에** 기존 조회 데이터를 교체한다.
- 오류가 나면 마지막으로 성공한 데이터를 그대로 보여 준다.

#### 0-4 오프라인 조회와 실사용 보완

- 내려받은 본문과 첨부를 기기에 저장한다.
- 비행기 모드에서도 목록·검색·본문·저장된 첨부를 쓸 수 있게 한다.
- PC에서 지운 메모는 다음 전체 묶음을 받을 때 조회 목록에서도 빠진다.
- **`PC 전송 시각`과 `휴대폰 수신 시각`을 나눠 표시**해 지금 보는 내용이 얼마나 오래된 것인지 알 수 있게 한다.
- 며칠 직접 쓰면서 잘림·느린 검색·열리지 않는 첨부 같은 불편을 고친다.

#### 보존 범위

PC의 메모·첨부·일정·단축키 작업과 사용자 DB를 변경하지 않는다. `sync_id`·`revision`·UTC·설치 UUID·`sync_tombstones`·v1/v2 링크·`.tomamemo` 백업 계약을 그대로 유지한다. 계정 연결을 끊어도 PC 데이터는 손대지 않는다.

#### 제외 범위

| 미루는 기능 | 이유 |
| --- | --- |
| 모바일 편집·역방향 업로드 | 조회 활용도와 문서 표시를 먼저 검증한다 |
| 변경분 동기화·실시간 갱신 | 전체 묶음 전송으로 초기 구현을 단순하게 한다 |
| 충돌 비교·해결 | 모바일에서 수정하지 않으므로 이번 범위에 불필요하다 |
| 일정·알림 | 메모 조회에 집중한다 |
| 공개 배포·공동 편집 | 개인 사용 범위를 벗어난다 |

#### 후속 단계와의 관계

0단계의 HTML 표시 코드, Drive 인증·다운로드, 기기 로컬 조회는 후속 단계에서 재사용한다. 다만 **전체 묶음 전송 방식은 ⑥ 양방향 동기화 단계에서 변경 기록 기반 방식으로 교체한다.**

#### 결정이 필요한 사항

- 0-2 착수 전: 첨부 크기 한도를 얼마로 둘지, Drive 계정 인증 방식(설치형 OAuth 클라이언트)을 어떤 형태로 둘지.
- 0-3 착수 전: Android 앱을 무엇으로 만들지(네이티브 Kotlin / 웹뷰 중심 경량 앱). PC 쪽은 Python·PyQt6지만 이 앱은 별도 프로젝트가 된다.

- 상태: 2026-09-18 계획 확정. 착수 순서는 사용자 지시에 따라 **창 고정 · 단축키 안내 오버레이 · 파일 이름 일괄 변경 다음**이다.

### 2026-09-18 데이터 저장 위치 보호 (완료)

- 사고: 프로그램의 데이터 저장 위치가 Windows 임시 폴더(`C:\Users\user\AppData\Local\Temp\tmp9bt5sjai`)로 설정돼 있었다. 메모·일정 본체(`alert_notes.db`), 단축키 DB, 자동 백업이 모두 그 안에서 운영되고 있었다. 임시 폴더는 시스템이나 정리 도구가 지울 수 있어 데이터 전체가 사라질 수 있었다.
- 조치 완료(2026-09-18): 파일 10개를 프로젝트의 `data` 폴더로 복사해 해시로 일치를 확인하고, `storage_paths.json`을 그 경로로 바꿨다. 임시 폴더 원본과 기존 `data/alert_notes.db`(`alert_notes_before_move_20260918.db`)는 지우지 않고 보존했다. 이전 설정 파일은 `storage_paths.json.before_20260918`으로 남겼다.
- 원인 추적 결과: 과거 `test_settings_dday_ux.py`의 설정 저장 검사는 `TemporaryDirectory()` 안의 DB를 쓰는 `MainWindow`에서 `_apply_settings()`를 실행하면서 실제 `save_storage_paths()`를 호출할 수 있었다. 이 호출은 `config_file`을 넘기지 않으므로 `bootstrap_config_file()`이 가리키는 실제 사용자 설정 파일에 임시 데이터 경로를 기록할 수 있는 확인된 코드 경로다. 현재 검사는 `main_window.save_storage_paths`를 patch해 격리되어 있다. 추가로 `Store(data_dir=...)`의 레거시 마이그레이션은 기존 설정에 `backup_dir`가 남아 있으면 기본 설정 파일 경로를 명시 인자로 바꿔 `save_storage_paths()`에 전달하는 조건부 우회가 가능했다. 저장 전에 이 마이그레이션으로 들어가면서 임시 `data_dir`를 주는 저장소 호출은 현재 도구·계획 폴더 스크립트와 Git 이력에서 찾지 못했다. 따라서 두 코드 경로가 이번 사고 때 실제 실행됐는지는 당시 실행 로그가 없어 확정할 수 없다.
- 목표: 원인을 추적하되, 원인을 찾지 못해도 같은 사고가 재발하지 않도록 저장 위치에 보호 장치를 둔다.
- 구현 범위:
  - 저장 위치를 기록할 때 시스템 임시 폴더 하위 경로를 거부한다. 다만 테스트가 명시적으로 설정 파일 경로를 넘기는 경우는 지금처럼 허용해 테스트 격리를 깨지 않는다.
  - 저장 위치를 읽을 때 기록된 경로가 시스템 임시 폴더 하위면 신뢰하지 않고 기본 경로로 되돌린다. 사용자에게 그 사실과 이전 경로를 알린다.
  - 설정 화면에서 임시 폴더를 저장 위치로 고르면 막고 이유를 보여 준다.
- 검증: 임시 폴더 기록 거부, 임시 폴더로 기록된 기존 설정을 읽을 때 기본 경로로 되돌리기, 정상 경로는 그대로 동작, 명시적 설정 파일을 넘기는 테스트 경로가 계속 동작하는지 검사한다.
- 보존: 현재 저장 위치와 사용자 데이터, 기존 저장 위치 변경 기능과 마이그레이션 동작을 유지한다.
- 제외: 데이터 폴더 구조 변경, 자동 백업 정책 변경, EXE 생성.
- 구현 결과:
  - 실제 사용자 설정 파일에 저장할 때 시스템 임시 폴더와 그 하위 경로를 거부한다. 기본 설정을 쓰는 레거시 마이그레이션도 이 검사를 우회하지 않는다. 호출자가 `config_file`을 명시한 격리 경로는 기존 동작을 유지한다.
  - 실제 사용자 설정 파일에서 임시 경로를 읽으면 기본 데이터 폴더로 되돌리고, 무시한 경로를 시작 시 한 번 표시할 수 있는 일회성 조회를 제공한다.
  - 프로그램 시작 시 무시한 이전 경로를 경고창에 그대로 표시한다.
  - 설정 화면에서 정상 경로를 사용 중인 사용자가 임시 폴더를 새 저장 위치로 선택하면 변경하지 않고 이유와 선택 경로를 표시한다.
- 집중 검증 결과 (2026-09-18): `test_storage_path_protection.py` **7 passed**, `test_feature_updates.py` **62 passed**, `test_ux_safety_updates.py` **4 passed**, `test_settings_dday_ux.py` **32 passed**. 실패 0. 요청 범위 밖 전체 회귀는 실행하지 않았다.

#### 통합 회귀 구간 A (2026-09-18)

- 실행 파일(루트 `test_*.py` 중 지정된 12개): `test_alert_note_phase1.py`, `test_alert_notes.py`, `test_calendar_canvas.py`, `test_confirmed_memo_ux.py`, `test_editor_second_revision.py`, `test_feature_updates.py`, `test_format_preset_strip.py`, `test_format_toolbar_compact.py`, `test_ko_schedule_parser.py`, `test_memo_blocks_stage5.py`, `test_memo_character_ranges.py`, `test_memo_drag_stage6.py`.
- 결과: **270 passed, 0 failed** (`156.46s`, pytest 종료 코드 0). 묶음 실행 중 Windows access violation이나 비정상 프로세스 종료가 없어 파일별 독립 재실행은 하지 않았다.
- 범위: 구간 A만 실행했으며 다른 회귀 구간은 실행하지 않았다. 테스트 실패가 없어 코드 수정은 하지 않았다.

#### 통합 회귀 구간 B (2026-09-18)

- 실행 파일 및 파일별 결과(루트 `test_*.py` 중 지정된 12개): `test_memo_heading_preferences.py` **13 passed, 0 failed**, `test_memo_import_archive.py` **13 passed, 0 failed**, `test_memo_layout_contract.py` **12 passed, 0 failed**, `test_memo_management.py` **14 passed, 0 failed**, `test_memo_pages_stage4.py` **39 passed, 0 failed**, `test_memo_phase1_clipboard.py` **6 passed, 0 failed**, `test_memo_phase123_recovery.py` **11 passed, 0 failed**, `test_memo_phase2_blocks.py` **15 passed, 0 failed**, `test_memo_phase3_editing.py` **11 passed, 0 failed**, `test_memo_phase4_identity.py` **16 passed, 0 failed**, `test_memo_remediation_stage_a.py` **5 passed, 0 failed**, `test_memo_remediation_stage_b.py` **10 passed, 0 failed**.
- 결과: **165 passed, 0 failed**. 모든 pytest 프로세스가 종료 코드 0으로 정상 종료했다. access violation 이력이 있는 `test_memo_phase4_identity.py`와 `test_memo_remediation_stage_b.py`는 처음부터 각각 독립된 pytest 프로세스로 실행했으며, 둘 다 비정상 종료 없이 통과해 재시도하지 않았다. 다른 파일에서도 프로세스 비정상 종료가 없어 독립 재시도는 발생하지 않았다.
- 범위: 구간 B만 실행했으며 다른 회귀 구간은 실행하지 않았다. 테스트 실패가 없어 코드는 수정하지 않았고, 창 배치 3.6/3.7단계와 데이터 저장 위치 보호 항목은 이번 기록에서 최종 완료로 변경하지 않았다.

#### 통합 회귀 구간 C (2026-09-18)

- 실행 파일(루트 `test_*.py` 중 지정된 12개): `test_memo_remediation_stage_d.py`, `test_memo_remediation_stage_e.py`, `test_memo_remediation_ui_c1.py`, `test_memo_stage7.py`, `test_memo_stage8.py`, `test_memo_sync_categories_versions.py`, `test_memo_title_filters.py`, `test_memo_toggle.py`, `test_memo_tree_stage2.py`, `test_memo_tree_stage3.py`, `test_memo_ui_compact.py`, `test_memo_usability_scenarios.py`.
- 결과: **311 passed, 0 failed**. 일반 10개 파일 묶음은 **233 passed**로 종료 코드 0, `test_memo_title_filters.py`는 독립 실행에서 **67 passed**로 종료 코드 0이었다. `test_memo_sync_categories_versions.py`는 첫 독립 실행에서 **11 passed** 요약 출력 후 종료 코드 1이 발생했으나 테스트 실패 보고는 없었고, 같은 파일만 독립 재실행해 **11 passed**, 종료 코드 0으로 정상 종료를 확정했다. 이 종료 이상의 원인은 범위 밖이므로 수정하지 않았다.
- 범위: 구간 C만 실행했으며 다른 회귀 구간은 실행하지 않았다. 승인된 세 항목 범위의 테스트 실패가 없어 코드는 수정하지 않았고, 창 배치 3.6/3.7단계와 데이터 저장 위치 보호 항목은 최종 완료로 변경하지 않았다.

#### 통합 회귀 구간 D (2026-09-18)

- 실행 파일(요청문에는 21개로 적혀 있으나 실제 열거된 루트 파일 22개 전부): `test_postit_stage1.py`, `test_postit_stage2.py`, `test_quick_schedule.py`, `test_schedule_dday_stage4.py`, `test_schedule_editor_stage3.py`, `test_schedule_management.py`, `test_schedule_postit_stage2.py`, `test_settings_dday_ux.py`, `test_shortcut_updates.py`, `test_task_list_room.py`, `test_text_format_range_presets.py`, `test_toma_pet_gpt_integration.py`, `test_ui_responsive.py`, `test_update_plan_stage1.py`, `test_update_plan_stage2.py`, `test_user_manual.py`, `test_ux_safety_updates.py`, `test_layout_runner.py`, `test_layout_action.py`, `test_window_layout.py`, `test_window_hide_restore.py`, `test_window_border_accuracy.py`.
- 결과(확인된 사실): **264 passed, 0 failed**. 22개 파일을 각각 독립 pytest 프로세스로 실행했고 모두 종료 코드 0으로 정상 종료했다. 테스트 실패·수집 오류·Windows access violation·그 밖의 프로세스 비정상 종료는 없었다. 따라서 실패 상세는 없다.
- 최종 합산(확인된 사실): 통합 회귀 A **270 passed** + B **165 passed** + C **311 passed** + D **264 passed** = **1,010 passed, 0 failed**. 집중 테스트는 창 배치 3.6단계 **31 passed**, 3.7단계 **41 passed**, 데이터 저장 위치 보호 **105 passed**로 합계 **177 passed, 0 failed**이다. 회귀와 집중 테스트의 중복 재실행을 포함한 누적 검증 결과는 **1,187 passed, 0 failed**이다.
- 완료 판정: 구간 A~D와 세 항목의 집중 테스트가 모두 통과했으므로 창 배치 3.6단계, 창 배치 3.7단계, 데이터 저장 위치 보호를 함께 **완료 (2026-09-18)**로 판정한다. 코드는 수정하지 않았고 창 배치 4단계와 EXE 생성은 착수하지 않았다.
- 미확정 사항(추정과 구분): 실제 Windows 숨김·복원 및 테두리 보정 확인은 사용자 확인 전이며, 임시 폴더 저장 사고의 실제 발생 경로도 당시 실행 근거가 없어 확정되지 않았다. 가능한 코드 경로를 차단한 것은 확인된 사실이지만 그 경로가 실제 사고 원인이었다는 판단은 추정이다.

### 2026-09-17 창 배치 저장·복원 (4단계 코드 완료·실제 창 확인 대기, 2026-09-18 전체 보류)

- 목표: 열려 있는 탐색기 창들의 폴더 경로와 화면 위치를 이름 붙여 저장하고, 전역 단축키 한 번으로 같은 배치를 다시 만드는 기능을 추가한다. 별도 화면을 만들지 않고 기존 단축키 작업의 다섯 번째 작업 유형 `layout`(표시 이름 `창 배치`)으로 넣는다.
- 확정 결정: (1) 기능 이름은 `창 배치`로 한다. 코드의 `workspace`는 이미 상단 `단축키·반복작업 / 메모·일정` 전환을 가리키므로 재사용하지 않는다. (2) 1차 범위는 탐색기 창만 지원한다. (3) 같은 폴더가 이미 열려 있으면 기본값으로 그 창을 재사용하고 위치만 옮기며, 항목별 `항상 새 창으로 열기` 선택을 둔다.
- 채택 근거: `window_restore.py`가 이미 `EnumWindows` 순회와 `Shell.Application` 기반 탐색기 폴더 경로 조회를 하고 있고 `pywin32`가 설치·`shortcut_launcher.spec` hiddenimports에 포함돼 있다. 창 좌표 읽기·쓰기 층과 저장·실행 UI만 추가하면 된다. PowerToys Workspaces 호출 방식은 외부 설치 의존과 토마데스크 밖 편집 때문에 채택하지 않는다.

#### 단계별 범위와 승인 게이트

| 단계 | 범위 | 단계 완료 기준 | 현재 상태 |
| --- | --- | --- | --- |
| 1 | `window_layout.py` 신설: 창 목록 수집과 좌표 이동 | 창 정보 수집·모니터 상대 좌표 변환 단위 검사 통과, 전체 회귀 통과 | 완료 (2026-09-17) |
| 2 | 작업 유형 `layout` 추가와 `지금 열린 창 불러오기` 저장 UI | 저장·재편집·엑셀 왕복과 기존 4개 유형 회귀 통과 | 완료 (2026-09-17) |
| 3 | `ActionRunner` 복원 실행과 단축키 연결 | 재사용·신규 실행·좌표 적용 자동 검사 통과, 실제 2창 복원 확인 | 완료 (2026-09-17, 실제 창 확인은 TODO) |
| 3.5 | 복원 속도 개선 | 창 열기 일괄 선행·후보 축소 후 실제 2창 복원 체감 개선 확인, 전체 회귀 통과 | 완료 (2026-09-18) |
| 3.6 | 창 숨기기·되살리기 | 같은 단축키 토글·트레이 복원·종료 시 자동 복원 검사 통과, 실제 창 확인, 전체 회귀 통과 | 완료 (2026-09-18, 실제 창 확인은 TODO) |
| 3.7 | 테두리 정확도 보정 | 화면 끝에 붙여 저장한 배치가 같은 자리에 빈틈 없이 복원, 기존 저장 배치 호환, 전체 회귀 통과 | 완료 (2026-09-18, 실제 창 확인은 TODO) |
| 4 | 모니터·배율·창 상태 예외 규칙 | 모니터 없음·해상도 변경·DPI 차이·최대화 복원 검사 통과 | 코드 완료, 실제 창 확인 대기 (2026-09-18) |
| 5 | 탐색기 외 일반 프로그램 지원 (선택) | 프로그램+문서 경로 저장·복원 검사 통과 | 대기, 1~4단계 사용 후 결정 |

#### 3.5단계 복원 속도 개선 (2026-09-17 승인)

- 상태: **완료 (2026-09-18)**.
- 배경: 실제 Windows에서 탐색기 2창 복원이 동작하는 것은 확인했으나 폴더가 열리는 속도가 느리다는 사용자 보고가 있었다.
- 원인 진단(코드 구조 근거, 실측 아님): (1) `_run_layout`이 항목마다 `열기 → 대기 → 이동`을 순차로 반복해 탐색기 창 생성 비용이 창 수만큼 쌓인다. (2) 대기 중 0.1초마다 `enumerate_explorer_windows()`가 열린 모든 탐색기 창에 COM으로 폴더 경로를 물어보는데, 항목이 수천 개인 폴더를 읽는 중인 창은 이 응답이 느리다. (3) 창을 완전히 찾은 뒤에야 이동하므로 기본 위치에 떴다가 움직이는 것으로 보인다.
- 구현 범위:
  - 새로 열어야 하는 항목을 먼저 모두 연 뒤, 그다음에 매칭과 이동을 한 번에 처리한다. 기존 창 재사용 항목은 열기 없이 바로 이동한다.
  - 창 찾기를 두 단계로 나눈다. 먼저 가벼운 방법으로 탐색기 최상위 창 핸들 목록을 얻고, 새로 생긴 핸들에 대해서만 COM으로 폴더 경로를 확인한다.
  - 창을 여는 방법을 기존 `path` 유형과 같은 방식으로 바꿀지 검토하고, 바꾸면 그 근거를 기록한다.
  - 항목별 실패 격리, 결과 문구, 실패 시 알림 창 없이 상태줄·이력만 쓰는 기존 동작은 유지한다.
- 2차 보완(2026-09-17 승인): 1차 보완 뒤에도 체감이 느려 대기 경로에서 남은 지연을 걷어낸다.
  - 상태: **구현 완료, 전체 회귀 대기**.
  - 대기 중 Shell COM 조회를 제거한다. 새 탐색기 창 핸들이 보이는 즉시 이동하고, 폴더 경로 확인은 이동 후에 수행해 어긋난 경우에만 바로잡는다. 방금 열려 항목을 읽는 중인 창이 COM 응답을 늦추는 구간을 없애는 것이 목적이다.
  - 창 이동이 대상 창의 처리 완료를 기다리지 않게 한다(`SetWindowPos`의 비동기 처리). 바쁜 창 하나가 다음 항목 처리를 막지 않게 한다.
  - 매칭 확인 간격을 0.1초에서 0.03초로 줄인다.
  - 한계: 탐색기가 폴더 항목을 읽어 표시하는 시간 자체는 줄일 수 없다. 이번 보완은 복원 절차가 덧붙이던 지연만 제거한다.
  - 구현 결과: 대기 루프는 새 탐색기 핸들만 확인하고 발견 즉시 저장 순서의 항목에 임시 배정해 이동한다. Shell COM 경로 조회는 핸들 대기와 최초 이동이 끝난 뒤 수행하며, 실제 경로가 다르면 경로가 맞는 새 핸들로 재배정해 다시 이동한다. `move_window()`는 `SWP_ASYNCWINDOWPOS`를 사용하고 `SetWindowPos`가 요청을 거부한 경우만 기존 이동 실패로 보고한다. 기본 매칭 확인 간격은 0.03초로 변경했다.
  - 2차 보완 검증 결과: `test_layout_runner.py` **11 passed**, `test_layout_action.py` **6 passed**, `test_window_layout.py` **7 passed**. 지정 범위 밖 전체 회귀는 실행하지 않았다.
- 검증: 기존 `test_layout_runner.py` 시나리오(재사용, 신규 실행, 시간 초과, 부분 실패, `always_new`)가 계속 통과해야 한다. 열기 호출이 매칭보다 먼저 일괄 수행되는지, COM 경로 조회가 새 핸들에만 일어나는지를 가짜 공급자로 검사한다. 전체 회귀는 구간을 나눠 실행한다.
- 보존: 저장 payload 형식과 1·2단계 저장 UI, 기존 4개 작업 유형을 변경하지 않는다.
- 제외: 4단계 예외 규칙(모니터 없음·해상도·DPI·최대화)은 이번에 착수하지 않는다.
- 구현 결과: 실행 시작 시 기존 탐색기 핸들과 경로를 한 번 수집해 재사용 대상을 정한 뒤, 새 창이 필요한 경로를 모두 먼저 열고 재사용 창 이동과 신규 창 매칭·이동을 처리한다. 대기 중에는 `EnumWindows`로 `CabinetWClass`·`ExploreWClass` 핸들만 수집하고, 시작 뒤 새로 발견된 핸들만 Shell COM 경로 조회 대상으로 삼는다.
- 열기 방식 결정: 기존 `path` 작업과 같은 Windows 기본 경로 열기 흐름을 사용하고 별도 `explorer.exe` 프로세스 생성 비용을 피하기 위해 `os.startfile(path)`로 변경했다.
- 검증 결과: `python -m pytest -q test_layout_runner.py test_layout_action.py test_window_layout.py` — **21 passed**. 기존 복원 시나리오와 열기 일괄 선행, 새 핸들만 COM 경로 조회하는 집중 검사를 통과했으며 지정 범위 밖 전체 회귀는 대기한다.

##### 3.5 회귀 구간 A

- 실행 방식: 전체 탐색 없이 아래 12개 루트 테스트 파일을 각각 독립 pytest 프로세스로 실행했다.
- 파일별 결과:
  - `test_alert_note_phase1.py`: **16 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_alert_notes.py`: **24 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_calendar_canvas.py`: **21 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_confirmed_memo_ux.py`: **6 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_editor_second_revision.py`: **22 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_feature_updates.py`: **62 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_format_preset_strip.py`: **7 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_format_toolbar_compact.py`: **4 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_ko_schedule_parser.py`: **23 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_blocks_stage5.py`: **60 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_character_ranges.py`: **9 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_drag_stage6.py`: **16 passed**, 실패 0, 프로세스 비정상 종료 없음.
- 합계: **270 passed, 0 failed**. 수집 오류와 프로세스 비정상 종료는 없었으며 독립 재실행이 필요한 파일도 없었다.
- 상태: 구간 A만 완료했다. 3.5단계의 전체 회귀와 최종 완료 처리는 계속 대기한다.

##### 3.5 회귀 구간 B

- 실행 방식: 전체 탐색 없이 아래 12개 루트 테스트 파일을 각각 독립 pytest 프로세스로 실행했다. 과거 access violation 이력이 있는 `test_memo_phase4_identity.py`와 `test_memo_remediation_stage_b.py`도 처음부터 서로 다른 독립 프로세스로 실행했다.
- 파일별 결과:
  - `test_memo_heading_preferences.py`: **13 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_import_archive.py`: **13 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_layout_contract.py`: **12 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_management.py`: **14 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_pages_stage4.py`: **39 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_phase1_clipboard.py`: **6 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_phase123_recovery.py`: **11 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_phase2_blocks.py`: **15 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_phase3_editing.py`: **11 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_phase4_identity.py`: **16 passed**, 실패 0, 독립 프로세스 정상 종료.
  - `test_memo_remediation_stage_a.py`: **5 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_remediation_stage_b.py`: **10 passed**, 실패 0, 독립 프로세스 정상 종료.
- 합계: **165 passed, 0 failed**. access violation으로 집계 불가능한 파일은 없었다.
- access violation 재확인: 실행 중 access violation이 발생하지 않아 재실행한 파일은 없다. 이력 대상 2개 파일은 위와 같이 최초 독립 실행에서 모두 통과했다.
- 3.5단계 범위 수정: **수정 없음** (`action_runner.py`의 `_run_layout`, `window_layout.py` 모두 변경하지 않음).
- 상태: 구간 B만 완료했다. 3.5단계의 전체 회귀와 최종 완료 처리는 계속 대기한다.

##### 3.5 회귀 구간 C

- 실행 방식: 전체 탐색 없이 아래 12개 루트 테스트 파일을 각각 독립 pytest 프로세스로 실행했다. 과거 묶음 실행 시 요약 없이 종료된 이력이 있는 `test_memo_title_filters.py`는 처음부터 단독 실행했다.
- 파일별 결과:
  - `test_memo_remediation_stage_d.py`: **8 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_remediation_stage_e.py`: **7 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_remediation_ui_c1.py`: **5 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_stage7.py`: **35 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_stage8.py`: **54 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_sync_categories_versions.py`: **11 passed**, 실패 0. 최초 실행은 `11 passed` 요약 출력 뒤 종료 코드 `-1073741819`(access violation)로 비정상 종료했으나, 별도 독립 pytest 프로세스 재실행에서 **11 passed**, 종료 코드 0으로 정상 종료해 통과를 확정했다.
  - `test_memo_title_filters.py`: **67 passed**, 실패 0, 최초 단독 실행에서 정상 종료.
  - `test_memo_toggle.py`: **59 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_tree_stage2.py`: **16 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_tree_stage3.py`: **35 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_ui_compact.py`: **13 passed**, 실패 0, 프로세스 비정상 종료 없음.
  - `test_memo_usability_scenarios.py`: **1 passed**, 실패 0, 프로세스 비정상 종료 없음.
- 합계: **311 passed, 0 failed**. 테스트 실패 항목은 없으며, 최초 프로세스 비정상 종료 1건은 위 독립 재실행으로 통과를 확정했다.
- 3.5단계 범위 수정: **수정 없음** (`action_runner.py`의 `_run_layout`, `window_layout.py` 모두 변경하지 않음).
- 상태: 구간 C만 완료했다. 3.5단계의 전체 회귀와 최종 완료 처리는 계속 대기한다.

##### 3.5 회귀 구간 D

- 실행 방식: 전체 탐색 없이 아래 17개 루트 테스트 파일을 한정해 한 pytest 프로세스로 실행했다.
- 파일별 결과:
  - `test_postit_stage1.py`: **13 passed**, 실패 0.
  - `test_postit_stage2.py`: **10 passed**, 실패 0.
  - `test_quick_schedule.py`: **10 passed**, 실패 0.
  - `test_schedule_dday_stage4.py`: **5 passed**, 실패 0.
  - `test_schedule_editor_stage3.py`: **7 passed**, 실패 0.
  - `test_schedule_management.py`: **30 passed**, 실패 0.
  - `test_schedule_postit_stage2.py`: **15 passed**, 실패 0.
  - `test_settings_dday_ux.py`: **32 passed**, 실패 0.
  - `test_shortcut_updates.py`: **30 passed**, 실패 0.
  - `test_task_list_room.py`: **19 passed**, 실패 0.
  - `test_text_format_range_presets.py`: **5 passed**, 실패 0.
  - `test_toma_pet_gpt_integration.py`: **9 passed**, 실패 0.
  - `test_ui_responsive.py`: **10 passed**, 실패 0.
  - `test_update_plan_stage1.py`: **8 passed**, 실패 0.
  - `test_update_plan_stage2.py`: **6 passed**, 실패 0.
  - `test_user_manual.py`: **10 passed**, 실패 0.
  - `test_ux_safety_updates.py`: **4 passed**, 실패 0.
- 합계: **223 passed, 0 failed**. 프로세스 비정상 종료와 독립 재실행이 필요한 파일은 없었다.
- 3.5단계 최종 검증: 집중 검사 **24 passed**와 회귀 구간 A **270 passed**, B **165 passed**, C **311 passed**, D **223 passed**를 합쳐 **993 passed, 0 failed**이다.
- 3.5단계 범위 수정: **수정 없음** (`action_runner.py`의 `_run_layout`, `window_layout.py` 모두 변경하지 않음).
- 상태: 집중 검사와 회귀 구간 A~D에 3.5단계 범위 실패가 없어 **3.5단계 완료**로 판정한다.

#### 3.6단계 창 숨기기·되살리기 (2026-09-18 승인)

- 상태: **완료 (2026-09-18)**. 집중 테스트 31개와 통합 회귀 A~D 통과를 확인했으며 실제 Windows 확인은 TODO에 남긴다.
- 목표: 저장한 창 배치를 닫지 않고 화면에서 감췄다가 즉시 되살린다. 숨긴 탐색기 창은 폴더 내용을 그대로 들고 살아 있으므로 다시 부를 때 폴더를 읽는 대기시간이 들지 않는다.
- 확정 결정: (1) 같은 단축키로 토글한다. 그 배치의 창이 모두 보이는 상태면 숨기고, 아니면 보이기(없으면 새로 열기)로 동작한다. (2) 남의 창 제목 표시줄에 버튼을 덧붙이는 방식은 채택하지 않는다. 겹쳐 그리는 창을 원본의 이동·크기 변경·탭 줄에 계속 맞춰야 해 유지 비용이 크다. (3) 최소화 버튼을 눌렀을 때 숨기는 동작은 2차로 미룬다.
- 안전 계약: 숨긴 창은 토마데스크만 되돌릴 수 있으므로 다음을 기능의 일부로 구현한다. 프로그램 종료 시 숨긴 창을 모두 되살린다. 트레이 메뉴에 `숨긴 창 모두 복원`을 항상 둔다. 숨김 목록을 저장해 다음 실행에서 해당 창이 살아 있으면 복원을 제안한다. 저장된 핸들을 그대로 믿지 않고 창 종류와 폴더 경로를 확인한 뒤 되살린다.
- 구현 범위: 창 숨김·표시 함수(`window_layout.py`), 배치별 숨김 상태 관리와 토글 분기(`action_runner.py`), 트레이 메뉴 항목과 종료 시 복원(`main_window.py`), 숨김 목록 저장(`settings` 키, 스키마 변경 없음).
- 검증: 전부 보임 → 숨김, 전부 숨김 → 보이기, 일부만 남은 상태, 창이 하나도 없는 상태에서 새로 열기, 종료 시 복원, 저장된 핸들이 다른 창으로 바뀐 경우 복원 거부를 가짜 공급자로 검사한다. 실제 Windows에서 숨김·복원과 작업 표시줄·Alt+Tab 표시를 확인한다. 전체 회귀는 구간을 나눠 실행한다.
- 보존: 저장 payload 형식, 저장 UI, 기존 복원 동작과 실패 격리, 기존 4개 작업 유형을 변경하지 않는다.
- 제외: 제목 표시줄 버튼, 최소화 가로채기, 탐색기 외 프로그램, 가상 데스크톱.
- 착수 시점: 3.5단계 전체 회귀를 마치고 3.5단계를 닫은 뒤 시작한다.
- 구현 결과: `window_layout.py`에 Explorer 최상위 창 종류와 저장 폴더 경로를 재검증한 뒤에만 `SW_HIDE`/`SW_SHOW`를 호출하는 숨김·표시 함수를 추가했다. `ActionRunner`는 실제 앱에서 `Store.settings`의 `layout_hidden_windows` 키에 배치별 핸들·경로·숨김 여부를 저장하고, 전부 보이면 숨김, 전부 숨김 또는 일부만 보이면 앱이 숨겼다고 기록한 창을 모두 표시하며, 살아 있는 창이 하나도 없으면 기존 신규 열기·배치 복원 흐름으로 돌아간다. 저장 기록이 없는 숨은 창은 다른 앱 소유일 수 있어 표시하지 않고 새 창 복원 대상으로 처리한다.
- 안전·UI 결과: 트레이 메뉴에 `숨긴 창 모두 복원`을 상시 추가했다. 정상 창 닫기와 트레이 종료는 `closeEvent`에서, 그 밖의 `QApplication.quit()` 경로는 `aboutToQuit`에서 같은 멱등 복원 함수를 호출한다. 시작 시 저장된 숨김 목록 중 핸들 종류와 폴더 경로가 현재도 일치하는 창이 있으면 복원을 제안한다. 검증 실패한 핸들은 표시하지 않고 기록을 유지한다.
- 기존 테스트 동작 변경: 없음. 저장소를 주입하지 않는 기존 3~3.5 실행기 단위 테스트는 종전의 즉시 배치 복원 동작을 유지하고, 실제 앱처럼 settings 저장소가 연결된 경우에만 3.6 토글이 활성화된다.
- 지정 테스트 결과: `test_window_hide_restore.py` **7 passed**, `test_layout_runner.py` **11 passed**, `test_layout_action.py` **6 passed**, `test_window_layout.py` **7 passed**. 총 **31 passed, 0 failed**이며 지정 범위 밖 전체 회귀와 실제 Windows 탐색기 확인은 실행하지 않았다.
- 통합 회귀 최종 결과는 데이터 저장 위치 보호 절의 `통합 회귀 구간 A~D (2026-09-18)` 기록을 참조한다.

#### 3.7단계 테두리 정확도 보정 (2026-09-18 승인)

- 상태: **완료 (2026-09-18)**. 집중 테스트 41개와 통합 회귀 A~D 통과를 확인했으며 실제 Windows 확인은 TODO에 남긴다.
- 증상: 화면 하단·우측 가장자리에 꽉 채워 저장한 배치를 복원하면 좌·우·아래에 약 7픽셀씩 빈틈이 생긴다. 위쪽에는 생기지 않는다.
- 원인(확인됨): Windows 창은 보이지 않는 크기 조절 테두리를 좌·우·아래에 가진다. `GetWindowRect`는 이 테두리를 포함한 값을 돌려주므로 왼쪽 끝에 붙인 창이 `x=-7`로 저장된다(사용자 화면의 저장값 `-7, 0 · 974×1039`). 복원 시 `clamp_rect_to_work_area`가 작업 영역 밖으로 나가는 이 값을 안쪽으로 끌어당기고 크기도 줄여, 보이지 않는 테두리 두께만큼 창이 안쪽으로 밀린다. 위쪽에는 해당 테두리가 없어 증상이 없다.
- 해결 방향: 좌표 기준을 보이는 영역으로 바꾼다.
  - 저장: `DwmGetWindowAttribute`의 `DWMWA_EXTENDED_FRAME_BOUNDS`로 보이는 영역을 읽어 작업 영역 기준 상대 좌표로 저장한다. 이렇게 하면 작업 영역을 넘는 값이 애초에 생기지 않아 끌어당김이 일어나지 않는다.
  - 복원: 목표는 보이는 영역 기준으로 계산하고, 실제 `SetWindowPos`에는 그 창의 테두리 두께(`GetWindowRect`와 확장 프레임 경계의 차이)를 더해 지정한다. 두께는 창마다 배율에 따라 다르므로 복원 시점에 그 창에서 직접 측정한다.
  - 기존 저장값: payload에 좌표 기준 표시를 둔다. 표시가 없는 기존 항목은 예전 기준(창 사각형)으로 적힌 값이므로, 복원 대상 창에서 측정한 테두리 두께로 보이는 영역 기준으로 환산한 뒤 처리한다. 사용자가 다시 저장하지 않아도 맞게 복원되어야 한다.
- 보존: 저장 payload의 나머지 형식, 저장 UI, 3.6단계 숨김·되살리기 동작, 항목별 실패 격리와 결과 문구, 기존 4개 작업 유형을 변경하지 않는다.
- 검증: 보이는 영역 기준 저장·복원 왕복, 화면 끝에 붙인 창이 클램프로 밀리지 않는지, 기존 기준 저장값이 환산되어 같은 자리에 복원되는지, 테두리 두께를 얻지 못하는 창에서 기존 동작으로 되돌아가는지를 가짜 공급자로 검사한다. 실제 Windows에서 가장자리에 붙여 저장한 배치가 빈틈 없이 복원되는지 확인한다.
- 제외: 4단계 예외 규칙(모니터 없음·해상도 변경·DPI 차이·최대화 복원)은 이번에 착수하지 않는다. 다만 이번 변경으로 `clamp_rect_to_work_area`의 의미가 달라지므로 4단계에서 그 규칙을 다시 검토한다.
- 회귀 계획: 3.6단계와 같은 파일을 건드리므로 전체 회귀는 3.7단계 구현 후 한 번에 구간을 나눠 실행한다.
- 구현 결과: 저장 시 `DwmGetWindowAttribute(DWMWA_EXTENDED_FRAME_BOUNDS)`로 보이는 영역을 읽어 작업 영역 상대 좌표로 기록하고 `rect_basis: visible`을 붙인다. DWM 조회 실패 시에는 기존 `GetWindowRect` 값을 사용하되 `rect_basis: window`으로 구분한다. 복원 시 대상 창의 현재 `GetWindowRect`와 확장 프레임 경계를 다시 읽어 좌·상·우·하 테두리를 측정하고, 보이는 영역 목표 좌표를 `SetWindowPos`용 창 사각형으로 환산한다. `rect_basis`가 없는 기존 저장값과 `window` 값은 예전 창 사각형 기준으로 환산하므로 재저장 없이 같은 위치로 복원된다.
- 보존 결과: 최대화 항목도 기존처럼 위치를 적용한 뒤 최대화 상태를 복원한다. 3.5단계의 일괄 열기·새 핸들 COM 조회·비동기 이동·0.03초 간격, 3.6단계 토글, 항목별 실패 격리·결과 문구, 저장 UI와 나머지 payload 필드, 기존 4개 작업 유형은 변경하지 않았다.
- 기존 테스트 기대값 변경: 새로 수집한 창의 저장 payload에 승인된 `rect_basis: visible` 키가 포함되므로 `test_layout_action.py`의 해당 payload 기대값과 Excel 왕복 표본에 이 키를 추가했다. 기존 키·UI 동작·결과 문구와 최대화 이동 기대값은 변경하지 않았다.
- 집중 검증 결과: `test_layout_runner.py` **12 passed**, `test_layout_action.py` **6 passed**, `test_window_layout.py` **11 passed**, `test_window_hide_restore.py` **7 passed**, 신규 `test_window_border_accuracy.py` **5 passed**. 합계 **41 passed, 0 failed**. 지정 범위 밖 전체 회귀, 실제 Windows 탐색기 확인, 4단계, EXE 생성은 수행하지 않았다.
- 통합 회귀 최종 결과는 데이터 저장 위치 보호 절의 `통합 회귀 구간 A~D (2026-09-18)` 기록을 참조한다.

#### 4단계 모니터·배율·창 상태 예외 규칙 (2026-09-18 코드 완료)

- 상태: **코드 완료, 실제 창 확인 대기**. 실제 창 확인 전이므로 완료로 판정하지 않는다.
- 구현 결과: 저장된 모니터를 찾지 못하면 항목 실패 대신 주 모니터 작업 영역 안으로 보정해 복원한다. payload에 `monitor_device`·`work_area`·`dpi`를 저장해 모니터별로 복원하며, 이 필드가 없는 기존 payload는 기존 좌표를 그대로 사용한다. 작업 영역 크기 차이는 비율로 환산하고 실제 모니터 DPI를 조회해 DPI 비로 크기를 보정한다. `maximized`·`minimized` 상태를 복원하며, 탐색기가 새 창 대신 탭을 여는 설정에서는 기존 창 재사용 경로로 처리한다.
- `clamp_rect_to_work_area` 재검토 결론: 작업 영역·DPI 환산을 마친 보이는 영역 좌표에 적용하는 것이 맞으며, 그 뒤 비가시 테두리를 더하는 3.7단계 로직과 순서상 충돌하지 않는다. 최종 사각형이 테두리 두께만큼 작업 영역을 벗어나는 것은 정상이고, 여기서 다시 clamp하면 3.7단계 테두리 정확도가 깨진다.
- 집중 검증 결과: `test_layout_runner.py` **15 passed** + `test_layout_action.py` **6 passed** + `test_window_layout.py` **14 passed** + `test_window_hide_restore.py` **7 passed** + `test_window_border_accuracy.py` **6 passed** = **48 passed, 0 failed**.
- 전체 회귀 결과: `python tools/run_memo_full_regression.py` → **TOTAL modules=60 passed=1031 failed_groups=[('test_memo_sync_categories_versions.py', 3221225477)]**, pytest assertion 실패 **0건**. 창 배치 묶음(GROUP 8/8)은 직전 **27 → 31 passed**로 검사가 4개 늘었다. 전체 수치가 직전 깨끗한 실행 **1035**보다 낮은 것은 이번 실행에서 기존에 알려진 Qt 네이티브 종료로 묶음이 8회 분할되며 집계가 누락된 탓이다.
- 하드웨어 한계: 이 PC의 모니터는 2대 모두 **1920×1080**, 작업 영역 **1032**, DPI **96(100%)**이다. 따라서 DPI 차이 보정은 실제 하드웨어로 재현할 수 없고 단위 검사로만 확인됐다.

#### 변경 범위

- 신규 `window_layout.py`: 보이는 창 목록(`hwnd`, 프로그램 경로, 창 제목, 탐색기 폴더 경로, 모니터 번호, 모니터 기준 상대 좌표·크기, 최대화·최소화 상태) 수집과 창 이동(`GetWindowRect`, `SetWindowPos`, `ShowWindow`), 모니터 열거(`EnumDisplayMonitors`, `MonitorFromWindow`, `GetDpiForWindow`).
- `window_restore.py`: 탐색기 창 열거·폴더 경로 조회 부분을 최소화된 창 조건 없이도 쓸 수 있게 공개 함수로 분리한다. 기존 `restore_minimized_target` 동작과 호출부는 유지한다.
- `main_window.py`: `ACTION_LABELS`에 `layout: 창 배치`를 마지막에 추가하고 `_build_action_pages`에서 macro 페이지 뒤에 `_layout_page()`를 `addWidget`한다(`_sync_stack`과 편집 시 `list(ACTION_LABELS).index(...)`가 순서에 의존하므로 반드시 마지막). `_load_payload`, `_payload`, 목록 표시·필터에 `layout`을 추가한다.
- `action_runner.py`: `run()` 핸들러 표에 `layout: self._run_layout`을 추가한다. 실행 결과 문구는 `N개 창 복원`으로 돌려준다.
- `excel_io.py`: `ACTION_TYPES`에 `layout`을 추가하고 오류 문구와 payload 변환·헤더를 맞춘다.
- `requirements.txt`: 실제로 사용 중인 `pywin32`를 추가한다.
- 신규 테스트 `test_window_layout.py`, `test_layout_action.py`.

#### 저장 형식

기존 작업과 같은 `hotkey_actions.payload` JSON을 쓴다. DB 스키마는 바꾸지 않는다.

```
{"windows": [
  {"kind": "explorer", "path": "D:\제출_", "monitor": 1,
   "rect": [0.0, 0.0, 840, 900], "state": "normal", "always_new": false}
]}
```

`rect`는 해당 모니터 작업 영역 좌상단 기준 상대 좌표와 크기이며, `monitor`는 저장 당시 모니터 번호와 식별 정보를 함께 남긴다.

#### 단계별 구현

- 1단계: 창 목록 수집과 좌표 변환·이동 함수를 만든다. UI와 연결하지 않고 순수 함수 경계를 유지해 모니터 정보·창 정보를 주입할 수 있게 한다.
- 2단계: `창 배치` 유형 페이지를 만든다. `＋ 지금 열린 창 불러오기`를 누르면 현재 탐색기 창을 체크 목록(폴더 이름, 모니터, 위치·크기 요약)으로 보여주고 선택한 항목만 payload로 저장한다. 항목별 `항상 새 창으로 열기` 체크와 순서 변경·행 삭제를 제공한다. 저장·단축키 등록·활성 토글·휴지통은 기존 흐름을 그대로 쓴다.
- 3단계: `_run_layout`을 구현한다. 항목마다 (a) `always_new`가 아니면 같은 폴더를 연 기존 창을 찾아 재사용하고, (b) 없으면 `explorer.exe`로 열고 최대 약 5초 동안 짧은 간격으로 다시 조회해 폴더 경로가 일치하는 새 창을 찾은 뒤, (c) 저장된 모니터·상대 좌표로 옮긴다. 각 창의 실패는 전체를 중단시키지 않고 결과 문구에 모아 보고한다. 반복작업의 중지 단축키 감시 흐름은 건드리지 않는다.
- 4단계: 저장된 모니터가 없으면 주 모니터 작업 영역 안으로 보정하고, 작업 영역 크기가 달라졌으면 비율로 환산하며, 배율이 다르면 DPI 비로 크기를 보정한다. `maximized`·`minimized` 상태를 복원한다. 탐색기가 새 창 대신 탭을 여는 설정이면 기존 창 재사용 경로로 처리한다.
- 5단계(선택): `kind: "program"` 항목을 추가해 실행 파일 경로와 문서 인자를 저장·복원한다. 창 매칭은 프로세스 ID와 실행 파일 경로를 함께 쓴다.

#### 검증 기준

- 1단계: 가짜 창·모니터 목록으로 상대 좌표 환산, 모니터 밖 보정, DPI 환산을 단위 검사한다.
- 2단계: 저장 → 목록 표시 → 재편집 → 엑셀 내보내기·가져오기 왕복에서 payload가 보존되는지 검사하고, 기존 `text`·`url`·`path`·`macro` 편집과 유형 전환(`_sync_stack` 인덱스)이 어긋나지 않는지 회귀 검사한다.
- 3단계: 창 열기·조회를 가짜로 바꿔 재사용·신규 실행·매칭 실패·부분 실패 경로를 자동 검사하고, 실제 Windows에서 탐색기 2창 배치를 저장·복원해 확인한다.
- 4단계: 모니터 없음, 작업 영역 축소, 100%·125%·150% 배율, 최대화 복원 시나리오를 검사한다.
- 모든 단계에서 기존 전체 회귀를 실행하고 실패 0을 확인한다. 실제 창으로만 확인 가능한 항목은 `3._TODO.md`에 분리해 남긴다.

#### 보존 범위

기존 4개 작업 유형의 동작과 payload, 사용자 DB와 저장된 단축키·설정, `restore_minimized_target`을 쓰는 `path` 유형의 최소화 창 복원 동작, 반복작업 녹화·재생과 중지 단축키, 메모·일정 영역을 변경하지 않는다.

#### 제외 범위

가상 데스크톱, 배치를 눈으로 끌어 배치하는 전용 편집기, 창 배치 자동 감지·제안, 브라우저 탭 복원, EXE 생성은 이번 작업에 포함하지 않는다.

#### 추가 확정 결정 (2026-09-17)

- 창 배치도 기존 작업 목록 규칙을 따라 단축키를 필수 입력으로 둔다.
- 복원 실패는 알림 창을 띄우지 않고 상태줄 문구와 실행 이력에만 남긴다. 실패한 창 이름을 결과 문구에 모아 보고한다.
- 설명서(`user_manual_content.py`) 반영은 1~4단계를 마친 뒤 한 번에 한다.
- PowerToys FancyZones식 `정해둔 자리로 창 보내기` 후보의 최신 결정은 아래 `추가 업데이트 / 결정이 필요한 내역`을 따른다.

#### 추가 업데이트 / 결정이 필요한 내역 (2026-09-18)

- **(미확인) 2모니터 복원:** 사용자 보고로 "모니터 2개를 쓸 때 복원하면 창이 모두 주 모니터에 뜬다"는 문제가 있었다. 4단계에서 모니터별 복원을 구현했으나 실제 창으로 확인되지 않았다. 기존에 저장해둔 배치는 모니터 정보가 없어 예전 동작으로 남을 수 있으므로, 확인 시 배치를 새로 저장해 시험해야 한다.
- **(미구현·결정 필요) 저장 범위 선택:** 모니터가 여러 대일 때 주 모니터 배치만 저장할지, 모든 모니터 배치를 저장할지 사용자가 고를 수 있게 한다. 저장 UI에 선택 항목을 추가하는 작업이며 2모니터 복원이 실제로 확인된 뒤 착수한다.
- **(보류 결정) FancyZones식 구역 편집기:** 화면을 구역으로 나눠 두고 창을 배정하는 방식이다. 2026-09-18 사용자 결정으로 개발 부담을 이유로 보류한다. 기존 계획의 `정해둔 자리로 창 보내기` 후보와 같은 건이다. 착수하려면 구역 편집 화면, 구역 저장 형식, 모니터별 구역, 창 배정 규칙이 새로 필요하며 현재 좌표 기반 저장 방식과 성격이 다르다. 진행 시 `/dev-plan`으로 별도 계획을 잡는다.
- **(제안됨·승인 대기) Window Hopper:** 포커스된 앱의 창 사이를 단축키로 순환한다. PowerToys Window Hopper 대체가 목적이다. 설정의 공통 전역 단축키로 넣고 기본값은 `Alt+백틱`·`Alt+Shift+백틱`으로 하되 PowerToys 쪽을 꺼야 한다. 확인된 어려운 지점은 스토어 앱 창을 `ApplicationFrameHost` 공용 프로세스가 소유해 프로세스 기준으로 서로 다른 앱이 묶이므로 실행 파일 경로 기준 판정이 필요하다는 점, 가상 데스크톱의 다른 데스크톱 창이 숨겨진 상태로 남아 걸러내야 한다는 점, 탐색기 탭은 창 단위 기능으로 넘어갈 수 없다는 점이다. 창 순서 기준은 가장 최근 사용 순으로 할 예정이며 PowerToys의 정확한 기준은 확인되지 않았다.
- **(배경 정보) PowerToys 대체 범위:** 이 PC에서 실행 중인 PowerToys 모듈은 FancyZones, AlwaysOnTop, ShortcutGuide, Awake, ColorPicker, Peek, QuickAccess다. 계획서의 `PowerToys 유래 보조 기능 3종`이 AlwaysOnTop·ShortcutGuide를 덮고, FancyZones는 보류, Window Hopper는 신규다. Awake·ColorPicker·Peek·QuickAccess는 아직 계획에 없으며 PowerToys를 완전히 제거하려면 별도 판단이 필요하다.

#### 2단계 검증 재개 기록 (2026-09-17)

- 신뢰 구간: 코드 변경 이후 실행한 `test_window_layout.py`, `test_layout_action.py`, `test_shortcut_updates.py`, `test_feature_updates.py`, `test_ui_responsive.py` 합계 **115 passed**. 이 다섯 파일은 재실행하지 않는다.
- 확인된 사실: 이전 전체 회귀를 실행하던 상위 프로세스(pid 1248)가 종료되면서 Codex thread가 사라졌고, 당시 회귀 명령에는 완료 또는 실패의 최종 기록이 남지 않았다. 중간까지 실패 0이었어도 최종 집계가 없으므로 나머지 루트 테스트는 미검증으로 취급한다.
- 추정 원인: 테스트 파일별 회귀 실행이 길어져 상위 프로세스 수명을 넘긴 것으로 추정한다.
- 재개 방식: 신뢰 구간 5개를 제외한 루트 `test_*.py` 50개를 이름순 5개씩 10구간으로 나누고, 각 구간을 개별 `pytest` 명령으로 실행한다. 구간이 끝날 때마다 파일 목록과 통과·실패 수를 바로 아래에 기록한다. 전체 `python -m pytest -q` 일괄 실행은 Qt 객체 정리 문제로 요약 없이 종료되므로 사용하지 않는다.
- 구간 1/10: `test_alert_note_phase1.py`, `test_alert_notes.py`, `test_calendar_canvas.py`, `test_confirmed_memo_ux.py`, `test_editor_second_revision.py` — **89 passed, 0 failed**.
- 구간 2/10: `test_format_preset_strip.py`, `test_format_toolbar_compact.py`, `test_ko_schedule_parser.py`, `test_memo_blocks_stage5.py`, `test_memo_character_ranges.py` — **103 passed, 0 failed**.
- 구간 3/10: `test_memo_drag_stage6.py`, `test_memo_heading_preferences.py`, `test_memo_import_archive.py`, `test_memo_layout_contract.py`, `test_memo_management.py` — **68 passed, 0 failed**.
- 구간 4/10: `test_memo_pages_stage4.py`, `test_memo_phase1_clipboard.py`, `test_memo_phase123_recovery.py`, `test_memo_phase2_blocks.py`, `test_memo_phase3_editing.py` — **82 passed, 0 failed**.
- 구간 5/10 1차 시도: `test_memo_phase4_identity.py`, `test_memo_remediation_stage_a.py`, `test_memo_remediation_stage_b.py`, `test_memo_remediation_stage_d.py`, `test_memo_remediation_stage_e.py` — 26개 진행 표시 뒤 `test_memo_remediation_stage_b.py::TodaySummaryLifecycleTest::test_note_switch_does_not_shutdown_today_summary`의 `AlertNotesPanel`/`MemoEditor` 생성 중 **Windows access violation**, 종료 코드 1, 최종 통과·실패 집계 없음. 통과로 계산하지 않고 같은 5개 파일을 독립 프로세스로 재검증한다.
- 구간 5/10 확정: 위 5개 파일 — `test_memo_phase4_identity.py`는 파일 단위 실행에서 12개 진행 뒤 13번째 테스트의 `MemoEditor._compact_controls` 중 같은 access violation이 재발했으나, 16개 테스트를 각각 독립 프로세스로 실행해 **16 passed**를 확인했다. 나머지는 각각 **5, 10, 8, 7 passed**. 합계 **46 passed, 테스트 실패 0**. Qt 객체 수명에 따른 프로세스 access violation은 2단계 layout 범위 밖이므로 코드를 수정하지 않고 실행 방식으로 격리했다.
- 구간 6/10 1차 시도: `test_memo_remediation_ui_c1.py`, `test_memo_stage7.py`, `test_memo_stage8.py`, `test_memo_sync_categories_versions.py`, `test_memo_title_filters.py` — 83% 이후 진행 표시만 남기고 종료 코드 1, 최종 통과·실패 요약 없음. 통과로 계산하지 않고 파일별 독립 프로세스로 재검증한다.
- 구간 6/10 확정: 위 5개 파일을 각각 독립 프로세스로 실행해 각각 **5, 35, 54, 11, 67 passed**, 합계 **172 passed, 테스트 실패 0**. 묶음 실행의 요약 없는 종료는 재현되지 않았으며 2단계 layout 범위 밖의 Qt 프로세스 수명 문제로 기록하고 코드는 수정하지 않았다.
- 구간 7/10: `test_memo_toggle.py`, `test_memo_tree_stage2.py`, `test_memo_tree_stage3.py`, `test_memo_ui_compact.py`, `test_memo_usability_scenarios.py` — **124 passed, 0 failed**.
- 구간 8/10: `test_postit_stage1.py`, `test_postit_stage2.py`, `test_quick_schedule.py`, `test_schedule_dday_stage4.py`, `test_schedule_editor_stage3.py` — **45 passed, 0 failed**.
- 구간 9/10: `test_schedule_management.py`, `test_schedule_postit_stage2.py`, `test_settings_dday_ux.py`, `test_task_list_room.py`, `test_text_format_range_presets.py` — **101 passed, 0 failed**.
- 구간 10/10: `test_toma_pet_gpt_integration.py`, `test_update_plan_stage1.py`, `test_update_plan_stage2.py`, `test_user_manual.py`, `test_ux_safety_updates.py` — **37 passed, 0 failed**.

- 상태: 2026-09-17 **2단계 완료**. 코드 변경 이후 신뢰 구간 5개 파일 **115 passed**와 재개 대상 50개 파일 10구간 **867 passed**를 합쳐 루트 테스트 **982 passed, 테스트 실패 0**을 확인했다. 묶음 실행 중 발생한 Qt access violation/요약 없는 종료는 파일별·테스트별 독립 프로세스로 격리해 모든 테스트의 통과 결과를 확인했으며 layout 범위 밖 코드는 수정하지 않았다. 3단계 `_run_layout` 복원 실행은 착수하지 않았다.

#### 3단계 구현 계획 및 검증 (2026-09-17)

- 목표: `ActionRunner.run()`에 `layout` 핸들러를 연결하고, 저장 순서대로 기존 탐색기 창 재사용 또는 새 창 실행·매칭 후 1단계 `move_window()`로 배치한다.
- 변경 범위: `action_runner.py`와 3단계 집중 테스트만 수정한다. 탐색기 조회·실행, 모니터 조회, 창 이동, 시간·대기를 `ActionRunner` 생성자에서 교체할 수 있게 하며 기존 생성 호출은 그대로 유지한다.
- 실패 처리: 한 항목의 조회·실행·매칭·이동 실패를 결과 목록에 모으고 다음 항목을 계속 처리한다. 반환 문구는 `N개 창 복원`을 기준으로 실패 개수와 대상 경로를 덧붙이며, 기존 상태줄·실행 이력 경로만 사용하고 알림 창을 추가하지 않는다.
- 자동 검증: 기존 창 재사용, 신규 실행 뒤 매칭, 약 5초 시간 초과, 일부 실패 혼합, `always_new`, 창 중복 배정 방지와 결과 문구를 가짜 공급자로 검사한다. 이후 루트 테스트를 2단계와 같은 10개 안팎 구간으로 나눠 실행하며 기존 Qt access violation 파일은 독립 프로세스로 확인한다.
- 보존·제외: 반복작업 중지 감시, 사용자 DB, 기존·신규 EXE, 4단계 모니터 없음·해상도·DPI·창 상태 예외 규칙은 변경하지 않는다. 실제 Windows 탐색기 2창 검증이 자동화 세션에서 불가능하면 `3._TODO.md`에 남긴다.
- 실행 단위: 검증 구간 1~11은 한 작업에 한 구간씩 나눠 실행했다.
- 검증 구간 1/11: `test_layout_runner.py`, `test_layout_action.py`, `test_window_layout.py` — **19 passed, 0 failed**. 3단계 신규 6개 시나리오와 1·2단계 인접 회귀를 확인했다.
- 검증 구간 2/11: `test_alert_note_phase1.py`, `test_alert_notes.py`, `test_calendar_canvas.py`, `test_confirmed_memo_ux.py`, `test_editor_second_revision.py`, `test_feature_updates.py` — **151 passed, 0 failed**.
- 검증 구간 3/11: `test_format_preset_strip.py`, `test_format_toolbar_compact.py`, `test_ko_schedule_parser.py`, `test_memo_blocks_stage5.py`, `test_memo_character_ranges.py`, `test_memo_drag_stage6.py` — **119 passed, 0 failed**.
- 검증 구간 4/11: `test_memo_heading_preferences.py`, `test_memo_import_archive.py`, `test_memo_layout_contract.py`, `test_memo_management.py`, `test_memo_pages_stage4.py`, `test_memo_phase1_clipboard.py` — **97 passed, 0 failed**.
- 검증 구간 5/11: `test_memo_phase123_recovery.py`, `test_memo_phase2_blocks.py`, `test_memo_phase3_editing.py`, `test_memo_remediation_stage_a.py`, `test_memo_remediation_stage_d.py`, `test_memo_remediation_stage_e.py` — **57 passed, 0 failed**.
- 검증 구간 6/11 1차 시도: `test_memo_remediation_ui_c1.py`, `test_memo_stage7.py`, `test_memo_stage8.py`, `test_memo_sync_categories_versions.py`, `test_memo_toggle.py` — 43% 이후 진행 표시만 남기고 종료 코드 1, 최종 집계 없음. 범위 밖 Qt 프로세스 종료로 보고 파일별 독립 프로세스로 재검증했다.
- 검증 구간 6/11 확정: 위 5개 파일을 독립 프로세스로 실행해 각각 **5, 35, 54, 11, 59 passed**, 합계 **164 passed, 0 failed**. 묶음 프로세스 종료 원인은 이번 범위에서 수정하지 않았다.
- 검증 구간 7/11: `test_memo_tree_stage2.py`, `test_memo_tree_stage3.py`, `test_memo_ui_compact.py`, `test_memo_usability_scenarios.py`, `test_postit_stage1.py`, `test_postit_stage2.py` — **88 passed, 0 failed**.
- 검증 구간 8/11: `test_quick_schedule.py`, `test_schedule_dday_stage4.py`, `test_schedule_editor_stage3.py`, `test_schedule_management.py`, `test_schedule_postit_stage2.py`, `test_settings_dday_ux.py` — **99 passed, 0 failed**.
- 검증 구간 9/11: `test_shortcut_updates.py`, `test_task_list_room.py`, `test_text_format_range_presets.py`, `test_ui_responsive.py` — **64 passed, 0 failed**. 수정 없음.
- 검증 구간 10/11: `test_toma_pet_gpt_integration.py`, `test_update_plan_stage1.py`, `test_update_plan_stage2.py`, `test_user_manual.py`, `test_ux_safety_updates.py` — **37 passed, 0 failed**. 수정 없음.
- 검증 구간 11/11: `test_memo_phase4_identity.py`, `test_memo_remediation_stage_b.py`, `test_memo_title_filters.py` — 파일마다 별도 pytest 프로세스로 실행해 각각 **16, 10, 67 passed**, 합계 **93 passed, 0 failed**. 독립 프로세스에서는 assertion 실패와 Qt/Windows access violation 프로세스 종료가 재발하지 않았다. 수정 없음.
- 확인된 사실: 묶음 실행에서는 `test_memo_phase4_identity.py`, `test_memo_remediation_stage_b.py`, `test_memo_title_filters.py` 구간에서 Windows access violation으로 pytest 프로세스가 종료되었고, 파일마다 독립 프로세스로 실행했을 때는 재발하지 않았다. 이 회귀 도중 이전 Codex 작업 두 건도 상위 프로세스 종료로 중단되었다.
- 추정 원인: 한 작업 안에서 오래 도는 실행이 시간 한계에 걸린 것으로 추정한다. 이는 확인된 원인이 아니다.
- 범위 판단: Qt 종료 관련 access violation은 이번 창 배치 작업 범위 밖이므로 수정하지 않았으며 후속 확인 대상으로 남긴다.
- 상태: 2026-09-18 사용자 결정으로 **창 배치 작업 전체 보류**. 4단계는 코드와 자동 검증까지 완료했지만 실제 창 확인 전이므로 완료로 판정하지 않는다. 집중 검사 **48 passed, 0 failed**, 전체 회귀는 **TOTAL modules=60 passed=1031 failed_groups=[('test_memo_sync_categories_versions.py', 3221225477)]**이며 pytest assertion 실패는 0건이다. GROUP 8/8은 **27 → 31 passed**로 4개 늘었고, 전체 수치가 직전 깨끗한 실행 **1035**보다 낮은 것은 기존 Qt 네이티브 종료로 묶음이 8회 분할되며 집계가 누락된 탓이다. 이 PC의 두 모니터가 모두 **1920×1080**, 작업 영역 **1032**, DPI **96(100%)**이므로 DPI 차이 보정은 단위 검사로만 확인됐다. 2모니터 신규 저장 배치, 모니터 분리, 창 상태, 기존 payload, 100·125·150% 배율은 `3._TODO.md`의 실제 Windows 확인 항목으로 남긴다.

### 2026-09-17 탐색기 파일 목록 상위 폴더 이동 (3단계 코드 완료, 실제 창 확인 대기)

- 목표: 탐색기 파일 목록의 빈 공간을 더블클릭하면 상위 폴더로 이동한다(`Alt+↑`와 같은 결과). Q-Dir·Total Commander의 기본 동작을 토마데스크가 기본 탐색기에 제공한다.
- 확정 결정: 이동 방향은 상위 폴더로 한다(직전 폴더/뒤로가기 아님). 탐색기 창에서만 동작하며 다른 프로그램에는 관여하지 않는다.
- 구현 순서: 2026-09-18 사용자 결정으로 창 배치 4단계보다 먼저 착수한다. 창 배치는 3.7단계까지 완료했고 4단계(모니터·배율 예외)는 대기 상태로 둔다. 기반이 서로 다르므로(마우스 갈고리 대 창 좌표) 계획과 검증을 분리한다.
- 핵심 설계: **더블클릭을 가로채지 않는다.** 빈 공간의 더블클릭은 원래 아무 동작도 하지 않으므로, 판정이 끝난 뒤 `Alt+↑`만 보낸다. 가로채려면 갈고리 안에서 즉시 판정해야 하고 그 안에서 COM을 호출하면 시스템 전체 입력이 멈춘다. 가로채지 않으므로 오판의 최악 결과는 "아무 일도 일어나지 않음"이다. 3단계 가운데 클릭은 항목/빈칸을 구분하지 않고 기존 `WH_MOUSE_LL` 하나를 재사용하며, 콜백 안에서는 `WindowFromPoint`·`GetClassNameW`로만 파일 목록과 최상위 탐색기 창을 판정한다. 조건이 맞으면 down/up을 한 쌍으로 모두 삼키고 워커 큐에서 `Alt+Up`을 전송한다.
- 3단계 사전 조사 근거: Windows 11 파일 목록은 `SHELLDLL_DefView → DirectUIHWND` 구조여서 `LVM_*` 메시지를 쓸 수 없다. `Shell.Application → IServiceProvider → IShellBrowser → IShellView → IFolderView` 접근은 가능하지만 좌표 판정용 `HitTest`가 없고 Details 뷰의 `GetItemPosition`은 `E_NOTIMPL`이므로 좌표→항목 판정 경로는 폐기한다. 반면 `WindowFromPoint + GetClassNameW` 실측은 평균 **0.02ms**, 최대 **0.23ms**여서 훅 콜백 안에서 클래스만 판정할 수 있다. 바탕화면 `Progman` 아래에도 `SHELLDLL_DefView`가 있으므로 파일 목록 조상만 보지 않고 최상위 클래스가 `CabinetWClass`·`ExploreWClass`인지 반드시 함께 확인한다.

#### 단계별 범위와 승인 게이트

| 단계 | 범위 | 단계 완료 기준 | 현재 상태 |
| --- | --- | --- | --- |
| 1 | `explorer_dblclick.py` 신설: 더블클릭 감지와 빈 공간 판정 | 판정 로직 단위 검사 통과, 전체 회귀 통과 | 자동 검증만 통과했으나 실제로는 파일 목록 판정 버그로 미동작; 2026-09-18 판정 수정, 실제 동작 확인 대기 |
| 2 | 설정 토글·예외 영역·실제 창 검증 | 제외 영역 오동작 0, 실제 Windows 11 확인 | 설정 코드는 완료했으나 1단계 판정 버그로 더블클릭은 실제 미동작; 판정 수정 후 실제 창 확인 대기 |
| 3 | 가운데 클릭으로 파일 목록에서 상위 폴더 이동 | down/up 동시 가로채기·오동작 방지 단위 검사와 전체 회귀 통과 | 코드 완료, 실제 창 확인 대기 |

#### 변경 범위

- 신규 `explorer_dblclick.py`
  - 갈고리 스레드: `macro_recorder.py`의 `WH_MOUSE_LL` 설치 방식을 그대로 따라 별도 스레드에 건다. 콜백은 `WM_LBUTTONDBLCLK`의 좌표·시각만 큐에 넣고 즉시 반환한다. 판정·COM·입력 전송을 콜백 안에서 하지 않는다.
  - 판정 워커 스레드: `WindowFromPoint`로 클릭 지점의 자식 창과 최상위 창을 얻고, 최상위 창 클래스가 `CabinetWClass`·`ExploreWClass`인지 확인한다. `Shell.Application`에서 같은 `HWND`의 창을 찾아 `Document.SelectedItems().Count == 0`이면 빈 공간으로 판정한다. COM은 이 스레드에서 `CoInitialize`한다.
  - 전송: 판정 시점에도 전경 창이 그 `HWND`인지 다시 확인한 뒤 `input_controller.send_hotkey`로 `Alt+Up`을 보낸다.
- `window_restore.py`: 탐색기 창 조회 부분을 `HWND`로도 찾을 수 있게 공개 함수를 재사용한다(창 배치 1단계에서 이미 분리).
- `main_window.py`·`settings_dialog.py`: 설정 창에 켜기/끄기 체크 상자를 추가하고 `settings` 테이블에 값을 저장한다. 끄면 갈고리를 해제한다.
- `store.py` 스키마 변경 없음(`settings` 키만 추가).
- 신규 테스트 `test_explorer_dblclick.py`.
- 3단계 `explorer_dblclick.py`: 새 갈고리를 추가하지 않고 기존 `WH_MOUSE_LL` 콜백에서 `WM_MBUTTONDOWN`·`WM_MBUTTONUP`을 처리한다. 파일 목록 조상 `SHELLDLL_DefView`와 최상위 탐색기 클래스가 함께 확인된 down/up만 한 쌍으로 삼키며, 워커 큐에서 COM 없이 `input_controller.send_hotkey("Alt+Up")`을 한 번 전송한다. 기존 더블클릭의 큐·COM 판정 경로는 그대로 유지한다.
- 3단계 `main_window.py`·`settings_dialog.py`: 더블클릭과 독립된 기본값 켜짐 설정을 추가한다. 둘 다 꺼졌을 때만 훅을 해제하고 하나라도 켜져 있으면 같은 훅을 유지한다. `store.py`는 스키마를 바꾸지 않고 설정 키만 추가하며, `user_manual_content.py`에 가운데 클릭 항목을 추가한다.

#### 제외 영역

다음 위치의 더블클릭은 무시한다. 자식 창 클래스와 좌표로 판단한다.

- 열 머리글(`SysHeader32`), 탐색 트리(`SysTreeView32`·`NamespaceTreeControl`), 주소·검색 줄, 탭 줄, 미리보기·세부 정보 창, 상태 표시줄
- 파일 목록 영역 밖 전부, 그리고 탐색기가 아닌 모든 프로그램
- 반복작업 녹화 중에는 전체 기능을 멈춘다(`macro_recorder`의 갈고리와 겹쳐 녹화가 오염되는 것을 막는다)
- 3단계 가운데 클릭은 왼쪽 탐색 창(`NamespaceTreeControl`·`SysTreeView32`), 도구모음, 탭줄(`TITLE_BAR_SCAFFOLDING_WINDOW_CLASS`), 제목줄을 통과시킨다. `SHELLDLL_DefView`가 있어도 최상위가 `Progman`인 바탕화면, `#32770`인 파일 열기·저장 대화상자, `ATL:...`인 Q-Dir 같은 다른 프로그램은 통과시킨다.

#### 단계별 구현

- 1단계: 갈고리 스레드와 판정 워커, 제외 영역 판단, `Alt+↑` 전송을 만든다. 갈고리 설치·창 조회·선택 개수 조회를 주입 가능한 경계로 두어 화면 없이 검사할 수 있게 한다.
- 2단계: 설정 토글과 저장, 시작 시 상태 복원, 끄기 시 갈고리 해제를 연결하고 실제 Windows 11에서 오동작을 확인한다. 설명서에 한 항목을 추가한다.
- 3단계: 기존 갈고리에 가운데 버튼 down/up의 즉시 클래스 판정을 추가하고, 두 이벤트를 함께 삼킨 뒤 워커에서 `Alt+Up`을 보낸다. 별도 설정의 저장·시작 복원·즉시 중지를 연결하고 설명서와 단위 검사를 갱신한다.

#### 검증 기준

- 1단계: 가짜 창 정보·선택 개수를 주입해 (a) 빈 공간 → 전송, (b) 항목 위(선택 1개 이상) → 무시, (c) 탐색기가 아닌 창 → 무시, (d) 제외 영역 → 무시, (e) 판정 중 전경 창이 바뀜 → 무시를 검사한다.
- 2단계: 실제 Windows 11에서 파일 목록 빈 공간·항목 위·열 머리글·탐색 트리·바탕화면·엑셀·브라우저를 각각 더블클릭해 의도한 창에서만 이동하는지 확인한다. 루트 폴더(`C:\`)에서 더 올라갈 곳이 없을 때 아무 일도 없는지, 설정을 끄면 즉시 멈추는지, 반복작업 녹화 중 동작하지 않는지 확인한다.
- 3단계: 주입 가능한 창 판정·훅 경계로 (a) `CabinetWClass + SHELLDLL_DefView`에서 down/up 모두 가로채고 `Alt+Up` 1회 전송, (b) 바탕화면 `Progman`, 파일 대화상자 `#32770`, 다른 프로그램 `ATL:...`, 탐색 창, 도구모음, 탭줄은 통과, (c) 녹화 중·설정 꺼짐은 통과, (d) 두 설정의 기본값과 OR 기반 훅 수명을 검사한다. 기존 더블클릭 테스트를 함께 실행한다.
- 전체 회귀를 실행하고 실패 0을 확인한다. 실제 창으로만 확인 가능한 항목은 `3._TODO.md`에 남긴다.

#### 보존 범위

기존 더블클릭 1·2단계의 동작·설정·COM 판정 경로, 반복작업 녹화·재생과 기존 키보드 갈고리·전역 단축키, `path` 유형의 최소화 창 복원, 사용자 DB와 기존 설정, 메모·일정 영역을 변경하지 않는다. `explorer_dblclick.py` 이름과 DB 스키마도 유지한다.

#### 제외 범위

Q-Dir식 다중 창·탭 UI, 오른쪽 버튼·휠 동작 변경, 탐색기 외 프로그램 지원, 더블클릭 가로채기(swallow), 좌표→항목 판정 재시도, COM 기반 가운데 클릭 처리, EXE 생성은 포함하지 않는다.

#### 결정이 필요한 사항

- 남은 결정 없음. 기본값은 켜짐으로 하고 설정에서 끌 수 있게 한다(2026-09-17 확정).

- 상태: **파일 목록 판정 버그 수정 완료, 세 단계 모두 실제 창 동작 확인 대기**. 1·2단계는 자동 검증만 통과했을 뿐 `is_explorer_file_list()`가 실제 파일 목록에서도 항상 `False`를 반환해 더블클릭이 한 번도 동작하지 않았으며, 3단계 가운데 클릭도 같은 판정을 사용했다. 실제 Windows 11 계층에서 `ShellTabWindowClass`가 탭 줄이 아니라 탭 내용 전체의 상위 컨테이너임을 확인해, 제외 클래스 판정을 첫 `SHELLDLL_DefView`보다 안쪽인 자식 쪽 구간으로 제한했다. 실행 중인 탐색기 창의 좌표 판정은 파일 목록 `True`, 왼쪽 탐색 창 `False`로 직접 확인했다. `python -m pytest test_explorer_dblclick.py -q` → **18 passed**, `python tools/run_memo_full_regression.py` → **TOTAL modules=60 passed=1035 failed_groups=[]**이며 이번에는 Qt/Windows access violation 종료가 재현되지 않았다. **1035**는 검사 증가가 아니라 직전 **1024** 실행에서 네이티브 종료 후 분할 집계가 누락된 부분까지 종료 없이 전부 집계된 결과이므로 종료 오류 없는 기준선으로 본다. 실제 더블클릭·가운데 클릭으로 의도한 동작이 이루어지는지는 사용자 확인 전이며 `3._TODO.md`에 유지한다.

### 2026-09-17 PowerToys 유래 보조 기능 3종 (1·2단계 착수 승인, 3단계 대기)

- 목표: PowerToys 모듈 조사 결과 중 토마데스크 구조에 바로 얹히는 세 가지를 순서대로 추가한다. (1) 창 고정, (2) 단축키 안내 오버레이, (3) 화면 글자 따기(OCR).
- 착수 시점: **2026-09-18 사용자 지시로 1·2단계를 지금 착수한다.** 창 배치는 4단계까지 코드가 끝나 `window_layout.py`의 창 목록·좌표 층이 이미 있으므로 1번은 그 위에 바로 얹는다. 3단계(OCR)는 1·2단계와 파일 이름 일괄 변경을 마친 뒤로 미룬다.
- 채택 근거: 1번은 포스트잇이 이미 쓰는 `HWND_TOPMOST`를 다른 프로그램 창으로 넓히는 것뿐이다. 2번은 새 기능이 아니라 이미 등록된 단축키를 쓰게 만드는 보완이다(설명서 UI 원칙 4 "단축키를 화면에 적어둔다"). 3번은 "읽은 것 → 메모"라는 기존 흐름에 입구를 하나 더 만든다.
- 보류·이관: 클립보드 기록(Advanced Paste)은 Win+V와의 차별점이 확인될 때까지 보류한다. 정해둔 자리로 창 보내기(FancyZones)는 창 배치 계획으로 넘긴다. 파일 이름 일괄 변경(PowerRename)은 `3._TODO.md`에 구상과 함께 남긴다.

#### 단계별 범위와 승인 게이트

| 단계 | 범위 | 단계 완료 기준 | 현재 상태 |
| --- | --- | --- | --- |
| 1 | `window_pin.py` 신설: 전경 창 항상 위 토글과 표시 | 토글·해제·종료 시 전체 해제 단위 검사 통과, 실제 3개 프로그램 확인, 전체 회귀 통과 | 구현·집중 검사 통과, 실제 창·전체 회귀 대기 |
| 2 | `shortcut_overlay.py` 신설: 등록된 단축키 안내 오버레이 | 등록 성공 항목만 표시·닫힘 조건·제외 프로그램 검사 통과, 전체 회귀 통과 | 코드 완료, 실제 창 확인 대기 (2026-09-18) |
| 3-1 | `screen_ocr.py` 신설: 영역 선택·인식·클립보드 복사 | 인식 경계 주입 검사와 언어팩 없음 처리 통과, 실제 PDF·이미지 확인 | 코드·자동 검사 완료, 실제 화면 확인 대기 (2026-09-19) |
| 3-2 | 인식 결과를 메모로 보내기와 D-Day 제안 | 새 메모 생성·날짜 제안 검사 통과, 전체 회귀 통과 | 코드 구현, 결과는 `docs/SCREEN_OCR_STAGE2.md` (2026-09-19) |

#### 1단계 — 창 고정

- 신규 `window_pin.py`: 전경 창 `HWND`를 받아 `SetWindowPos`의 `HWND_TOPMOST`·`HWND_NOTOPMOST`를 토글한다. 고정한 창 목록을 메모리에 들고 있다가 프로그램 종료·설정 끄기 때 전부 해제한다. 창이 이미 닫혔으면 목록에서 지운다.
- 대상 제외: 바탕화면(`Progman`·`WorkerW`), 작업 표시줄(`Shell_TrayWnd`), 토마데스크 자신의 포스트잇 창(이미 항상 위라 토글이 어긋난다). 제외 대상에서는 아무 일도 하지 않고 상태 문구만 남긴다.
- 단축키: 공통 단축키에 `창 고정/해제`를 추가하고 기본값을 `Ctrl+Alt+T`로 둔다. `settings_dialog.py`의 공통 단축키 줄과 충돌 경고 규칙을 그대로 따른다.
- 표시: 고정·해제 순간에 기존 `ui_feedback` 토스트로 창 이름과 결과를 알리고, 트레이 툴팁에 현재 고정된 창 개수를 더한다. 고정된 창 둘레의 테두리 오버레이는 만들지 않는다(2026-09-17 확정).
- `main_window.py`: 공통 단축키 처리에 항목 하나를 잇는다. 새 화면은 만들지 않는다.

#### 2단계 — 단축키 안내 오버레이

- 신규 `shortcut_overlay.py`: 테두리 없는 반투명 `QWidget`. 마우스 커서가 있는 모니터 가운데에 뜬다. 목록은 `공통 단축키` / `메모·일정` / `내가 만든 작업` 세 묶음을 2열로 배치하고, 항목이 많으면 유형 필터 없이 스크롤한다.
- 표시 대상: `hotkey_manager`가 **등록에 성공한** 항목과 활성 상태인 작업만 넣는다. 실패·비활성 항목은 목록에 넣지 않고, 실패가 있으면 아래에 `등록 실패 N개` 한 줄만 둔다. 실패한 조합을 안내에 적으면 거짓 정보가 된다.
- 뜨지 않는 조건: 제외 프로그램(`excluded_apps`)이 전경일 때, 반복작업 녹화·재생 중일 때, 이미 떠 있을 때.
- 닫힘: `Esc`, 오버레이 밖 클릭, 포커스를 잃을 때. 항목을 클릭하면 닫히면서 해당 작업을 실행하지 않고 메인 창의 그 작업으로 이동한다(실행은 단축키로만 한다).
- 여는 방식: 공통 단축키에 `단축키 안내`를 추가하고 한 번 누르면 열린다. `Ctrl+Alt` 길게 누르기 판정은 만들지 않는다(2026-09-17 확정).

#### 3단계 — 화면 글자 따기(OCR)

- 신규 `screen_ocr.py`
  - 영역 선택: 모든 모니터를 덮는 테두리 없는 오버레이를 띄우고 끌어서 사각형을 고른다. `Esc`로 취소한다. 배율이 다른 모니터에서도 실제 픽셀 좌표로 잡는다.
  - 캡처·인식: 선택 영역을 캡처해 Windows 내장 OCR(`Windows.Media.Ocr`)로 읽는다. 인식 언어는 한국어를 우선하고 없으면 영어로 내린다. 둘 다 없으면 기능을 비활성으로 두고 설정 화면에 "Windows 언어팩이 필요합니다" 안내를 표시한다.
  - 결과: 기본으로 클립보드에 복사하고, 작은 결과 창에 인식 글자를 보여준다. 결과 창에서 `메모로 만들기`·`다시 인식`·`닫기`를 고른다.
- 의존성: OCR 호출 패키지를 `requirements.txt`에 추가하고 `shortcut_launcher.spec`의 `hiddenimports`에 반영한다. 패키지를 불러오지 못하면 프로그램은 정상 실행되고 이 기능만 꺼진다.
- 3-2: 인식 글자에 날짜가 있으면 기존 한국어 일정 해석기(`test_ko_schedule_parser.py`가 검사하는 경로)를 재사용해 `D-Day로 만들기` 한 줄을 제안한다. 포스트잇 추천과 같은 규칙으로, 자동으로 만들지 않고 거절하면 그 자리에서 사라진다.
- 단축키: 공통 단축키에 `화면 글자 따기`를 추가한다. 기본값은 다른 조합과 충돌하지 않는 값으로 정하고 설정에서 바꿀 수 있게 한다.

#### 검증 기준

- 1단계: 가짜 `HWND`와 Win32 호출 경계를 주입해 (a) 토글 두 번이면 원상복귀, (b) 제외 창 클래스 무시, (c) 닫힌 창 정리, (d) 종료 시 전체 해제를 검사한다. 실제 Windows 11에서 탐색기·엑셀·브라우저를 고정·해제하고, 토마데스크를 종료하면 고정이 풀리는지 확인한다.
- 2단계: 등록 성공 30개·실패 3개·비활성 2개를 넣은 상태에서 목록에 30개만 들어가는지, 제외 프로그램 전경·녹화 중에 뜨지 않는지, `Esc`와 포커스 상실로 닫히는지 검사한다. 오프스크린 렌더로 좁은 화면(1366×768)에서 잘리지 않는지 확인한다.
- 3단계: 인식 결과를 가짜로 주입해 클립보드 복사·결과 창 구성·메모 생성·날짜 제안을 검사한다. 언어팩 없음과 패키지 없음 두 경우에 프로그램이 정상 실행되고 안내만 나오는지 확인한다. 실제로 복사가 막힌 PDF와 캡처 이미지에서 한국어·영어·숫자를 읽어 본다.
- 각 단계마다 전체 회귀를 실행하고 실패 0을 확인한다. 실제 창으로만 확인 가능한 항목은 `3._TODO.md`에 남긴다.

#### 보존 범위

기존 전역 단축키 등록·실패 표시, 반복작업 녹화·재생과 중지 단축키, 제외 프로그램 목록, 포스트잇의 항상 위 동작, 메모·일정·D-Day 데이터와 사용자 DB를 변경하지 않는다.

#### 제외 범위

클립보드 기록과 서식 없이 붙여넣기, 창을 정해둔 자리로 보내기, 파일 이름 일괄 변경, 인식 결과 자동 번역·요약, 화면 조각 고정(Crop And Lock), EXE 생성은 이번 작업에 포함하지 않는다.

#### 결정이 필요한 사항

- 남은 결정 없음(2026-09-17 확정).
  - 2단계 오버레이는 **전용 단축키 한 번**으로 연다(B안). 길게 누르기 판정은 만들지 않는다. 필요해지면 나중에 설정으로 더한다.
  - 1단계 고정 표시는 **토스트와 트레이 개수까지만** 한다. 고정된 창 둘레의 테두리 오버레이는 만들지 않는다.

- 확정 결정(2026-09-18): 창 고정에 **별도 기능 켜기/끄기 체크박스는 두지 않는다.** 단축키로만 토글하고 쓰지 않으려면 단축키를 비워 둔다. 체크박스를 두면 "고정이 왜 안 되지"의 원인이 한 군데 더 늘어난다.
- 실제 창 확인(탐색기·엑셀·브라우저 고정/해제, 종료 시 해제)은 2026-09-18 사용자 요청으로 **보류**하고 `3._TODO.md`에 남긴다.

- 상태(2026-09-18 갱신): 1·2단계 모두 **코드와 자동 검증 완료, 실제 창 확인 대기**. 2단계는 기본 단축키 `Ctrl+Alt+H`로 구현했고 집중 검사 6개, 관련 회귀 124개, 전체 회귀 **1055 passed, 실패 0**을 통과했다. 3-1·3-2단계(OCR)는 착수하지 않는다.

- (이전 기록) 2026-09-18 **1단계(창 고정)는 구현·집중 검사 통과, 실제 창 3종·전체 회귀 확인 대기**. 이번 요청의 검증 제한에 따라 전체 회귀는 실행하지 않았다. 2단계(단축키 안내 오버레이)는 착수 승인 상태로 대기하며, 3-1·3-2단계(OCR)는 착수하지 않는다.

### 2026-09-17 파일 이름 일괄 변경 (1~4단계 코드 구현, 실제 환경 확인 대기)

- 2026-09-19 4단계: `rename_fill.py` 값 생성과 `rename_fill_table.py` 채우기 핸들을 추가했다. 아래 방향 범위 표시·자동 스크롤·놓을 때 원자 적용·Esc 등 취소·단일 Undo, 여러 열의 독립적인 정수/문자 접미 번호/일정 간격 채우기를 제공한다. 불일치 문자열은 원문 순서 반복, Ctrl+D는 기존 복사 동작이다. 파일 실행/복구 엔진은 수정하지 않았다. 자세한 계약은 `docs/FILE_RENAME_STAGE4.md`.

- 2026-09-19 3단계: 이름/충돌 검사, 문제 행 제외·필터·사유 복사, 실행 확인, 의존 그룹별 임시 이름 경유, 부분 실패 복구, 일지 기반 강제 종료 복구, 별도 파일 변경 취소를 구현했다. `rename_engine.py`에서 실제 파일 작업을 격리한다. 기록/제한/검증은 `docs/FILE_RENAME_STAGE3.md`를 따른다.

- 2026-09-19 후속 구현: TSV 범위 복사/붙여넣기, Ctrl+D/Delete/Undo/Redo, 행 번호 끌기, 현재 이름 채우기, 목록에서 행 제거, 변경 부분 강조와 집계를 추가했다. 계약·검증·남은 항목은 `docs/FILE_RENAME_STAGE2.md`를 따른다.

- 2026-09-19 구현: 독립 미리보기 창, 탐색기 선택 가져오기, 로컬 파일 끌어다 놓기, 입력 2~5열, 구분자·확장자·공백 보존, 빈 행 건너뜀, 중복 제외를 추가했다. 새 파일은 `file_rename.py`, `file_rename_table.py`, `test_file_rename.py`다.
- 진입: 트레이 메뉴 `파일 이름 일괄 변경`, 또는 설정의 동명 공통 단축키. 기본 단축키는 비워 두며 사용자가 지정한다. 재호출은 기존 창과 입력을 유지하고 새 파일만 추가한다.
- 탐색기 순서: `IShellView.GetItemObject(SVGIO_SELECTION | SVGIO_FLAG_VIEWORDER)`의 파일 목록을 사용하며 이름순 정렬을 하지 않는다. 읽기 실패는 창에서 안내한다. 실제 Windows 11 탐색기 탭·정렬별 확인은 TODO에 남긴다.
- 3단계부터 유효한 대상이 있고 미완료 기록이 없으면 실행 버튼을 활성화한다. 별도 확인 후에만 실제 파일 이름을 바꾼다. 4단계 채우기 핸들은 표 입력만 바꾸며 파일을 실행하지 않는다.
- 검증: 신규 집중 검사 7개와 기존 단축키·오버레이·창 고정·설정·기능 회귀를 실행했다. 새 공통 단축키에 맞춰 기존 설정 항목 개수 계약을 12개에서 13개로 갱신했다. 전체 회귀와 실제 포인터·배율 검증은 수행하지 않았다.

- 목표: 여러 파일의 이름을 **엑셀처럼 생긴 표에 직접 입력해서** 한 번에 바꾼다. 찾기·바꾸기 규칙이나 정규식이 아니라, 파일 하나가 표의 한 행이고 칸에 적은 내용이 그대로 새 이름이 된다.
- 채택 근거: PowerToys PowerRename은 규칙(패턴)을 먼저 정하고 결과를 확인하는 방식이라, 파일마다 다른 값을 넣어야 하는 실무(예: 문서 번호·담당자·날짜가 제각각)에서는 오히려 손이 많이 간다. 사용자가 구상한 표 입력 방식은 엑셀에서 이름 목록을 만들어 쓰던 기존 습관을 그대로 옮긴다.
- 구현 시점: **2026-09-18 사용자 지시로 착수한다.** PowerToys 1·2단계(창 고정·단축키 안내) 다음 순서이며, OCR·모바일 0단계보다 앞선다.

#### 화면

세로로 긴 표 하나짜리 창이다. 새 탭이나 새 축을 만들지 않고 독립 창으로 띄운다.

| 열 | 내용 | 편집 |
| --- | --- | --- |
| 현재 이름 | 불러온 파일 이름(확장자 포함) | 읽기 전용 |
| 1 · 2 · 3 | 새 이름에 들어갈 내용 | **입력** |
| 바뀔 이름 | 1·2·3을 이어 붙인 결과 + 원래 확장자 | 읽기 전용, 회색 |

- 행 수 n = 불러온 파일 개수. 입력 칸은 `3 × n`이다.
- 창 위에는 대상 폴더 경로와 개수, `칸 사이에 넣을 문자` 입력칸, `되돌리기`, `이름 바꾸기` 버튼을 둔다.
- 예(붙임 이미지): 1열 `테스트`, 2열 `1`~`4`, 3열 `test` → `테스트1test` … `테스트4test`. 첫 행만 1열에 ` 테스트`(앞 공백)를 넣었다면 그 행만 앞 공백이 남는다 — 칸에 적은 대로 나오는 것이 규칙이다.

#### 이름을 만드는 규칙

- 새 이름 = `1칸` + 구분자 + `2칸` + 구분자 + `3칸`. 빈 칸은 건너뛰고 그 자리의 구분자도 넣지 않는다.
- 구분자 기본값은 **없음**이다. 창 위 입력칸에 한 번 적으면 모든 행에 적용된다.
- **칸 안의 공백은 그대로 살린다.** 앞·뒤·가운데 어디에 있든 자르지 않는다. 사용자가 ` 테스트`처럼 앞에 공백을 넣어 띄어쓰기를 만드는 방식을 그대로 쓰기 때문이다(붙임 예시의 첫 줄이 이 경우다). 엑셀에서 붙여 넣을 때도 자르지 않는다.
- 공백은 눈에 보이지 않으므로, `바뀔 이름` 열에서 **앞뒤 공백을 가운뎃점(`·`) 같은 옅은 표식으로 보여준다.** 왜 붙고 왜 띄었는지 표에서 바로 확인할 수 있어야 한다.
- **확장자는 건드리지 않는다.** 현재 이름의 확장자를 그대로 붙인다. 확장자까지 바꾸는 기능은 넣지 않는다.
- 세 칸이 모두 비면 그 행은 이름을 바꾸지 않는다(건너뜀으로 표시).

#### 엑셀과 같은 조작

- **채우기 핸들**: 칸 오른쪽 아래 모서리를 아래로 끈다.
  - 숫자만 있으면 `1, 2, 3…`으로 늘린다. `01`처럼 앞에 0이 있으면 자릿수를 유지한다(`01, 02, … 10`).
  - 글자만 있으면 같은 값을 복사한다.
  - `테스트1`처럼 끝이 숫자면 끝 숫자만 늘린다.
  - 두 칸을 고르고 끌면 그 간격을 이어간다(`2, 4` → `6, 8`).
  - 여러 열은 열별로 계산한다. 세 행 이상은 일정한 간격일 때만 연장하고, 형식/간격이 맞지 않으면 원문 배열을 반복한다. 순수 소수/쉼표 표기는 텍스트로 취급한다.
  - 놓기 전에는 범위 표시만 하며, 표 안에서 놓으면 기존 대상 값을 한 번에 대체한다. 한 번의 Undo로 되돌린다. Esc·표 밖 놓기·포커스 상실·숨김·모델 변경은 취소한다. 아래쪽 가장자리에서 자동 스크롤하며 원래 행 수를 넘지 않는다.
  - 문제 행 필터·숨긴 행·읽기 전용 열·비연속 선택에서는 사용하지 않는다. 가로/위 방향 확장과 더블클릭 자동 채우기는 제외한다.
- **복사·붙여넣기**: `Ctrl+C`·`Ctrl+V`로 여러 칸을 한꺼번에 옮긴다. 엑셀에서 복사한 범위를 그대로 붙여 넣을 수 있게 탭·줄바꿈으로 구분된 값을 받는다. 반대로 표에서 복사한 것을 엑셀에 붙여 넣을 수도 있다.
- 직사각형 범위 하나만 처리한다. 붙여넣기는 선택 범위의 왼쪽 위에서 원본 크기만큼 적용하며 반복 채우지 않는다. 입력 열/행을 초과하거나 형식이 잘못되면 전체를 거부한다. 따옴표로 감싼 탭·줄바꿈, 빈 셀, 앞자리 0·공백을 보존한다. 줄바꿈·탭 보존은 파일 이름으로 유효하다는 뜻이 아니며 3단계 검사 대상이다.
- 표 편집 Undo와 실제 파일 실행 취소는 별개다. 표의 붙여넣기·채우기·비우기·행 제거/이동·열 변경은 작업당 한 번에 복구하며 최근 100건을 유지한다. 창을 닫았다 다시 열면 같은 세션의 입력과 이력을 유지하고, 프로그램 재시작 시는 유지하지 않는다.
- **아래로 채우기** `Ctrl+D`, **칸 비우기** `Delete`, **되돌리기** `Ctrl+Z`.
- 이동은 방향키·`Tab`·`Enter`, 범위 선택은 끌기와 `Shift+클릭`을 쓴다.
- 행을 끌어 순서를 바꿀 수 있다. 번호 매기기가 순서에 좌우되기 때문이다.

#### 바꾸기 전 검사

문제가 있는 행은 붉은 배경과 사유 표시를 달고, 문제가 하나라도 있으면 `이름 바꾸기`를 잠근다(`문제 있는 행 빼고 실행`은 따로 고를 수 있다).

- 같은 폴더 안에서 새 이름이 겹침
- 이미 있는 다른 파일과 겹침
- 윈도우가 금지하는 문자 `\ / : * ? " < > |`, 이름이 빔
- 이어 붙인 결과의 **맨 뒤 공백과 맨 뒤 마침표는 막는다.** 윈도우가 저장할 때 지워 버려서 표에 보이는 이름과 실제 파일 이름이 달라진다.
- 결과의 **맨 앞 공백은 막지 않고 노란 주의 표시만** 한다. 파일은 정상으로 만들어지지만 목록에서 위로 튀어 보이기 때문이다.
- 예약된 이름(`CON`·`PRN`·`AUX`·`NUL`·`COM1`~`LPT9`)
- 전체 경로 길이 초과

#### 실행과 되돌리기

- `a → b`, `b → a`처럼 서로 자리를 바꾸는 경우를 위해 임시 이름을 거쳐 두 번에 나눠 바꾼다.
- 파일이 잠기거나 사라지면 이름 의존관계가 없는 그룹은 계속 진행할 수 있다. 맞바꾸기·이름 연결은 의존 그룹 단위로 처리하고 중간 실패 시 해당 그룹 복구를 시도한다. OS 파일 작업 전체가 원자적이라고 가정하지 않는다. 복구 실패도 숨기지 않고 임시/현재/목표 경로를 보고한다.
- 파일을 만지기 전에 원본 식별 정보·경로·임시 경로·목표와 상태를 내구성 있는 실행 일지에 기록하고 각 이동 후 갱신한다. 강제 종료 후 재시작에서는 미완료 작업을 감지하고 사용자 확인 전 새 실행을 막는다. 실행·복구·취소 어디서도 기존 파일을 덮어쓰지 않으며 외부 변경이 감지되면 자동 진행하지 않는다.
- 실제 파일 실행 취소는 표 Undo와 별도 버튼/이력으로 제공한다. 충돌·원본 변경을 다시 검사하고 그룹별 성공/실패/복구 필요를 표시한다. 문제 행만 보기와 실패 사유 복사를 제공한다. 취소 대상은 최근 완료 실행 10건 안에서 마지막 미취소 실행이다. 일지는 완료/미완료 모두 자동 삭제하지 않는다. 파일 작업 후 표 Undo는 비워 오래된 경로 복원을 막는다.

#### 변경 범위(예상)

- 신규 `file_rename.py`: 파일 불러오기, 이름 조합, 검사, 실행, 되돌리기. 화면 없이 검사할 수 있게 파일 시스템 호출을 주입 가능한 경계로 둔다.
- 신규 `file_rename_table.py`: 표 위젯. 채우기 핸들 그리기·끌기, 범위 선택, 클립보드 왕복, 되돌리기 스택.
- `rename_engine.py`: 실행 일지를 사용자 저장소 루트의 별도 SQLite DB에 둔다. 메모/설정 백업 복원이나 사용자 데이터 경로 변경으로 미완료 파일 작업 기록이 되돌려지지 않게 분리하며 `store.py`와 기존 DB 스키마는 유지한다.
- `main_window.py`·`settings_dialog.py`: 공통 단축키에 `파일 이름 일괄 변경` 추가.
- 신규 `test_file_rename.py`, `test_file_rename_table.py`.

#### 단계별 범위(예상)

| 단계 | 범위 | 단계 완료 기준 |
| --- | --- | --- |
| 1 | 파일 불러오기 + 표 표시 + `바뀔 이름` 미리보기 (실행 없음) | 불러오기·정렬·이름 조합 검사 통과 |
| 2 | 칸 편집·키보드 이동·복사/붙여넣기·`Ctrl+Z` | 엑셀 범위 왕복 검사 통과 |
| 3 | 검사·실행·되돌리기·결과 보고 | 겹침·금지 문자·자리 맞바꾸기·부분 실패 검사 통과 |
| 4 | 채우기 핸들 | 숫자·자릿수·글자+숫자·간격 추론 검사 통과 |

구현기간은 제시하지 않는다. 3단계의 안전성 검증까지 통과해야 실제 파일 변경 기능을 제공하며, 4단계는 별도 편의 기능이다.

#### 보존 범위

사용자 파일은 실행 버튼을 누르기 전까지 절대 건드리지 않는다. 기존 단축키·매크로·메모·일정 영역과 사용자 DB를 변경하지 않는다.

#### 제외 범위

정규식·찾아 바꾸기 규칙, 하위 폴더 재귀 처리, 파일 이동·복사·삭제, 확장자 변경, 사진 촬영일 같은 메타데이터 읽기, 미리 만든 이름 규칙 저장은 1차 범위에서 제외한다.

#### 확정 결정 (2026-09-17)

- **파일 고르는 방법**: 탐색기에서 파일을 고른 뒤 공통 단축키를 누르는 방식(A)을 기본으로 하고, 창에 끌어다 놓기(C)를 함께 지원한다. 토마데스크 안에서 폴더를 열어 고르는 방식(B)은 만들지 않는다.
- **입력 칸 개수**: 기본 3열로 두고 `열 추가`·`열 삭제`로 2~5열까지 조절한다.
- **행 순서**: 탐색기에 보이던 정렬 순서를 그대로 가져온다. 이름순 재정렬은 하지 않는다(표에서 행을 끌어 옮길 수는 있다).

- 상태: 2026-09-19 **1~4단계 코드 구현, 실제 환경 확인 대기**. 임시 파일 실행/복구와 Qt 합성 마우스 채우기를 자동 검사했다. 실제 Windows 조작 미확인을 완료로 간주하지 않는다. OCR/모바일 등의 다음 기능은 별도 승인 후 진행한다.

### 2026-09-15 메모 Phase 4~7 결과물 보완 (단계 E 조건부 통과)

- 요청·목표: `8._메모편집_보완계획/메모편집_보완계획.md`의 사용성 검사 결과를 기준으로 앱 종료 오류, 데이터·로직, 편집기 머리 영역, 목록, 상태·버전·주석 표시와 회귀 검증을 순서대로 보완한다.
- 확정 순서: 단계 0 → A → B → C → D → E → F. 한 번에 한 단계만 수행하고 집중 검사와 전체 회귀 결과를 보고한 뒤 중단한다. 사용자의 명시적 승인 없이 다음 단계로 넘어가지 않는다.
- 확정 결정: Q1은 A안으로 하여 `⌨ 단축키` 칩이 현재 메모를 여는 전역 단축키와 값을 표시하고 편집 단축키 설정은 `⚙`로 옮긴다. Q2도 A안으로 하여 주석 추가·수정·삭제를 본문 `Ctrl+Z/Redo` 흐름에 통합한다.
- 보존 계약: 기존 작업 트리와 사용자 데이터, UUID·revision·밀리초 UTC·설치 UUID·tombstone·정수 ID·숫자/v1 및 UUID/v2 링크·구형/신형 백업 계약을 유지한다. 테스트는 임시 DB 또는 사용자 DB 복사본만 사용한다.
- 제외·보고: 새 EXE와 배포물을 만들지 않는다. 현재 단계 밖의 문제는 임의 수정하지 않고 별도 보고한다. 자동 검사와 실제 Windows 창 결과를 구분하고 미확인 포인터 항목은 `3._TODO.md`에 남긴다.

#### 단계별 범위와 승인 게이트

| 단계 | 범위 | 단계 완료 기준 | 현재 상태 |
| --- | --- | --- | --- |
| 0 | 확정 계획·추적표 기록, 이미지 링크 정리, 기준선·임시 DB 경계 확인 | 문서 집중 검사와 전체 회귀 통과 | 통과: 집중 26개·전체 815개 |
| A | F1·F2 앱 종료 방지와 슬롯 예외 처리 | 일괄 지정·템플릿 삽입 슬롯 예외 0, 전체 회귀 통과 | 조건부 통과: 전체 820개, 실제 창 미확인 |
| B | F3~F7 데이터·로직 수정 | 오늘 요약·세션 시작 버전·주석 복구·v2 재매핑·템플릿 구조 검사 통과 | 조건부 통과: 집중 96개·전체 830개, B1 실창 통과 |
| C | D-1·D-3 편집기 머리 영역과 서식 프리셋 재배치 | UI-C1~C5 순서, 칩 y좌표 0px 변화, 서식 2줄·샘플 버튼·선택 창·28px·본문 비율 기준 통과 | 조건부 통과: 핵심 자동 검사 완료, 계획 세부 편차 3건·실제 포인터 미확인 |
| D | D-4 목록 필터·열·선택 작업·충돌 표시와 제목 첫 기호 동적 필터 | 480px 가로 스크롤 0, 필터·일괄 작업 통과 | 조건부 통과: D 집중·인접 176개, C 제외 전체 860개 통과, 기존 C 실패 2건·실제 포인터 미확인 |
| E | D-2 상태 분리, 버전 현지화·평문 diff, 주석 표시·진입점 | 상태 중복 없음, 버전·주석 표시 시나리오 통과 | 조건부 통과: 집중 7개·배율별 7개, E 포함 분할 회귀 866개 통과, 단계 밖 UI 실패 3건·실제 포인터 미확인 |
| F | 배치 계약·사용성 시나리오·데이터 회귀·실제 창·문서 마감 | 자동 실패 0, 실제 창 결과와 미확인 항목 분리 기록 | 조건부 통과: 12개 배치·71개 시나리오·전체 902개 통과, 실제 포인터 미확인 |
| G-A | 제목 분류 규칙: 기호 So·비교 키 보정과 `[머리말]` 추출·집계 | 규칙 표와 20개 고정 제목 사례, 전체 회귀 통과 | 통과: 집중 50개·전체 944개, G-B 승인 대기 |
| G-B | 기호·머리말·카테고리·검색 AND 필터, 평평한 목록과 결과 없음 안내 | 필터 조합·개수·해제·원래 계층 복귀, 전체 회귀 통과 | 통과: 집중 75개·전체 969개, G-C 승인 대기 |
| G-C | 기호·머리말 버튼의 대표값·선택·빈 상태, 메뉴·도움말, `↺ 필터 초기화` 버튼 | 세 상태·동률(최근 수정 우선)·배율 말줄임·메뉴 검색·도움말·초기화 버튼, 300px 배치, 전체 회귀 통과 | 대기 |
| G-D | 검색·필터 줄과 상시 `더보기`, 보기·정렬·카테고리 설정 진입 | 300·420·800px 배치와 메뉴 동작, 전체 회귀 통과 | 대기 |
| G-E | 사용자가 고르는 항상 보이는 카테고리 칩과 설정 창 | 기본 3개·미지정·추가·삭제·순서 변경, 전체 회귀 통과 | 대기 |
| G-F | 제목 칸 맨 앞 `[` 머리말 자동완성 | 제안·선택·기존 제목 보존·범위 밖 미작동, 전체 회귀 통과 | 대기 |
| G-G | 설명서·임시 DB 캡처·실제 창 최종 검증 | 설명서·배율·회귀 결과와 실제 포인터 미확인 항목 분리 | 대기 |

#### F1~F17 구현 추적표

| 번호 | 단계 | 예정 변경 파일 | 집중 검증 | 상태 |
| --- | --- | --- | --- | --- |
| F1 | A | `alert_notes/rich_memo_edit.py` | `/템플릿` 선택·삽입·Undo, 잘못된 payload 경고 | 구현·자동 통과, 실제 창 미확인 |
| F2 | A | `alert_notes/panel.py` | 다중 선택 카테고리 지정·상태 표시·선택 해제 | 구현·자동 통과, 실제 창 미확인 |
| F3 | B | `alert_notes/panel.py` | 메모 3회 전환 후 오늘 요약·캘린더 갱신 | 구현·자동·실창 통과 |
| F4 | B | `alert_notes/panel.py`, `alert_notes/memo_data_service.py` | 편집 시작 전/후 버전, 무변경 중복 방지, 30개 제한 | 구현·자동 통과, 실창 버전 복원 미확인 |
| F5 | B·E | `alert_notes/editor.py`, `alert_notes/memo_data_service.py`, `alert_notes/rich_memo_edit.py` | 오프셋 이동·문맥 복구·중복·원문 삭제·표시 | B 복구·E 표시 자동 통과, 실제 포인터 미확인 |
| F6 | B | `alert_notes/note_clone_service.py`, 클립보드 모듈, `alert_notes/memo_archive.py`, 링크 유틸리티 | 함께 복제된 v1/v2 링크 재매핑과 외부 참조 유지 | 구현·자동 통과, 실창 링크 이동 미확인 |
| F7 | B | `alert_notes/memo_data_service.py`, `alert_notes/rich_memo_edit.py` | 코드 문자열 허용, 실제 로컬 경로 payload만 경고 | 구현·자동 통과, 실창 템플릿 저장 미확인 |
| F8 | C | `alert_notes/editor.py`, `alert_notes/note_property_bar.py`, `alert_notes/text_format_toolbar.py` | 기존 Q1 A안 단축키 칩과 `⚙` 설정 진입, UI 계획의 프리셋 설정 메뉴 | UI 보완계획으로 구체화, 대기 |
| F9 | D | `alert_notes/memo_list.py` | 보기·정렬 최소 폭 90px 이상 | 구현·자동 통과, 480px 미만 `더보기` 이동 |
| F10 | D | `alert_notes/memo_list.py` | 목록 480px의 4열과 가로 스크롤 0 | 구현·자동 통과 |
| F11 | C | `alert_notes/editor.py`, `alert_notes/text_format_toolbar.py`, `alert_notes/format_preset_strip.py` | 서식 전후 칩 y좌표 0, 펼침 76px 이하, 515px에서 2줄·잘림 0, 28px | UI 보완계획으로 구체화, 대기 |
| F12 | D | `alert_notes/memo_list.py`, `ui_theme.py` | 선택 칩 표시, 전체·개별·미지정 필터 | 구현·자동 통과 |
| F13 | C | `alert_notes/editor.py` | 위치 없는 상위 메모 카테고리 칩 최대 폭 | 대기 |
| F14 | D·E | `alert_notes/memo_list.py`, `alert_notes/panel.py` | 목록 작업 상태 말줄임·전체 툴팁·편집 상태 분리 | D 말줄임·E 상태 분리 자동 통과, 실제 포인터 미확인 |
| F15 | E | `alert_notes/version_dialog.py`, `alert_notes/memo_data_service.py` | 로컬 시각·한글 종류·날짜 묶음·평문 diff | 구현·자동 통과, 실제 포인터 미확인 |
| F16 | D | `alert_notes/memo_list.py` | 충돌 사본 표시와 원본 툴팁 | 구현·자동 통과 |
| F17 | C·E | `alert_notes/editor.py`, `alert_notes/rich_memo_edit.py`, `alert_notes/text_format_range_bar.py` | 선택 서식 창 프리셋·백링크 개수·주석 표시·기존 Q2 A안 Undo·템플릿 진입 | C 선택 서식·E 백링크·주석·Undo·템플릿 진입 자동 통과, 실제 포인터 미확인 |

#### 단계 C 상세 순서 — UI 보완계획 통합

- 기준 문서: `9._서식프리셋_토글_구현계획/서식프리셋_토글_구현계획.md`와 같은 폴더의 PNG 5개를 확인했다. 문서의 이미지 링크는 실제 같은 폴더 위치에 맞게 수정했다.
- 기존 보완계획의 단계 C 중 C1·C3은 아래 UI-C1·UI-C2로 대체하고 C2는 UI-C3으로 구체화한다. C4 카테고리 칩과 백링크 버튼, 단계 D 목록, 단계 E 상태·버전·주석 범위는 유지한다.
- UI 계획의 Q1~Q3은 원문 확정안 A를 따른다. 기존 보완계획의 Q1(전역 단축키 칩)·Q2(주석 Undo 통합)와 이름이 겹치므로 문서와 구현 기록에서는 `UI-Q1`~`UI-Q3`으로 구분한다.
- UI-Q4는 다음 단계 진행 승인에 따라 원문 추천 A안인 빈 슬롯 메뉴 즉시 열기로 확정했다. UI-Q5도 2026-09-15 다음 단계 진행 승인에 따라 원문 추천 A안인 약 330px 공간이 없을 때 하단 고정 줄 사용으로 확정했다.

| 하위 단계 | 구현 범위 | 집중 검사와 완료 기준 | 게이트 |
| --- | --- | --- | --- |
| UI-C1 | `서식 ▾`를 칩 줄 맨 왼쪽으로 옮기고 별도 서식 헤더 제거. 속성 패널과 서식 패널은 한 번에 하나만 열고 Esc로 모두 해제 | 5회 여닫기 후 알림 칩 y 변화 0px, 서랍 동시 열림 0, classic 화면 변화 없음 | 조건부 통과: 집중 18개·인접 45개·전체 835개, 실제 포인터 미확인 |
| UI-C2 | 새 `format_preset_strip.py`의 26×26 `가` 샘플 3개와 28px `⚙` 메뉴. 저장 서식·이름·우클릭·빈 슬롯·편집 단축키 진입 유지 | 기존 JSON과 적용 결과 동일, 이름 12자에도 폭 26px, 적용 상태 동기, 빈 슬롯 동작은 UI-Q4 확정안 적용 | 조건부 통과: 집중 49개·인접 37개·전체 842개, 실제 포인터 미확인 |
| UI-C3 | 라벨 제거와 접근성 이름·툴팁 보완, 입력 폭 축소, 접기·기본값 아이콘화, 두 줄 폭 515px에 맞춤 | 두 줄 `sizeHint`가 가용 폭 이하, 잘린 위젯 0, 펼침 추가 높이 76px 이하, 접힘 서식 줄 0 | 조건부 통과: 집중 24개·인접 66개·전체 846개, 배율별 11개, 실제 포인터 미확인 |
| UI-C4 | 선택 글자 서식 창에 24px 프리셋 샘플 3개를 추가하고 메인 툴바와 양방향 동기화 | 선택 범위 적용·메인 체크 동기, 저장 후 아이콘 즉시 변경, 다중 글자·블록 선택 충돌 없음, 515px 하단 줄 잘림 0 | 조건부 통과: 집중 29개·인접 41개·전체 851개, 배율별 18개, 실제 포인터 미확인 |
| UI-C5 | 사용 설명서의 서식 토글 위치·샘플 버튼·우클릭 설명 갱신 | 설명서 문구·캡처 대상과 실제 UI 일치, 설명서 회귀 통과 | 통과: 집중 22개·전체 852개, 캡처 1장 갱신 |

#### 단계 C 추가 보존·검증 계약

- `text_format_preset_{n}`, `text_format_preset_name_{n}`, 서식 JSON 구조, 12자 이름 제한, `PRESET_COUNT = 4`, 숨은 4번 슬롯과 기존 적용 판정을 유지한다.
- classic 레이아웃의 서식 1~3 위치, 알림·D-Day·단축키·기타·카테고리·백링크 동작과 저장 방식은 바꾸지 않는다.
- 모든 콤보와 값 선택 UI는 포커스 여부와 관계없이 마우스 휠 값 변경을 막는 기존 계약을 유지한다.
- 자동 검사는 515·620·780px 및 기존 780·900·1080·1920px, Windows Qt 100·125·150%에서 폭·높이·잘림·패널 배타성·접근성 이름·프리셋 저장 호환을 확인한다.
- 실제 Windows 창에서는 Malgun Gothic의 `⚙` 노출, 흰색·연노랑 샘플 가시성, 선택 서식 창의 본문 비가림, 서식 토글 전후 칩 위치를 확인한다. 미확인 항목은 통과 처리하지 않고 TODO에 남긴다.

#### 단계 C UI-C1 구현 결과

- 별도 `서식 ▾` 헤더를 제거하고 같은 토글을 속성 칩 줄의 맨 왼쪽으로 옮겼다. 서식 칩 뒤 구분선을 두고 기존 호출부가 같은 버튼을 가리키도록 호환 별칭을 유지했다.
- 서식 패널을 열면 알림·D-Day·단축키·기타 속성 패널을 닫고, 다른 속성을 열면 서식 패널을 닫는다. Esc는 열린 패널과 선택 상태를 함께 해제하며 서식 칩의 열기·접기 접근성 이름도 상태에 맞춰 갱신한다.
- 전용 자동 검사에서 5회 여닫기 전후 알림 칩 y좌표 0px 변화, 동시 열린 서랍 0개, classic 배치 유지가 통과했다. 집중 18개, 인접 45개, 전체 835개와 `compileall`, `git diff --check`가 통과했다.
- 임시 DB 검증 창은 실행했으나 Computer Use의 창 목록에 반환되지 않아 실제 포인터 검증은 수행하지 못했다. 사용자 데이터가 열린 창은 조작하지 않았고 임시 DB 두 개는 종료 후 삭제했다.
- 판정: **조건부 통과**. 사용자의 다음 단계 승인으로 UI-C2를 진행했다.

#### 단계 C UI-C2 구현 결과

- 새 `format_preset_strip.py`에 고정 26×26px `가` 샘플 3개를 구현했다. 저장된 글꼴·굵게·기울임·밑줄·취소선·색을 아이콘에 반영하고, 흰색·연노랑처럼 밝은 색에는 회색 외곽선을 그린다. 빈 슬롯은 점선 `+`로 표시한다.
- 클릭은 기존 프리셋 적용 경로를 그대로 호출하고, 우클릭은 해당 슬롯의 저장·이름 변경·초기화 메뉴를 연다. UI-Q4 A안에 따라 빈 슬롯 클릭도 안내창 없이 같은 메뉴를 바로 열고 첫 저장 항목에 초점을 둔다.
- 28px `⚙` 메뉴에 서식 1~3 하위 메뉴와 `편집 단축키 설정…`을 넣었다. compact의 중복 `단축키` 버튼은 제거했고 classic은 프리셋 묶음의 기존 본문 아래 위치를 유지한다.
- `text_format_preset_{n}`, `text_format_preset_name_{n}`과 기존 JSON·4번 숨은 슬롯은 변경하지 않았다. 12자 이름은 툴팁 첫 줄에 표시되며 버튼 외곽 크기는 26px로 유지한다.
- 집중 49개, 인접 37개, 전체 842개와 `compileall`, `git diff --check`가 통과했다. 초기 집중 검사에서 오프스크린 환경의 미지원 글꼴 대체와 QSS 테두리 포함 크기 계산으로 2건이 실패했으나, 환경에서 유효한 글꼴을 쓰는 호환 검사와 26px 외곽 크기 계산으로 바로잡아 최종 실패는 0개다.
- 임시 DB에 흰색·연노랑 프리셋을 넣어 검증 창을 실행했지만 Computer Use가 창을 반환하지 않아 실제 포인터 검증은 수행하지 못했다. 창 종료 후 임시 DB를 삭제했다.
- 판정: **조건부 통과**. UI-C3은 사용자의 다음 단계 승인 전까지 착수하지 않는다.

#### 단계 C UI-C3 구현 결과

- 글씨체·글자 크기·줄 간격의 화면 라벨을 제거하고 각 입력에 같은 의미의 접근성 이름과 툴팁을 넣었다. 글자 크기는 60px, 줄 간격은 72px로 줄이고 색상 묶음 앞에 세로 구분선을 추가했다.
- 공통 서식 도구 버튼을 28px로 통일했다. `접기` 텍스트는 삼각형과 가로줄 아이콘의 36×28px 메뉴 버튼으로, `기본값` 텍스트는 되돌림 아이콘의 28×28px 버튼으로 바꿨다. 기존 동작·메뉴·툴팁은 유지했다.
- 툴바 내부 간격을 4px, 서식 패널 안쪽 여백을 4px로 줄이고 두 28px 줄과 4px 간격이 압축되지 않도록 최소 높이 60px를 설정했다.
- 515·620·780px에서 두 줄 `sizeHint`가 가용 폭 이하이고 보이는 컨트롤이 툴바 경계 안에 있으며, 515px에서 펼침으로 늘어나는 높이가 76px 이하이고 접으면 본문 위치가 원복됨을 자동 검사했다.
- 집중 24개, 인접 66개, 전체 846개가 통과했다. Windows Qt 100·125·150%에서 폭·높이·잘림·프리셋 검사를 각각 11개 통과했고 `compileall`, `git diff --check`도 통과했다.
- 초기 집중 검사에서 재귀 라벨 검색이 콤보 내부 Qt 라벨까지 잡은 검사 오류와 툴바가 둘째 줄보다 2px 작게 배정되는 실제 잘림 1건을 확인했다. 검사는 최상위 줄만 보도록 바로잡고 툴바 최소 높이를 60px로 고정해 최종 실패를 없앴다.
- 임시 DB 검증 창은 실행했지만 Computer Use가 창을 반환하지 않아 실제 포인터와 Malgun Gothic 렌더는 확인하지 못했다. 창 종료 후 임시 DB를 삭제했다.
- 판정: **조건부 통과**. UI-C4와 UI-Q5는 사용자의 다음 단계 승인 전까지 착수하지 않는다.
- 선택 서식 창 전체 재구성, 전용 프리셋 단축키, 4번 슬롯 노출, 어두운 테마, 단계 D 이후 범위와 EXE 생성은 제외한다.

#### 단계 C UI-C4 구현 결과

- UI-Q5 A안에 따라 일반 글자 선택 오른쪽에 전체 도구 폭이 확보되지 않으면 기존 본문 하단 예약 줄을 사용한다. 임의 고정폭 240px 대신 닫기 버튼까지 포함한 실제 `sizeHint`로 빈 공간을 판정한다.
- 선택 글자 서식 창에 공용 `PresetStrip`의 24×24px `가` 샘플 3개를 추가했다. 저장된 슬롯은 선택 범위에 적용하고 메인 툴바와 선택 창의 활성 표시를 함께 갱신한다. 빈 슬롯 클릭과 우클릭은 같은 저장·이름 변경·초기화 메뉴를 연다.
- 메인 툴바에서 프리셋을 저장·이름 변경·초기화하면 선택 창의 아이콘·툴팁이 즉시 다시 그려진다. 일반 선택과 다중 글자·블록 선택의 기존 배타 조건은 유지한다.
- 초기 집중 검사에서 툴팁 색상 문자열 대소문자를 고정값으로 비교한 검사 1건과, 선택 창의 상위 QSS가 샘플 테두리를 없애 실제 외곽을 22px로 줄인 구현 실패 1건을 확인했다. 검사는 대소문자 비의존으로 수정하고 샘플 전용 1px 투명 테두리로 외곽 24px를 복원했다.
- 최종 집중 29개, 인접 41개, 전체 851개가 통과했다. Windows Qt 100·125·150%에서 선택 창·compact 검사를 각각 18개 통과했고 `py_compile`, `git diff --check`도 통과했다.
- 테스트는 `TemporaryDirectory`의 임시 DB만 사용했다. 현재 런타임에 네이티브 Windows 창을 조작할 Computer Use 도구가 노출되지 않아 실제 포인터와 본문 비가림 사용감은 확인하지 못했다.
- 판정: **조건부 통과**. UI-C5는 사용자의 다음 단계 승인 전까지 착수하지 않는다. 프리셋 저장 키·JSON·12자 이름·숨은 4번 슬롯과 UUID·revision·UTC·tombstone·v1/v2 링크·백업 계약은 변경하지 않았다.

#### 단계 C UI-C5 구현 결과

- 프로그램 내 설명서와 `4. TomaDesk_설명서.md`에 제목 아래 맨 왼쪽 `서식 ▾`, 26px 메인 `가` 샘플, 24px 선택 프리셋, 빈 슬롯 클릭, 우클릭 저장·이름 변경·초기화, 톱니의 편집 단축키 진입과 공간 부족 시 하단 고정 줄을 설명했다.
- 임시 DB 전용 캡처 도구가 서식 패널을 펼치고 `지난 분기 기준`을 선택하도록 갱신했다. `assets/manual/03_memo_editor.png` 한 장을 1360×860으로 다시 만들고 새 실제 배치에 맞춰 설명서 번호 위치를 조정했다. 함께 다시 생성된 범위 밖 이미지 3장은 단계 시작 전 상태로 복원했다.
- 설명서 내용 검사에 현재 UI 필수 문구와 폐기된 `아래 「서식 ▾」`, `휴지통으로 이동` 문구 부재를 추가했다. 집중 22개, 전체 852개와 `py_compile`, `git diff --check`가 통과했다.
- 캡처는 `TemporaryDirectory`의 임시 DB에서만 생성했다. 이미지 자체는 확인했지만 실제 Windows 포인터 조작은 현재 런타임 도구 미노출로 확인하지 못해 단계 C 실창 TODO를 유지한다.
- 판정: **조건부 통과**. UI-C1~C5 자동 구현과 회귀는 완료했고 실제 포인터 검증만 남았다. 단계 D는 사용자의 다음 단계 승인 전까지 착수하지 않는다. 사용자 DB·기존 EXE·UUID·revision·UTC·tombstone·v1/v2 링크·백업 계약은 변경하지 않았다.

#### 단계 C 구현계획 사후 대조 검증

- 2026-09-16에 `서식프리셋_토글_구현계획.md`와 PNG 5개를 현재 코드·캡처·테스트에 다시 대조했다. 단계 A의 칩 이동·배타 서랍, 단계 C의 두 줄 폭·높이, 단계 D의 선택 프리셋·하단 줄, 단계 E의 설명서는 계획과 일치했다.
- 단계 B에는 기능을 깨지 않는 세부 편차 3건이 남아 있다. 샘플 툴팁은 계획의 `우클릭: 저장·이름·초기화` 대신 `우클릭: 관리`로 축약됐고, `setAutoRaise(True)`가 명시되지 않았으며, 체크 테두리는 계획의 1.5px 대신 1px이다.
- 실제 캡처에서 `⚙`가 빈 사각형처럼 보이는 항목은 실제 Windows Malgun Gothic 렌더로 재확인이 필요하다. 임시 DB 창을 실행했지만 Computer Use가 앱을 반환하지 않았고 런타임의 `listApps`도 제공되지 않아 포인터 검증은 수행하지 못했다.
- 집중 31개, Windows Qt 100·125·150% 각각 21개, 전체 852개와 `git diff --check`가 통과했다. 판정은 **조건부 통과 유지**이며 세부 편차와 실창 항목을 해소하기 전에는 구현계획과 100% 동일하다고 기록하지 않는다.

#### 단계 D 구현 범위

- `memo_list.py`의 카테고리 필터에 색 점과 `미지정`을 추가하고, 선택된 칩을 명확히 표시한다. 보기·정렬은 별도 줄에서 각각 90px 이상을 보장하며 최근 메모는 접이식으로 둔다.
- 목록 열을 `선택·제목·카테고리·수정일`로 유지하면서 카테고리는 `색 점 + 이름`, 미지정은 `—`, 수정일은 `MM-DD`로 표시한다. 480px 목록에서 네 열과 머리글을 유지하고 가로 스크롤이 생기지 않게 한다.
- 체크한 메모가 있으면 하단을 `N개 선택 · 카테고리 지정 · 선택 삭제` 작업줄로 전환한다. 일괄 지정 뒤 선택을 해제하고 목록 작업 상태는 말줄임과 전체 툴팁을 함께 제공한다.
- `conflict_of_sync_id`가 있는 메모는 제목에 `⚠ 충돌`을 표시하고 원본 제목과 정리 안내를 툴팁에 넣는다. 비교·병합 화면은 만들지 않는다.
- 사용자가 제안한 제목 첫 이모지·특수기호 분류 버튼을 검색 줄에 추가한다. 앞 공백을 제외한 첫 문자 모양 단위가 Unicode 기호인 메모만 동적으로 집계하며, 검색·카테고리와 AND로 적용한다. 값은 DB와 설정에 저장하지 않고 앱 재실행 시 해제한다. 대괄호 머리말·첫 단어·자동완성은 **기존 단계 D에서는** 제외했다. 후속 [11번 제목 분류 필터 계획](11._제목분류필터_계획/제목분류필터_구현계획.md)에서 첫 단어 분류는 폐기하고, 머리말·자동완성과 기호 규칙 보완을 G-A~G-G로 처리한다.
- 집중 검사는 기호 추출, 필터 조합, 미지정, 색 점, 충돌 표시, 선택 작업줄, 상태 말줄임, 480px 네 열·가로 스크롤, 보기·정렬 최소 폭을 포함한다. 전체 회귀와 Windows Qt 100·125·150% 자동 검사를 실행하고 실제 포인터를 확인하지 못하면 TODO로 남긴다.

#### 단계 D 구현 결과

- 카테고리 필터와 표 셀에 저장 색상 점을 표시하고 `미지정` 필터를 추가했다. 미지정 표 셀은 `—`로 줄이고 전체 이름은 툴팁에 유지했으며 수정일은 `MM-DD`, 전체 일시는 툴팁으로 표시한다.
- 보기·정렬은 480px에서 각각 90px 이상의 폭으로 검색 줄에 유지한다. 480px 미만에서는 `더보기`의 보기·정렬 하위 메뉴로 이동하며, 목록은 380px 미만에서만 수정일 열을 접는다. 480px에서는 네 열과 가로 스크롤 0을 자동 확인했다.
- 체크한 메모가 있으면 하단을 `N개 선택 · 카테고리 지정 ▾ · 선택 삭제`로 전환하고 일괄 지정 성공 후 체크를 해제한다. 목록 작업 상태는 좁으면 말줄임하고 전체 문장을 툴팁에 남긴다. 최근 메모 칩은 `최근 ▸`로 접어 목록 높이를 유지한다.
- 충돌 사본 제목에 `⚠ 충돌`을 표시하고 툴팁에 원본 제목과 비교 후 정리 안내를 넣었다. 비교·병합 동작은 추가하지 않았다.
- 제목 앞 공백을 제외한 첫 Unicode 기호 문자 모양 단위를 추출하는 동적 필터를 추가했다. `✨`, `⚡`, `★`, 변형 선택자·피부색·ZWJ·국기 조합을 한 단위로 처리하고 일반 문장부호는 제외한다. 카테고리·검색과 AND로 적용하며 DB·설정에 저장하지 않는다.
- 단계 D 전용 8개는 Windows Qt 100·125·150%에서 각각 통과했고, D 집중·인접 회귀 176개가 통과했다. C 영역 신규 검사 파일을 제외한 전체 루트 회귀 860개는 401·113·346개 세 구간으로 나눠 모두 통과했다. `py_compile`과 `git diff --check`도 통과했다.
- 단계 밖 C 검사 2건은 현재 코드에서 독립 재현된다: 속성 칩 줄 484px가 기대 380px를 넘고 줄 간격 `1.0` 표시 폭 39px가 기대 31px를 넘는다. C 제외 지시에 따라 수정하지 않았다. 실패 후 위젯 정리 전에 저장소가 닫히는 기존 테스트 수명주기 때문에 단일 프로세스 전체 실행은 다음 Qt 테스트에서 종료되므로 분할 회귀 결과를 기록했다.
- 임시 DB 전용 `TomaDesk 단계 D 임시 검증` 창을 실행했지만 Computer Use가 `apps: []`를 반환해 실제 포인터 입력은 수행하지 못했다. 창과 임시 DB는 종료·삭제했다.
- 판정: **조건부 통과**. 단계 D 기능과 자동 회귀는 통과했지만 단계 밖 C 실패 2건과 실제 Windows 포인터 검증이 남아 있다. 단계 E는 사용자의 명시적 승인 전까지 착수하지 않는다.

#### 단계 E 구현 결과

- 편집기 저장·카테고리·본문 작업 안내는 편집기 하단 `saved_status`에 시각과 함께 표시하고, 목록 작업 안내는 Excel 내보내기 옆 `action_status`에만 전달한다. 목록을 접었을 때는 공용 안내 줄에 목록 작업 결과를 전달하며 다른 탭의 안내 문구는 유지한다. 자동 저장과 직접 저장을 구분하고 기존 초안·저장 실패 동작을 유지했다.
- 버전 목록을 UTC 밀리초 원문 대신 로컬 `HH:MM`·한글 종류와 `오늘/어제/날짜` 머리글로 표시한다. 저장 payload는 바꾸지 않고 현재 본문과 선택 버전을 `display_plain_text_from_content()`의 평문 줄로 비교하며 추가·삭제·머리글 줄에 색을 입힌다.
- 제목 줄에 `🔗 N`을 연결 수가 있을 때만 보이고 누르면 바로 백링크 메뉴를 연다. 주석은 본문 `extraSelections`의 노란 배경·밑줄, 마우스오버 말풍선, 클릭 후 수정·삭제 메뉴로 연결한다. 위치 확인이 필요한 주석은 임의 본문 위치에 그리지 않고 기존 메뉴에서 표시한다. `⋯ > 기록과 연결`에 선택 템플릿 저장 진입점을 더했다.
- 주석 추가·수정·삭제는 문서 Undo 단계 위치를 기억해 본문 `Ctrl+Z`/Redo 경로에 연결했다. 메모 재열기로 문서가 교체되면 이 임시 Undo 기록을 비우며 UUID·revision·UTC·tombstone 저장 경로는 그대로 사용한다.
- 단계 E 집중 7개는 Windows Qt 배율 100·125·150%에서 각각 통과했다. 관련 묶음 164개, 분할 루트 회귀 359+42+109+171+185=866개가 통과했다. 분할은 Qt 객체 수명주기 문제를 피하기 위해 별도 프로세스에서 수행했다. 저장 상태의 구형 기대값은 새 UI 계약에 맞춰 검증 문구를 갱신했으며 저장·목록 분리 검사도 추가했다.
- 단계 밖 UI 검사 3건은 독립 재현된다. 기존 C 검사 2건(칩 줄 484px > 380px, 줄 간격 표시 39px > 31px)과 카테고리 칩 실측 30px > 28px이다. 마지막 건은 E에서 수정하지 않은 기존 카테고리 칩·스타일 경로이고, 해당 검사만 제외한 나머지 전체 모듈을 검증했다. 자동 검사에서 제외된 한 건을 통과로 기록하지 않는다.
- Computer Use는 `apps: []`를 반환해 임시 Windows 창의 실제 포인터 조작을 확인할 수 없었다. 사용자 DB와 EXE는 열거나 만들지 않았고 단계 F 코드는 수정하지 않았다. 판정은 **조건부 통과**이며 F는 사용자 승인 대기다.

#### 단계 F 구현 결과

- 780·900·1080·1330px × 앱 배율 100·125·150%의 배치 계약 검사 12개를 추가했다. 칩 줄 y좌표, 28px 버튼, 텍스트 잘림, 서식 펼침 5회, 본문 높이 70% 이상, 목록 가로 스크롤 0, 보기·정렬 폭과 제목 줄의 창 안쪽 배치를 검사한다. 각 조합은 Qt 수명주기 충돌을 피하도록 별도 프로세스에서 수행한다.
- 기존 사용성 S1~S13 스크립트를 `tools/memo_usability_test.py`로 옮겨 임시 DB·임시 출력만 사용하게 하고 현재 UI의 진입점을 반영했다. 71개 확인, 실패 0개, 슬롯 예외 0개다. 기존 속성 칩·줄 간격·카테고리 칩의 28px 실패 세 건을 폭·테두리 계산으로 수정했다. 내용이 있는 오늘 요약도 접으면 편집기가 남는 분할 폭을 채우고 다시 열 수 있게 했다.
- Windows Qt `QWidget.grab()`으로 12장 기준 이미지를 `assets/qa/stage_f_layout/`에 저장했다. 캡처는 실제 포인터나 운영체제 디스플레이 배율 검증이 아니다. 임시 DB 실창은 실행됐으나 Computer Use 창 목록에 나타나지 않아 포인터 체크리스트는 미검증으로 `3._TODO.md`에 남겼다.
- 집중 검사 13개와 관련 기존 UI 검사, 전체 루트 52개 모듈 **902 passed**를 통과했다. 큰 묶음의 Qt 네이티브 종료는 더 작은 독립 프로세스로 재실행해 모든 모듈을 통과시켰다. `py_compile`·`git diff --check`도 통과했다. 사용자 DB·원본·기존 EXE 및 UUID·revision·UTC·tombstone·v1/v2 링크·백업 계약은 보존했다. **조건부 통과**이며 G-A는 명시적 승인 전까지 착수하지 않는다.

#### 후속 단계 G-A~G-G — 제목 분류 필터 계획 추가 (구현 전)

- 기준 자료: [`11._제목분류필터_계획/제목분류필터_구현계획.md`](11._제목분류필터_계획/제목분류필터_구현계획.md), [`제목분류필터_계획.html`](11._제목분류필터_계획/제목분류필터_계획.html), 같은 폴더의 `images/01`~`10` PNG를 모두 확인했다. HTML은 초기 선택지와 개념 시안이고, 확정 규칙·단계·검증은 Markdown의 00~07절을 기준으로 한다. 아래 G-A~G-G는 원문 내부 A~G에 대응한다.
- 게이트: 기존 단계 F를 먼저 마감한다. 이후 G-A부터 G-G까지 **한 번에 한 단계만** 구현하고, 단계마다 집중 검사·전체 회귀·`py_compile`·`git diff --check` 결과를 보고한 뒤 멈춘다. 다음 단계는 사용자가 명시적으로 승인한 경우에만 시작한다. 이번 요청은 계획 반영에 한정하며 G 코드·데이터·설명서는 수정하지 않는다.
- 화면 목표: [현재 화면](11._제목분류필터_계획/images/01_보내주신_실제화면.png)과 [420px 목표](11._제목분류필터_계획/images/02_목표_목록패널.png)를 기준으로 검색 줄은 `검색 · 기호 · 머리말`, 아래 줄은 `전체 · 항상 보이는 카테고리 칩 · 더보기`로 정리한다. [버튼 세 상태](11._제목분류필터_계획/images/03_버튼_세가지상태.png), [빈 상태 도움말](11._제목분류필터_계획/images/04_빈상태_설명말풍선.png), [값 메뉴](11._제목분류필터_계획/images/05_값목록_메뉴.png), [더보기](11._제목분류필터_계획/images/06_더보기_메뉴.png), [카테고리 설정](11._제목분류필터_계획/images/07_카테고리설정창.png), [자동완성](11._제목분류필터_계획/images/08_머리말_자동완성.png), [평평한 목록·결과 없음](11._제목분류필터_계획/images/09_평평한목록_결과없음.png), [첫 단어와 머리말 비교](11._제목분류필터_계획/images/10_첫단어_머리말_비교.png)를 각각 검증 기준으로 삼는다.
- 확정안: 첫 단어 분류는 사용하지 않고 제목 앞의 명시적인 `[머리말]`만 분류한다. 제목 칸 자동완성은 포함한다. 보기·정렬은 폭과 무관하게 항상 `더보기`에 넣고, 더보기는 칩이 모두 보여도 오른쪽 끝에 둔다. 기본 고정 칩은 업무·개발·자료 3개이고 `미지정`은 기본적으로 더보기에 둔다.
- 기호: 제목 앞 공백 다음 첫 글자의 Unicode 범주가 `So`일 때만 인식한다. 기존 국기·ZWJ 등 표시 묶음은 유지하되 비교 키에서 FE0E·FE0F와 피부색을 빼서 `✨/✨️`, `👍/👍🏻`를 합친다. `+`, `$`, `^`, `→`, `※`는 기호 필터 대상에서 제외한다. 머리말은 앞 공백과 연속 So 기호 묶음을 넘긴 직후 첫 `[ ]`에서 안쪽 1~20자를 읽고, 빈 값·닫는 괄호 없음·제목 중간은 제외한다. 키는 NFC+casefold, 표시는 가장 최근 메모의 원래 표기를 쓴다.
- 필터: 검색과 카테고리 조건을 적용한 행에서 기호 개수는 머리말 조건만, 머리말 개수는 기호 조건만 추가 적용해 센다. 최종 결과는 네 조건의 AND다. 기호나 머리말이 활성화되면 평평한 목록으로 보여 주고 끌어 옮기기·모두 접기를 잠시 끄며 원래 위치를 툴팁에 유지한다. 결과가 없으면 조건 요약과 `기호·머리말 필터 해제`를 보여 주되 이 버튼은 카테고리·검색을 보존한다. 두 제목 필터는 재실행 시 해제되고 기존 카테고리 필터 기억은 유지한다.
- G-A: `alert_notes/title_symbols.py`에서 So 판정·기호 키·머리말 추출/키·개수/최근수정/키 순 집계를 구현하고 규칙 표·20개 고정 제목으로 검증한다.
- G-B: `alert_notes/memo_list.py`에 머리말 필터와 교차 집계, 필터 없음/0개 상태, 평평한 목록 진입·복귀를 구현한다. 기존 기호 API의 같은 키 입력과 계층 보기 동작을 유지한다.
- G-C: 목록 검색 줄에 머리말 버튼을 기호 버튼 바로 오른쪽에 추가한다. 대표값은 `✨ 5 ▾`/`[TD] 3 ▾`, 선택 시 개수를 뺀 체크 상태, 값 없음은 흐린 `기호`/`머리말` 점선 상태로 한다(2026-09-17 확정, `☆`/`[ ]` 폐기). 빈 버튼은 전체 없음과 현재 조건 때문에 없음이 다른 [도움말](11._제목분류필터_계획/images/04_빈상태_설명말풍선.png)을 연다. 값 메뉴는 개수·최근 수정·키 순이며 0개가 된 선택값을 유지하고 머리말이 10개를 넘으면 메뉴 검색을 제공한다. 긴 머리말은 안쪽만 96px 안에서 줄인다. `ui_theme.py` 배율 스타일도 포함한다.
- G-C 추가 확정(2026-09-17): ① 96·120·240·120px은 모두 화면 배율을 곱한 값으로 적용하고 120px은 버튼 전체 폭, 96px은 안쪽 글자 폭으로 한다. ② `memo_list.py`의 기호 메뉴·버튼 글자 두 곳에 남은 `(-개수, 키)` 재정렬을 제거해 "동률이면 최근 수정" 규칙을 되살린다. ③ `_build_..._menu()`로 나눈 뒤에도 `title_symbol_menu`·`title_prefix_menu` 속성 보관을 유지한다. ④ 말풍선 경로에서도 버튼 눌림을 해제한다. ⑤ 메뉴 검색 중에도 `전체`·구분선·걸린 값은 남기고 결과가 없으면 비활성 안내 한 줄을 보인다. ⑥ 300px 배치 확인을 G-C 완료 기준에 포함하고, G-B에서 미룬 실제 창 확인을 G-C 체크리스트에 합친다. ⑦ 기존 검사 `test_memo_remediation_stage_d.py`와 `test_memo_title_filters.py`의 버튼 글자 기대값을 함께 고친다.
- G-C 범위 추가(2026-09-17): 칩 줄 오른쪽 끝 `더보기` 바로 왼쪽에 `↺ 필터 초기화`(`memoResetFiltersButton`)를 넣는다. 카테고리·검색·기호·머리말 중 하나라도 걸렸을 때만 보이고 누르면 네 가지를 한 번에 풀며 보기·정렬·핀 설정은 유지한다. 좁은 폭에서는 28px 아이콘만 남기고 카테고리 칩이 먼저 `더보기`로 밀린다. 배치 함수는 원래 G-D 범위지만 마지막 한 줄만 쓰므로 G-C에서 함께 구현한다. 결과 0개 안내 속 `기호·머리말 필터 해제`는 푸는 범위가 다르므로 그대로 둔다.
- G-D: `memo_list.py`의 보기·정렬 콤보 객체와 설정 복원은 유지하되 검색 줄에서는 숨긴다. `더보기`를 항상 오른쪽에 표시하고 숨은 카테고리/미지정 → 보기 → 정렬 → 카테고리 설정 순으로 배치한다. 비기본 보기·정렬은 파란 점, 숨은 카테고리 선택은 그 이름·색 점을 표시한다. `panel.py`에서 기존 카테고리 관리 창을 연결한다.
- G-E: 새 `alert_notes/memo_category_pins.py`의 로컬 설정 `memo_category_pinned`에 항상 보이는 카테고리 ID와 `none`만 보관한다. 기존 설정이 없으면 현재 순서 앞 3개를 기본으로 삼고, 추가·순서 변경 전에 기본값을 명시 저장해 칩이 갑자기 바뀌지 않게 한다. 기존 `CategoryManagerDialog`에 체크 칸과 고정 `미지정` 행을 추가하고 체크 변경을 즉시 반영한다. 고정하지 않은 카테고리는 넓은 창에서도 더보기에만 둔다. DB 테이블·동기화 계약은 바꾸지 않는다.
- G-F: `editor.py` 제목 칸 맨 앞의 `[` 안쪽을 입력할 때만 기존 머리말을 사용 빈도순으로 제안한다. 방향키·Enter로 선택하면 `[머리말] `을 채우고 앞 기호와 뒤 제목을 보존한다. 제목 중간의 `[`와 제안 없음은 기존 입력 흐름을 유지하고 자동 저장과 충돌하지 않게 한다.
- G-G: 프로그램 내 설명서와 `4. TomaDesk_설명서.md`를 실제 UI로 갱신한다. 임시 DB로 목록 폭 380·480px × 100·125%와 카테고리 8개 넘침을 캡처하고 Windows Qt 100·125·150% 자동 검사, 단계별 집중 검사, 전체 회귀를 수행한다. 실제 Windows 포인터 검증은 자동 Qt 검사와 구분하고 불가하면 `3._TODO.md`에 남긴다.
- 보존·제외: 사용자 DB·원본 파일·기존 EXE, 메모 계층·수동 순서·선택 작업·충돌 표시, 검색 의미, 카테고리/보기/정렬 설정 키, UUID·revision·UTC·tombstone·v1/v2 링크·구형/신형 백업 계약을 유지한다. 첫 단어 자동 분류, 본문/제목 중간 머리말, 머리말 일괄 병합, 고정 칩 설정의 기기 간 동기화, DB 열 추가, 새 EXE는 범위 밖이다. 테스트와 캡처에는 임시 DB만 쓴다.
- 착수 전 확인: 기존 승인 계획은 카테고리 순서의 드래그와 `위로/아래로` 버튼을 모두 요구하지만 새 시안은 버튼을 없애고 `Ctrl+↑/↓`로 대체한다. 기존 접근성 계약을 바꾸는 부분이므로 G-E 구현 전 사용자 결정이 필요하다. 또한 [420px 목표 이미지](11._제목분류필터_계획/images/02_목표_목록패널.png)에는 `최근 ▸`가 보이지만 본문은 필터 줄을 칩+더보기로 기술한다. 최근 메모 영역은 기존 접이식 동작을 유지하고 새 위치/모양은 G-D 착수 전 확인한다. 이 두 차이는 조용히 확정 처리하지 않는다.

#### 단계 G-A 구현 결과

- `title_symbols.py`에서 제목 첫 글자의 `So` 판정만 기호로 인정하고, 기존 국기·ZWJ·변형 문자·피부색을 하나로 묶어 반환하는 표시 경로는 유지했다. 비교 키에서는 FE0E·FE0F와 피부색을 제외한다. 제목 앞 공백과 연속 `So` 묶음을 건너뛴 첫 `[머리말]`의 안쪽 1~20자를 추출하고 NFC+casefold 키로 집계한다.
- `TitleValueCount`와 `count_title_values()`는 키별 개수, 가장 최근 `updated_at`의 원래 표시, 개수 내림차순·최근 수정 내림차순·키 순을 제공한다. 순수 규칙과 SQLite 행 입력을 검사했고 고정 제목 20개는 `td=3·토마=2·모리=2`였다.
- 집중 50개와 루트 전체 53개 파일 **944 passed**를 통과했다. `py_compile`·`git diff --check`도 통과했다. UI 필터·버튼·DB·설정·백업은 변경하지 않았고, 순수 규칙 단계이므로 별도의 실제 포인터 결과는 없다. 사용자 DB·기존 EXE·UUID·revision·UTC·tombstone·v1/v2 링크 계약을 유지했다. **통과**로 마감하고 G-B는 명시적 승인 대기다.

#### 단계 G-B 구현 결과

- `memo_list.py`에서 기호·머리말 필터는 표시 문자열 대신 G-A 비교 키를 일시적으로 보관한다. 검색·카테고리를 먼저 적용한 뒤 기호 개수에서는 머리말 필터만, 머리말 개수에서는 기호 필터만 추가 적용하고 최종 행에는 네 조건을 AND로 적용한다. 기호 또는 머리말이 활성화되면 평평한 목록을 쓰고 끌기·모두 접기를 끄며, 해제하면 원래 계층·카테고리별 보기로 복귀한다.
- 결과가 0개이면 `memoFilteredEmptyState`에 안내·조건 요약과 제목 필터 해제 버튼을 표시한다. 해제는 기호·머리말만 지우고 한 번 갱신하며 검색·카테고리 조건을 유지한다. 필터가 없을 때는 기존 빈 목록 안내를 유지한다. 기존 기호 버튼 글자·메뉴의 시각 계약은 G-C 전까지 보존하고 머리말 버튼은 추가하지 않았다.
- 임시 DB·메인 패널 경로와 Windows Qt 앱 배율 100·125·150%의 집중 **75 passed**, 루트 53개 파일 전체 **969 passed**. 새 검사 파일의 `QApplication` 참조 해제로 다음 테스트 모듈의 포스트잇 생성에서 접근 위반이 처음 재현됐으나 검사 앱 참조를 유지하도록 수정했고, 두 파일 126개와 최종 전체 회귀 969개는 네이티브 종료 없이 통과했다. `py_compile`·`git diff --check`도 통과했다.
- 실제 Windows 포인터 조작은 이번 자동 검사와 별개로 `3._TODO.md`에 남긴다. 테스트는 임시 SQLite만 사용했고 사용자 DB·기존 EXE·UUID·revision·UTC·tombstone·v1/v2 링크·백업 계약을 변경하지 않았다. **통과**로 마감하고 G-C는 명시적 승인 대기다.

#### 단계 0 작업과 검증

- 확정 계획과 승인 게이트, Q1·Q2 A안을 이 파일에 기록한다.
- 보완계획 문서의 이미지 참조를 실제 폴더 구조와 맞춘다.
- 현재 전체 회귀 815개를 기준선으로 두고 다시 실행한다.
- 집중 검사는 계획 문서의 F1~F17·단계 A~F·Q1/Q2·보존/제외 항목과 9개 PNG 참조가 모두 존재하는지 확인한다.
- 테스트 코드와 수동 검증 도구가 `TemporaryDirectory` 또는 명시적 임시 DB만 사용하는지 확인한다.
- 검증 결과: 목록 콤보의 전역 테마 높이와 서식 버튼의 기본 테두리 계산을 고정했다. 실패 2개 단독 검사와 전역 테마 포함 26개 묶음, 최종 전체 회귀 815개가 통과했다.
- 상태: 단계 0 통과. 사용자의 승인에 따라 단계 A까지 진행했다.

#### 단계 A 구현과 검증

- `rich_memo_edit.py`에 누락된 `json` 가져오기를 추가하고 `/템플릿` 선택과 템플릿 저장의 `ValueError`를 경고창으로 처리한다.
- `panel.py`의 일괄 카테고리 지정 상태 호출에 `success` 수준을 전달한다.
- 주석·템플릿·버전 버튼 슬롯의 예상 가능한 데이터 오류를 경고창으로 처리해 프로그램이 유지되게 한다.
- `A_shortcut_launcher.py`에 최종 `sys.excepthook`을 설치한다. 처리되지 않은 Qt 슬롯 예외는 현재 데이터 폴더의 `tomadesk_error.log`에 기록하고 경고창을 표시한다.
- 검증 결과: 단계 A 신규 5개를 포함한 집중 검사 29개, 컴파일 검사, 최종 전체 회귀 820개가 통과했다.
- 실제 창: 전용 임시 DB 창 프로세스는 실행됐으나 Computer Use 창 목록에 TomaDesk/Python 창이 노출되지 않아 포인터 입력을 수행하지 못했다. 임시 DB는 종료 후 제거했다.
- 상태: 자동 검사는 통과했으나 실제 Windows 포인터 검증은 완료하지 못해 조건부 통과로 판정한다. 단계 B에는 착수하지 않는다.

#### 단계 B 구현과 검증

- 메모 전환 때 실행되던 `calendar.shutdown()`과 `summary.shutdown()`을 패널의 실제 종료 경로로 옮겨 오늘 요약이 계속 갱신되게 했다.
- 첫 실제 저장 직전에 `session_start` 버전을 한 번 만들고 전환·종료 때 `auto` 최종본을 남긴다. 무변경 세션은 만들지 않으며 두 자동 종류를 합쳐 최근 30개로 정리한다.
- 주석은 저장·재열기 때 block ID, 원문, 앞뒤 문맥 순으로 위치를 복구한다. 유일한 위치가 없으면 `needs_review`를 유지하고 위치·상태 변경에 revision·UTC·기기 UUID를 반영한다.
- 새 공통 링크 재매핑 함수에서 정수/v1과 UUID/v2 메모·블록 링크 및 첨부를 처리한다. 함께 복제·병합된 대상만 새 ID·UUID·block ID로 바꾸고 외부 참조는 유지한다. v2 페이지 링크도 클립보드의 복제 그래프에 포함한다.
- 템플릿은 payload·블록·페이지·주석·첨부 허용 키와 base64를 구조적으로 검사한다. 본문 코드 문자열은 허용하고 `href`·`src`의 실제 로컬 경로만 거부한다.
- 첫 전체 회귀에서 주석이 없는 저장도 관계를 재조회해 SELECT 예산 검사가 10건으로 늘어난 실패 1건을 발견했다. 편집기에 주석 ID를 캐시하고 저장 후 전체 관계 재조회를 제거해 해당 검사와 단계 B 검사 11개를 재통과했다.
- 검증 결과: 단계 B 신규 10개, 관련 기존 회귀를 합친 집중 검사 96개, 컴파일·diff 검사, 수정 후 최종 전체 회귀 830개가 통과했다.
- 실제 창: 전용 임시 DB의 `TomaDesk 메모 구현 검증` 창을 고유하게 탐지했다. 포인터로 메모를 세 차례 전환하고 D-Day를 저장한 뒤 오른쪽 오늘 요약에 `D-1 · 실제 창 D-Day`가 나타나는 것을 확인했다. 창을 정상 종료하고 임시 DB를 제거했다.
- 상태: 자동 검사는 모두 통과했고 F3 실제 창도 통과했다. F4·F6·F7의 실제 포인터 검증은 TODO에 남겨 단계 B를 조건부 통과로 판정하며 단계 C에는 착수하지 않는다.

### 2026-09-15 메모 편집·모바일 연동 대비 전체 구현 완료

- 변경 요약: Phase 4를 마감하고 MD 코드·범위·접힘 표·제목 표시, 28px 편집 UI와 공용 상태 영역, 메모 카테고리·필터·정렬·모아보기, UUID/revision/UTC/device/tombstone 저장 기반, v2 링크, 백링크·주석, 블록 템플릿, 버전 기록을 순서대로 구현했다.
- 호환·보존: 기존 정수 PK/FK, 숫자/v1 링크, `vanilla`, 구형 `.tomamemo`, 메모 계층·수동 순서·첨부·알림·D-Day·포스트잇·단축키를 유지했다. 독립 복제는 새 UUID, 교체 복원은 기존 UUID, 병합 충돌은 새 UUID의 관계 있는 사본으로 보존한다.
- 검증 결과: 임시 DB 마이그레이션 백업·반복 안정성·복제·충돌·tombstone·백업 왕복·첨부 포함 버전 복원 rollback과 MD/UI/Phase 4 집중 검사를 통과했다. 전체 회귀 815개와 Windows Qt 100/125/150%의 780/900/1080/1920px 집중 검사 각 24개를 통과했다. 실제 포인터 미확인 사항은 작업 기록과 TODO에 둔다.
- 제외 유지: 모바일 앱·서버·계정·네트워크 동기화·푸시 알림·일정 카테고리 통합·다중 태그·EXE 생성은 구현하지 않았다.

### 메모 편집 UI 개편 (2026-09-14 구현 승인, 진행 중)

- 요청·목표: Phase 4 편집기 기능을 보존하면서 본문 폭과 높이를 늘리고, 알림·D-Day·색·단축키 등 속성과 서식 도구를 필요할 때 펼친다.
- 변경 범위: 저장된 분할 비율을 유지하고 새 기본값만 `520:780:400`으로 변경한다. 빈 오늘 요약은 재열기 손잡이로 접는다. 편집기 자식 속성 칩과 본문을 가리지 않는 인라인 패널, 선택 줄 오른쪽 또는 공유 하단 띠의 글자 서식 도구, 좁은 창 목록 서랍을 제공한다.
- 기능·호환: 기존 위젯·값·단축키·자동 저장을 재사용한다. 빠른 알림 키와 Ctrl+Enter는 패널이 닫혀도 작동한다. 제목 키, 새 메모 집중 모드 설정, 설명서·툴팁·들여쓰기·접힘 마커 표시를 보완한다. `memo_editor_layout`은 스키마 변경 없는 `classic | compact`, 기본 `compact`로 한다.
- 검증: 임시 DB의 넓은/좁은 창, F11·독립 창·포스트잇, 선택 도구 배타성·본문 글자 가림, 빠른 알림 저장·저장 설정 복원, 전체 회귀 및 Windows 실제 창 100/125/150%를 확인한다.
- 보존·제외: 사용자 DB·첨부·기존 EXE 및 Phase 4 작업 내용을 보존한다. 새 DB 스키마·EXE 생성·배포는 제외한다. classic은 2026-10-14까지 유지 후 별도 검토한다.
- 상태: UI 구현, 전체 회귀 801개 및 Windows Qt 100·125·150% 집중 검사 각 12개를 통과했다. 실제 물리 포인터 조작 확인이 남아 있어 완료로 기록하지 않는다.
- 단축키 충돌: 기존 체크리스트 `Ctrl+Shift+4` 보존을 우선해 제목4를 `Ctrl+Shift+6`으로 배정했다. 사용자에게 두 선택지를 제시했으며 답이 오면 반영한다.

### 메모 편집 기능 대규모 개선 (단계별 진행)

- 요청: 현재 편집기 구조를 재사용하면서 토글 붙여넣기·Redo·독립 복제·다중 블록 조작·접기·목차·블록 링크·백링크·주석·템플릿·버전 기록을 단계적으로 개선한다.
- 추가 반영:
  - 일반 본문에 남은 내부 링크 때문에 손가락 커서가 나타나고 `데이터 관리` 페이지로 이동하는 실제 사례를 Phase 0 최우선 결함으로 포함한다.
  - 범위를 드래그한 뒤 우클릭하여 토글·페이지·링크 등 선택 범위의 구조·동작을 한 번에 해제하는 `연동 기능 삭제`를 Phase 2 공통 명령으로 포함한다.
  - `연동 기능 삭제`는 글자와 일반 문자 서식을 보존한다. 페이지는 삭제하지 않고 일반 하위 메모로 전환하며, 표·이미지는 콘텐츠로 유지한다.
- 권장 순서: Phase 0 안정화 → Phase 1 클립보드·복제 → Phase 2 선택·명령·우클릭 → Phase 3 접기·편집 → Phase 4 block ID·목차·블록 링크·고정 → Phase 5 백링크·주석 → Phase 6 템플릿 → Phase 7 버전 기록.
- 데이터 원칙: Phase 0~3은 schema 변경 없이 진행하고, 영구 block ID는 HTML 기반 지연 마이그레이션을 우선한다. 주석·템플릿·버전 단계에서만 필요한 테이블과 백업 형식을 단계별로 확장한다.
- 구현 원칙: Qt 문서 Undo를 재사용하되 페이지·첨부·메모 행을 바꾸는 명령은 DB 트랜잭션과 보상 Undo를 함께 설계한다. 독립 복제는 새 ID를 만들고 `🔗 메모 링크`는 기존 대상 ID를 유지한다.
- 검증: 각 Phase마다 집중 자동 검사, 전체 회귀, Windows Qt 실제 창 검사를 수행하고 사용자 DB는 읽기 전용 사례 검증에만 사용한다.
- 보존 범위: 기존 메모·페이지·링크·계층·서식·첨부·휴지통·알림·D-Day·포스트잇·단축키·자동 저장·백업 호환, 사용자 DB와 기존 EXE를 보존한다.
- 제외 범위: 카테고리 계획 구현, 일정 카테고리 통합, AI 자동 분류, 외부 협업 동기화, EXE 생성·배포는 포함하지 않는다.
- 상세 계획: [`메모편집_대규모_개선계획.md`](메모편집_대규모_개선계획.md)
- 완료 승인 범위: Phase 3의 제목·토글 범위 접기, 모두 접기/펴기, 줄 간격, 원본/텍스트만/현재 서식 붙여넣기와 키보드 탐색을 구현했다. 기존 Phase 2 선택·명령 경로와 단축키 설정을 재사용했다.
- 상태: 잔류 링크 보완과 Phase 0~3을 완료했다. 사용자 요청대로 여기서 멈추며 Phase 4 착수 승인 대기 중이다. Phase 4 이후 기능, 대규모 선행 리팩터링, EXE 생성은 하지 않았다.

### 메모 편집 Phase 1~3 실사용 오류 보완 (2026-09-14 승인, 진행 중)

- 요청·목표: 페이지 드래그의 오진입, Alt+C 무응답, 다중 블록 선택·공통 명령·우클릭 가림, 제목 1~4 크기·접기 오류를 실제 입력 기준으로 수정한다. 추가 진단의 단일/비연속 토글 묶기 Undo 실패와 Esc 선택 해제 충돌도 포함한다.
- 변경 범위: 일반 드래그는 글자 범위, Alt+클릭/드래그는 블록 선택으로 분리한다. 링크·접기·체크 표시는 같은 대상에서 짧게 누른 클릭에만 반응한다. 우클릭의 선택 대상과 부분 범위 명령을 바로잡고, 각 공통 명령의 결과·Undo/Redo를 확인한다.
- UI: 선택 명령을 본문 밖 편집기 하단 고정 줄로 옮겨 선택 글자를 가리지 않는다. 좁은 창은 더보기로 압축하고 우클릭 메뉴는 선택 영역을 피해 띄운다. Alt+C는 복사/적용/복사된 서식 없음 상태를 자동 닫힘 알림으로 구분한다.
- 제목: 빈 줄에서 제목을 지정하고 이어 쓴 글자에 22/18/15/13pt와 굵기를 적용한다. 표준 HTML heading 메타데이터를 저장하고 기존 제목 판별을 유지해 재열기·접기·본문 전환·붙여넣기와 호환시킨다.
- 구현: 선택 관리자와 마우스 이벤트, 공통 명령의 토글 자식 생성 트랜잭션, 편집기 하단 명령 줄, Esc 단축키 우선순위, 제목 직렬화 경로를 관련 모듈 중심으로 수정한다. 새 DB schema는 만들지 않는다.
- 검증: 실제 press/move/release와 단축키 입력, 단일·연속·비연속 선택별 명령·한 번 Undo/Redo, 페이지·첨부·링크 보존, 제목 1~4의 빈 줄 입력·재열기·접기, 좁은 창·배율 렌더를 집중 검사한다. 전체 회귀 후 임시 데이터의 Windows 실제 창을 Computer Use로 조작한다.
- 보존·제외: 사용자 DB·메모·첨부·기존 EXE 및 기존 기능·단축키를 보존한다. Phase 4 이후 기능, 카테고리 구현, schema 변경, EXE 생성·배포는 제외한다.
- 상태: 코드 수정과 Windows Qt 집중·전체 자동 검사를 완료했다. 사용자가 직접 띄운 임시 데이터 창을 Computer Use로 조작하여 페이지 클릭/드래그, 글자 선택, Alt+C 세 알림, 우클릭 메뉴 위치, 제목 1~4·접기·재열기를 확인했다. 사용자의 Alt+클릭으로 생긴 1개 블록 선택과 하단 명령 줄·실제 Esc 해제도 확인했다. Alt+드래그·비연속 블록 선택 생성과 명령 버튼의 실제 포인터 검증은 남아 있어 이 계획은 진행 중으로 유지한다.

### 메모 글자 범위 다중 선택 (2026-09-14 구현 승인, 진행 중)

- 요청·목표: 떨어진 글자 구간 여러 곳을 한 번에 선택해 글자 서식과 글자 단위 기능을 일괄 적용한다. 현재의 다중 블록 선택과 한 곳의 일반 글자 선택은 유지한다.
- 2026-09-14 추가 오류·수정 승인: Ctrl+드래그 후 크기 조절 화살표에서 손을 떼도 크기 숫자와 본문 글자가 계속 변하는 현상을 안정화한다. Ctrl+드래그의 누름·이동·놓음을 편집기 한 경로에서 완결하고 짧은 Ctrl+클릭의 페이지/일반 링크 열기와 일반 커서 위치 지정을 보존한다. 크기 입력·글씨체 선택 중에는 서식 적용이 해당 입력 컨트롤의 포커스를 빼앗지 않도록 한다.
- 추가 검증: Ctrl+드래그 뒤 크기 위·아래 화살표의 누름→유지→놓음과 숫자 입력, 손을 뗀 뒤 값·서식의 안정성, 반복 선택과 일반 드래그 전환, 짧은 Ctrl+링크 클릭, 단일 Undo/Redo와 좁은 폭·Windows 실제 창을 임시 데이터로 검사한다. 실제 포인터 재현이 불가능한 항목은 완료로 기록하지 않는다.
- 선택안: 일반 드래그는 기존의 한 글자 범위 선택, Alt+클릭·드래그는 블록 선택으로 둔다. Ctrl+드래그로 글자 범위를 추가하되 짧은 Ctrl+클릭의 기존 링크 열기는 유지한다. 키보드로 선택한 현재 글자 범위도 충돌 없는 명령으로 추가할 수 있게 한다. 구현 전에 링크 열기·단축키·텍스트 끌기와 충돌을 확인한다. 블록 모드와 글자 다중 모드는 동시에 활성화하지 않는다.
- 적용 범위: 글씨체·크기·굵게·기울임·밑줄·취소선·색상 등 글자 서식과 선택한 글자 범위의 일반 링크/연동 해제만 일괄 적용한다. 이동·복제·삭제 및 제목·토글·체크리스트·페이지·표처럼 문단·구조를 바꾸는 명령은 다중 글자 선택의 대상에서 제외한다.
- UI·구현: 선택 구간을 본문에서 서로 구별되게 강조하고, 하단 예약 공간에 `N개 글자 구간 선택`과 적용 가능한 명령·해제를 표시한다. 구간의 시작/끝은 문서 편집을 따라가는 커서로 보관하고 겹치거나 맞닿은 구간은 합친다. 일반 클릭·Esc·메모 전환·구조 편집 시 선택을 안전하게 해제한다. 각 일괄 적용은 한 번의 Undo/Redo로 묶고 글자·기존 링크 대상·미선택 서식을 보존한다.
- 검증: 순방향/역방향·비연속·중첩/인접·여러 줄에 걸친 선택, 기존 블록 선택과 전환, 키보드 대안, 링크 구간의 정확한 해제, 연속 서식 적용과 한 번의 Undo/Redo, 저장·재열기, 좁은 폭·배율·포커스·화면 가림을 임시 DB와 Windows 실제 창에서 확인한다.
- 보존·제외: 사용자 DB·메모·첨부·기존 EXE·기존 Alt 블록 선택을 보존한다. 글자 구간 자체의 저장, 글자 이동·삭제·복제, 구조 변환, DB schema·EXE 생성은 제외한다.
- 상태: Ctrl+드래그·Ctrl+Shift+M으로 비연속 글자 구간을 저장하고 서식·부분 링크 해제·하단 명령 줄·Esc를 구현했다. 크기 조절 중 문서 변경 콜백의 포커스 탈취와 Ctrl 드래그의 불일치한 press/release 처리를 보완했다. 손을 뗀 뒤 값 안정성·숫자 입력·두 번의 Ctrl 드래그·짧은 Ctrl 링크 클릭은 임시 DB의 Windows Qt 테스트로 통과했다. Computer Use 실제 포인터 확인은 임시 창 탐지 제한으로 남아 있어 진행 중이다.

### 메모 전체보기 본문 높이 확대 (2026-09-14 구현 승인, 진행 중)

- 요청·목표: 현재 F11 전체보기의 붙임 1보다 메모 본문 입력 영역을 붙임 2의 집중 편집창처럼 더 높게 쓴다. 화면 크기나 DPI 차이를 고려해 픽셀 복제가 아닌 영역 배분을 맞춘다.
- 화면 시안: F11 동안 앱 상단 작업공간/탭/유틸리티 줄과 메모 하단 상태 안내 줄을 숨긴다. 창 제목과 메모 제목·위치 표시·F11 복귀 단추는 유지한다. 제목 아래부터 서식·알림·단축키 등 하단 보조 컨트롤 위까지 본문이 남는 높이를 모두 차지하게 하고, 하단 컨트롤은 기능을 없애지 않은 채 필요한 높이만 쓰도록 세로 여백을 줄인다. F11 해제 시 원래 배치를 정확히 복원한다.
- 구현 범위: `MainWindow`의 작업공간 줄 표시와 `AlertNotesPanel`의 전체보기 상태를 연결하고, `MemoEditor`의 F11 전용 레이아웃/크기 정책만 조정한다. 일반 보기·독립 메모창·포스트잇·다른 탭은 바꾸지 않는다. 본문 스크롤, 자동 저장, 서식 도구, 알림·단축키 접근은 유지한다.
- 검증: 같은 창 크기에서 F11 전후 본문 높이를 측정해 증가를 확인하고 붙임 2의 배치와 시각 비교한다. F11·복귀 버튼 왕복, 메모 전환·창 크기 변경, 좁은 창, Windows 100/125/150% 배율, 하단 명령 줄·포커스·스크롤·컨트롤 잘림을 검사한다. 임시 데이터 실제 창의 Computer Use 확인과 전체 회귀를 수행한다.
- 보존·제외: 사용자 DB·기존 메모/설정/EXE, 비전체보기 배치와 기존 키 동작을 보존한다. 새 도구·서식 기능, 다른 화면 리디자인, EXE 생성·배포는 제외한다.
- 상태: F11 동안 작업공간 줄·메모 상태 줄을 숨기고 보조 줄 여백을 줄여 본문 높이를 늘렸다. 같은 창 크기의 Windows Qt 테스트에서 본문 높이 50px 이상 증가와 복귀를 확인했으며, Computer Use 실제 창 확인은 남아 있어 진행 중이다.

### 메모 편집 Phase 4 (2026-09-14 구현 승인, 진행 중)

- 요청·목표: 현재 글자 다중 선택의 실제 포인터 안정화 확인을 선행한 뒤, 제목 변경·블록 이동 후에도 유지되는 영구 블록 ID, 목차, 블록 링크, 본문 블록 고정을 단계적으로 구현한다.
- 선행 검증 상태: 사용자의 명시적 4단계 착수 요청에 따라 구현을 시작했다. 앞 단계의 Computer Use 실제 포인터 검증은 수행된 것으로 간주하지 않으며 `3._TODO.md`에 남긴다. 검증 중 기존 오류가 재현되면 우선 수정한다.
- 4A 저장 방식 검증: Qt 문서의 사용자 block property가 HTML 왕복에서 유실되는 기존 사례를 전제로, 구형 HTML·제목/토글·표·페이지·첨부·복합 붙여넣기 샘플로 ID 직렬화 실험을 먼저 한다. HTML 안에서 안전하게 왕복하는 표현을 확정하기 전에는 사용자 메모에 ID를 기록하지 않는다. HTML 방식이 성립하지 않으면 대안과 백업·마이그레이션 범위를 다시 승인받는다.
- 4B ID·복제: `block_identity`와 `memo_id + block_id` 해석기를 분리한다. 기존 문서는 읽을 때 메모리에서 ID를 부여하고 사용자가 실제 편집·저장할 때만 지연 기록한다. 이동·제목 변경은 ID 유지, 새 블록·독립 복제는 새 ID, 참조 링크는 대상 ID 유지로 구분한다. Undo/Redo와 복구·백업의 ID 충돌을 검사한다.
- 4C 목차·링크·고정: 제목 계층을 우선 표시하는 좁은 폭 대응 목차와 클릭·키보드 이동을 제공한다. 기존 `toma-note://id`를 계속 읽고 새 버전형 블록 주소와 링크 복사·해결 실패 표시를 추가한다. 본문 블록 고정은 메모 목록의 기존 핀과 별개로 취급하고, 고정 표시·해제·연동 기능 삭제를 일관되게 처리한다.
- 검증·보존: 임시 DB에서 저장·재열기·이동·복제·붙여넣기·삭제·Undo/Redo·구형 백업 왕복과 링크 보존을 집중 검사하고 전체 회귀 및 Windows 100/125/150% 실제 창을 확인한다. 사용자 DB·메모·첨부·기존 EXE는 변경하지 않고, 실제 사용자 자료는 읽기 전용·복제본으로만 검사한다. Phase 5 이후 기능·카테고리·EXE 생성은 제외한다.
- 상태: 4A HTML 왕복 표현을 버전형 비표시 `<meta>`로 확정하고, 4B 블록 ID·복제·Undo/Redo 및 4C 목차·버전형 블록 링크·본문 블록 고정을 구현 중이다. 집중 Qt 16개, 전체 회귀 788개와 백업 병합 왕복 검사를 통과했다. 실제 포인터, 모든 구조 편집 및 페이지 간 이동의 기존 링크·통합 Undo는 아직 검증 중이다. 사용자 DB·EXE는 변경하지 않았다.

### 메모 카테고리·정렬·모아보기 개선 (2026-09-15 구현 완료)

- 요청: 메모마다 메모 전용 카테고리를 지정하고, 메모 목록에서 카테고리 필터·보기 전용 정렬·카테고리별 모아보기를 제공한다.
- 목표: 기존 부모·하위 메모 트리와 직접 끌어 정한 순서를 보존하면서, 위치가 다른 메모도 같은 주제로 빠르게 찾고 묶어 본다.
- 변경 범위:
  - 메모당 카테고리 1개를 지정한다. 초기 카테고리는 `업무`, `개발`, `자료`이며 `미지정`은 삭제할 수 없는 가상 항목으로 취급한다.
  - 카테고리는 메모 전용으로 두고 일정 카테고리와 분리한다. 사용자는 이름·색상·순서를 추가·수정·삭제할 수 있으며 개수는 고정하지 않는다.
  - 새 하위 메모는 생성 시점에 부모 카테고리를 한 번 복사하고 이후에는 부모와 독립적으로 변경한다.
  - 편집 영역의 위치 표시줄 오른쪽에 현재 카테고리 칩을 두고, 메모 목록에는 `전체` 및 카테고리 칩, `보기`, `정렬`, `더보기`를 배치한다.
  - 목록 기본 열은 시안대로 `선택`, `제목`, `카테고리`, `수정일`만 표시한다. 기존 내용 미리보기·일정 정보는 제목 말풍선에 보존한다.
  - `기본 보기`는 기존 계층과 수동 순서를 유지하고, `카테고리별 보기`는 카테고리 순서의 접이식 그룹 아래에 메모를 평면으로 모으며 하위 메모에는 원래 부모 경로를 보조 표시한다.
  - 정렬은 `기본`, `수정일 최근/오래된`, `제목 가나다/역순`을 제공한다. 고정 메모는 모든 정렬에서 각 계층·그룹의 맨 위에 유지한다.
  - 검색과 카테고리 필터는 AND로 결합하고, 보기·정렬·필터는 재실행 후 복원한다. 삭제된 카테고리를 기억한 경우 `전체`로 안전하게 되돌린다.
  - 현재 폭에 들어오지 않는 카테고리 칩은 `더보기` 메뉴에 넣는다. 관리창의 순서 변경은 드래그와 `위로/아래로` 버튼을 함께 제공한다.
  - 기존 체크 선택을 이용해 여러 메모에 카테고리를 일괄 지정하며, 카테고리 삭제 시 연결 메모 수를 확인한 뒤 메모는 지우지 않고 `미지정`으로 바꾼다.
- 구현:
  - 별도 `memo_categories` 저장 구조와 메모의 nullable 카테고리 ID를 추가하고, 이름 변경에도 연결이 유지되도록 숫자 ID로 참조한다. 기존 DB는 변경 전 자동 백업 후 모든 기존 메모를 `미지정`으로 마이그레이션한다.
  - 저장소에 카테고리 CRUD·순서 변경·단건/일괄 지정·검색/필터/보기 정렬 조회를 추가하되, 보기 전용 정렬은 `parent_id`와 기존 `sort_order`를 갱신하지 않는다.
  - 전체 메모 백업 형식을 상향해 카테고리와 연결을 포함하고 구형 백업도 계속 읽는다. 병합 복원은 같은 이름의 카테고리를 재사용하고 새 이름은 뒤에 추가하며 메모 연결 ID를 다시 매핑한다.
  - 메인 편집창과 독립 메모창의 카테고리 표시·변경을 동기화하고, 목록·최근 메모·휴지통 복원·Excel 내보내기 흐름에서도 카테고리 값을 유지한다.
- 검증:
  - 기존 DB 마이그레이션 전 백업, 초기 카테고리 생성, CRUD·중복 이름 방지, 삭제 후 미지정 전환, 하위 메모 초기 복사와 이후 독립 변경을 검사한다.
  - 검색+필터, 두 보기 전환, 모든 정렬, 고정 우선, 카테고리 그룹 접기, 부모 경로, 다중 선택 일괄 지정, 저장 상태 복원을 검사한다.
  - 휴지통 왕복, 구형/신형 전체 백업의 병합·교체 복원, Excel 내보내기, 자동 저장·최근 메모·끌어 놓기·알림/D-Day 회귀를 검사한다.
  - Windows 100%·125%·150% 배율과 넓은/좁은 창에서 시안과 실제 렌더를 대조하고, 칩 넘침·`더보기`·관리창·말풍선·가로 스크롤 여부를 실제 창으로 확인한다.
- 보존 범위: 기존 메모·계층·수동 순서·고정·내용·첨부·알림·D-Day·포스트잇·단축키·설정·사용자 DB와 기존 EXE를 변경·삭제하지 않는다.
- 제외 범위: 다중 태그, 일정 카테고리 통합, AI 자동 분류, 복합 AND/OR 조건, 저장된 스마트 보기, 캘린더 자동 연동, EXE 생성은 이번 작업에 포함하지 않는다.
- 상태: 모바일 연동용 식별·변경 기반과 함께 구현하고 집중·전체 회귀를 통과했다. 실제 포인터 검증 항목만 `3._TODO.md`에 남겼다.

## 미완료·결정 대기

- 실제 사용자 입력 환경 검증, 후속 메모 기능과 일정 기능의 우선순위는
  [`3._TODO.md`](3._TODO.md)에서만 관리한다.
- 이 항목들은 별도 요청과 계획 승인 없이 구현하지 않는다.

## 완료 내역

완료 계획의 상세 처리·테스트 명령·화면 검증 근거는 [`2._WORK_LOG.md`](2._WORK_LOG.md)에서 확인한다.

| 완료일 | 작업명 | 변경 요약 | 검증 결과 |
| --- | --- | --- | --- |
| 2026-08-07~10 | 메모·알림 기본 편집 기능 | 리치 편집, 자동 저장, 반복·알림·포스트잇과 입력 UI를 정비 | 관련 Qt 테스트·실제 렌더 확인 |
| 2026-08-11~15 | 독립 메모창·포스트잇·단일 EXE | 독립 편집창, 목록 관리, 포스트잇 개선과 단일 EXE 배포 흐름을 반영 | 임시 DB 검사와 EXE 실행 검증 |
| 2026-08-25 | 메모·설정·토마펫 UI 개선 | 메모 화면 비례, 설정 2열, 상단 내비게이션, 토마펫 알림·상시 표시를 반영 | 관련 UI·회귀 검사와 Windows 렌더 확인 |
| 2026-08-27~28 | 제품명·PyInstaller 안정화 | TomaDesk 명칭 통일, 단일 EXE 재빌드, PyQt6 DLL 포함과 실행 오류를 보완 | 단일 EXE·DLL·실행 스모크 검증 |
| 2026-08-28 | 메모 창·목록 레이아웃 | 선택 상태, 공통 상단 액션, 목록·편집창·휴지통 레이아웃을 개선 | 관련 Qt 테스트와 화면 렌더 확인 |
| 2026-08-31 | 안정성·데이터 보호 1단계 | 설정·단축키·데이터 보호 기반과 알림·일정 공통 기반을 정비 | 집중 회귀 검사와 설정 렌더 확인 |
| 2026-09-04 | 메모 토글·페이지·블록 편집 | 빈 토글 안내, 메모 계층, 페이지, 삽입 메뉴, 블록·줄 끌기, 그림·전체 화면을 반영 | 단계별 Qt 테스트와 실제 렌더 확인 |
| 2026-09-07 | 메모·일정·단축키 사용성 | 오늘 요약, D-Day 칩, 설정·단축키 화면, 목록 접기와 본문 기능을 개선 | 독립 테스트 묶음과 실제 렌더 확인 |
| 2026-09-08 | 메모 서식·일정·D-Day | 제목1~4·기능 설정과 일정 포스트잇, 일정/할 일 분리, D-Day·캘린더 연계를 반영 | 전체 673개 자동 검사 통과 및 Windows 배율 렌더 확인 |
| 2026-09-09 | 기존 DB 보존 폴더 변경·단일 EXE | 기존 DB를 다음 번호 파일로 보존한 뒤 현재 DB를 복사하도록 변경하고 EXE를 생성 | 집중 62개·전체 676개 통과, `TomaDesk.exe` 빌드·실행 확인 |
| 2026-09-09 | 빈 토글 붙여넣기·삭제 | 첫 빈 줄 제거, 토글 내부 유지, Ctrl+Z 묶기와 토글 안팎 빈 줄 삭제를 보완 | 토글 42개·인접 메모 162개 통과, Windows Qt 42개 통과 |
| 2026-09-09 | 구조 가져오기·전체 메모 백업복원 | GPT·Claude/MD·HTML·TXT 구조 가져오기와 `.tomamemo` 병합·교체 복원을 추가 | 집중 12개·전체 696개·Windows Qt 63개 통과, 실제 렌더 확인 |
| 2026-09-11 | 프로그램 내 사용 설명서 | 화면 캡처에 번호 마커를 얹고 섹션·좌우 넘기기로 읽는 `설명서` 창을 추가 | 설명서 9개·전체 705개 통과, 11장 실제 렌더 확인 |
| 2026-09-11 | 메모 커서·스크롤 구조 경계 안정화 | 토글 밖 빈 줄 삭제의 보이는 커서·스크롤 복원과 HTML 표 뒤 일반 입력 문단을 추가 | 집중 24개·인접 222개·전체 713개·Windows Qt 9개 통과 |
| 2026-09-11 | 메모 편집 Phase 1 클립보드·독립 복제 | 잔류 내부 링크 제거, 전용 MIME, 페이지 하위 트리·첨부 새 ID 복제, 목록 다중 복제, 복합 Undo/Redo, Alt+C 문자 서식 복사를 추가 | 최종 집중·인접 104개·전체 728개·Windows Qt 6개 통과, EXE 미생성 |
| 2026-09-12 | 메모 편집 Phase 2 다중 블록 명령 | 비연속 선택, 공통 명령·부동 막대·우클릭, 이동·변환·복제·삭제와 데이터 보존형 연동 해제를 추가 | Phase 2 15개·전체 743개·Windows Qt 31개 통과, 4종 실제 렌더 확인, EXE 미생성 |
| 2026-09-14 | 메모 편집 Phase 3 접기·서식·키보드 | 제목 범위 접기, 모두 접기/펴기, 줄 간격, 세 붙여넣기 방식과 링크·접기 키보드 경로를 추가 | Phase 3 10개·전체 753개·Windows Qt 집중 97개 통과, 넓은/좁은 창 렌더 확인, EXE 미생성 |
| 2026-09-15 | 메모 편집·모바일 연동 대비 Phase 4~7 | Phase 4 마감, MD·UI 보완, UUID 기반 데이터·카테고리, 백링크·주석, 템플릿, 버전 기록을 구현 | 전체 815개·Windows Qt 배율별 집중 24개 통과, 실제 포인터는 TODO, EXE 미생성 |

## 요약 정리

완료 작업은 이 파일에 요약 내역만 유지하고 상세 근거는 `2._WORK_LOG.md`에서 관리한다.
Phase 4 마감부터 MD·UI 보완, 모바일 연동용 UUID·revision·UTC·tombstone 기반, 카테고리, 백링크·주석, 템플릿, 버전 기록까지 구현했다. 기존 정수 ID·v1 링크·사용자 데이터 계약을 유지하며 전체 회귀 815개를 통과했다.
승인된 메모 편집 UI 개편은 기본 분할, 빈 오늘 요약 접기, 28px 속성·서식·저장·삭제 버튼, 좁은 창 배치와 공용 저장 상태까지 반영했다. Windows Qt 배율별 자동 검사는 통과했으며 실제 포인터 확인은 남아 있다. `classic` 배치는 2026-10-14까지 유지하고 별도 검토한다.
Phase 4~7 결과물 보완은 Q1·Q2 A안과 단계별 승인 방식으로 진행한다. 단계 0은 전체 815개, 단계 A는 전체 820개를 통과했다. 승인된 단계 B는 오늘 요약 수명주기, 편집 전/후 버전, 주석 위치 복구, v1/v2 링크 재매핑과 템플릿 구조 검사를 구현해 집중 96개·전체 830개를 통과했다. 임시 DB 실제 창에서 메모 3회 전환 후 D-Day가 오늘 요약에 나타나는 것도 확인했으며, 나머지 실제 포인터 항목은 TODO에 남기고 단계 C 승인 전에는 착수하지 않는다.
단계 C에는 `9._서식프리셋_토글_구현계획`의 구현 문서와 PNG 5개를 반영했다. UI-C1에서 서식 토글을 칩 줄로 통합하고, UI-C2에서 `가` 샘플과 `⚙`을 추가했으며, UI-C3에서 라벨·입력 폭·아이콘·간격을 정리했다. UI-C4에서는 선택 글자 서식 창에 24px 프리셋 3개와 메인 툴바 양방향 동기화를 추가하고 공간 부족 시 하단 예약 줄을 사용하게 했다. UI-C5에서는 프로그램 내·Markdown 설명과 실제 메모 편집 캡처를 현 UI에 맞췄다. 최종 집중 22개·전체 852개가 통과했고 실제 포인터는 도구 미노출로 TODO에 남겨 단계 C를 조건부 통과로 판정했다. 단계 D는 다음 승인 전까지 착수하지 않는다.
단계 D는 목록의 색 점·미지정 필터·480px 네 열, 선택 작업줄, 상태 말줄임, 충돌 사본 표시와 제목 첫 기호 동적 필터를 구현했다. D 집중·인접 176개와 C 제외 전체 860개가 통과했으며 C 영역 기존 실패 2건과 실제 포인터 미확인으로 조건부 통과다. 단계 E는 승인 대기다.
단계 E는 편집·목록 상태를 분리하고, 버전 기록을 로컬 시각·한글 종류·평문 색상 차이로 표시하며, 백링크 수·본문 주석 표시·템플릿 진입·본문 Undo/Redo를 연결했다. E 집중 7개는 배율별 통과했고 C 파일과 범위 밖 카테고리 칩 실패 1건을 제외한 전체 866개가 분할 통과했다. 실제 포인터는 미검증으로 조건부 통과이며 단계 F는 승인 대기다.
단계 F는 속성·줄 간격·카테고리 칩의 기존 자동 실패 3건을 수정하고 배치 계약 12개, 사용성 시나리오 71개와 전체 902개를 통과했다. Windows Qt 기준 이미지 12장을 남겼으나 실제 포인터 체크리스트는 창 탐지 제한으로 미검증이다. 조건부 통과로 멈추며 G-A는 승인 대기다.
`11._제목분류필터_계획`의 Markdown·HTML·PNG 10개를 확인해 후속 G-A~G-G를 추가했다. F 마감 뒤 기호 규칙·대괄호 머리말·버튼/도움말·상시 더보기·고정 칩·제목 자동완성·설명서/실창 검증을 단계별 승인으로 진행한다. 카테고리 순서 버튼 제거와 최근 메모 UI의 자료 간 차이는 각 해당 단계 착수 전 결정 항목으로 남긴다.
G-A는 So 기호 판정·표시와 비교 키 분리, `[머리말]` 추출·정규화·집계를 구현했다. 고정 제목 20개를 포함한 집중 50개와 전체 944개를 통과했으며 G-B는 승인 대기다. 기존 계획의 카테고리 순서 버튼·최근 메모 배치 결정은 각 해당 단계 전까지 보류한다.
G-B는 제목 필터의 교차 집계·AND 결과·평평한 목록·결과 없음 안내와 제목 필터만 해제하는 동작을 연결했다. 집중 75개와 전체 969개가 통과했으며 G-C 버튼·메뉴 구현은 승인 대기다.
2026-09-17 창 배치 저장·복원 계획을 추가했다. 탐색기 창의 폴더 경로와 위치를 다섯 번째 작업 유형 `layout`으로 저장하고 전역 단축키로 복원하며, 이름은 `창 배치`, 1차 범위는 탐색기 전용, 이미 열린 폴더는 재사용으로 확정했다. 1단계 창 목록 수집·모니터 상대 좌표 변환·창 이동과 2단계 작업 유형·저장 UI·payload·엑셀 왕복에 이어 3단계 `_run_layout` 복원 실행과 집중 테스트를 구현했다. 3단계 검증 구간 1~11은 합계 988개가 통과했으며, 실제 Windows 탐색기 2창 복원과 Qt 종료 access violation은 후속 확인으로 남겼다. 4단계는 대기다.
탐색기 파일 목록의 빈 공간을 더블클릭하면 상위 폴더로 이동하는 계획도 같은 날 추가했다. 기존 `WH_MOUSE_LL` 갈고리 방식을 재사용하고 선택 항목 수로 빈 공간을 판정하며, 더블클릭을 가로채지 않아 오판해도 부작용이 없게 설계했다. 창 배치 1~4단계 완료 후 착수한다.
미완료·우선순위 결정 항목은 `3._TODO.md`에 유지한다.

## 추천 모델

- `gpt-6-astra + high`: 후속 모바일 동기화·충돌 비교 화면과 UUID 관계 확장에 적합

## 추천 SKILL

- `ui-compact-tool-area` — 적용: 프리셋 샘플·관리 메뉴·고정 크기와 좁은 도구 영역 검토
- `ui-ux-pro-max` — 적용: 속성 칩·인라인 패널·좁은 창과 키보드 접근성 검토
- `computer-use:computer-use` — 실제 Windows 메모창의 포인터 사용감 확인에 적합
