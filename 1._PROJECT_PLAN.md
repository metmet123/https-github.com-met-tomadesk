# 프로젝트 계획

## 진행 중인 계획

### 2026-09-15 메모 Phase 4~7 결과물 보완 (단계 E 조건부 통과)

- 요청·목표: `보완계획/메모편집_보완계획.md`의 사용성 검사 결과를 기준으로 앱 종료 오류, 데이터·로직, 편집기 머리 영역, 목록, 상태·버전·주석 표시와 회귀 검증을 순서대로 보완한다.
- 확정 순서: 단계 0 → A → B → C → D → E → F. 한 번에 한 단계만 수행하고 집중 검사와 전체 회귀 결과를 보고한 뒤 중단한다. 사용자의 명시적 승인 없이 다음 단계로 넘어가지 않는다.
- 확정 결정: Q1은 A안으로 하여 `⌨ 단축키` 칩이 현재 메모를 여는 전역 단축키와 값을 표시하고 편집 단축키 설정은 `⚙`로 옮긴다. Q2도 A안으로 하여 주석 추가·수정·삭제를 본문 `Ctrl+Z/Redo` 흐름에 통합한다.
- 보존 계약: 기존 작업 트리와 사용자 데이터, UUID·revision·밀리초 UTC·설치 UUID·tombstone·정수 ID·숫자/v1 및 UUID/v2 링크·구형/신형 백업 계약을 유지한다. 테스트는 임시 DB 또는 사용자 DB 복사본만 사용한다.
- 제외·보고: 새 EXE와 배포물을 만들지 않는다. 현재 단계 밖의 문제는 임의 수정하지 않고 별도 보고한다. 자동 검사와 실제 Windows 창 결과를 구분하고 미확인 포인터 항목은 `3._TODO.md`에 남긴다.

#### 단계별 범위와 승인 게이트

| 단계 | 범위 | 단계 완료 기준 | 현재 상태 |
| --- | --- | --- | --- |
| 0 | 확정 계획·추적표 기록, 이미지 링크 정리, 기준선·임시 DB 경계 확인 | 문서 집중 검사와 전체 회귀 통과 | 통과: 집중 26개·전체 815개 |
| A | F1·F2 앱 종료 방지와 슬롯 예외 처리 | 일괄 지정·템플릿 삽입 슬롯 예외 0, 전체 회귀 통과 | 조건부 통과: 전체 820개, 실제 창 미확인 |
| B | F3~F7 데이터·로직 수정 | 오늘 요약·세션 시작 버전·주석 복구·v2 재매핑·템플릿 구조 검사 통과 | 조건부 통과: 집중 96개·전체 830개, B1 실창 통과 |
| C | D-1·D-3 편집기 머리 영역과 서식 프리셋 재배치 | UI-C1~C5 순서, 칩 y좌표 0px 변화, 서식 2줄·샘플 버튼·선택 창·28px·본문 비율 기준 통과 | 조건부 통과: 핵심 자동 검사 완료, 계획 세부 편차 3건·실제 포인터 미확인 |
| D | D-4 목록 필터·열·선택 작업·충돌 표시와 제목 첫 기호 동적 필터 | 480px 가로 스크롤 0, 필터·일괄 작업 통과 | 조건부 통과: D 집중·인접 176개, C 제외 전체 860개 통과, 기존 C 실패 2건·실제 포인터 미확인 |
| E | D-2 상태 분리, 버전 현지화·평문 diff, 주석 표시·진입점 | 상태 중복 없음, 버전·주석 표시 시나리오 통과 | 조건부 통과: 집중 7개·배율별 7개, E 포함 분할 회귀 866개 통과, 단계 밖 UI 실패 3건·실제 포인터 미확인 |
| F | 배치 계약·사용성 시나리오·데이터 회귀·실제 창·문서 마감 | 자동 실패 0, 실제 창 결과와 미확인 항목 분리 기록 | 조건부 통과: 12개 배치·71개 시나리오·전체 902개 통과, 실제 포인터 미확인 |
| G-A | 제목 분류 규칙: 기호 So·비교 키 보정과 `[머리말]` 추출·집계 | 규칙 표와 20개 고정 제목 사례, 전체 회귀 통과 | 통과: 집중 50개·전체 944개, G-B 승인 대기 |
| G-B | 기호·머리말·카테고리·검색 AND 필터, 평평한 목록과 결과 없음 안내 | 필터 조합·개수·해제·원래 계층 복귀, 전체 회귀 통과 | 통과: 집중 75개·전체 969개, G-C 승인 대기 |
| G-C | 기호·머리말 버튼의 대표값·선택·빈 상태, 메뉴·도움말 | 세 상태·동률·말줄임·메뉴 검색·도움말, 전체 회귀 통과 | 대기 |
| G-D | 검색·필터 줄과 상시 `더보기`, 보기·정렬·카테고리 설정 진입 | 300·420·800px 배치와 메뉴 동작, 전체 회귀 통과 | 대기 |
| G-E | 사용자가 고르는 항상 보이는 카테고리 칩과 설정 창 | 기본 3개·미지정·추가·삭제·순서 변경, 전체 회귀 통과 | 대기 |
| G-F | 제목 칸 맨 앞 `[` 머리말 자동완성 | 제안·선택·기존 제목 보존·범위 밖 미작동, 전체 회귀 통과 | 대기 |
| G-G | 설명서·임시 DB 캡처·실제 창 최종 검증 | 설명서·배율·회귀 결과와 실제 포인터 미확인 항목 분리 | 대기 |

#### F1~F17 구현 추적표

| 번호 | 단계 | 예정 변경 파일 | 집중 검증 | 상태 |
| --- | --- | --- | --- | --- |
| F1 | A | `alert_notes/rich_memo_edit.py` | `/템플릿` 선택·삽입·Undo, 잘못된 payload 경고 | 구현·자동 통과, 실제 창 미확인 |
| F2 | A | `alert_notes/panel.py` | 다중 선택 카테고리 지정·상태 표시·선택 해제 | 구현·자동 통과, 실제 창 미확인 |
| F3 | B | `alert_notes/panel.py` | 메모 3회 전환 후 오늘 요약·캘린더 갱신 | 구현·자동·실창 통과 |
| F4 | B | `alert_notes/panel.py`, `alert_notes/memo_data_service.py` | 편집 시작 전/후 버전, 무변경 중복 방지, 30개 제한 | 구현·자동 통과, 실창 버전 복원 미확인 |
| F5 | B·E | `alert_notes/editor.py`, `alert_notes/memo_data_service.py`, `alert_notes/rich_memo_edit.py` | 오프셋 이동·문맥 복구·중복·원문 삭제·표시 | B 복구·E 표시 자동 통과, 실제 포인터 미확인 |
| F6 | B | `alert_notes/note_clone_service.py`, 클립보드 모듈, `alert_notes/memo_archive.py`, 링크 유틸리티 | 함께 복제된 v1/v2 링크 재매핑과 외부 참조 유지 | 구현·자동 통과, 실창 링크 이동 미확인 |
| F7 | B | `alert_notes/memo_data_service.py`, `alert_notes/rich_memo_edit.py` | 코드 문자열 허용, 실제 로컬 경로 payload만 경고 | 구현·자동 통과, 실창 템플릿 저장 미확인 |
| F8 | C | `alert_notes/editor.py`, `alert_notes/note_property_bar.py`, `alert_notes/text_format_toolbar.py` | 기존 Q1 A안 단축키 칩과 `⚙` 설정 진입, UI 계획의 프리셋 설정 메뉴 | UI 보완계획으로 구체화, 대기 |
| F9 | D | `alert_notes/memo_list.py` | 보기·정렬 최소 폭 90px 이상 | 구현·자동 통과, 480px 미만 `더보기` 이동 |
| F10 | D | `alert_notes/memo_list.py` | 목록 480px의 4열과 가로 스크롤 0 | 구현·자동 통과 |
| F11 | C | `alert_notes/editor.py`, `alert_notes/text_format_toolbar.py`, `alert_notes/format_preset_strip.py` | 서식 전후 칩 y좌표 0, 펼침 76px 이하, 515px에서 2줄·잘림 0, 28px | UI 보완계획으로 구체화, 대기 |
| F12 | D | `alert_notes/memo_list.py`, `ui_theme.py` | 선택 칩 표시, 전체·개별·미지정 필터 | 구현·자동 통과 |
| F13 | C | `alert_notes/editor.py` | 위치 없는 상위 메모 카테고리 칩 최대 폭 | 대기 |
| F14 | D·E | `alert_notes/memo_list.py`, `alert_notes/panel.py` | 목록 작업 상태 말줄임·전체 툴팁·편집 상태 분리 | D 말줄임·E 상태 분리 자동 통과, 실제 포인터 미확인 |
| F15 | E | `alert_notes/version_dialog.py`, `alert_notes/memo_data_service.py` | 로컬 시각·한글 종류·날짜 묶음·평문 diff | 구현·자동 통과, 실제 포인터 미확인 |
| F16 | D | `alert_notes/memo_list.py` | 충돌 사본 표시와 원본 툴팁 | 구현·자동 통과 |
| F17 | C·E | `alert_notes/editor.py`, `alert_notes/rich_memo_edit.py`, `alert_notes/text_format_range_bar.py` | 선택 서식 창 프리셋·백링크 개수·주석 표시·기존 Q2 A안 Undo·템플릿 진입 | C 선택 서식·E 백링크·주석·Undo·템플릿 진입 자동 통과, 실제 포인터 미확인 |

#### 단계 C 상세 순서 — UI 보완계획 통합

- 기준 문서: `보완계획/UI보완계획/서식프리셋_토글_구현계획.md`와 같은 폴더의 PNG 5개를 확인했다. 문서의 이미지 링크는 실제 같은 폴더 위치에 맞게 수정했다.
- 기존 보완계획의 단계 C 중 C1·C3은 아래 UI-C1·UI-C2로 대체하고 C2는 UI-C3으로 구체화한다. C4 카테고리 칩과 백링크 버튼, 단계 D 목록, 단계 E 상태·버전·주석 범위는 유지한다.
- UI 계획의 Q1~Q3은 원문 확정안 A를 따른다. 기존 보완계획의 Q1(전역 단축키 칩)·Q2(주석 Undo 통합)와 이름이 겹치므로 문서와 구현 기록에서는 `UI-Q1`~`UI-Q3`으로 구분한다.
- UI-Q4는 다음 단계 진행 승인에 따라 원문 추천 A안인 빈 슬롯 메뉴 즉시 열기로 확정했다. UI-Q5도 2026-09-15 다음 단계 진행 승인에 따라 원문 추천 A안인 약 330px 공간이 없을 때 하단 고정 줄 사용으로 확정했다.

| 하위 단계 | 구현 범위 | 집중 검사와 완료 기준 | 게이트 |
| --- | --- | --- | --- |
| UI-C1 | `서식 ▾`를 칩 줄 맨 왼쪽으로 옮기고 별도 서식 헤더 제거. 속성 패널과 서식 패널은 한 번에 하나만 열고 Esc로 모두 해제 | 5회 여닫기 후 알림 칩 y 변화 0px, 서랍 동시 열림 0, classic 화면 변화 없음 | 조건부 통과: 집중 18개·인접 45개·전체 835개, 실제 포인터 미확인 |
| UI-C2 | 새 `format_preset_strip.py`의 26×26 `가` 샘플 3개와 28px `⚙` 메뉴. 저장 서식·이름·우클릭·빈 슬롯·편집 단축키 진입 유지 | 기존 JSON과 적용 결과 동일, 이름 12자에도 폭 26px, 적용 상태 동기, 빈 슬롯 동작은 UI-Q4 확정안 적용 | 조건부 통과: 집중 49개·인접 37개·전체 842개, 실제 포인터 미확인 |
| UI-C3 | 라벨 제거와 접근성 이름·툴팁 보완, 입력 폭 축소, 접기·기본값 아이콘화, 두 줄 폭 515px에 맞춤 | 두 줄 `sizeHint`가 가용 폭 이하, 잘린 위젯 0, 펼침 추가 높이 76px 이하, 접힘 서식 줄 0 | 조건부 통과: 집중 24개·인접 66개·전체 846개, 배율별 11개, 실제 포인터 미확인 |
| UI-C4 | 선택 글자 서식 창에 24px 프리셋 샘플 3개를 추가하고 메인 툴바와 양방향 동기화 | 선택 범위 적용·메인 체크 동기, 저장 후 아이콘 즉시 변경, 다중 글자·블록 선택 충돌 없음, 515px 하단 줄 잘림 0 | 조건부 통과: 집중 29개·인접 41개·전체 851개, 배율별 18개, 실제 포인터 미확인 |
| UI-C5 | 사용 설명서의 서식 토글 위치·샘플 버튼·우클릭 설명 갱신 | 설명서 문구·캡처 대상과 실제 UI 일치, 설명서 회귀 통과 | 통과: 집중 22개·전체 852개, 캡처 1장 갱신 |

#### 단계 C 추가 보존·검증 계약

- `text_format_preset_{n}`, `text_format_preset_name_{n}`, 서식 JSON 구조, 12자 이름 제한, `PRESET_COUNT = 4`, 숨은 4번 슬롯과 기존 적용 판정을 유지한다.
- classic 레이아웃의 서식 1~3 위치, 알림·D-Day·단축키·기타·카테고리·백링크 동작과 저장 방식은 바꾸지 않는다.
- 모든 콤보와 값 선택 UI는 포커스 여부와 관계없이 마우스 휠 값 변경을 막는 기존 계약을 유지한다.
- 자동 검사는 515·620·780px 및 기존 780·900·1080·1920px, Windows Qt 100·125·150%에서 폭·높이·잘림·패널 배타성·접근성 이름·프리셋 저장 호환을 확인한다.
- 실제 Windows 창에서는 Malgun Gothic의 `⚙` 노출, 흰색·연노랑 샘플 가시성, 선택 서식 창의 본문 비가림, 서식 토글 전후 칩 위치를 확인한다. 미확인 항목은 통과 처리하지 않고 TODO에 남긴다.

#### 단계 C UI-C1 구현 결과

- 별도 `서식 ▾` 헤더를 제거하고 같은 토글을 속성 칩 줄의 맨 왼쪽으로 옮겼다. 서식 칩 뒤 구분선을 두고 기존 호출부가 같은 버튼을 가리키도록 호환 별칭을 유지했다.
- 서식 패널을 열면 알림·D-Day·단축키·기타 속성 패널을 닫고, 다른 속성을 열면 서식 패널을 닫는다. Esc는 열린 패널과 선택 상태를 함께 해제하며 서식 칩의 열기·접기 접근성 이름도 상태에 맞춰 갱신한다.
- 전용 자동 검사에서 5회 여닫기 전후 알림 칩 y좌표 0px 변화, 동시 열린 서랍 0개, classic 배치 유지가 통과했다. 집중 18개, 인접 45개, 전체 835개와 `compileall`, `git diff --check`가 통과했다.
- 임시 DB 검증 창은 실행했으나 Computer Use의 창 목록에 반환되지 않아 실제 포인터 검증은 수행하지 못했다. 사용자 데이터가 열린 창은 조작하지 않았고 임시 DB 두 개는 종료 후 삭제했다.
- 판정: **조건부 통과**. 사용자의 다음 단계 승인으로 UI-C2를 진행했다.

#### 단계 C UI-C2 구현 결과

- 새 `format_preset_strip.py`에 고정 26×26px `가` 샘플 3개를 구현했다. 저장된 글꼴·굵게·기울임·밑줄·취소선·색을 아이콘에 반영하고, 흰색·연노랑처럼 밝은 색에는 회색 외곽선을 그린다. 빈 슬롯은 점선 `+`로 표시한다.
- 클릭은 기존 프리셋 적용 경로를 그대로 호출하고, 우클릭은 해당 슬롯의 저장·이름 변경·초기화 메뉴를 연다. UI-Q4 A안에 따라 빈 슬롯 클릭도 안내창 없이 같은 메뉴를 바로 열고 첫 저장 항목에 초점을 둔다.
- 28px `⚙` 메뉴에 서식 1~3 하위 메뉴와 `편집 단축키 설정…`을 넣었다. compact의 중복 `단축키` 버튼은 제거했고 classic은 프리셋 묶음의 기존 본문 아래 위치를 유지한다.
- `text_format_preset_{n}`, `text_format_preset_name_{n}`과 기존 JSON·4번 숨은 슬롯은 변경하지 않았다. 12자 이름은 툴팁 첫 줄에 표시되며 버튼 외곽 크기는 26px로 유지한다.
- 집중 49개, 인접 37개, 전체 842개와 `compileall`, `git diff --check`가 통과했다. 초기 집중 검사에서 오프스크린 환경의 미지원 글꼴 대체와 QSS 테두리 포함 크기 계산으로 2건이 실패했으나, 환경에서 유효한 글꼴을 쓰는 호환 검사와 26px 외곽 크기 계산으로 바로잡아 최종 실패는 0개다.
- 임시 DB에 흰색·연노랑 프리셋을 넣어 검증 창을 실행했지만 Computer Use가 창을 반환하지 않아 실제 포인터 검증은 수행하지 못했다. 창 종료 후 임시 DB를 삭제했다.
- 판정: **조건부 통과**. UI-C3은 사용자의 다음 단계 승인 전까지 착수하지 않는다.

#### 단계 C UI-C3 구현 결과

- 글씨체·글자 크기·줄 간격의 화면 라벨을 제거하고 각 입력에 같은 의미의 접근성 이름과 툴팁을 넣었다. 글자 크기는 60px, 줄 간격은 72px로 줄이고 색상 묶음 앞에 세로 구분선을 추가했다.
- 공통 서식 도구 버튼을 28px로 통일했다. `접기` 텍스트는 삼각형과 가로줄 아이콘의 36×28px 메뉴 버튼으로, `기본값` 텍스트는 되돌림 아이콘의 28×28px 버튼으로 바꿨다. 기존 동작·메뉴·툴팁은 유지했다.
- 툴바 내부 간격을 4px, 서식 패널 안쪽 여백을 4px로 줄이고 두 28px 줄과 4px 간격이 압축되지 않도록 최소 높이 60px를 설정했다.
- 515·620·780px에서 두 줄 `sizeHint`가 가용 폭 이하이고 보이는 컨트롤이 툴바 경계 안에 있으며, 515px에서 펼침으로 늘어나는 높이가 76px 이하이고 접으면 본문 위치가 원복됨을 자동 검사했다.
- 집중 24개, 인접 66개, 전체 846개가 통과했다. Windows Qt 100·125·150%에서 폭·높이·잘림·프리셋 검사를 각각 11개 통과했고 `compileall`, `git diff --check`도 통과했다.
- 초기 집중 검사에서 재귀 라벨 검색이 콤보 내부 Qt 라벨까지 잡은 검사 오류와 툴바가 둘째 줄보다 2px 작게 배정되는 실제 잘림 1건을 확인했다. 검사는 최상위 줄만 보도록 바로잡고 툴바 최소 높이를 60px로 고정해 최종 실패를 없앴다.
- 임시 DB 검증 창은 실행했지만 Computer Use가 창을 반환하지 않아 실제 포인터와 Malgun Gothic 렌더는 확인하지 못했다. 창 종료 후 임시 DB를 삭제했다.
- 판정: **조건부 통과**. UI-C4와 UI-Q5는 사용자의 다음 단계 승인 전까지 착수하지 않는다.
- 선택 서식 창 전체 재구성, 전용 프리셋 단축키, 4번 슬롯 노출, 어두운 테마, 단계 D 이후 범위와 EXE 생성은 제외한다.

#### 단계 C UI-C4 구현 결과

- UI-Q5 A안에 따라 일반 글자 선택 오른쪽에 전체 도구 폭이 확보되지 않으면 기존 본문 하단 예약 줄을 사용한다. 임의 고정폭 240px 대신 닫기 버튼까지 포함한 실제 `sizeHint`로 빈 공간을 판정한다.
- 선택 글자 서식 창에 공용 `PresetStrip`의 24×24px `가` 샘플 3개를 추가했다. 저장된 슬롯은 선택 범위에 적용하고 메인 툴바와 선택 창의 활성 표시를 함께 갱신한다. 빈 슬롯 클릭과 우클릭은 같은 저장·이름 변경·초기화 메뉴를 연다.
- 메인 툴바에서 프리셋을 저장·이름 변경·초기화하면 선택 창의 아이콘·툴팁이 즉시 다시 그려진다. 일반 선택과 다중 글자·블록 선택의 기존 배타 조건은 유지한다.
- 초기 집중 검사에서 툴팁 색상 문자열 대소문자를 고정값으로 비교한 검사 1건과, 선택 창의 상위 QSS가 샘플 테두리를 없애 실제 외곽을 22px로 줄인 구현 실패 1건을 확인했다. 검사는 대소문자 비의존으로 수정하고 샘플 전용 1px 투명 테두리로 외곽 24px를 복원했다.
- 최종 집중 29개, 인접 41개, 전체 851개가 통과했다. Windows Qt 100·125·150%에서 선택 창·compact 검사를 각각 18개 통과했고 `py_compile`, `git diff --check`도 통과했다.
- 테스트는 `TemporaryDirectory`의 임시 DB만 사용했다. 현재 런타임에 네이티브 Windows 창을 조작할 Computer Use 도구가 노출되지 않아 실제 포인터와 본문 비가림 사용감은 확인하지 못했다.
- 판정: **조건부 통과**. UI-C5는 사용자의 다음 단계 승인 전까지 착수하지 않는다. 프리셋 저장 키·JSON·12자 이름·숨은 4번 슬롯과 UUID·revision·UTC·tombstone·v1/v2 링크·백업 계약은 변경하지 않았다.

#### 단계 C UI-C5 구현 결과

- 프로그램 내 설명서와 `4. TomaDesk_설명서.md`에 제목 아래 맨 왼쪽 `서식 ▾`, 26px 메인 `가` 샘플, 24px 선택 프리셋, 빈 슬롯 클릭, 우클릭 저장·이름 변경·초기화, 톱니의 편집 단축키 진입과 공간 부족 시 하단 고정 줄을 설명했다.
- 임시 DB 전용 캡처 도구가 서식 패널을 펼치고 `지난 분기 기준`을 선택하도록 갱신했다. `assets/manual/03_memo_editor.png` 한 장을 1360×860으로 다시 만들고 새 실제 배치에 맞춰 설명서 번호 위치를 조정했다. 함께 다시 생성된 범위 밖 이미지 3장은 단계 시작 전 상태로 복원했다.
- 설명서 내용 검사에 현재 UI 필수 문구와 폐기된 `아래 「서식 ▾」`, `휴지통으로 이동` 문구 부재를 추가했다. 집중 22개, 전체 852개와 `py_compile`, `git diff --check`가 통과했다.
- 캡처는 `TemporaryDirectory`의 임시 DB에서만 생성했다. 이미지 자체는 확인했지만 실제 Windows 포인터 조작은 현재 런타임 도구 미노출로 확인하지 못해 단계 C 실창 TODO를 유지한다.
- 판정: **조건부 통과**. UI-C1~C5 자동 구현과 회귀는 완료했고 실제 포인터 검증만 남았다. 단계 D는 사용자의 다음 단계 승인 전까지 착수하지 않는다. 사용자 DB·기존 EXE·UUID·revision·UTC·tombstone·v1/v2 링크·백업 계약은 변경하지 않았다.

#### 단계 C 구현계획 사후 대조 검증

- 2026-09-16에 `서식프리셋_토글_구현계획.md`와 PNG 5개를 현재 코드·캡처·테스트에 다시 대조했다. 단계 A의 칩 이동·배타 서랍, 단계 C의 두 줄 폭·높이, 단계 D의 선택 프리셋·하단 줄, 단계 E의 설명서는 계획과 일치했다.
- 단계 B에는 기능을 깨지 않는 세부 편차 3건이 남아 있다. 샘플 툴팁은 계획의 `우클릭: 저장·이름·초기화` 대신 `우클릭: 관리`로 축약됐고, `setAutoRaise(True)`가 명시되지 않았으며, 체크 테두리는 계획의 1.5px 대신 1px이다.
- 실제 캡처에서 `⚙`가 빈 사각형처럼 보이는 항목은 실제 Windows Malgun Gothic 렌더로 재확인이 필요하다. 임시 DB 창을 실행했지만 Computer Use가 앱을 반환하지 않았고 런타임의 `listApps`도 제공되지 않아 포인터 검증은 수행하지 못했다.
- 집중 31개, Windows Qt 100·125·150% 각각 21개, 전체 852개와 `git diff --check`가 통과했다. 판정은 **조건부 통과 유지**이며 세부 편차와 실창 항목을 해소하기 전에는 구현계획과 100% 동일하다고 기록하지 않는다.

#### 단계 D 구현 범위

- `memo_list.py`의 카테고리 필터에 색 점과 `미지정`을 추가하고, 선택된 칩을 명확히 표시한다. 보기·정렬은 별도 줄에서 각각 90px 이상을 보장하며 최근 메모는 접이식으로 둔다.
- 목록 열을 `선택·제목·카테고리·수정일`로 유지하면서 카테고리는 `색 점 + 이름`, 미지정은 `—`, 수정일은 `MM-DD`로 표시한다. 480px 목록에서 네 열과 머리글을 유지하고 가로 스크롤이 생기지 않게 한다.
- 체크한 메모가 있으면 하단을 `N개 선택 · 카테고리 지정 · 선택 삭제` 작업줄로 전환한다. 일괄 지정 뒤 선택을 해제하고 목록 작업 상태는 말줄임과 전체 툴팁을 함께 제공한다.
- `conflict_of_sync_id`가 있는 메모는 제목에 `⚠ 충돌`을 표시하고 원본 제목과 정리 안내를 툴팁에 넣는다. 비교·병합 화면은 만들지 않는다.
- 사용자가 제안한 제목 첫 이모지·특수기호 분류 버튼을 검색 줄에 추가한다. 앞 공백을 제외한 첫 문자 모양 단위가 Unicode 기호인 메모만 동적으로 집계하며, 검색·카테고리와 AND로 적용한다. 값은 DB와 설정에 저장하지 않고 앱 재실행 시 해제한다. 대괄호 머리말·첫 단어·자동완성은 **기존 단계 D에서는** 제외했다. 후속 [11번 제목 분류 필터 계획](11._제목분류필터_계획/제목분류필터_구현계획.md)에서 첫 단어 분류는 폐기하고, 머리말·자동완성과 기호 규칙 보완을 G-A~G-G로 처리한다.
- 집중 검사는 기호 추출, 필터 조합, 미지정, 색 점, 충돌 표시, 선택 작업줄, 상태 말줄임, 480px 네 열·가로 스크롤, 보기·정렬 최소 폭을 포함한다. 전체 회귀와 Windows Qt 100·125·150% 자동 검사를 실행하고 실제 포인터를 확인하지 못하면 TODO로 남긴다.

#### 단계 D 구현 결과

- 카테고리 필터와 표 셀에 저장 색상 점을 표시하고 `미지정` 필터를 추가했다. 미지정 표 셀은 `—`로 줄이고 전체 이름은 툴팁에 유지했으며 수정일은 `MM-DD`, 전체 일시는 툴팁으로 표시한다.
- 보기·정렬은 480px에서 각각 90px 이상의 폭으로 검색 줄에 유지한다. 480px 미만에서는 `더보기`의 보기·정렬 하위 메뉴로 이동하며, 목록은 380px 미만에서만 수정일 열을 접는다. 480px에서는 네 열과 가로 스크롤 0을 자동 확인했다.
- 체크한 메모가 있으면 하단을 `N개 선택 · 카테고리 지정 ▾ · 선택 삭제`로 전환하고 일괄 지정 성공 후 체크를 해제한다. 목록 작업 상태는 좁으면 말줄임하고 전체 문장을 툴팁에 남긴다. 최근 메모 칩은 `최근 ▸`로 접어 목록 높이를 유지한다.
- 충돌 사본 제목에 `⚠ 충돌`을 표시하고 툴팁에 원본 제목과 비교 후 정리 안내를 넣었다. 비교·병합 동작은 추가하지 않았다.
- 제목 앞 공백을 제외한 첫 Unicode 기호 문자 모양 단위를 추출하는 동적 필터를 추가했다. `✨`, `⚡`, `★`, 변형 선택자·피부색·ZWJ·국기 조합을 한 단위로 처리하고 일반 문장부호는 제외한다. 카테고리·검색과 AND로 적용하며 DB·설정에 저장하지 않는다.
- 단계 D 전용 8개는 Windows Qt 100·125·150%에서 각각 통과했고, D 집중·인접 회귀 176개가 통과했다. C 영역 신규 검사 파일을 제외한 전체 루트 회귀 860개는 401·113·346개 세 구간으로 나눠 모두 통과했다. `py_compile`과 `git diff --check`도 통과했다.
- 단계 밖 C 검사 2건은 현재 코드에서 독립 재현된다: 속성 칩 줄 484px가 기대 380px를 넘고 줄 간격 `1.0` 표시 폭 39px가 기대 31px를 넘는다. C 제외 지시에 따라 수정하지 않았다. 실패 후 위젯 정리 전에 저장소가 닫히는 기존 테스트 수명주기 때문에 단일 프로세스 전체 실행은 다음 Qt 테스트에서 종료되므로 분할 회귀 결과를 기록했다.
- 임시 DB 전용 `TomaDesk 단계 D 임시 검증` 창을 실행했지만 Computer Use가 `apps: []`를 반환해 실제 포인터 입력은 수행하지 못했다. 창과 임시 DB는 종료·삭제했다.
- 판정: **조건부 통과**. 단계 D 기능과 자동 회귀는 통과했지만 단계 밖 C 실패 2건과 실제 Windows 포인터 검증이 남아 있다. 단계 E는 사용자의 명시적 승인 전까지 착수하지 않는다.

#### 단계 E 구현 결과

- 편집기 저장·카테고리·본문 작업 안내는 편집기 하단 `saved_status`에 시각과 함께 표시하고, 목록 작업 안내는 Excel 내보내기 옆 `action_status`에만 전달한다. 목록을 접었을 때는 공용 안내 줄에 목록 작업 결과를 전달하며 다른 탭의 안내 문구는 유지한다. 자동 저장과 직접 저장을 구분하고 기존 초안·저장 실패 동작을 유지했다.
- 버전 목록을 UTC 밀리초 원문 대신 로컬 `HH:MM`·한글 종류와 `오늘/어제/날짜` 머리글로 표시한다. 저장 payload는 바꾸지 않고 현재 본문과 선택 버전을 `display_plain_text_from_content()`의 평문 줄로 비교하며 추가·삭제·머리글 줄에 색을 입힌다.
- 제목 줄에 `🔗 N`을 연결 수가 있을 때만 보이고 누르면 바로 백링크 메뉴를 연다. 주석은 본문 `extraSelections`의 노란 배경·밑줄, 마우스오버 말풍선, 클릭 후 수정·삭제 메뉴로 연결한다. 위치 확인이 필요한 주석은 임의 본문 위치에 그리지 않고 기존 메뉴에서 표시한다. `⋯ > 기록과 연결`에 선택 템플릿 저장 진입점을 더했다.
- 주석 추가·수정·삭제는 문서 Undo 단계 위치를 기억해 본문 `Ctrl+Z`/Redo 경로에 연결했다. 메모 재열기로 문서가 교체되면 이 임시 Undo 기록을 비우며 UUID·revision·UTC·tombstone 저장 경로는 그대로 사용한다.
- 단계 E 집중 7개는 Windows Qt 배율 100·125·150%에서 각각 통과했다. 관련 묶음 164개, 분할 루트 회귀 359+42+109+171+185=866개가 통과했다. 분할은 Qt 객체 수명주기 문제를 피하기 위해 별도 프로세스에서 수행했다. 저장 상태의 구형 기대값은 새 UI 계약에 맞춰 검증 문구를 갱신했으며 저장·목록 분리 검사도 추가했다.
- 단계 밖 UI 검사 3건은 독립 재현된다. 기존 C 검사 2건(칩 줄 484px > 380px, 줄 간격 표시 39px > 31px)과 카테고리 칩 실측 30px > 28px이다. 마지막 건은 E에서 수정하지 않은 기존 카테고리 칩·스타일 경로이고, 해당 검사만 제외한 나머지 전체 모듈을 검증했다. 자동 검사에서 제외된 한 건을 통과로 기록하지 않는다.
- Computer Use는 `apps: []`를 반환해 임시 Windows 창의 실제 포인터 조작을 확인할 수 없었다. 사용자 DB와 EXE는 열거나 만들지 않았고 단계 F 코드는 수정하지 않았다. 판정은 **조건부 통과**이며 F는 사용자 승인 대기다.

#### 단계 F 구현 결과

- 780·900·1080·1330px × 앱 배율 100·125·150%의 배치 계약 검사 12개를 추가했다. 칩 줄 y좌표, 28px 버튼, 텍스트 잘림, 서식 펼침 5회, 본문 높이 70% 이상, 목록 가로 스크롤 0, 보기·정렬 폭과 제목 줄의 창 안쪽 배치를 검사한다. 각 조합은 Qt 수명주기 충돌을 피하도록 별도 프로세스에서 수행한다.
- 기존 사용성 S1~S13 스크립트를 `tools/memo_usability_test.py`로 옮겨 임시 DB·임시 출력만 사용하게 하고 현재 UI의 진입점을 반영했다. 71개 확인, 실패 0개, 슬롯 예외 0개다. 기존 속성 칩·줄 간격·카테고리 칩의 28px 실패 세 건을 폭·테두리 계산으로 수정했다. 내용이 있는 오늘 요약도 접으면 편집기가 남는 분할 폭을 채우고 다시 열 수 있게 했다.
- Windows Qt `QWidget.grab()`으로 12장 기준 이미지를 `assets/qa/stage_f_layout/`에 저장했다. 캡처는 실제 포인터나 운영체제 디스플레이 배율 검증이 아니다. 임시 DB 실창은 실행됐으나 Computer Use 창 목록에 나타나지 않아 포인터 체크리스트는 미검증으로 `3._TODO.md`에 남겼다.
- 집중 검사 13개와 관련 기존 UI 검사, 전체 루트 52개 모듈 **902 passed**를 통과했다. 큰 묶음의 Qt 네이티브 종료는 더 작은 독립 프로세스로 재실행해 모든 모듈을 통과시켰다. `py_compile`·`git diff --check`도 통과했다. 사용자 DB·원본·기존 EXE 및 UUID·revision·UTC·tombstone·v1/v2 링크·백업 계약은 보존했다. **조건부 통과**이며 G-A는 명시적 승인 전까지 착수하지 않는다.

#### 후속 단계 G-A~G-G — 제목 분류 필터 계획 추가 (구현 전)

- 기준 자료: [`11._제목분류필터_계획/제목분류필터_구현계획.md`](11._제목분류필터_계획/제목분류필터_구현계획.md), [`제목분류필터_계획.html`](11._제목분류필터_계획/제목분류필터_계획.html), 같은 폴더의 `images/01`~`10` PNG를 모두 확인했다. HTML은 초기 선택지와 개념 시안이고, 확정 규칙·단계·검증은 Markdown의 00~07절을 기준으로 한다. 아래 G-A~G-G는 원문 내부 A~G에 대응한다.
- 게이트: 기존 단계 F를 먼저 마감한다. 이후 G-A부터 G-G까지 **한 번에 한 단계만** 구현하고, 단계마다 집중 검사·전체 회귀·`py_compile`·`git diff --check` 결과를 보고한 뒤 멈춘다. 다음 단계는 사용자가 명시적으로 승인한 경우에만 시작한다. 이번 요청은 계획 반영에 한정하며 G 코드·데이터·설명서는 수정하지 않는다.
- 화면 목표: [현재 화면](11._제목분류필터_계획/images/01_보내주신_실제화면.png)과 [420px 목표](11._제목분류필터_계획/images/02_목표_목록패널.png)를 기준으로 검색 줄은 `검색 · 기호 · 머리말`, 아래 줄은 `전체 · 항상 보이는 카테고리 칩 · 더보기`로 정리한다. [버튼 세 상태](11._제목분류필터_계획/images/03_버튼_세가지상태.png), [빈 상태 도움말](11._제목분류필터_계획/images/04_빈상태_설명말풍선.png), [값 메뉴](11._제목분류필터_계획/images/05_값목록_메뉴.png), [더보기](11._제목분류필터_계획/images/06_더보기_메뉴.png), [카테고리 설정](11._제목분류필터_계획/images/07_카테고리설정창.png), [자동완성](11._제목분류필터_계획/images/08_머리말_자동완성.png), [평평한 목록·결과 없음](11._제목분류필터_계획/images/09_평평한목록_결과없음.png), [첫 단어와 머리말 비교](11._제목분류필터_계획/images/10_첫단어_머리말_비교.png)를 각각 검증 기준으로 삼는다.
- 확정안: 첫 단어 분류는 사용하지 않고 제목 앞의 명시적인 `[머리말]`만 분류한다. 제목 칸 자동완성은 포함한다. 보기·정렬은 폭과 무관하게 항상 `더보기`에 넣고, 더보기는 칩이 모두 보여도 오른쪽 끝에 둔다. 기본 고정 칩은 업무·개발·자료 3개이고 `미지정`은 기본적으로 더보기에 둔다.
- 기호: 제목 앞 공백 다음 첫 글자의 Unicode 범주가 `So`일 때만 인식한다. 기존 국기·ZWJ 등 표시 묶음은 유지하되 비교 키에서 FE0E·FE0F와 피부색을 빼서 `✨/✨️`, `👍/👍🏻`를 합친다. `+`, `$`, `^`, `→`, `※`는 기호 필터 대상에서 제외한다. 머리말은 앞 공백과 연속 So 기호 묶음을 넘긴 직후 첫 `[ ]`에서 안쪽 1~20자를 읽고, 빈 값·닫는 괄호 없음·제목 중간은 제외한다. 키는 NFC+casefold, 표시는 가장 최근 메모의 원래 표기를 쓴다.
- 필터: 검색과 카테고리 조건을 적용한 행에서 기호 개수는 머리말 조건만, 머리말 개수는 기호 조건만 추가 적용해 센다. 최종 결과는 네 조건의 AND다. 기호나 머리말이 활성화되면 평평한 목록으로 보여 주고 끌어 옮기기·모두 접기를 잠시 끄며 원래 위치를 툴팁에 유지한다. 결과가 없으면 조건 요약과 `기호·머리말 필터 해제`를 보여 주되 이 버튼은 카테고리·검색을 보존한다. 두 제목 필터는 재실행 시 해제되고 기존 카테고리 필터 기억은 유지한다.
- G-A: `alert_notes/title_symbols.py`에서 So 판정·기호 키·머리말 추출/키·개수/최근수정/키 순 집계를 구현하고 규칙 표·20개 고정 제목으로 검증한다.
- G-B: `alert_notes/memo_list.py`에 머리말 필터와 교차 집계, 필터 없음/0개 상태, 평평한 목록 진입·복귀를 구현한다. 기존 기호 API의 같은 키 입력과 계층 보기 동작을 유지한다.
- G-C: 목록 검색 줄에 머리말 버튼을 기호 버튼 바로 오른쪽에 추가한다. 대표값은 `✨ 5 ▾`/`[TD] 3 ▾`, 선택 시 개수를 뺀 체크 상태, 값 없음은 `☆`/`[ ]` 점선 상태로 한다. 빈 버튼은 전체 없음과 현재 조건 때문에 없음이 다른 [도움말](11._제목분류필터_계획/images/04_빈상태_설명말풍선.png)을 연다. 값 메뉴는 개수·최근 수정·키 순이며 0개가 된 선택값을 유지하고 머리말이 10개를 넘으면 메뉴 검색을 제공한다. 긴 머리말은 안쪽만 96px 안에서 줄인다. `ui_theme.py` 배율 스타일도 포함한다.
- G-D: `memo_list.py`의 보기·정렬 콤보 객체와 설정 복원은 유지하되 검색 줄에서는 숨긴다. `더보기`를 항상 오른쪽에 표시하고 숨은 카테고리/미지정 → 보기 → 정렬 → 카테고리 설정 순으로 배치한다. 비기본 보기·정렬은 파란 점, 숨은 카테고리 선택은 그 이름·색 점을 표시한다. `panel.py`에서 기존 카테고리 관리 창을 연결한다.
- G-E: 새 `alert_notes/memo_category_pins.py`의 로컬 설정 `memo_category_pinned`에 항상 보이는 카테고리 ID와 `none`만 보관한다. 기존 설정이 없으면 현재 순서 앞 3개를 기본으로 삼고, 추가·순서 변경 전에 기본값을 명시 저장해 칩이 갑자기 바뀌지 않게 한다. 기존 `CategoryManagerDialog`에 체크 칸과 고정 `미지정` 행을 추가하고 체크 변경을 즉시 반영한다. 고정하지 않은 카테고리는 넓은 창에서도 더보기에만 둔다. DB 테이블·동기화 계약은 바꾸지 않는다.
- G-F: `editor.py` 제목 칸 맨 앞의 `[` 안쪽을 입력할 때만 기존 머리말을 사용 빈도순으로 제안한다. 방향키·Enter로 선택하면 `[머리말] `을 채우고 앞 기호와 뒤 제목을 보존한다. 제목 중간의 `[`와 제안 없음은 기존 입력 흐름을 유지하고 자동 저장과 충돌하지 않게 한다.
- G-G: 프로그램 내 설명서와 `4. TomaDesk_설명서.md`를 실제 UI로 갱신한다. 임시 DB로 목록 폭 380·480px × 100·125%와 카테고리 8개 넘침을 캡처하고 Windows Qt 100·125·150% 자동 검사, 단계별 집중 검사, 전체 회귀를 수행한다. 실제 Windows 포인터 검증은 자동 Qt 검사와 구분하고 불가하면 `3._TODO.md`에 남긴다.
- 보존·제외: 사용자 DB·원본 파일·기존 EXE, 메모 계층·수동 순서·선택 작업·충돌 표시, 검색 의미, 카테고리/보기/정렬 설정 키, UUID·revision·UTC·tombstone·v1/v2 링크·구형/신형 백업 계약을 유지한다. 첫 단어 자동 분류, 본문/제목 중간 머리말, 머리말 일괄 병합, 고정 칩 설정의 기기 간 동기화, DB 열 추가, 새 EXE는 범위 밖이다. 테스트와 캡처에는 임시 DB만 쓴다.
- 착수 전 확인: 기존 승인 계획은 카테고리 순서의 드래그와 `위로/아래로` 버튼을 모두 요구하지만 새 시안은 버튼을 없애고 `Ctrl+↑/↓`로 대체한다. 기존 접근성 계약을 바꾸는 부분이므로 G-E 구현 전 사용자 결정이 필요하다. 또한 [420px 목표 이미지](11._제목분류필터_계획/images/02_목표_목록패널.png)에는 `최근 ▸`가 보이지만 본문은 필터 줄을 칩+더보기로 기술한다. 최근 메모 영역은 기존 접이식 동작을 유지하고 새 위치/모양은 G-D 착수 전 확인한다. 이 두 차이는 조용히 확정 처리하지 않는다.

#### 단계 G-A 구현 결과

- `title_symbols.py`에서 제목 첫 글자의 `So` 판정만 기호로 인정하고, 기존 국기·ZWJ·변형 문자·피부색을 하나로 묶어 반환하는 표시 경로는 유지했다. 비교 키에서는 FE0E·FE0F와 피부색을 제외한다. 제목 앞 공백과 연속 `So` 묶음을 건너뛴 첫 `[머리말]`의 안쪽 1~20자를 추출하고 NFC+casefold 키로 집계한다.
- `TitleValueCount`와 `count_title_values()`는 키별 개수, 가장 최근 `updated_at`의 원래 표시, 개수 내림차순·최근 수정 내림차순·키 순을 제공한다. 순수 규칙과 SQLite 행 입력을 검사했고 고정 제목 20개는 `td=3·토마=2·모리=2`였다.
- 집중 50개와 루트 전체 53개 파일 **944 passed**를 통과했다. `py_compile`·`git diff --check`도 통과했다. UI 필터·버튼·DB·설정·백업은 변경하지 않았고, 순수 규칙 단계이므로 별도의 실제 포인터 결과는 없다. 사용자 DB·기존 EXE·UUID·revision·UTC·tombstone·v1/v2 링크 계약을 유지했다. **통과**로 마감하고 G-B는 명시적 승인 대기다.

#### 단계 G-B 구현 결과

- `memo_list.py`에서 기호·머리말 필터는 표시 문자열 대신 G-A 비교 키를 일시적으로 보관한다. 검색·카테고리를 먼저 적용한 뒤 기호 개수에서는 머리말 필터만, 머리말 개수에서는 기호 필터만 추가 적용하고 최종 행에는 네 조건을 AND로 적용한다. 기호 또는 머리말이 활성화되면 평평한 목록을 쓰고 끌기·모두 접기를 끄며, 해제하면 원래 계층·카테고리별 보기로 복귀한다.
- 결과가 0개이면 `memoFilteredEmptyState`에 안내·조건 요약과 제목 필터 해제 버튼을 표시한다. 해제는 기호·머리말만 지우고 한 번 갱신하며 검색·카테고리 조건을 유지한다. 필터가 없을 때는 기존 빈 목록 안내를 유지한다. 기존 기호 버튼 글자·메뉴의 시각 계약은 G-C 전까지 보존하고 머리말 버튼은 추가하지 않았다.
- 임시 DB·메인 패널 경로와 Windows Qt 앱 배율 100·125·150%의 집중 **75 passed**, 루트 53개 파일 전체 **969 passed**. 새 검사 파일의 `QApplication` 참조 해제로 다음 테스트 모듈의 포스트잇 생성에서 접근 위반이 처음 재현됐으나 검사 앱 참조를 유지하도록 수정했고, 두 파일 126개와 최종 전체 회귀 969개는 네이티브 종료 없이 통과했다. `py_compile`·`git diff --check`도 통과했다.
- 실제 Windows 포인터 조작은 이번 자동 검사와 별개로 `3._TODO.md`에 남긴다. 테스트는 임시 SQLite만 사용했고 사용자 DB·기존 EXE·UUID·revision·UTC·tombstone·v1/v2 링크·백업 계약을 변경하지 않았다. **통과**로 마감하고 G-C는 명시적 승인 대기다.

#### 단계 0 작업과 검증

- 확정 계획과 승인 게이트, Q1·Q2 A안을 이 파일에 기록한다.
- 보완계획 문서의 이미지 참조를 실제 폴더 구조와 맞춘다.
- 현재 전체 회귀 815개를 기준선으로 두고 다시 실행한다.
- 집중 검사는 계획 문서의 F1~F17·단계 A~F·Q1/Q2·보존/제외 항목과 9개 PNG 참조가 모두 존재하는지 확인한다.
- 테스트 코드와 수동 검증 도구가 `TemporaryDirectory` 또는 명시적 임시 DB만 사용하는지 확인한다.
- 검증 결과: 목록 콤보의 전역 테마 높이와 서식 버튼의 기본 테두리 계산을 고정했다. 실패 2개 단독 검사와 전역 테마 포함 26개 묶음, 최종 전체 회귀 815개가 통과했다.
- 상태: 단계 0 통과. 사용자의 승인에 따라 단계 A까지 진행했다.

#### 단계 A 구현과 검증

- `rich_memo_edit.py`에 누락된 `json` 가져오기를 추가하고 `/템플릿` 선택과 템플릿 저장의 `ValueError`를 경고창으로 처리한다.
- `panel.py`의 일괄 카테고리 지정 상태 호출에 `success` 수준을 전달한다.
- 주석·템플릿·버전 버튼 슬롯의 예상 가능한 데이터 오류를 경고창으로 처리해 프로그램이 유지되게 한다.
- `A_shortcut_launcher.py`에 최종 `sys.excepthook`을 설치한다. 처리되지 않은 Qt 슬롯 예외는 현재 데이터 폴더의 `tomadesk_error.log`에 기록하고 경고창을 표시한다.
- 검증 결과: 단계 A 신규 5개를 포함한 집중 검사 29개, 컴파일 검사, 최종 전체 회귀 820개가 통과했다.
- 실제 창: 전용 임시 DB 창 프로세스는 실행됐으나 Computer Use 창 목록에 TomaDesk/Python 창이 노출되지 않아 포인터 입력을 수행하지 못했다. 임시 DB는 종료 후 제거했다.
- 상태: 자동 검사는 통과했으나 실제 Windows 포인터 검증은 완료하지 못해 조건부 통과로 판정한다. 단계 B에는 착수하지 않는다.

#### 단계 B 구현과 검증

- 메모 전환 때 실행되던 `calendar.shutdown()`과 `summary.shutdown()`을 패널의 실제 종료 경로로 옮겨 오늘 요약이 계속 갱신되게 했다.
- 첫 실제 저장 직전에 `session_start` 버전을 한 번 만들고 전환·종료 때 `auto` 최종본을 남긴다. 무변경 세션은 만들지 않으며 두 자동 종류를 합쳐 최근 30개로 정리한다.
- 주석은 저장·재열기 때 block ID, 원문, 앞뒤 문맥 순으로 위치를 복구한다. 유일한 위치가 없으면 `needs_review`를 유지하고 위치·상태 변경에 revision·UTC·기기 UUID를 반영한다.
- 새 공통 링크 재매핑 함수에서 정수/v1과 UUID/v2 메모·블록 링크 및 첨부를 처리한다. 함께 복제·병합된 대상만 새 ID·UUID·block ID로 바꾸고 외부 참조는 유지한다. v2 페이지 링크도 클립보드의 복제 그래프에 포함한다.
- 템플릿은 payload·블록·페이지·주석·첨부 허용 키와 base64를 구조적으로 검사한다. 본문 코드 문자열은 허용하고 `href`·`src`의 실제 로컬 경로만 거부한다.
- 첫 전체 회귀에서 주석이 없는 저장도 관계를 재조회해 SELECT 예산 검사가 10건으로 늘어난 실패 1건을 발견했다. 편집기에 주석 ID를 캐시하고 저장 후 전체 관계 재조회를 제거해 해당 검사와 단계 B 검사 11개를 재통과했다.
- 검증 결과: 단계 B 신규 10개, 관련 기존 회귀를 합친 집중 검사 96개, 컴파일·diff 검사, 수정 후 최종 전체 회귀 830개가 통과했다.
- 실제 창: 전용 임시 DB의 `TomaDesk 메모 구현 검증` 창을 고유하게 탐지했다. 포인터로 메모를 세 차례 전환하고 D-Day를 저장한 뒤 오른쪽 오늘 요약에 `D-1 · 실제 창 D-Day`가 나타나는 것을 확인했다. 창을 정상 종료하고 임시 DB를 제거했다.
- 상태: 자동 검사는 모두 통과했고 F3 실제 창도 통과했다. F4·F6·F7의 실제 포인터 검증은 TODO에 남겨 단계 B를 조건부 통과로 판정하며 단계 C에는 착수하지 않는다.

### 2026-09-15 메모 편집·모바일 연동 대비 전체 구현 완료

- 변경 요약: Phase 4를 마감하고 MD 코드·범위·접힘 표·제목 표시, 28px 편집 UI와 공용 상태 영역, 메모 카테고리·필터·정렬·모아보기, UUID/revision/UTC/device/tombstone 저장 기반, v2 링크, 백링크·주석, 블록 템플릿, 버전 기록을 순서대로 구현했다.
- 호환·보존: 기존 정수 PK/FK, 숫자/v1 링크, `vanilla`, 구형 `.tomamemo`, 메모 계층·수동 순서·첨부·알림·D-Day·포스트잇·단축키를 유지했다. 독립 복제는 새 UUID, 교체 복원은 기존 UUID, 병합 충돌은 새 UUID의 관계 있는 사본으로 보존한다.
- 검증 결과: 임시 DB 마이그레이션 백업·반복 안정성·복제·충돌·tombstone·백업 왕복·첨부 포함 버전 복원 rollback과 MD/UI/Phase 4 집중 검사를 통과했다. 전체 회귀 815개와 Windows Qt 100/125/150%의 780/900/1080/1920px 집중 검사 각 24개를 통과했다. 실제 포인터 미확인 사항은 작업 기록과 TODO에 둔다.
- 제외 유지: 모바일 앱·서버·계정·네트워크 동기화·푸시 알림·일정 카테고리 통합·다중 태그·EXE 생성은 구현하지 않았다.

### 메모 편집 UI 개편 (2026-09-14 구현 승인, 진행 중)

- 요청·목표: Phase 4 편집기 기능을 보존하면서 본문 폭과 높이를 늘리고, 알림·D-Day·색·단축키 등 속성과 서식 도구를 필요할 때 펼친다.
- 변경 범위: 저장된 분할 비율을 유지하고 새 기본값만 `520:780:400`으로 변경한다. 빈 오늘 요약은 재열기 손잡이로 접는다. 편집기 자식 속성 칩과 본문을 가리지 않는 인라인 패널, 선택 줄 오른쪽 또는 공유 하단 띠의 글자 서식 도구, 좁은 창 목록 서랍을 제공한다.
- 기능·호환: 기존 위젯·값·단축키·자동 저장을 재사용한다. 빠른 알림 키와 Ctrl+Enter는 패널이 닫혀도 작동한다. 제목 키, 새 메모 집중 모드 설정, 설명서·툴팁·들여쓰기·접힘 마커 표시를 보완한다. `memo_editor_layout`은 스키마 변경 없는 `classic | compact`, 기본 `compact`로 한다.
- 검증: 임시 DB의 넓은/좁은 창, F11·독립 창·포스트잇, 선택 도구 배타성·본문 글자 가림, 빠른 알림 저장·저장 설정 복원, 전체 회귀 및 Windows 실제 창 100/125/150%를 확인한다.
- 보존·제외: 사용자 DB·첨부·기존 EXE 및 Phase 4 작업 내용을 보존한다. 새 DB 스키마·EXE 생성·배포는 제외한다. classic은 2026-10-14까지 유지 후 별도 검토한다.
- 상태: UI 구현, 전체 회귀 801개 및 Windows Qt 100·125·150% 집중 검사 각 12개를 통과했다. 실제 물리 포인터 조작 확인이 남아 있어 완료로 기록하지 않는다.
- 단축키 충돌: 기존 체크리스트 `Ctrl+Shift+4` 보존을 우선해 제목4를 `Ctrl+Shift+6`으로 배정했다. 사용자에게 두 선택지를 제시했으며 답이 오면 반영한다.

### 메모 편집 기능 대규모 개선 (단계별 진행)

- 요청: 현재 편집기 구조를 재사용하면서 토글 붙여넣기·Redo·독립 복제·다중 블록 조작·접기·목차·블록 링크·백링크·주석·템플릿·버전 기록을 단계적으로 개선한다.
- 추가 반영:
  - 일반 본문에 남은 내부 링크 때문에 손가락 커서가 나타나고 `데이터 관리` 페이지로 이동하는 실제 사례를 Phase 0 최우선 결함으로 포함한다.
  - 범위를 드래그한 뒤 우클릭하여 토글·페이지·링크 등 선택 범위의 구조·동작을 한 번에 해제하는 `연동 기능 삭제`를 Phase 2 공통 명령으로 포함한다.
  - `연동 기능 삭제`는 글자와 일반 문자 서식을 보존한다. 페이지는 삭제하지 않고 일반 하위 메모로 전환하며, 표·이미지는 콘텐츠로 유지한다.
- 권장 순서: Phase 0 안정화 → Phase 1 클립보드·복제 → Phase 2 선택·명령·우클릭 → Phase 3 접기·편집 → Phase 4 block ID·목차·블록 링크·고정 → Phase 5 백링크·주석 → Phase 6 템플릿 → Phase 7 버전 기록.
- 데이터 원칙: Phase 0~3은 schema 변경 없이 진행하고, 영구 block ID는 HTML 기반 지연 마이그레이션을 우선한다. 주석·템플릿·버전 단계에서만 필요한 테이블과 백업 형식을 단계별로 확장한다.
- 구현 원칙: Qt 문서 Undo를 재사용하되 페이지·첨부·메모 행을 바꾸는 명령은 DB 트랜잭션과 보상 Undo를 함께 설계한다. 독립 복제는 새 ID를 만들고 `🔗 메모 링크`는 기존 대상 ID를 유지한다.
- 검증: 각 Phase마다 집중 자동 검사, 전체 회귀, Windows Qt 실제 창 검사를 수행하고 사용자 DB는 읽기 전용 사례 검증에만 사용한다.
- 보존 범위: 기존 메모·페이지·링크·계층·서식·첨부·휴지통·알림·D-Day·포스트잇·단축키·자동 저장·백업 호환, 사용자 DB와 기존 EXE를 보존한다.
- 제외 범위: 카테고리 계획 구현, 일정 카테고리 통합, AI 자동 분류, 외부 협업 동기화, EXE 생성·배포는 포함하지 않는다.
- 상세 계획: [`메모편집_대규모_개선계획.md`](메모편집_대규모_개선계획.md)
- 완료 승인 범위: Phase 3의 제목·토글 범위 접기, 모두 접기/펴기, 줄 간격, 원본/텍스트만/현재 서식 붙여넣기와 키보드 탐색을 구현했다. 기존 Phase 2 선택·명령 경로와 단축키 설정을 재사용했다.
- 상태: 잔류 링크 보완과 Phase 0~3을 완료했다. 사용자 요청대로 여기서 멈추며 Phase 4 착수 승인 대기 중이다. Phase 4 이후 기능, 대규모 선행 리팩터링, EXE 생성은 하지 않았다.

### 메모 편집 Phase 1~3 실사용 오류 보완 (2026-09-14 승인, 진행 중)

- 요청·목표: 페이지 드래그의 오진입, Alt+C 무응답, 다중 블록 선택·공통 명령·우클릭 가림, 제목 1~4 크기·접기 오류를 실제 입력 기준으로 수정한다. 추가 진단의 단일/비연속 토글 묶기 Undo 실패와 Esc 선택 해제 충돌도 포함한다.
- 변경 범위: 일반 드래그는 글자 범위, Alt+클릭/드래그는 블록 선택으로 분리한다. 링크·접기·체크 표시는 같은 대상에서 짧게 누른 클릭에만 반응한다. 우클릭의 선택 대상과 부분 범위 명령을 바로잡고, 각 공통 명령의 결과·Undo/Redo를 확인한다.
- UI: 선택 명령을 본문 밖 편집기 하단 고정 줄로 옮겨 선택 글자를 가리지 않는다. 좁은 창은 더보기로 압축하고 우클릭 메뉴는 선택 영역을 피해 띄운다. Alt+C는 복사/적용/복사된 서식 없음 상태를 자동 닫힘 알림으로 구분한다.
- 제목: 빈 줄에서 제목을 지정하고 이어 쓴 글자에 22/18/15/13pt와 굵기를 적용한다. 표준 HTML heading 메타데이터를 저장하고 기존 제목 판별을 유지해 재열기·접기·본문 전환·붙여넣기와 호환시킨다.
- 구현: 선택 관리자와 마우스 이벤트, 공통 명령의 토글 자식 생성 트랜잭션, 편집기 하단 명령 줄, Esc 단축키 우선순위, 제목 직렬화 경로를 관련 모듈 중심으로 수정한다. 새 DB schema는 만들지 않는다.
- 검증: 실제 press/move/release와 단축키 입력, 단일·연속·비연속 선택별 명령·한 번 Undo/Redo, 페이지·첨부·링크 보존, 제목 1~4의 빈 줄 입력·재열기·접기, 좁은 창·배율 렌더를 집중 검사한다. 전체 회귀 후 임시 데이터의 Windows 실제 창을 Computer Use로 조작한다.
- 보존·제외: 사용자 DB·메모·첨부·기존 EXE 및 기존 기능·단축키를 보존한다. Phase 4 이후 기능, 카테고리 구현, schema 변경, EXE 생성·배포는 제외한다.
- 상태: 코드 수정과 Windows Qt 집중·전체 자동 검사를 완료했다. 사용자가 직접 띄운 임시 데이터 창을 Computer Use로 조작하여 페이지 클릭/드래그, 글자 선택, Alt+C 세 알림, 우클릭 메뉴 위치, 제목 1~4·접기·재열기를 확인했다. 사용자의 Alt+클릭으로 생긴 1개 블록 선택과 하단 명령 줄·실제 Esc 해제도 확인했다. Alt+드래그·비연속 블록 선택 생성과 명령 버튼의 실제 포인터 검증은 남아 있어 이 계획은 진행 중으로 유지한다.

### 메모 글자 범위 다중 선택 (2026-09-14 구현 승인, 진행 중)

- 요청·목표: 떨어진 글자 구간 여러 곳을 한 번에 선택해 글자 서식과 글자 단위 기능을 일괄 적용한다. 현재의 다중 블록 선택과 한 곳의 일반 글자 선택은 유지한다.
- 2026-09-14 추가 오류·수정 승인: Ctrl+드래그 후 크기 조절 화살표에서 손을 떼도 크기 숫자와 본문 글자가 계속 변하는 현상을 안정화한다. Ctrl+드래그의 누름·이동·놓음을 편집기 한 경로에서 완결하고 짧은 Ctrl+클릭의 페이지/일반 링크 열기와 일반 커서 위치 지정을 보존한다. 크기 입력·글씨체 선택 중에는 서식 적용이 해당 입력 컨트롤의 포커스를 빼앗지 않도록 한다.
- 추가 검증: Ctrl+드래그 뒤 크기 위·아래 화살표의 누름→유지→놓음과 숫자 입력, 손을 뗀 뒤 값·서식의 안정성, 반복 선택과 일반 드래그 전환, 짧은 Ctrl+링크 클릭, 단일 Undo/Redo와 좁은 폭·Windows 실제 창을 임시 데이터로 검사한다. 실제 포인터 재현이 불가능한 항목은 완료로 기록하지 않는다.
- 선택안: 일반 드래그는 기존의 한 글자 범위 선택, Alt+클릭·드래그는 블록 선택으로 둔다. Ctrl+드래그로 글자 범위를 추가하되 짧은 Ctrl+클릭의 기존 링크 열기는 유지한다. 키보드로 선택한 현재 글자 범위도 충돌 없는 명령으로 추가할 수 있게 한다. 구현 전에 링크 열기·단축키·텍스트 끌기와 충돌을 확인한다. 블록 모드와 글자 다중 모드는 동시에 활성화하지 않는다.
- 적용 범위: 글씨체·크기·굵게·기울임·밑줄·취소선·색상 등 글자 서식과 선택한 글자 범위의 일반 링크/연동 해제만 일괄 적용한다. 이동·복제·삭제 및 제목·토글·체크리스트·페이지·표처럼 문단·구조를 바꾸는 명령은 다중 글자 선택의 대상에서 제외한다.
- UI·구현: 선택 구간을 본문에서 서로 구별되게 강조하고, 하단 예약 공간에 `N개 글자 구간 선택`과 적용 가능한 명령·해제를 표시한다. 구간의 시작/끝은 문서 편집을 따라가는 커서로 보관하고 겹치거나 맞닿은 구간은 합친다. 일반 클릭·Esc·메모 전환·구조 편집 시 선택을 안전하게 해제한다. 각 일괄 적용은 한 번의 Undo/Redo로 묶고 글자·기존 링크 대상·미선택 서식을 보존한다.
- 검증: 순방향/역방향·비연속·중첩/인접·여러 줄에 걸친 선택, 기존 블록 선택과 전환, 키보드 대안, 링크 구간의 정확한 해제, 연속 서식 적용과 한 번의 Undo/Redo, 저장·재열기, 좁은 폭·배율·포커스·화면 가림을 임시 DB와 Windows 실제 창에서 확인한다.
- 보존·제외: 사용자 DB·메모·첨부·기존 EXE·기존 Alt 블록 선택을 보존한다. 글자 구간 자체의 저장, 글자 이동·삭제·복제, 구조 변환, DB schema·EXE 생성은 제외한다.
- 상태: Ctrl+드래그·Ctrl+Shift+M으로 비연속 글자 구간을 저장하고 서식·부분 링크 해제·하단 명령 줄·Esc를 구현했다. 크기 조절 중 문서 변경 콜백의 포커스 탈취와 Ctrl 드래그의 불일치한 press/release 처리를 보완했다. 손을 뗀 뒤 값 안정성·숫자 입력·두 번의 Ctrl 드래그·짧은 Ctrl 링크 클릭은 임시 DB의 Windows Qt 테스트로 통과했다. Computer Use 실제 포인터 확인은 임시 창 탐지 제한으로 남아 있어 진행 중이다.

### 메모 전체보기 본문 높이 확대 (2026-09-14 구현 승인, 진행 중)

- 요청·목표: 현재 F11 전체보기의 붙임 1보다 메모 본문 입력 영역을 붙임 2의 집중 편집창처럼 더 높게 쓴다. 화면 크기나 DPI 차이를 고려해 픽셀 복제가 아닌 영역 배분을 맞춘다.
- 화면 시안: F11 동안 앱 상단 작업공간/탭/유틸리티 줄과 메모 하단 상태 안내 줄을 숨긴다. 창 제목과 메모 제목·위치 표시·F11 복귀 단추는 유지한다. 제목 아래부터 서식·알림·단축키 등 하단 보조 컨트롤 위까지 본문이 남는 높이를 모두 차지하게 하고, 하단 컨트롤은 기능을 없애지 않은 채 필요한 높이만 쓰도록 세로 여백을 줄인다. F11 해제 시 원래 배치를 정확히 복원한다.
- 구현 범위: `MainWindow`의 작업공간 줄 표시와 `AlertNotesPanel`의 전체보기 상태를 연결하고, `MemoEditor`의 F11 전용 레이아웃/크기 정책만 조정한다. 일반 보기·독립 메모창·포스트잇·다른 탭은 바꾸지 않는다. 본문 스크롤, 자동 저장, 서식 도구, 알림·단축키 접근은 유지한다.
- 검증: 같은 창 크기에서 F11 전후 본문 높이를 측정해 증가를 확인하고 붙임 2의 배치와 시각 비교한다. F11·복귀 버튼 왕복, 메모 전환·창 크기 변경, 좁은 창, Windows 100/125/150% 배율, 하단 명령 줄·포커스·스크롤·컨트롤 잘림을 검사한다. 임시 데이터 실제 창의 Computer Use 확인과 전체 회귀를 수행한다.
- 보존·제외: 사용자 DB·기존 메모/설정/EXE, 비전체보기 배치와 기존 키 동작을 보존한다. 새 도구·서식 기능, 다른 화면 리디자인, EXE 생성·배포는 제외한다.
- 상태: F11 동안 작업공간 줄·메모 상태 줄을 숨기고 보조 줄 여백을 줄여 본문 높이를 늘렸다. 같은 창 크기의 Windows Qt 테스트에서 본문 높이 50px 이상 증가와 복귀를 확인했으며, Computer Use 실제 창 확인은 남아 있어 진행 중이다.

### 메모 편집 Phase 4 (2026-09-14 구현 승인, 진행 중)

- 요청·목표: 현재 글자 다중 선택의 실제 포인터 안정화 확인을 선행한 뒤, 제목 변경·블록 이동 후에도 유지되는 영구 블록 ID, 목차, 블록 링크, 본문 블록 고정을 단계적으로 구현한다.
- 선행 검증 상태: 사용자의 명시적 4단계 착수 요청에 따라 구현을 시작했다. 앞 단계의 Computer Use 실제 포인터 검증은 수행된 것으로 간주하지 않으며 `3._TODO.md`에 남긴다. 검증 중 기존 오류가 재현되면 우선 수정한다.
- 4A 저장 방식 검증: Qt 문서의 사용자 block property가 HTML 왕복에서 유실되는 기존 사례를 전제로, 구형 HTML·제목/토글·표·페이지·첨부·복합 붙여넣기 샘플로 ID 직렬화 실험을 먼저 한다. HTML 안에서 안전하게 왕복하는 표현을 확정하기 전에는 사용자 메모에 ID를 기록하지 않는다. HTML 방식이 성립하지 않으면 대안과 백업·마이그레이션 범위를 다시 승인받는다.
- 4B ID·복제: `block_identity`와 `memo_id + block_id` 해석기를 분리한다. 기존 문서는 읽을 때 메모리에서 ID를 부여하고 사용자가 실제 편집·저장할 때만 지연 기록한다. 이동·제목 변경은 ID 유지, 새 블록·독립 복제는 새 ID, 참조 링크는 대상 ID 유지로 구분한다. Undo/Redo와 복구·백업의 ID 충돌을 검사한다.
- 4C 목차·링크·고정: 제목 계층을 우선 표시하는 좁은 폭 대응 목차와 클릭·키보드 이동을 제공한다. 기존 `toma-note://id`를 계속 읽고 새 버전형 블록 주소와 링크 복사·해결 실패 표시를 추가한다. 본문 블록 고정은 메모 목록의 기존 핀과 별개로 취급하고, 고정 표시·해제·연동 기능 삭제를 일관되게 처리한다.
- 검증·보존: 임시 DB에서 저장·재열기·이동·복제·붙여넣기·삭제·Undo/Redo·구형 백업 왕복과 링크 보존을 집중 검사하고 전체 회귀 및 Windows 100/125/150% 실제 창을 확인한다. 사용자 DB·메모·첨부·기존 EXE는 변경하지 않고, 실제 사용자 자료는 읽기 전용·복제본으로만 검사한다. Phase 5 이후 기능·카테고리·EXE 생성은 제외한다.
- 상태: 4A HTML 왕복 표현을 버전형 비표시 `<meta>`로 확정하고, 4B 블록 ID·복제·Undo/Redo 및 4C 목차·버전형 블록 링크·본문 블록 고정을 구현 중이다. 집중 Qt 16개, 전체 회귀 788개와 백업 병합 왕복 검사를 통과했다. 실제 포인터, 모든 구조 편집 및 페이지 간 이동의 기존 링크·통합 Undo는 아직 검증 중이다. 사용자 DB·EXE는 변경하지 않았다.

### 메모 카테고리·정렬·모아보기 개선 (2026-09-15 구현 완료)

- 요청: 메모마다 메모 전용 카테고리를 지정하고, 메모 목록에서 카테고리 필터·보기 전용 정렬·카테고리별 모아보기를 제공한다.
- 목표: 기존 부모·하위 메모 트리와 직접 끌어 정한 순서를 보존하면서, 위치가 다른 메모도 같은 주제로 빠르게 찾고 묶어 본다.
- 변경 범위:
  - 메모당 카테고리 1개를 지정한다. 초기 카테고리는 `업무`, `개발`, `자료`이며 `미지정`은 삭제할 수 없는 가상 항목으로 취급한다.
  - 카테고리는 메모 전용으로 두고 일정 카테고리와 분리한다. 사용자는 이름·색상·순서를 추가·수정·삭제할 수 있으며 개수는 고정하지 않는다.
  - 새 하위 메모는 생성 시점에 부모 카테고리를 한 번 복사하고 이후에는 부모와 독립적으로 변경한다.
  - 편집 영역의 위치 표시줄 오른쪽에 현재 카테고리 칩을 두고, 메모 목록에는 `전체` 및 카테고리 칩, `보기`, `정렬`, `더보기`를 배치한다.
  - 목록 기본 열은 시안대로 `선택`, `제목`, `카테고리`, `수정일`만 표시한다. 기존 내용 미리보기·일정 정보는 제목 말풍선에 보존한다.
  - `기본 보기`는 기존 계층과 수동 순서를 유지하고, `카테고리별 보기`는 카테고리 순서의 접이식 그룹 아래에 메모를 평면으로 모으며 하위 메모에는 원래 부모 경로를 보조 표시한다.
  - 정렬은 `기본`, `수정일 최근/오래된`, `제목 가나다/역순`을 제공한다. 고정 메모는 모든 정렬에서 각 계층·그룹의 맨 위에 유지한다.
  - 검색과 카테고리 필터는 AND로 결합하고, 보기·정렬·필터는 재실행 후 복원한다. 삭제된 카테고리를 기억한 경우 `전체`로 안전하게 되돌린다.
  - 현재 폭에 들어오지 않는 카테고리 칩은 `더보기` 메뉴에 넣는다. 관리창의 순서 변경은 드래그와 `위로/아래로` 버튼을 함께 제공한다.
  - 기존 체크 선택을 이용해 여러 메모에 카테고리를 일괄 지정하며, 카테고리 삭제 시 연결 메모 수를 확인한 뒤 메모는 지우지 않고 `미지정`으로 바꾼다.
- 구현:
  - 별도 `memo_categories` 저장 구조와 메모의 nullable 카테고리 ID를 추가하고, 이름 변경에도 연결이 유지되도록 숫자 ID로 참조한다. 기존 DB는 변경 전 자동 백업 후 모든 기존 메모를 `미지정`으로 마이그레이션한다.
  - 저장소에 카테고리 CRUD·순서 변경·단건/일괄 지정·검색/필터/보기 정렬 조회를 추가하되, 보기 전용 정렬은 `parent_id`와 기존 `sort_order`를 갱신하지 않는다.
  - 전체 메모 백업 형식을 상향해 카테고리와 연결을 포함하고 구형 백업도 계속 읽는다. 병합 복원은 같은 이름의 카테고리를 재사용하고 새 이름은 뒤에 추가하며 메모 연결 ID를 다시 매핑한다.
  - 메인 편집창과 독립 메모창의 카테고리 표시·변경을 동기화하고, 목록·최근 메모·휴지통 복원·Excel 내보내기 흐름에서도 카테고리 값을 유지한다.
- 검증:
  - 기존 DB 마이그레이션 전 백업, 초기 카테고리 생성, CRUD·중복 이름 방지, 삭제 후 미지정 전환, 하위 메모 초기 복사와 이후 독립 변경을 검사한다.
  - 검색+필터, 두 보기 전환, 모든 정렬, 고정 우선, 카테고리 그룹 접기, 부모 경로, 다중 선택 일괄 지정, 저장 상태 복원을 검사한다.
  - 휴지통 왕복, 구형/신형 전체 백업의 병합·교체 복원, Excel 내보내기, 자동 저장·최근 메모·끌어 놓기·알림/D-Day 회귀를 검사한다.
  - Windows 100%·125%·150% 배율과 넓은/좁은 창에서 시안과 실제 렌더를 대조하고, 칩 넘침·`더보기`·관리창·말풍선·가로 스크롤 여부를 실제 창으로 확인한다.
- 보존 범위: 기존 메모·계층·수동 순서·고정·내용·첨부·알림·D-Day·포스트잇·단축키·설정·사용자 DB와 기존 EXE를 변경·삭제하지 않는다.
- 제외 범위: 다중 태그, 일정 카테고리 통합, AI 자동 분류, 복합 AND/OR 조건, 저장된 스마트 보기, 캘린더 자동 연동, EXE 생성은 이번 작업에 포함하지 않는다.
- 상태: 모바일 연동용 식별·변경 기반과 함께 구현하고 집중·전체 회귀를 통과했다. 실제 포인터 검증 항목만 `3._TODO.md`에 남겼다.

## 미완료·결정 대기

- 실제 사용자 입력 환경 검증, 후속 메모 기능과 일정 기능의 우선순위는
  [`3._TODO.md`](3._TODO.md)에서만 관리한다.
- 이 항목들은 별도 요청과 계획 승인 없이 구현하지 않는다.

## 완료 내역

완료 계획의 상세 처리·테스트 명령·화면 검증 근거는 [`2._WORK_LOG.md`](2._WORK_LOG.md)에서 확인한다.

| 완료일 | 작업명 | 변경 요약 | 검증 결과 |
| --- | --- | --- | --- |
| 2026-08-07~10 | 메모·알림 기본 편집 기능 | 리치 편집, 자동 저장, 반복·알림·포스트잇과 입력 UI를 정비 | 관련 Qt 테스트·실제 렌더 확인 |
| 2026-08-11~15 | 독립 메모창·포스트잇·단일 EXE | 독립 편집창, 목록 관리, 포스트잇 개선과 단일 EXE 배포 흐름을 반영 | 임시 DB 검사와 EXE 실행 검증 |
| 2026-08-25 | 메모·설정·토마펫 UI 개선 | 메모 화면 비례, 설정 2열, 상단 내비게이션, 토마펫 알림·상시 표시를 반영 | 관련 UI·회귀 검사와 Windows 렌더 확인 |
| 2026-08-27~28 | 제품명·PyInstaller 안정화 | TomaDesk 명칭 통일, 단일 EXE 재빌드, PyQt6 DLL 포함과 실행 오류를 보완 | 단일 EXE·DLL·실행 스모크 검증 |
| 2026-08-28 | 메모 창·목록 레이아웃 | 선택 상태, 공통 상단 액션, 목록·편집창·휴지통 레이아웃을 개선 | 관련 Qt 테스트와 화면 렌더 확인 |
| 2026-08-31 | 안정성·데이터 보호 1단계 | 설정·단축키·데이터 보호 기반과 알림·일정 공통 기반을 정비 | 집중 회귀 검사와 설정 렌더 확인 |
| 2026-09-04 | 메모 토글·페이지·블록 편집 | 빈 토글 안내, 메모 계층, 페이지, 삽입 메뉴, 블록·줄 끌기, 그림·전체 화면을 반영 | 단계별 Qt 테스트와 실제 렌더 확인 |
| 2026-09-07 | 메모·일정·단축키 사용성 | 오늘 요약, D-Day 칩, 설정·단축키 화면, 목록 접기와 본문 기능을 개선 | 독립 테스트 묶음과 실제 렌더 확인 |
| 2026-09-08 | 메모 서식·일정·D-Day | 제목1~4·기능 설정과 일정 포스트잇, 일정/할 일 분리, D-Day·캘린더 연계를 반영 | 전체 673개 자동 검사 통과 및 Windows 배율 렌더 확인 |
| 2026-09-09 | 기존 DB 보존 폴더 변경·단일 EXE | 기존 DB를 다음 번호 파일로 보존한 뒤 현재 DB를 복사하도록 변경하고 EXE를 생성 | 집중 62개·전체 676개 통과, `TomaDesk.exe` 빌드·실행 확인 |
| 2026-09-09 | 빈 토글 붙여넣기·삭제 | 첫 빈 줄 제거, 토글 내부 유지, Ctrl+Z 묶기와 토글 안팎 빈 줄 삭제를 보완 | 토글 42개·인접 메모 162개 통과, Windows Qt 42개 통과 |
| 2026-09-09 | 구조 가져오기·전체 메모 백업복원 | GPT·Claude/MD·HTML·TXT 구조 가져오기와 `.tomamemo` 병합·교체 복원을 추가 | 집중 12개·전체 696개·Windows Qt 63개 통과, 실제 렌더 확인 |
| 2026-09-11 | 프로그램 내 사용 설명서 | 화면 캡처에 번호 마커를 얹고 섹션·좌우 넘기기로 읽는 `설명서` 창을 추가 | 설명서 9개·전체 705개 통과, 11장 실제 렌더 확인 |
| 2026-09-11 | 메모 커서·스크롤 구조 경계 안정화 | 토글 밖 빈 줄 삭제의 보이는 커서·스크롤 복원과 HTML 표 뒤 일반 입력 문단을 추가 | 집중 24개·인접 222개·전체 713개·Windows Qt 9개 통과 |
| 2026-09-11 | 메모 편집 Phase 1 클립보드·독립 복제 | 잔류 내부 링크 제거, 전용 MIME, 페이지 하위 트리·첨부 새 ID 복제, 목록 다중 복제, 복합 Undo/Redo, Alt+C 문자 서식 복사를 추가 | 최종 집중·인접 104개·전체 728개·Windows Qt 6개 통과, EXE 미생성 |
| 2026-09-12 | 메모 편집 Phase 2 다중 블록 명령 | 비연속 선택, 공통 명령·부동 막대·우클릭, 이동·변환·복제·삭제와 데이터 보존형 연동 해제를 추가 | Phase 2 15개·전체 743개·Windows Qt 31개 통과, 4종 실제 렌더 확인, EXE 미생성 |
| 2026-09-14 | 메모 편집 Phase 3 접기·서식·키보드 | 제목 범위 접기, 모두 접기/펴기, 줄 간격, 세 붙여넣기 방식과 링크·접기 키보드 경로를 추가 | Phase 3 10개·전체 753개·Windows Qt 집중 97개 통과, 넓은/좁은 창 렌더 확인, EXE 미생성 |
| 2026-09-15 | 메모 편집·모바일 연동 대비 Phase 4~7 | Phase 4 마감, MD·UI 보완, UUID 기반 데이터·카테고리, 백링크·주석, 템플릿, 버전 기록을 구현 | 전체 815개·Windows Qt 배율별 집중 24개 통과, 실제 포인터는 TODO, EXE 미생성 |

## 요약 정리

완료 작업은 이 파일에 요약 내역만 유지하고 상세 근거는 `2._WORK_LOG.md`에서 관리한다.
Phase 4 마감부터 MD·UI 보완, 모바일 연동용 UUID·revision·UTC·tombstone 기반, 카테고리, 백링크·주석, 템플릿, 버전 기록까지 구현했다. 기존 정수 ID·v1 링크·사용자 데이터 계약을 유지하며 전체 회귀 815개를 통과했다.
승인된 메모 편집 UI 개편은 기본 분할, 빈 오늘 요약 접기, 28px 속성·서식·저장·삭제 버튼, 좁은 창 배치와 공용 저장 상태까지 반영했다. Windows Qt 배율별 자동 검사는 통과했으며 실제 포인터 확인은 남아 있다. `classic` 배치는 2026-10-14까지 유지하고 별도 검토한다.
Phase 4~7 결과물 보완은 Q1·Q2 A안과 단계별 승인 방식으로 진행한다. 단계 0은 전체 815개, 단계 A는 전체 820개를 통과했다. 승인된 단계 B는 오늘 요약 수명주기, 편집 전/후 버전, 주석 위치 복구, v1/v2 링크 재매핑과 템플릿 구조 검사를 구현해 집중 96개·전체 830개를 통과했다. 임시 DB 실제 창에서 메모 3회 전환 후 D-Day가 오늘 요약에 나타나는 것도 확인했으며, 나머지 실제 포인터 항목은 TODO에 남기고 단계 C 승인 전에는 착수하지 않는다.
단계 C에는 `보완계획/UI보완계획`의 구현 문서와 PNG 5개를 반영했다. UI-C1에서 서식 토글을 칩 줄로 통합하고, UI-C2에서 `가` 샘플과 `⚙`을 추가했으며, UI-C3에서 라벨·입력 폭·아이콘·간격을 정리했다. UI-C4에서는 선택 글자 서식 창에 24px 프리셋 3개와 메인 툴바 양방향 동기화를 추가하고 공간 부족 시 하단 예약 줄을 사용하게 했다. UI-C5에서는 프로그램 내·Markdown 설명과 실제 메모 편집 캡처를 현 UI에 맞췄다. 최종 집중 22개·전체 852개가 통과했고 실제 포인터는 도구 미노출로 TODO에 남겨 단계 C를 조건부 통과로 판정했다. 단계 D는 다음 승인 전까지 착수하지 않는다.
단계 D는 목록의 색 점·미지정 필터·480px 네 열, 선택 작업줄, 상태 말줄임, 충돌 사본 표시와 제목 첫 기호 동적 필터를 구현했다. D 집중·인접 176개와 C 제외 전체 860개가 통과했으며 C 영역 기존 실패 2건과 실제 포인터 미확인으로 조건부 통과다. 단계 E는 승인 대기다.
단계 E는 편집·목록 상태를 분리하고, 버전 기록을 로컬 시각·한글 종류·평문 색상 차이로 표시하며, 백링크 수·본문 주석 표시·템플릿 진입·본문 Undo/Redo를 연결했다. E 집중 7개는 배율별 통과했고 C 파일과 범위 밖 카테고리 칩 실패 1건을 제외한 전체 866개가 분할 통과했다. 실제 포인터는 미검증으로 조건부 통과이며 단계 F는 승인 대기다.
단계 F는 속성·줄 간격·카테고리 칩의 기존 자동 실패 3건을 수정하고 배치 계약 12개, 사용성 시나리오 71개와 전체 902개를 통과했다. Windows Qt 기준 이미지 12장을 남겼으나 실제 포인터 체크리스트는 창 탐지 제한으로 미검증이다. 조건부 통과로 멈추며 G-A는 승인 대기다.
`11._제목분류필터_계획`의 Markdown·HTML·PNG 10개를 확인해 후속 G-A~G-G를 추가했다. F 마감 뒤 기호 규칙·대괄호 머리말·버튼/도움말·상시 더보기·고정 칩·제목 자동완성·설명서/실창 검증을 단계별 승인으로 진행한다. 카테고리 순서 버튼 제거와 최근 메모 UI의 자료 간 차이는 각 해당 단계 착수 전 결정 항목으로 남긴다.
G-A는 So 기호 판정·표시와 비교 키 분리, `[머리말]` 추출·정규화·집계를 구현했다. 고정 제목 20개를 포함한 집중 50개와 전체 944개를 통과했으며 G-B는 승인 대기다. 기존 계획의 카테고리 순서 버튼·최근 메모 배치 결정은 각 해당 단계 전까지 보류한다.
G-B는 제목 필터의 교차 집계·AND 결과·평평한 목록·결과 없음 안내와 제목 필터만 해제하는 동작을 연결했다. 집중 75개와 전체 969개가 통과했으며 G-C 버튼·메뉴 구현은 승인 대기다.
미완료·우선순위 결정 항목은 `3._TODO.md`에 유지한다.

## 추천 모델

- `gpt-6-astra + high`: 후속 모바일 동기화·충돌 비교 화면과 UUID 관계 확장에 적합

## 추천 SKILL

- `ui-compact-tool-area` — 적용: 프리셋 샘플·관리 메뉴·고정 크기와 좁은 도구 영역 검토
- `ui-ux-pro-max` — 적용: 속성 칩·인라인 패널·좁은 창과 키보드 접근성 검토
- `computer-use:computer-use` — 실제 Windows 메모창의 포인터 사용감 확인에 적합
