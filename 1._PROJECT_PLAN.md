# 메모 편집·독립창·목록 관리 개선 계획

## 1. 사용자 요청과 해결할 문제

- 메모를 선택하거나 `+ 새 메모`를 누르지 않아도 편집 구역에서 바로 입력하고 자동 저장할 수 있게 한다.
- 자동 저장을 기본으로 유지하고 수동 저장 버튼은 즉시 저장·실패 재시도용으로 보존한다.
- 메모별 전역 단축키의 `메모 열기` 동작은 메인창 대신 구분선 오른쪽 편집 영역만 담은 독립창을 연다.
- 메모 목록과 알림내역에 선택 관리, 수정 시간 정렬, 검색·스크롤, 선택 삭제와 Excel 내보내기를 적용한다.
- 설정 저장 성공 후 사용자에게 완료 팝업을 표시한다.

## 2. 작업 목표와 완료 기준

- 제목 또는 본문 입력 시 400ms 자동저장으로 메모가 정확히 한 건 생성되고 빈 입력·옵션 변경만으로는 레코드가 생기지 않는다.
- 독립 편집창은 최소화, 최대화·복구, 닫기와 모든 가장자리·모서리 크기 조절을 지원하며 위치·크기를 복원한다.
- `메모 열기` 단축키는 메인창을 복구하지 않고 독립창만 활성화하며 `포스트잇 표시·숨김` 동작은 유지한다.
- 메모 목록은 `선택, 번호, 표시, 제목, 수정 시간, 내용 미리보기`를 표시하고 수정 시간 내림차순을 기본으로 한다.
- 메모·예정 알림·처리 내역 Excel은 체크 항목 우선, 체크가 없으면 현재 검색 결과 전체를 내보낸다.
- 기존 SQLite 구조, 메모·알림·반복·포스트잇·단축키 데이터와 Qt 종료 보호를 유지한다.

## 3. 주요 구현 작업

### 바로 입력과 저장

- 메모 미선택 상태의 편집기를 활성화하고 제목·본문 중 하나에 유효한 내용이 있을 때만 새 메모를 만든다.
- 제목이 비어 있으면 `새 메모`를 기본 제목으로 사용하고 생성된 ID를 현재 편집기에 연결한다.
- 첫 저장 시 검색어를 해제하고 생성 메모를 목록에서 선택하되 현재 입력과 커서 상태를 불필요하게 다시 로드하지 않는다.
- 자동 저장과 수동 저장은 동일한 저장 경로를 사용하고 실패 시 입력을 보존하며 `저장 실패` 상태를 표시한다.

### 전역 단축키용 독립 편집창

- Windows 기본 창 테두리를 사용하는 재사용 가능한 독립 편집창에 현재 `MemoEditor` 전체 기능을 배치한다.
- 기본 크기 약 680×880px, 최소 560×640px로 설정하고 마지막 위치·크기·최대화 상태를 저장·복원한다.
- 독립창 열기 전 메인 편집기의 보류 저장을 확정하고, 독립창 저장 후 목록·알림·캘린더와 단축키 등록을 갱신한다.
- 닫기 시 보류 저장을 확정한 뒤 창만 숨기고 프로그램과 알림 서비스는 계속 실행한다.
- 독립창에도 값 선택 UI 휠 차단과 서식·자동저장 타이머 종료 보호를 적용한다.

### 설정 저장 안내

- 메인 프로그램 설정과 `노트 단축키 설정`이 실제 반영된 뒤 `설정이 저장되었습니다.` 팝업을 한 번 표시한다.
- 검증·등록 실패와 취소 때는 성공 팝업을 표시하지 않고 기존 경고를 유지한다.
- 데이터 폴더 변경은 기존 재시작 안내만 표시해 중복 팝업을 피한다.

### 메모·알림 목록과 Excel

- 왼쪽 메모 목록을 스크롤 가능한 표로 바꾸고 `일반/포스트잇` 표시, 일반 텍스트 80자 미리보기와 수정 시간 정렬을 제공한다.
- 메모 목록 하단에 `전체 선택/해제`, `Excel 내보내기`, `선택 삭제`를 추가한다.
- 예정 알림과 처리 내역에도 전체 선택/해제, Excel 내보내기, 선택 삭제를 제공하고 기존 처리 기능은 유지한다.
- 메모·예정 알림·처리 내역은 각각 `메모목록`, `예정알림`, `처리내역` 시트의 별도 xlsx 파일로 내보낸다.
- 날짜는 `YYYY-MM-DD HH:mm`, 메모 본문은 전체 일반 텍스트로 기록하고 헤더 고정·필터·자동 열 너비를 적용한다.

## 4. 변경 대상과 보존 범위

- 주요 변경: 메모 편집기·패널·목록, 메인창 단축키 진입점, 알림내역 패널과 관련 테스트.
- 새 모듈: 독립 메모 편집창, 알림노트 전용 Excel 내보내기.
- 보존: SQLite 스키마와 사용자 데이터, 기존 `+ 새 메모`, 리치 텍스트·알림·반복·포스트잇·단축키, 기존 EXE와 배포물.
- 기존 토마펫 알림 연계의 말풍선·큐·처리 동작과 자산은 변경하지 않는다.

## 5. 테스트 및 실제 동작 검증

- 제목만·본문만·동시 입력, 빈 입력, 옵션만 변경, 자동저장·수동 저장·실패 재시도를 임시 DB에서 검증한다.
- 실제 전역 단축키로 독립창만 열리는지, 포스트잇 동작이 유지되는지 확인한다.
- 최소화·복구·닫기, 가장자리·모서리 드래그, 위치·크기 복원을 실제 Windows 창에서 확인한다.
- 설정 성공 팝업은 한 번, 실패·취소 때는 0번 표시되는지 검사한다.
- 메모 목록 필드·정렬·검색·체크·삭제와 연결 알림·포스트잇 정리를 검증한다.
- 세 Excel 유형의 체크 우선·검색 결과 대체, 시트·헤더·날짜·본문·파일 오류를 임시 파일로 검증한다.
- Qt 테스트는 관련 묶음 또는 독립 프로세스로 실행하고 실제 소스 프로그램의 연속 입력·자동저장·창 재호출·프로세스 유지를 확인한다.

## 6. 제외 범위

- SQLite 마이그레이션과 기존 데이터 값 변경
- `포스트잇 표시·숨김` 전역 단축키 동작 변경
- 새 상위 메모 목록 탭 추가와 요청하지 않은 UI 재설계
- 공통 라이브러리 조회·복사·변경
- EXE 생성·배포와 별도 가상환경 생성

## 7. 기존 토마펫 알림 연계 보존

- 예약·일정 알림은 기존 큰 팝업 대신 토마펫 말풍선으로 표시되는 현재 구현을 유지한다.
- 마우스 근접 방향 반응, 우클릭 저장 액션, 드래그 이동, 알림 큐와 완료·미루기·건너뛰기·열기 계약을 보존한다.
- 자산 로드나 표시 실패 시 기존 알림 팝업으로 대체되는 경로를 유지한다.
- 이번 작업에서는 토마펫 모듈과 자산을 수정하지 않는다.

## 8. 현재 진행 상태

- 현재 구현·원본 알림노트 목록·붙임 화면 확인: **완료**
- 사용자 선택 확정과 계획 승인: **완료**
- 계획 문서 갱신: **완료**
- 소스 구현: **완료**
- 자동 테스트와 실제 Windows 검증: **완료**
- 작업 기록과 TODO 정리: **완료**
- EXE 단일 파일 재빌드·실행 검증: **완료 (2026-08-11)**

## 9. EXE 단일 파일 재빌드 요청

### 요청과 목표

- 현재 소스를 Windows용 단일 실행 파일 `release/toma_shortcut_program.exe`로 다시 만든다.
- 콘솔 창 없이 실행되고, 실제 프로세스가 유지되는지 확인한다.

### 변경 대상과 보존 범위

- 실행: `build_release.ps1 -Clean`, 기존 PyInstaller 설정과 `shortcut_launcher.spec`를 사용한다.
- 교체: 이전 `release/toma_shortcut_program.exe`만 새 빌드 결과로 교체한다.
- 보존: `release/data`, 프로젝트 원본 소스, SQLite 사용자 데이터와 기존 기능은 수정하거나 삭제하지 않는다.

### 검증과 제외 범위

- 빌드 결과 EXE의 존재·크기를 확인하고, 기존 실행 인스턴스가 없음을 확인한 뒤 새 EXE를 약 5초간 실행해 프로세스 유지 여부를 검사한다.
- 이번 요청은 소스 기능 변경, UI 변경, 데이터 구조 변경, 공통 라이브러리 변경을 포함하지 않는다.

## 10. 2026-08-25 메모 화면 비례 크기 조절과 알림 버튼 축소

### 사용자 요청과 해결할 문제

- A 경계는 메모 목록과 메모 입력 영역 사이의 세로 분할선으로 유지하고, 마우스 드래그로 두 영역의 폭을 조절할 수 있게 한다.
- A를 드래그할 때 메모 목록 표의 `선택, 번호, 표시, 제목, 수정 시간, 내용 미리보기` 열이 설정된 비율을 유지하며 자동으로 폭을 조절하게 한다.
- B 경계는 메모 입력 영역과 오른쪽 여유 영역 사이의 세로 분할선으로 유지하고, 마우스 드래그로 메모 입력 영역 폭을 조절할 수 있게 한다.
- B를 드래그할 때 제목·서식·본문·알림·단축키·포스트잇 옵션의 기존 구성 비율이 유지되고 오른쪽 조작부가 잘리지 않게 한다.
- C의 빠른 알림 버튼은 두 줄 텍스트가 잘리지 않는 최소 내용 크기로 줄이고 불필요한 내부 여백과 가로 확장을 제거한다.

### 작업 목표와 완료 기준

- A와 B 분할선 모두 7px 조작 폭, 좌우 크기 조절 커서와 hover/pressed 표시를 제공하며 실제 마우스 드래그로 폭이 변한다.
- A 드래그 전후 목록 표의 각 열 비율 오차가 육안으로 느껴지지 않는 범위에서 유지되고 헤더·셀 텍스트가 겹치지 않는다.
- B 드래그 전후 편집기 내부 행과 입력 영역은 레이아웃 비율에 따라 재배치되며 가로 스크롤이나 오른쪽 버튼 잘림이 생기지 않는다.
- 빠른 알림 버튼은 `라벨 + 단축키`에 맞는 폭과 높이를 사용하고, 동일 행의 버튼 간 간격만 유지한다.
- 기존 메모 자동저장, 알림 저장·해제·초기화, 반복, 포스트잇, 색상, 단축키와 SQLite 데이터 계약은 그대로 유지한다.

### 변경 대상과 보존 범위

- 주요 변경 후보: `alert_notes/panel.py`, `alert_notes/memo_list.py`, `alert_notes/editor.py`, `ui_theme.py`와 관련 UI 테스트.
- 보존: 메모·알림·반복·포스트잇·단축키 동작, 데이터베이스 구조와 사용자 데이터, 현재 색상·폰트·탭 구성, 기존 EXE와 배포 파일.
- 공통 라이브러리와 토마펫 모듈은 조회·복사·수정하지 않는다.

### 주요 구현 작업

- A와 B 기존 `QSplitter` 핸들에 각각 접근성 이름을 부여하고, 두 핸들이 최소 폭을 침범하지 않으면서 독립적으로 드래그되는지 보강한다.
- 목록 표는 현재 열 너비를 기준으로 열별 비율을 정의하고, 목록 viewport 폭이 바뀔 때 사용 가능 폭에 맞춰 비례 재계산한다.
- 편집기는 강제 고정 폭 대신 현재 Qt 레이아웃의 stretch와 size policy를 사용하고, 좁은 폭에서는 행 단위 재배치로 모든 조작부를 보존한다.
- 빠른 알림 버튼은 grid의 균등 가로 확장을 제거하고 `sizeHint()` 기반 내용 맞춤 크기로 정렬한다.
- 값 선택 UI의 휠 변경 차단은 기존대로 유지한다.

### 테스트 및 실제 동작 검증

- A 드래그 전후 splitter 폭과 여섯 목록 열의 비율을 Qt 테스트로 확인한다.
- B 드래그 전후 제목·본문·알림·하단 옵션이 보이고 가로 스크롤 최대값이 0인지 확인한다.
- 빠른 알림 버튼의 텍스트, `sizeHint()`, 최소·최대 크기와 행 정렬을 확인한다.
- 1920×1080과 첨부 이미지 크기인 약 1478×907에서 실제 창을 실행해 A·B 드래그, 헤더 비율, 버튼 잘림을 화면으로 확인한다.
- 관련 메모·알림 집중 테스트와 소스 프로그램 실행 유지 여부를 확인한다.

### 제외 범위

- 알림 영역 접기, 버튼 종류 삭제, 기능 추가, 색상·폰트 전면 변경
- SQLite 스키마·사용자 데이터 변경
- EXE 생성·배포
- 포스트잇 독립창의 기능·디자인 변경

### 현재 진행 상태

- 첨부 이미지와 A·B·A-1·B-1·C 위치 해석: **완료**
- 기존 splitter·목록 헤더·편집기·빠른 알림 구현 확인: **완료**
- 계획 문서 갱신: **완료**
- 사용자 계획 승인: **완료**
- 소스 구현과 집중 테스트: **완료**
- 실제 Windows 창 화면·축소 재배치 확인: **완료**
- A·B 마우스 드래그와 비율 저장 자동 검증: **완료**
- 작업 기록과 TODO 정리: **완료**

## 11. 2026-08-25 메모 서식·설정 공간·탭 정렬·빠른 알림 배치 개선

### 사용자 요청과 해결할 문제

- 굵게·기울임·밑줄·취소선처럼 선택 상태가 있는 서식 버튼에 체크 표시를 추가해 현재 적용 상태를 문자와 색상 외에도 식별할 수 있게 한다.
- 현재 `▧` 문자로 표시되는 이미지 삽입 버튼을 사진 기능임을 바로 알 수 있는 직관적인 아이콘으로 바꾼다.
- 프로그램 설정 창의 넓고 긴 단축키 행과 하단 빈 공간을 줄이고, 사용 가능한 가로 공간을 실제 설정 항목 배치에 활용한다.
- `메모 편집·캘린더·알림내역` 탭 묶음의 시작점과 끝점을 위쪽 작업공간 버튼 묶음에 맞춰 열을 정렬한다.
- 5분부터 1일까지의 빠른 알림시간 버튼 9개를 한 줄에 배치한다.

### 작업 목표와 완료 기준

- B/I/U/S 서식 버튼은 선택 시 작은 `✓`가 함께 보이고 해제 시 사라지며, 기존 단축키와 실제 서식 적용은 그대로 동작한다.
- 이미지 삽입 버튼은 이모지나 모호한 문자 대신 사진 테두리·산·태양 형태의 선명한 벡터형 아이콘과 기존 툴팁·접근성 이름을 사용한다.
- 설정 창은 단축키 설정을 넓은 화면에서 2열, 공간이 부족하면 1열로 재배치하고 불필요한 하단 stretch를 제거하여 현재 기능을 더 작은 높이에서 모두 표시한다.
- 메모 하위 탭은 상단 작업공간 버튼의 왼쪽 시작선과 오른쪽 끝선에 맞고, DPI 배율 변경 시에도 눈에 띄는 어긋남이 생기지 않는다.
- 빠른 알림 버튼 9개는 모두 같은 y좌표의 한 행에 놓이고 텍스트·단축키가 잘리지 않으며 알림 시간 동작은 유지된다.

### 변경 대상과 보존 범위

- 주요 변경 후보: `alert_notes/text_format_toolbar.py`, `alert_notes/editor.py`, `alert_notes/panel.py`, `settings_dialog.py`, `ui_theme.py`와 관련 UI 테스트.
- 보존: 메모 서식 적용·이미지 삽입·자동저장·알림 시간·반복·단축키, 설정 저장·검증·백업·복원·폴더 선택, SQLite 데이터와 기존 사용자 설정.
- 현재 A·B 스플리터, 목록 비례 열, 휠 변경 차단, 포스트잇과 토마펫 기능은 변경하지 않는다.

### 주요 구현 작업

- checkable 서식 버튼의 상태 동기화 경로에서 선택 시 `✓`가 병기되도록 하고 버튼 폭과 접근성 이름은 안정적으로 유지한다.
- 서식 도구 내부에 작은 사진 윤곽 벡터 아이콘을 생성해 이미지 버튼에 적용한다.
- 설정 단축키 영역을 반응형 2열 그리드로 바꾸고 데이터·안전 카드와 저장 버튼 사이의 불필요한 확장 여백을 제거한다.
- 메모 전용 탭에 별도 object name과 정렬 규칙을 적용해 전역 탭 스타일에는 영향을 주지 않는다.
- 빠른 알림 grid의 모든 버튼을 한 행에 순서대로 배치하고 내용 맞춤 폭·간격을 유지한다.

### 테스트 및 실제 동작 검증

- B/I/U/S 클릭과 커서 서식 동기화 시 체크 표시·checked 상태·서식 적용이 일치하는지 검사한다.
- 이미지 삽입 버튼의 아이콘이 비어 있지 않고 기존 클릭 연결·툴팁·접근성 이름이 유지되는지 확인한다.
- 설정 창에서 9개 단축키 항목과 시작 모드·폴더·백업·복원·저장 기능이 모두 유지되고 2열/1열 재배치와 전체 높이 축소를 검사한다.
- 실제 메인창 좌표에서 메모 하위 탭과 위쪽 작업공간 버튼 묶음의 좌우 경계를 비교한다.
- 빠른 알림 9개 버튼이 한 행인지, 모든 텍스트와 단축키가 보이는지, 클릭 시 정확한 시간이 설정되는지 확인한다.
- 첨부 메인창 크기 약 1177×887과 설정 창에서 실제 Windows 화면을 확인하고 소스 프로그램 실행 유지 여부를 검사한다.

### 제외 범위

- 메모·알림·설정 기능 및 데이터 구조 변경
- 서식 도구 전체 재설계와 요청하지 않은 색상·폰트 변경
- 포스트잇 독립창 디자인 변경
- 공통 라이브러리 조회·복사·변경

### EXE 생성과 보존 범위

- 사용자 추가 요청에 따라 승인된 UI 변경과 검증이 끝난 소스를 단일 파일 `release/toma_shortcut_program.exe`로 다시 빌드한다.
- 기존 실행 프로세스를 확인하고 `build_release.ps1 -Clean`과 현재 PyInstaller 설정을 사용한다.
- `release/data`, 프로젝트 원본 데이터, 사용자 SQLite DB와 기존 문서·백업은 유지하고 이전 EXE만 새 결과물로 교체한다.
- 콘솔 창 없이 실행되는지와 빌드된 EXE 프로세스가 약 5초 이상 유지되는지 확인한다.

### 현재 진행 상태

- 첨부 메인창·설정 창과 관련 코드 확인: **완료**
- 계획 문서 갱신: **완료**
- 사용자 계획 승인: **완료 (EXE 생성 포함)**
- 소스 구현: **완료**
- 집중 테스트와 실제 Windows 화면 검증: **완료**
- 단일 EXE 빌드와 실행 검증: **완료**
- 작업 기록과 TODO 정리: **완료**

## 12. 2026-08-25 GPT펫 원본 기반 토마펫 알림·상시 표시 이식

### 사용자 요청과 해결할 문제

- 현재 Codex GPT펫 Toma의 실제 사용자 자산(`C:\Users\user\.codex\pets\toma`)과 런타임 프레임·시간·마우스 반응 계약을 프로젝트로 가져와 토마단축키런처에 적용한다.
- 프로젝트의 기존 토마펫 구현을 동작 기준으로 재사용하지 않고, GPT펫 원본 스프라이트와 상태별 재생 계약을 프로젝트 소유 복사본과 독립 상태 머신으로 이식한다.
- 알림용 토마펫과 상시 표시 토마펫을 독립 설정으로 제공하고, 빠른 재알림 9개·메모/일정 편집·완료 반응을 새 말풍선에서 제공한다.
- 설정창의 값 선택 UI는 포커스 상태의 휠과 누른 채 이동하는 드래그로 값이 바뀌지 않도록 보호한다.

### 작업 목표와 완료 기준

- `토마펫 알림 사용`은 기본 켬, `토마펫 상시 표시`는 기본 끔으로 독립 저장하고 네 가지 조합을 모두 지원한다.
- 상시 표시를 켜면 하나의 토마펫 창이 유지되고 마지막 화면 위치를 복원하며, 알림이 오면 별도 펫을 만들지 않고 같은 창에 말풍선을 결합한다.
- GPT펫 원본 `pet.json`과 `spritesheet.webp`를 프로젝트 자산으로 복사하고 SHA-256 일치 여부를 검증한다.
- GPT펫과 동일한 상태별 프레임 수와 프레임 시간을 적용한다.
- idle은 6프레임과 전용 neutral 프레임을 구분하고, 일반 반응은 3회 재생 후 idle을 6배 느린 속도로 반복한다.
- 펫 위에 마우스를 올리면 `jumping`, 주변에 커서가 있으면 22.5도 간격의 16방향 보기 프레임, 드래그 중 가로 이동이 4px 이상이면 `running-left/right`를 표시한다.
- 알림은 `review`를 3회 재생한 뒤 느린 idle로 기다리고, 확인·빠른 재알림·메모/일정 편집 버튼을 누르면 닫힌다.
- 우클릭 메뉴는 일시정지, 행동 재생, 캘린더, 빠른 메모, 위치 초기화와 숨기기를 제공하며 일시정지 중에도 알림은 우선 표시한다.
- 기존 알림 큐와 기존 팝업 fallback을 보존한다.

### 변경 대상과 보존 범위

- 주요 변경 후보: `settings_dialog.py`, `alert_notes/toma_pet_assets.py`, `alert_notes/toma_pet_alert.py`, `alert_notes/service.py`, `main_window.py`와 관련 테스트.
- 새 모듈 후보: GPT펫 재생 계약과 상시 펫 창을 관리하는 `alert_notes/toma_pet_motion.py`, `alert_notes/toma_pet_controller.py`.
- 보존: SQLite 스키마와 사용자 데이터, 단축키·매크로·알림 큐·포스트잇·트레이 동작, 기존 EXE와 `release/data`.
- 공통 라이브러리는 조회·복사·변경하지 않는다.

### 주요 구현 작업

- GPT펫 원본 자산을 사용자 펫 폴더에서 프로젝트 자산 폴더로 복사하고 해시·v2 크기·메타데이터를 검증한다.
- GPT펫의 실제 런타임 계약을 상수와 상태 머신으로 분리해 상태별 프레임 시간, 3회 반응, 느린 idle 전환과 neutral 프레임을 구현한다.
- 상시 토마펫 창은 투명·항상 위·테두리 없는 단일 인스턴스로 관리하고, 종료 시 타이머와 이벤트 연결을 안전하게 정리한다.
- 커서 위치를 펫 중심 기준으로 계산해 마우스 오버 점프와 주변 16방향 보기 반응을 우선순위에 따라 적용한다.
- 드래그 시작·이동·종료를 상태 머신과 연결하고 4px 임계값을 넘는 수평 방향에 따라 좌우 이동 프레임을 선택하며 위치를 저장한다.
- 설정창에 알림·상시 표시 체크박스와 위치 초기화 버튼을 추가하고 `toma_pet_alert_enabled`, `toma_pet_persistent_enabled`, `toma_pet_position`을 기존 settings 테이블에 저장한다.
- 알림 말풍선은 아이보리 표면, 짙은 남색 텍스트/테두리, 토마 방향 꼬리와 접근성 이름을 사용하고 빠른 재알림을 5개+4개로 배치한다.
- 설정창의 콤보박스·숫자·날짜·시간·슬라이더에서 휠과 값 변경 드래그를 차단하되 단일 클릭·목록 선택·키보드 입력은 유지한다.

### 테스트 및 실제 동작 검증

- 상태별 프레임 수·시간, 3회 반응 후 느린 idle, neutral 분리와 16방향 인덱스를 단위 테스트한다.
- 마우스 진입·이탈, 주변 방향 16개, 4px 드래그 임계값과 좌우 이동, 위치 저장·복원을 Qt 테스트로 검증한다.
- 알림/상시 표시 네 조합, 설정 재실행 복원, 위치 화면 보정, 일시정지 중 알림 우선 표시를 검증한다.
- 일반·반복·일정·D-Day 알림에서 9개 재알림 값, 확인, 메모/일정 편집, 다음 큐와 fallback을 검증한다.
- Qt 렌더 캡처로 말풍선 줄바꿈·두 줄 버튼·접근성 이름·화면 가장자리 배치를 확인하고 소스 프로그램을 일정 시간 실행해 타이머·종료 오류가 없는지 확인한다.
- 사용자 요청에 따라 Computer Use는 사용하지 않는다.

### 제외 범위

- GPT펫 원본 이미지의 재생성·보정·셀 편집
- SQLite 구조나 기존 사용자 데이터 변경
- 매크로 입력 순서·속도·반복 규칙 변경
- Codex 앱 내부 코드 수정이나 Codex 앱 자동 조작
- 이번 승인 범위에서의 EXE 생성·배포 및 Computer Use 검증

### 현재 진행 상태

- 현재 GPT펫 자산·프레임·방향 동작 검증: **완료**
- Codex 로컬 런타임의 상태·속도·hover·drag 계약 확인: **완료**
- 런처의 기존 토마 알림·작업 실행·배포 자산 경로 확인: **완료**
- 계획 문서 갱신: **완료**
- 사용자 계획 승인: **완료 (GPT펫 원본 사용·Computer Use 제외로 변경 승인)**
- GPT펫 원본 자산 해시 일치·프레임 계약 이식: **완료**
- 토마펫 알림·상시 표시·우클릭 메뉴·위치 복원 구현: **완료**
- 설정창 독립 옵션·값 선택 UI 휠/드래그 보호: **완료**
- 자동 테스트·렌더 캡처·독립 시각 검토·소스 실행 검사: **완료**
- EXE 생성: **제외 범위에 따라 미생성**

## 13. 2026-08-25 프로그램 설정창 무스크롤 2열 재배치

### 사용자 요청과 해결할 문제

- 프로그램 설정창의 모든 항목을 내부 스크롤 없이 한 화면에서 확인하고 조작할 수 있게 한다.
- 사용자에게 먼저 제시해 승인받은 시안처럼 단축키는 왼쪽 2열, 창·트레이와 토마펫은 오른쪽, 데이터·안전은 하단 가로형 카드로 배치한다.
- 불필요한 여백을 줄이되 글자·콤보박스·버튼의 가독성과 실제 클릭 영역은 유지한다.

### 작업 목표와 완료 기준

- 기본 창은 약 1050×720px의 가로형 레이아웃을 사용하며 내부 세로·가로 스크롤바가 나타나지 않는다.
- 9개 단축키가 왼쪽 카드의 2열에 모두 표시되고 각 라벨과 키 입력 컨트롤이 잘리거나 겹치지 않는다.
- 오른쪽에는 `창 및 트레이`, `토마펫` 카드가 위아래로 정렬되고 위치 초기화 버튼은 내용 맞춤 폭을 사용한다.
- `데이터 및 안전`은 전체 폭 하단 카드에서 경로·폴더 버튼과 백업·복원·안내를 두 개의 가로 행으로 표시한다.
- 저장·취소 버튼은 고정 하단에서 최소 36px 클릭 높이를 유지하고 모든 설정 저장·검증 동작은 기존과 동일하다.

### 변경 대상과 보존 범위

- 주요 변경: `settings_dialog.py`와 설정창 레이아웃 집중 테스트.
- 보존: 단축키 값·검증, 시작 모드, 토마펫 독립 설정과 위치 초기화, 폴더 선택·열기, 전체 백업·복원, 값 선택 UI의 휠·드래그 보호.
- SQLite 구조·사용자 데이터·메모·포스트잇·알림·토마펫 동작·기존 EXE는 변경하지 않는다.

### 주요 구현 작업

- 기존 내부 `QScrollArea`를 제거하고 고정 헤더·본문 2열 grid·하단 데이터 카드·고정 버튼 영역으로 구성한다.
- 단축키 필드는 승인 시안의 순서를 유지하면서 2열로 고정하고 카드·필드 간격과 높이를 4/8px 계열로 압축한다.
- 데이터 경로 표시를 한 줄 생략형으로 바꾸고 전체 경로는 선택과 툴팁으로 확인할 수 있게 한다.
- 창 높이가 부족해져도 요소를 축소하거나 숨기지 않고 최소 크기를 지켜 창 자체의 최소 크기로 보호한다.

### 테스트 및 실제 동작 검증

- 기본 크기에서 스크롤 영역이 없고 모든 카드·9개 단축키·버튼이 창 내부에 포함되는지 Qt 좌표로 검사한다.
- 단축키 값 수집, 중복 검증, 시작 모드, 토마펫 설정, 위치 초기화, 폴더 선택·백업·복원 연결과 입력 보호 회귀 테스트를 실행한다.
- 100%·125%·150% 배율 조건에서 설정창을 렌더링해 텍스트·버튼·카드의 겹침과 잘림을 확인한다.
- 실제 소스 프로그램을 일정 시간 실행해 설정창 생성과 프로세스 유지 여부를 확인한다.

### 제외 범위

- 설정 항목 추가·삭제와 저장 방식 변경
- 색상·폰트·컨트롤 스타일 전면 변경
- EXE 생성·배포와 Computer Use 검증

### 현재 진행 상태

- 기존 설정창·승인 시안 대조: **완료**
- 계획 문서 갱신과 사용자 시안 승인: **완료**
- 소스 구현: **완료**
- 자동 테스트·배율 렌더·실행 검증: **완료**
- 작업 기록과 TODO 정리: **완료**
- EXE 생성: **요청 범위가 아니므로 미생성**

## 14. 2026-08-25 메모 편집 화면 시안 구현

### 사용자 요청과 목표

- 승인된 시안처럼 메모 본문을 편집 화면의 중심으로 두고, 서식·상태·알림 조작을 본문 주변에 압축 배치한다.
- 기존 메모 저장, 서식, 알림, 반복, 포스트잇, 전역 단축키, 토마펫과 SQLite 사용자 데이터를 변경하지 않는다.

### 구현과 검증 계획

- 제목 다음에 핵심 서식 도구를 배치하고, `단축키 설정`은 글자 색상 조작부 바로 아래로 옮긴다.
- `서식 1~3`만 본문 아래 왼쪽에 노출하고, 슬롯 4의 저장 데이터는 삭제하지 않는다.
- 항상 위·포스트잇·저장 상태는 본문 아래 오른쪽에 정렬한다.
- 알림 예약의 기존 입력·반복·빠른 시간·저장/해제 조작을 접이식 상세 영역으로 감싸고, 접힘 상태는 기존 settings 테이블에 저장한다.
- 화면 빠른 알림은 첫 클릭을 현재 시각 기준으로 잡고 같은 버튼을 포함한 후속 클릭을 누적한다. 키보드 단축키의 즉시 저장은 유지한다.
- 일반·좁은 폭, 접기 상태 복원, 누적 시간, 서식/단축키/저장 회귀 테스트와 실제 소스 실행을 확인한다.

### 보존 및 제외 범위

- 보존: 값 선택 UI 휠 차단, 자동·수동 저장, D-Day, 알림 반복, 포스트잇, 기존 프리셋 데이터, 토마펫 연동.
- 제외: EXE 생성·배포, 데이터베이스 마이그레이션, 설정 창·단축키 작업 화면의 전면 재설계.

### 현재 진행 상태

- 승인된 시안·현재 편집기·테스트 구조 확인: **완료**
- 계획 문서 갱신: **완료**
- 소스 구현·자동 테스트·실행 검증: **완료**

## 15. 2026-08-25 상단 내비게이션 한 줄 통합과 EXE 재빌드

### 사용자 요청과 목표

- 작업공간 전환과 메모 하위 탭이 두 행을 차지하는 상단 영역을 한 줄로 합쳐 본문 공간을 확보한다.
- 기존 작업공간 전환, Shift+Tab, 메모 편집·캘린더·알림내역 전환과 접근성 동작을 유지한다.
- 변경 완료 후 콘솔 없는 단일 EXE를 생성하고 실제 실행을 확인한다.

### 구현과 검증 계획

- 상단 버튼은 내용 맞춤 폭과 36~40px 높이를 사용하고 바깥 여백을 6px로 줄인다.
- 메모·일정 작업공간에서만 `메모 편집·캘린더·알림내역` 보조 버튼을 같은 행에 표시하며, 기존 QTabWidget 페이지와 양방향 동기화한다.
- 기존 QTabWidget 탭 막대는 숨기되 페이지·신호·프로그램 진입 동작은 그대로 사용한다.
- Shift+Tab 안내는 오른쪽 끝에 두고 좁은 폭에서는 숨겨 가로 잘림을 방지한다.
- 한 줄 높이, 선택 상태, Shift+Tab, 하위 탭 전환, 프로그램에서 메모·일정 직접 열기를 집중 테스트한다.
- `build_release.ps1 -Clean`으로 `release/toma_shortcut_program.exe`만 교체하고, 존재하는 `release/data`는 건드리지 않는다. 결과 EXE를 약 5초 실행해 프로세스 유지를 확인한다.

### 보존 및 제외 범위

- 보존: 단축키·반복작업, 메모·캘린더·알림, 토마펫, SQLite 사용자 데이터, 기존 설정과 모든 하위 화면 기능.
- 제외: 상단 외 UI 전면 재설계, 데이터베이스 구조 변경, 기존 사용자 데이터 삭제.

### 현재 진행 상태

- 승인된 방향·현재 구현·빌드 스크립트 확인: **완료**
- 계획 문서 갱신: **완료**
- 소스 구현·자동 테스트·실행 검증: **완료**
- EXE 빌드·실행 검증·기록 정리: **완료**

## 16. 2026-08-27 프로그램명 `토마 데스크` 통일과 단일 EXE 생성

### 사용자 요청과 해결할 문제

- 좌측 상단 주 창 제목을 포함해 현재 제품명을 나타내는 사용자 노출 문구를 `토마 데스크`로 통일한다.
- 도움말 제목, 중복 실행 안내, 트레이 툴팁, 배포 안내와 사용설명서처럼 프로그램 제목이 표시되는 영역에서 이전 명칭이 남지 않게 한다.
- 변경 완료 후 콘솔 창이 없는 단일 실행 파일 `release/토마 데스크.exe`를 생성하고 실제 실행을 확인한다.

### 작업 목표와 완료 기준

- 실행 중인 주 창, 작업 표시줄·트레이, 관련 안내창과 도움말에서 제품명이 `토마 데스크`로 표시된다.
- 현재 사용자용 소스·배포 문서·설명서에서 `토마 단축키 런처`와 제품명 의미의 `단축키 런처`가 남지 않는다.
- 과거 작업 기록과 보관 문서는 당시 기록이므로 소급 변경하지 않는다.
- `release/토마 데스크.exe`가 단일 파일로 생성되고 콘솔 창 없이 실행되어 최소 5초간 프로세스가 유지된다.

### 변경 대상과 보존 범위

- 주요 변경 후보: `app_config.py`, `A_shortcut_launcher.py`, `main_window.py`, `generate_user_manual_pdf.py`, `DISTRIBUTION.md`, `shortcut_launcher.spec`, `build_release.ps1` 및 관련 테스트·사용설명서.
- 사용자 데이터 호환성을 위해 `%LOCALAPPDATA%/CodexShortcutLauncher`, `storage_paths.json`, SQLite 데이터 위치와 단일 인스턴스 mutex 문자열은 유지한다.
- 프로젝트 폴더명, 소스 진입점 파일명, 과거 `2._WORK_LOG.md`와 `docs/archive` 기록은 변경하지 않는다.
- 기존 단축키·매크로·메모·알림·포스트잇·토마펫·설정 동작과 SQLite 구조를 보존한다.
- 기존 `release/data`가 있으면 그대로 보존하고 새 EXE만 교체한다.

### 주요 구현 작업

- 공통 `APP_NAME`과 직접 작성된 사용자 노출 제목·도움말 문구를 `토마 데스크` 기준으로 정리한다.
- 배포 스크립트와 PyInstaller spec의 결과 파일명을 `토마 데스크.exe`로 맞춘다.
- 현재 배포 안내 및 사용설명서 제목을 새 이름으로 갱신하고 생성 결과의 제목 표시를 확인한다.
- 내부 호환성 식별자와 역사 기록은 제품명 문구와 구분해 유지한다.

### 테스트 및 실제 동작 검증

- 현재 사용자 노출 소스·문서에서 이전 제품명 잔존 여부를 검색한다.
- 관련 제목·도움말·트레이 표시 테스트와 핵심 회귀 테스트를 시스템 Python으로 실행한다.
- 실제 소스 프로그램을 실행해 주 창 제목과 프로세스 유지를 확인한다.
- 기존 실행 인스턴스를 확인한 뒤 `build_release.ps1 -Clean`으로 단일 EXE를 빌드한다.
- 결과 파일의 이름·크기·해시를 확인하고 약 5초간 실제 실행해 프로세스 유지 여부를 검사한다.
- 작업 결과를 `2._WORK_LOG.md`에 기록하고 미완료 항목만 `3._TODO.md`에 남긴다.

### 제외 범위

- 프로그램 기능·UI 배치·색상·아이콘 변경
- 프로젝트 폴더명과 내부 데이터 저장소 식별자 변경
- SQLite 스키마 또는 사용자 데이터 변경·삭제
- 과거 작업 기록과 보관 문서의 소급 명칭 변경

### 현재 진행 상태

- 기존 계획·표시명 사용 위치·빌드 설정 확인: **완료**
- 계획 문서 갱신: **완료**
- 사용자 계획 승인: **완료**
- 소스·문서·설명서 명칭 변경: **완료**
- 자동 테스트·실제 소스 실행 검증: **완료**
- 단일 EXE 빌드·실행 검증: **완료**
- 작업 기록과 TODO 정리: **완료**

## 17. 2026-08-27 요청 기준 단일 EXE 처음부터 재생성

### 사용자 요청과 목표

- 과거 EXE 생성 결과나 기록은 빌드 근거로 사용하지 않고, 현재 작업 폴더의 소스와 PyInstaller 설정을 기준으로 단일 EXE를 새로 만든다.
- 콘솔 창 없이 실행되는 최종 파일 `release/TomaDesk.exe`를 만들고 실제 실행 상태를 확인한다.

### 변경 대상과 보존 범위

- 실행: `build_release.ps1 -Clean`과 현재 `shortcut_launcher.spec`를 사용한다.
- 교체: 기존 `release/TomaDesk.exe`와 빌드 작업 폴더 `.package-build`만 새 빌드 과정에서 교체·정리한다.
- 보존: `release/data`, 프로젝트 원본 소스, SQLite 사용자 데이터, 백업·문서와 모든 프로그램 기능은 변경하거나 삭제하지 않는다.

### 구현 및 검증 계획

- 빌드 전 실행 중인 `TomaDesk` 또는 `toma_shortcut_program` 프로세스와 `release/data` 존재 여부를 확인해 파일 잠금과 보존 대상을 점검한다.
- PyInstaller로 새 단일 EXE를 생성하고, 결과 파일의 존재·크기·SHA-256을 확인한다.
- 실제 사용자 데이터와 분리된 임시 `LOCALAPPDATA`에서 EXE를 약 5초 실행해 프로세스가 유지되는지 확인한 뒤, 검증용 프로세스와 임시 폴더만 정리한다.
- 검증 결과를 `2._WORK_LOG.md`에 기록하고, 이번 요청에 직접 관련된 미완료 항목만 `3._TODO.md`에 남긴다.

### 제외 범위

- 프로그램 소스·UI·기능·SQLite 스키마·사용자 데이터 변경
- 기존 데이터·백업·문서 삭제 또는 공통 라이브러리 변경

### 현재 진행 상태

- 현재 빌드 설정·보존 대상·실행 인스턴스 사전 확인: **완료**
- 사용자 계획 승인: **완료**
- 새 단일 EXE 빌드·격리 실행 검증·작업 기록 정리: **완료**

## 18. 2026-08-28 요청 기준 단일 EXE 재빌드

### 사용자 요청과 목표

- 현재 작업 폴더의 소스와 설정을 기준으로 단일 EXE를 다시 생성한다.
- 콘솔 없는 `release/TomaDesk.exe`의 실제 실행 상태를 확인한다.

### 변경 대상과 보존 범위

- 교체: 기존 `release/TomaDesk.exe`와 빌드 작업 폴더 `.package-build`.
- 보존: 원본 소스, SQLite 사용자 데이터, 백업·문서와 모든 프로그램 기능.
- `release/data`가 존재하면 그대로 유지하며, 이번 확인에서는 해당 폴더가 없었다.

### 구현 및 검증 계획

- 실행 중 인스턴스가 없는 상태에서 `build_release.ps1 -Clean`으로 단일 EXE를 새로 빌드한다.
- 결과 파일의 존재·크기·SHA-256을 확인한다.
- 임시 `LOCALAPPDATA`에서 약 5초 실행해 프로세스 유지를 확인하고, 검증용 프로세스와 임시 폴더만 정리한다.
- 결과를 `2._WORK_LOG.md`에 기록하고 기존 미완료 항목은 유지한다.

### 제외 범위

- 프로그램 소스·UI·기능·SQLite 스키마·사용자 데이터 변경
- 기존 데이터·백업·문서 삭제 또는 공통 라이브러리 변경

### 현재 진행 상태

- 실행 인스턴스 종료 및 보존 대상 확인: **완료**
- 새 단일 EXE 빌드·격리 실행 검증·작업 기록 정리: **완료**

## 19. 2026-08-28 PyQt6 `concrt140.dll` EXE 실행 오류 수정

### 사용자 요청과 해결할 문제

- 새로 생성한 `release/TomaDesk.exe` 실행 시 `pyqt6_runtime_hook.py`가 `PyQt6\\Qt6\\bin\\concrt140.dll`을 찾지 못해 시작하지 않는 오류를 수정한다.

### 작업 목표와 완료 기준

- PyInstaller 내부에서 `concrt140.dll`을 런타임 훅이 참조하는 `PyQt6\\Qt6\\bin` 경로에 배치한다.
- 새 단일 EXE가 예외 창 없이 실제로 시작되고 최소 5초간 실행 상태를 유지한다.

### 변경 대상과 보존 범위

- 변경: `shortcut_launcher.spec`의 Qt 런타임 DLL 배치 경로, `pyqt6_runtime_hook.py`의 DLL 검색 경로 등록, 새 `release/TomaDesk.exe`, 빌드 기록.
- 보존: 런타임 훅의 로딩 순서, 원본 소스·UI·기능, SQLite 사용자 데이터, 백업·문서와 `release/data`.

### 주요 구현 작업

- 사용자 지정 Qt DLL의 PyInstaller 대상 경로를 `PyQt6/Qt6/bin`으로 맞춘다.
- 런타임 훅에서 Qt DLL 폴더와 PyInstaller 임시 압축 해제 루트를 Windows DLL 검색 경로로 등록한 뒤 해당 DLL을 로드한다.
- `build_release.ps1 -Clean`으로 EXE를 다시 만들고, PyInstaller 아카이브에서 `concrt140.dll`의 실제 배치 경로를 확인한다.

### 테스트 및 실제 동작 검증

- 새 EXE 아카이브에 `PyQt6\\Qt6\\bin\\concrt140.dll`이 포함됐는지 확인한다.
- 임시 `LOCALAPPDATA`에서 EXE를 약 5초 실행해 예외 창 없이 프로세스가 유지되는지 확인한다.
- 결과를 `2._WORK_LOG.md`에 기록하고, 기존 미완료 항목은 유지한다.

### 제외 범위

- 프로그램 기능·UI·SQLite 스키마·사용자 데이터 변경
- 기존 데이터·백업·문서 삭제 또는 공통 라이브러리 변경

### 현재 진행 상태

- 오류 재현 정보·런타임 훅·EXE 아카이브 분석: **완료**
- 사용자 계획 승인: **완료**
- DLL 배치 경로 수정·EXE 재빌드·실행 검증·기록 정리: **완료**

## 20. 2026-08-28 요청 기준 단일 EXE 생성

### 사용자 요청과 목표

- 현재 소스와 PyInstaller 설정으로 콘솔 없는 단일 실행 파일 `release/TomaDesk.exe`를 생성한다.
- 실제 실행 파일이 독립된 임시 데이터 경로에서 정상적으로 최소 5초간 실행되는지 확인한다.

### 변경 대상과 보존 범위

- 생성·교체: `release/TomaDesk.exe`와 빌드 작업 폴더 `.package-build`.
- 보존: 원본 Python 소스, `data/`, `backup/`, SQLite 사용자 데이터, 문서, 기존 기능 및 `release/data`가 존재할 경우 그 내용.
- 현재 확인 결과 `release` 폴더와 실행 중인 `toma_shortcut_program` 프로세스는 없다.

### 주요 구현 작업

- `build_release.ps1 -Clean`으로 현재 `shortcut_launcher.spec` 기반 단일 EXE를 생성한다.
- 생성 파일의 존재·크기·SHA-256을 확인한다.
- 임시 `LOCALAPPDATA`에서 실행해 프로세스 유지와 오류 대화상자 부재를 확인한 뒤 검증용 프로세스만 종료한다.

### 테스트 및 실제 동작 검증

- PyInstaller 빌드 로그에서 성공 여부와 런타임 훅·필수 DLL 포함 상태를 확인한다.
- 새 EXE를 약 5초간 격리 실행하고 종료 코드·프로세스 상태를 확인한다.
- 실제 결과와 제한사항을 `2._WORK_LOG.md`에 기록하고, 기존 미완료 항목은 `3._TODO.md`에 유지한다.

### 제외 범위

- 프로그램 소스·UI·기능·SQLite 스키마·사용자 데이터 변경
- 기존 데이터·백업·문서 삭제 또는 공통 라이브러리 변경

### 현재 진행 상태

- 현재 빌드 설정·보존 대상·실행 인스턴스 사전 확인: **완료**
- 사용자 계획 승인: **완료**
- 새 단일 EXE 빌드·격리 실행 검증·작업 기록 정리: **완료**

## 21. 2026-08-28 메모창 선택 상태 및 공통 상단 액션 UI 개선

### 사용자 요청과 해결할 문제

- 메모 서식 버튼의 고정 폭·문자 체크 표시·기본 파란색을 제거하고 실제 선택 상태만 파란색으로 표현한다.
- 빠른 알림을 한 줄로 정리하고 반복 유형·반복 종료·메모 전역 단축키 동작을 배타적 선택 컨트롤로 바꾼다.
- `시작/휴지통/설정/도움말`을 모든 작업공간에서 보이는 공통 상단 바 우측으로 이동한다.

### 작업 목표와 완료 기준

- 서식 토글과 프리셋은 기본 중립색이며 실제 활성 상태에서만 Qt checked state와 파란색 선택 스타일을 사용한다.
- `서식 1~3`은 같은 크기이고 `기본값/단축키`는 같은 행에서 바로 인접한다.
- 빠른 알림 9개는 한 행에 배치되고 기존 시간·단축키·알림 저장 동작을 유지한다.
- 반복 유형 5개 segmented control, 반복 종료 3개 라디오, 메모 단축키 동작 2개 라디오는 각각 정확히 하나만 선택된다.
- 상단 공통 액션 4개는 단축키·반복작업과 메모·일정의 모든 하위 탭에서 계속 보인다.

### 변경 대상과 보존 범위

- 주요 변경: `alert_notes/text_format_toolbar.py`, `alert_notes/editor.py`, `alert_notes/recurrence_controls.py`, `main_window.py`, `ui_theme.py`와 관련 테스트.
- 보존: `none/daily/weekdays/weekly/monthly`, `never/date/count`, `open/postit` 저장값과 기존 호환 속성·신호·SQLite 구조·사용자 데이터.
- 기존 메모·서식 적용·알림 저장·반복 계산·전역 단축키 등록·포스트잇·자동저장 기능을 유지한다.

### 주요 구현 작업

- B/I/U/S·글머리표·체크리스트를 콘텐츠 기준 폭의 checkable 버튼으로 통일하고 `✓` 텍스트를 제거한다.
- 프리셋 적용 상태를 별도로 추적해 직접 적용 후 현재 서식이 유지될 때만 선택 표시하고 세 버튼 크기를 통일한다.
- `기본값` 다음에 `단축키` 버튼을 배치하며 빠른 알림 9개를 단일 행으로 만든다.
- 기존 combo는 숨은 호환 어댑터로 유지하고 visible segmented/radio 그룹과 양방향 동기화한다.
- 상단 공통 액션을 workspace mode bar 우측으로 옮기고 기존 목록 제목 행의 빈 공간을 정리한다.

### 테스트 및 실제 동작 검증

- 서식·프리셋 선택 상태와 intrinsic width, 버튼 인접 순서, 빠른 알림 단일 행을 Qt 테스트로 확인한다.
- 세 배타적 선택 그룹의 로드·변경·저장값과 조건부 날짜·횟수 UI를 검증한다.
- 공통 상단 액션의 위치·모든 탭 표시·기존 콜백을 검사한다.
- 1247×849, 1420×720, 독립 메모창 680×880에서 잘림·공백·스크롤을 실제 렌더로 확인한다.
- 관련 테스트와 실제 소스 프로그램 실행 유지 여부를 확인하고 사용자 데이터가 변경되지 않았음을 검증한다.

### 제외 범위

- AppKit·SwiftUI 도입, 색상·폰트·전체 화면 구조 변경
- SQLite 스키마·사용자 데이터·공통 라이브러리 변경
- EXE 생성·배포

### 현재 진행 상태

- 현행 위젯·레이아웃·호환 경로 확인: **완료**
- 사용자 계획 승인 및 계획 문서 갱신: **완료**
- UI 구현과 자동 테스트: **완료**
- 실제 화면·소스 실행 검증: **완료**
- 작업 기록과 TODO 정리: **완료**

## 22. 2026-08-28 메모목록·편집창·휴지통 레이아웃 개선

### 사용자 요청과 해결할 문제

- 메모목록과 휴지통의 첫 번째 헤더를 글자 대신 전체선택 체크박스로 통일한다.
- 서식 버튼 크기, 글씨체 콤보박스의 빈 폭, 빠른 알림 배치와 메모 편집창 폭을 정돈한다.
- 휴지통에서 체크한 여러 작업·메모·일정을 한 번에 복원할 수 있게 한다.

### 작업 목표와 완료 기준

- 메모목록과 휴지통 헤더 체크박스가 전체·부분·미선택 상태를 행 체크와 양방향 동기화한다.
- B/I/U/S/글머리표/체크리스트와 이미지 삽입 버튼의 실제 가로·세로 크기가 같다.
- 글씨체 콤보박스는 글꼴명과 화살표에 필요한 폭만 사용하고 뒤쪽 크기·색상 컨트롤이 왼쪽으로 이동한다.
- 빠른 알림 9개는 한 행에서 첫·마지막 버튼을 양끝에 맞추고 사이 간격을 균등하게 사용한다.
- 독립 메모창과 메인 편집 패널은 확보된 폭만큼 축소되어도 잘림·줄바꿈·가로 스크롤이 없다.

### 변경 대상과 보존 범위

- 주요 변경: 메모목록, 서식 툴바, 메모 편집기, 독립창, 휴지통과 관련 Qt 테스트·테마.
- 보존: 메모·서식·알림·포스트잇·자동저장·단축키·복원 콜백, SQLite 스키마와 기존 사용자 데이터.
- 글자색 선택 버튼과 빠른 색상, 기존 창 위치·높이 및 사용자가 이후 조절한 크기를 유지한다.

### 주요 구현 작업

- 메모목록에 `SelectAllHeader`를 연결하고 하단의 중복 전체선택 버튼을 제거한다.
- 휴지통의 ID 표시 열을 체크 열로 바꾸고 행 데이터에 복원 대상을 보관해 체크 항목을 일괄 복원한다.
- 글씨체 콤보박스를 내용 맞춤 폭으로 바꾸고 서식 토글과 이미지 버튼에 공통 크기를 적용한다.
- 빠른 알림 버튼 사이에 균등한 확장 간격을 두며 바깥쪽 추가 여백은 두지 않는다.
- 독립창 기본·최소 폭과 메인 편집 최소 폭을 축소하고 기존 저장 폭에는 한 번만 호환 조정을 적용한다.

### 테스트 및 실제 동작 검증

- 메모목록·휴지통의 전체·부분선택, 다건/단건 복원, 선택 삭제·내보내기와 행 열기를 검사한다.
- 서식/이미지 버튼 크기, 선택 서식, 글씨체 폭, 색상 컨트롤과 빠른 알림 양끝·균등 배치를 검사한다.
- 축소 독립창과 1247×849·1420×720 메인창을 렌더해 잘림·가로 스크롤·불필요한 공백을 확인한다.
- 관련 Qt 테스트와 격리된 소스 실행을 검증하고 실제 사용자 DB는 변경하지 않는다.

### 제외 범위

- SQLite 스키마·공통 라이브러리·기존 기능의 데이터 계약 변경
- 사용자 요청이 없는 EXE 생성·배포

### 현재 진행 상태

- 현행 구조·참조 이미지 확인과 사용자 계획 승인: **완료**
- 계획 문서 반영: **완료**
- UI 구현과 자동 테스트: **완료**
- 실제 화면·소스 실행 검증과 작업 기록 정리: **완료**

## 23. 2026-08-31 TomaDesk 안정성·데이터 보호·기능 통합 장기 업데이트

### 사용자 요청과 해결할 문제

- 2026-08-31 개선사항 분석을 프로그램의 새 장기 업데이트 목표로 편입하고, 위험도와 선행 관계에 따라 단계별로 구현한다.
- 기본 새 메모 단축키 등록 실패와 설정 저장 차단, 확인 없는 Excel 전체 교체, D-Day 알림 유실, 08시 이전 일정 누락, 알림 미러 일정의 사용자 편집 소실처럼 재현된 문제를 우선 해결한다.
- 단축키·메모·알림·D-Day·일정이 각각 동작하는 현재 장점은 유지하면서, 목록·검색·Excel·휴지통·백업·오류 안내의 일관성과 기능 간 연결을 개선한다.

### 작업 목표와 완료 기준

- P0/P1 데이터 유실·설정 차단·표시 누락 문제를 먼저 수정하고 재현 테스트를 회귀 테스트로 고정한다.
- 각 단계는 `계획 승인 → 구현 → 집중 테스트 → 격리된 실제 실행/화면 확인 → 작업 기록` 순서로 완료한 뒤 다음 단계로 넘어간다.
- 첨부 분석의 파일 위치·테스트 수·재현 결과는 구현 시작 시 현재 소스와 임시 DB에서 다시 확인한다.
- 기존 사용자 DB를 직접 변경하지 않고 복사본 또는 임시 DB로 마이그레이션·복원·데이터 정합성을 검증한다.
- 단계별 완료 항목만 `2._WORK_LOG.md`에 기록하고, 미완료·사용자 선택 대기 항목만 `3._TODO.md`에 유지한다.
- 사용자가 명시적으로 요청하지 않는 한 EXE를 생성하거나 기존 배포 파일을 교체하지 않는다.

### 전체 우선순위와 단계 구성

#### 1단계 — 오류·데이터 보호·회귀 위험

1. 3-수식키 파서와 `HotkeyBuilder` 정책을 정합화하고 모든 기본 단축키가 같은 파서를 통과하게 한다.
2. 기본값만으로 설정 저장이 차단되는 문제를 해소하고 단축키 중복 오류가 다른 설정 저장 전체를 막지 않게 한다.
3. D-Day 알림 해제·삭제·완료 시 `reminders`와 `notes.d_day_alert` 상태가 서로 어긋나지 않게 한다.
4. Excel 가져오기 전에 교체 수·기존 삭제 수·백업 위치를 미리 보여 주고 명시적 확인을 받는다.
5. 알림에서 파생된 일정의 제목·상세·분류·미리 알림이 동기화 과정에서 소실되지 않게 한다.
6. 체크하지 않은 현재 행이 삭제 대상으로 자동 선택되는 파괴적 동작 폴백을 제거한다.
7. 매크로 녹화·재생 중 알림 폴링을 일시 중지하고 종료 후 밀린 알림을 안전하게 다시 확인한다.
8. 전체 복원 확인 문구와 실제 동작을 일치시키고 백업 회전 파일 패턴을 바로잡는다.

완료 기준:

- 기본 단축키 파서 통과, 설정 무변경 저장 성공, Excel 가져오기 취소 시 DB 무변경을 자동 테스트한다.
- D-Day 알림 해제·삭제·완료 조합에서 배지·대화상자·DB 상태가 모두 일치하는지 검증한다.
- 미러 일정 수정 후 원본 알림을 바꿔도 보존하기로 한 필드와 미리 알림이 유지되는지 검증한다.
- 실제 Windows에서 선택한 새 메모 단축키와 긴급 중지 단축키의 등록·입력을 확인한다.

#### 2단계 — 자주 사용하는 핵심 흐름과 성능

1. 숨겨진 일괄 액션 툴바를 의도에 맞게 노출해 선택 작업의 활성/비활성 및 실행 테스트 진입점을 제공한다.
2. 메모 자동저장 때 전역 단축키 전체를 해제·재등록하지 않도록 변경 감지 또는 부분 갱신 구조로 분리한다.
3. 단축키 등록 성공·충돌·제외 상태를 목록에서 구분하고 실패 상세와 다시 등록 동작을 제공한다.
4. 메모 자동저장 경로에서 캘린더·알림 이력 전체 재조회를 분리해 타이핑 지연을 줄인다.
5. 캘린더 일간/주간 타임라인을 24시간으로 확장하고 기본 스크롤 위치를 08:00에 둔다.

완료 기준:

- 메모·일정 각 200건의 임시 데이터에서 자동저장 시 재등록 횟수, DB 조회 횟수와 UI 응답을 측정한다.
- 00:00·07:59·08:00·23:59 일정의 표시, 슬롯 생성, 드래그 이동과 현재 시각 표시를 검증한다.
- 중복 단축키를 의도적으로 만들어 실패 표시·상세·재시도가 실제 등록 결과와 일치하는지 확인한다.
- 툴바 노출과 반응형 배치가 기존 표 선택·삭제·필터 동작을 방해하지 않는지 실제 화면에서 확인한다.

#### 3단계 — 기능 간 연결과 일괄 작업

1. 메모 알림과 일정 미리 알림을 예정 알림 목록·처리 이력·Excel 내보내기에서 함께 조회한다.
2. D-Day를 임박순으로 한눈에 보는 전용 구역을 추가하고 메모 열기·추가 동작을 연결한다.
3. 메모 목록에 알림·D-Day 상태를 열 또는 배지로 표시한다.
4. 알림 미러 일정을 시각적으로 구분하고 이동·삭제·편집 제한 이유와 원본 메모 이동 동작을 제공한다.
5. 캘린더 검색과 카테고리 필터를 활성화하고 분산된 카테고리 표시 정의를 통일한다.
6. 예정/이력 표 정렬, 휴지통 날짜 표시·복원 결과·영구 삭제, 일정 Excel 내보내기를 보강한다.

완료 기준:

- 과거·오늘·미래 D-Day와 알림 켬/끔 조합의 표시·정렬·열기 동작을 검증한다.
- 예정 알림에서 메모 알림과 일정 알림이 모두 보이고 각 처리 결과가 올바른 저장소에 기록되는지 확인한다.
- 1180px·1080px·900px·820px에서 D-Day 구역과 캘린더 필터의 표시·대체 진입점을 렌더링해 확인한다.
- 기존 형식의 백업 JSON을 임시 DB로 복원한 뒤 새 목록과 내보내기가 정상 동작하는지 검증한다.

#### 4단계 — 편의 기능과 장기 개선

1. 수동 저장 초안에 메모 단축키와 동작 값을 함께 복원한다.
2. UI 배율을 저장하고 메인창·독립 메모창·포스트잇까지 일관되게 적용한다.
3. 통합 검색 결과의 HTML 원문 노출과 본문 HTML 태그 오검색을 줄인다.
4. 매크로 JSON, 일정 미리 알림, 반복 요일 입력 오류를 사용자가 이해할 수 있는 한국어 안내로 바꾼다.
5. 메모 편집기에서 해당 메모의 여러 예정 알림을 조회·선택·수정·해제할 수 있게 한다.
6. 도움말의 단축키 안내를 현재 설정값과 맞추고 일정의 `all_day`·`priority` 저장값을 화면에 반영한다.

완료 기준:

- 수동 저장 초안의 모든 편집값이 종료·재실행 후 복원되는지 확인한다.
- Windows 배율 100%·125%·150%·175%에서 설정·메모·캘린더·포스트잇을 렌더링하고 재시작 후 배율 유지 여부를 확인한다.
- 알림 3건이 연결된 메모에서 목록·선택 수정·개별 해제를 검증한다.
- 종일·우선순위·자정 경계·반복 일정의 표시와 알림 시각을 확인한다.

### 단계 공통 보존 범위

- SQLite 기존 테이블·컬럼과 `notes.d_day_*`, `reminders.occurrence_kind/series_id/scheduled_at`, `schedule_items.source_reminder_id` 계약을 우선 보존한다.
- 기존 `settings` 키, 창 위치·스플리터·초안·포스트잇 상태와 `database_bundle` JSON 형식 및 `legacy_name="hotkeys"` 경로를 유지한다.
- 저장된 단축키 문자열 표기와 매크로 `payload`의 `steps`·`timing_mode`·`playback_speed`·`repeat_count`·`excluded_apps` 구조 및 레거시 입력 호환을 유지한다.
- 포스트잇의 `normal`·`title`·`badge` 3단 표시, 자동저장, 전경 앱별 단축키 제외, 긴급 중지와 기존 백업·복원 동작을 보존한다.
- 값 선택 UI는 포커스 상태에서도 마우스 휠로 값이 바뀌지 않게 하고, 새로 추가하거나 수정하는 선택 위젯도 같은 규칙을 적용한다.
- 스키마 변경이 불가피하다고 확인되면 별도 계획 변경과 사용자 재승인 전에는 적용하지 않는다.

### 구현 전에 확정할 사용자 선택

- 새 메모 단축키: **3-수식키 지원 확대로 확정·1단계 반영 완료**.
- 알림 미러 일정: **읽기 전용과 원본 알림의 부분 동기화로 확정·1단계 반영 완료**.
- D-Day 전용 구역: 캘린더 좌측, 메모 목록 상단 또는 별도 하위 탭 중 위치 선정과 좁은 화면 대체 진입점.
- 캘린더 기본 표시: 24시간 전체 또는 사용자 지정 업무 시간대. 현재 계획 기본안은 24시간 전체와 08:00 초기 스크롤이다.
- 새 메모 단축키로 연 빈 메모: 즉시 생성 유지 또는 의미 있는 내용 입력 시 생성.
- 밀린 알림: 개별 순차 표시 또는 요약 화면 제공.

### 추가 현황 점검

- 현재 사용자 DB는 읽기 전용으로 조사하거나 복사본에서만 검증한다.
- 08시 이전 일정, `d_day_alert=1`이지만 deadline 알림이 없는 메모, 편집 흔적이 있는 미러 일정, 남은 수동 저장 초안, 누적 백업 파일과 현 파서가 거부하는 저장 단축키를 단계별 구현 전에 점검한다.
- 일괄 액션 툴바 은닉 의도, 캘린더 카테고리 라벨의 역할, 좁은 화면의 D-Day 위치, 다수 포스트잇 배지 배치와 캘린더 서랍 오버레이는 실제 화면에서 다시 확인한다.

### 제외 범위

- 전체 알림·반복 체계의 일괄 스키마 통합 또는 기존 데이터 형식의 파괴적 변경.
- 사용자가 승인하지 않은 UI 전면 재설계, 공통 라이브러리 조회·복사·변경과 EXE 생성·배포.
- 실제 사용자 DB를 이용한 파괴적 시험, 기존 백업·배포 결과물·완료 기록 삭제.
- 27개 우선순위 밖의 추가 기능은 별도 요청과 계획 변경 없이 포함하지 않는다.

### 현재 진행 상태

- 첨부 개선사항 분석과 기존 계획 대조: **완료**
- 장기 업데이트 목표·보존 범위·4단계 로드맵 계획 반영: **완료**
- 사용자 계획 승인 및 1단계 선택 사항 확정: **완료**
- 1단계 구현·테스트·실제 동작 검증·기록: **완료**
- 2단계 구현·테스트·실제 동작 검증·기록: **미착수**
- 3단계 구현·테스트·실제 동작 검증·기록: **미착수**
- 4단계 구현·테스트·실제 동작 검증·기록: **미착수**

## 기존 완료 상태

- 메모 자동저장·독립창·목록 관리와 2026-08-11 단일 EXE 재빌드·실행 검증은 완료 상태를 유지한다.
- 2026-08-15 포스트잇 1·2단계 기능과 데이터 보호 계약은 완료 상태를 유지하며 이번 작업에서 변경하지 않는다.

## 24. 2026-09-08 메모편집 강화

### 사용자 요청과 해결할 문제

- 새로 추가된 `메모편집_강화_작업명세.md`의 제목 스타일 4단계, 기능 항목 순서 변경,
  제목 줄앞 입력과 줄앞 입력 설정 기능을 현재 메모 편집기에 반영한다.
- 현재 `＋ 기능 넣기` 메뉴는 정적인 `INSERT_ITEMS` 순서를 사용하고, 줄앞 입력은
  `RichMemoTextEdit.TYPING_RULES`에 고정되어 있어 사용자가 순서나 조합을 바꿀 수 없다.
- 제목 역할이 저장 가능한 문서 서식으로 따로 정의되어 있지 않아 사용자가 글자 크기와 굵기를
  매번 직접 조절해야 한다.
- 명세의 `0. 이전 채팅 반영 항목` 1~4와 제목 수치·기본 입력 조합은 아직 비어 있으므로,
  빠진 요구와 기본값을 사용자 승인 전에 확정해야 한다.

### 작업 목표와 완료 기준

- 제목1~4가 굵기, 단계별 글자 크기, 자간과 위아래 여백을 가진 줄 단위 스타일로 동작한다.
- 커서가 있는 한 줄 또는 선택 영역에 걸친 모든 줄에 제목 스타일을 적용할 수 있고,
  저장·재실행 후에도 HTML 서식이 유지된다.
- `＋` 기능 화면에서 항목을 길게 눌러 끌면 놓을 자리 표시와 하이라이트가 보이고,
  확정 순서가 앱 재실행 뒤에도 유지된다.
- 기능 순서는 메인 편집기 `＋`, `/` 빠른 메뉴와 포스트잇 `＋`에서 동일하게 적용된다.
- 제목1~4와 기존 줄앞 입력 기능의 활성 조합을 설정에서 바꿀 수 있으며, 빈 값·중복·접두 충돌을
  저장 전에 설명과 함께 막는다.
- 조합 변경과 기본값 복원은 열려 있는 메인 편집기·독립 메모창·포스트잇에 즉시 반영된다.
- 줄앞 입력 문자를 지우고 기능을 적용하는 전체 과정은 `beginEditBlock/endEditBlock`으로 묶어
  `Ctrl+Z` 한 번에 입력 직전 상태로 돌아간다.
- 기존 메모 HTML, 서식 1~3, 토글·체크리스트·페이지·표·메모 링크·그림·자동저장과 사용자 DB를
  손상시키지 않는다.

### 사용자 승인으로 확정한 선택

1. **기능 화면 위치**: 현재 코드에는 별도 기능 탭이 없으므로 `＋ 기능 넣기`를
   `기능 / 설정` 두 탭의 재사용 팝업으로 확장하는 안을 기본으로 한다.
2. **제목 기본값**: 제목1~4를 `22 / 18 / 15 / 13pt`로 제안한다. 모두 굵게를 적용하고,
   단계별 자간과 위아래 여백은 별도 상수로 둔다.
3. **제목 줄앞 입력**: 널리 쓰이는 Markdown 방식인 `# `, `## `, `### `, `#### `를 제안한다.
4. **기존 별칭 보존**: 글머리표 `-`·`*`, 체크리스트 `[]`·`[ ]`, 인용문 `"`·`|` 중
   대표 조합 하나만 설정에서 바꾸고 나머지는 호환 별칭으로 유지하는 안을 권장한다.
5. **누락 요구**: `메모편집_강화_작업명세.md`의 빈 `0. 이전 채팅 반영 항목`에 실제로 들어갈
   1~4번 요청이 있다면 구현 전에 받아 본 계획에 추가한다.

### 변경 대상과 보존 범위

- `alert_notes/insert_menu.py`: 항목별 안정적인 ID, 종류, 기본 순서와 기본 줄앞 입력 정의.
- 새 `alert_notes/insert_preferences.py`: 순서·조합 기본값, JSON 읽기/검증/저장과 변경 신호.
- 새 `alert_notes/insert_panel.py`: `기능 / 설정` 팝업, 길게 눌러 끌기, 놓을 자리 표시,
  조합 편집과 기본값 복원 UI.
- `alert_notes/rich_memo_edit.py`: 제목 스타일 적용, 선택 줄 처리, 동적 줄앞 입력과 한 번 되돌리기.
- `alert_notes/text_format_toolbar.py`, `alert_notes/postit.py`: 새 기능 팝업 연결과 즉시 갱신.
- `alert_notes/sqlite_store.py`: 기존 `settings` 테이블을 통한 순서·조합 JSON 저장만 사용한다.
  새 테이블이나 기존 메모 스키마 변경은 하지 않는다.
- `ui_theme.py`: 팝업 탭, 끌기 손잡이·placeholder·충돌 안내의 최소 스타일만 추가한다.
- 관련 테스트는 기존 단계 테스트를 보존하고 새 전용 테스트 파일로 분리한다.
- 사용자 메모·알림·일정·단축키·백업 파일, 기존 EXE와 공통 라이브러리는 변경하지 않는다.

### 주요 구현 작업

#### 1단계 — 규칙과 저장 구조

- `INSERT_ITEMS`의 표시 이름을 저장 키로 쓰지 않고 영문 안정 ID를 추가한다.
- 기본 순서, 사용자 순서와 알 수 없는 구버전 값을 안전하게 합치는 정규화 함수를 만든다.
- 줄앞 입력은 표시 문자열, 공백 필요 여부와 실행 메서드를 한곳에서 만들고,
  `RichMemoTextEdit`의 고정 `TYPING_RULES` 중복을 제거한다.
- `memo_insert_order_v1`, `memo_typing_triggers_v1`처럼 버전이 있는 settings 키에 JSON으로 저장한다.
- 저장값이 없거나 손상되면 기본값으로 돌아가되 기존 메모 내용은 건드리지 않는다.

#### 2단계 — 제목1~4 문서 서식

- 제목 단계별 크기·자간·위아래 여백을 불변 설정으로 분리한다.
- `QTextCharFormat`에는 굵기·크기·자간을, `QTextBlockFormat`에는 위아래 여백을 적용한다.
- 선택이 없으면 현재 줄, 선택이 있으면 걸친 모든 줄에 같은 제목 단계를 적용한다.
- 일반 본문으로 되돌리는 길을 함께 제공하고, 제목 줄에서 Enter를 누른 다음 줄은 본문으로 시작하게 한다.
- 제목 적용·줄앞 입력 문자 제거·커서 복원을 한 편집 블록으로 묶는다.

#### 3단계 — 기능 순서 끌기와 설정 화면

- `＋` 팝업에 `기능`과 `설정` 탭을 만들고 메인 편집기와 포스트잇이 같은 위젯 규칙을 사용한다.
- 기능 목록은 약 350ms 길게 누른 뒤에만 끌기가 시작되게 해 일반 클릭과 구분한다.
- 끌기 중 placeholder, 놓을 행 하이라이트와 취소 복원을 제공하고, 실제 순서가 바뀐 뒤에만 저장한다.
- 설정 탭은 각 기능의 줄앞 입력, 중복·접두 충돌 안내, 초기화와 저장 상태를 보여 준다.
- 순서 초기화와 조합 초기화는 범위를 분리하고 확인 문구로 무엇이 바뀌는지 명확히 알린다.

#### 4단계 — 세 편집 화면 동기화

- 설정 저장 시 공유 객체가 변경 신호를 보내 이미 열린 메인 편집기·독립창·포스트잇의 메뉴와
  줄앞 입력 규칙을 다시 읽게 한다.
- `/` 메뉴의 검색·키보드 이동·설명은 유지하면서 사용자 순서만 적용한다.
- 기존 기능 메서드가 없는 화면에서는 지금처럼 해당 항목을 비활성 또는 제외해 빈 동작을 만들지 않는다.

### 테스트 및 실제 동작 검증

- 순서 저장·재로드·초기화, 신규 항목 합치기와 손상 JSON 복구를 임시 DB로 검증한다.
- 제목1~4의 크기 순서, 굵기, 자간·여백, 한 줄·여러 줄 적용과 HTML 재로드를 검사한다.
- `# `부터 `#### `까지 문자 제거·제목 적용·커서 위치와 `Ctrl+Z` 한 번 복원을 검사한다.
- 기존 줄앞 입력 전체와 새 조합의 중복·접두 충돌·빈 값 차단, 변경 즉시 반영을 검사한다.
- 길게 누르기 전 클릭, 길게 누른 뒤 끌기, 취소, 위·아래 이동, placeholder와 재실행 후 순서를 검사한다.
- 메인 `＋`, `/` 메뉴와 포스트잇 `＋`의 항목 순서가 같은지 확인한다.
- 기존 `test_memo_blocks_stage5.py`, `test_memo_stage7.py`, `test_memo_stage8.py`와 관련 UI 테스트를
  파일별 독립 프로세스로 실행한다.
- 680×880 독립창, 900px 경계, 1247×849와 1420×720 메인창에서 한글 제목·탭·긴 항목명이
  잘리지 않는지 `QT_QPA_PLATFORM=windows`와 `WA_DontShowOnScreen` 렌더로 확인한다.
- 실제 마우스로 길게 누르기와 놓을 자리 표시를 확인하고, 포스트잇에서도 기능 실행을 확인한다.
- 621번째 설정 검사는 실제 `%LOCALAPPDATA%`를 쓰지 않도록 별도 격리 보완 계획이 승인되기 전에는
  보호를 풀어 실행하지 않는다. 현재 안전한 기준선 620개 통과를 회귀 기준으로 사용한다.

### 제외 범위

- 제목 스타일 외의 본문 렌더러 전면 교체와 Markdown 전체 문법 지원.
- 기존 서식 1~3 삭제·변환, 저장된 메모 HTML 일괄 마이그레이션.
- 메모·알림·일정 SQLite 스키마 변경과 사용자 데이터 정리.
- 공통 라이브러리 조회·복사·변경.
- 사용자 요청이 없는 EXE 생성·배포.
- 작업명세의 빈 `0. 이전 채팅 반영 항목`을 추측해 구현하는 일.

### 현재 진행 상태

- 새 작업명세 확인: **완료**
- 현재 서식 도구·기능 메뉴·줄앞 입력·settings 저장 구조 확인: **완료**
- 인수인계 TODO와 프로젝트 계획 반영: **완료**
- 기능 화면 해석·기본 제목값·줄앞 입력값·기존 별칭 정책 확정: **완료 (2026-09-08 사용자 권장안 승인)**
- UI 시안 제작과 선택: **진행 중**
- 소스 구현: **진행 중**
- 자동 테스트와 실제 렌더·마우스 검증: **미착수**
- 작업 기록·TODO 정리: **미착수**
- EXE 생성: **제외 범위**

## 25. 2026-09-08 일정·D-Day 추가 업데이트

### 사용자 요청과 해결할 문제

- `5._인수인계_2.md`의 추가 업데이트를 전체 계획에 반영하고, 미구현 작업을 단계별로 진행한다.
- 그날 마감과 할 일을 작은 일정 포스트잇으로 확인·완료할 수 없어 캘린더나 요약 화면을 다시 열어야 한다.
- 전체 일정 편집은 일정과 할 일을 한 폼에서 종류로 나눠 입력해, 할 일에도 불필요한 종료 입력이 보인다.
- 지난 D-Day 누적, 목표 시각 부재, D-Day만 보기와 편집 중 시간 겹침 확인이 아직 남아 있다.
- `인수인계2`의 완료 항목은 실제 코드에서 존재를 확인했으므로 다시 구현하지 않고 회귀 보존 대상으로 둔다.
- `인수인계2`의 26개 파일·555건 검사는 이전 시점 수치다. 현재 안전한 기준선은
  27개 파일 중 620개 통과·실패 0개와 격리되지 않은 설정 검사 1개다.

### 작업 목표와 완료 기준

- 일정 표시 포스트잇에서 이전날·오늘·다음날 이동, 당일·이번 주·임박순·우선도순 보기가 동작한다.
- 완료 체크는 기본 삭선으로 즉시 피드백하고, 설정에서 선택한 경우에만 휴지통으로 보낸다.
- 포스트잇 높이는 실제 행 `sizeHint()`를 바탕으로 내용에 맞게 변하고 기본 최대 8줄을 넘으면 스크롤한다.
- 포스트잇 단축키는 기본값을 비워 다른 프로그램의 조합을 선점하지 않으며 사용자가 설정한 뒤에만 등록한다.
- 전체 편집 진입점은 `＋ 새 일정`과 `＋ 새 할 일`로 구분하고, 할 일은 종료 줄 없이 마감 하나만 입력한다.
- 저장된 항목은 저장된 종류로 올바른 폼을 열며 기존 데이터가 잘못 분류된 경우 자동 변환하지 않고 안내한다.
- 지난 D-Day는 사라지지 않고 별도 묶음에서 일괄 완료할 수 있으며, 목표 날짜·시각을 같은 줄에서 확인한다.
- 캘린더에서 D-Day만 골라 볼 수 있고, 편집 폼 아래에서 같은 날 다른 일정과 시간 겹침을 확인한다.
- 기존 빠른 입력 팝오버, 일정·알림·메모·D-Day 데이터와 사용자 설정을 보존한다.

### 확정된 설계 원칙

- 일정 표시 포스트잇은 사용자 붙임 이미지의 흰 카드·파란 날짜 머리말·작은 문자 배지·우측 시각·
  완료 삭선·하단 안내 스타일을 사용한다. 네 가지 보기는 날짜 아래의 얇은 선택 탭으로 보완한다.
- 지난 D-Day는 날짜가 지났다는 이유로 숨기거나 자동 완료하지 않는다.
- 빠른 입력 팝오버는 일정/할 일 종류 토글을 유지하고, 전체 편집 화면만 두 폼으로 나눈다.
- 일정 포스트잇 기본 보기는 당일, 기본 최대 높이는 8줄, 완료 표시는 삭선, 단축키는 비움으로 한다.
- 높이 계산은 고정 행 높이가 아니라 실제 위젯 크기를 측정하는 `today_summary._rebalance()` 방식을 따른다.
- 애매한 완료 처리·표시 범위와 위치는 설정 또는 사용자 시안 선택으로 결정하고 자동 생성하지 않는다.
- 설정창에는 `QScrollArea`를 넣지 않는다. 새 설정이 현재 높이 여유를 넘으면 카드 배치를 먼저 다시 잡는다.

### 사용자 승인으로 확정한 선택

1. 할 일 전체 편집 폼에 `D-Day로 세기`를 넣는다.
2. 지난 D-Day는 오늘 요약 맨 위의 접이식 묶음으로 둔다.
3. 일정 포스트잇 기본 표시 대상은 일정+할 일이며 메모 D-Day는 기본 해제한다.
4. 반복 일정·반복 할 일 완료는 이번 회차만 처리한다.
5. 설정창은 기존 카드 스타일을 유지하면서
   `단축키 / 일정·D-Day / 프로그램 / 데이터` 범주 탭으로 나눈다.

### 변경 대상과 보존 범위

- 새 `alert_notes/schedule_postit.py`: 일정 표시 포스트잇 창, 날짜 이동·보기·완료·자동 높이.
- 새 `alert_notes/schedule_postit_settings.py`: 설정값 정의·검증과 설정 UI 묶음.
- `alert_notes/schedule_store.py`: 표시 범위별 조회와 완료 처리를 기존 저장 계약 위에서 제공한다.
- `alert_notes/today_summary.py`: 지난 D-Day 묶음·일괄 완료와 일정 포스트잇 사이의 표시 역할 정리.
- `alert_notes/schedule_editor.py`: 일정/할 일 전체 편집 폼 분기와 같은 날 일정 요약.
- `alert_notes/calendar.py`, `calendar_month_view.py`: D-Day 필터와 각 전체 편집 진입점 연결.
- `main_window.py`, `settings_dialog.py`, `hotkey_manager.py`: 포스트잇 생명주기·설정·선택 단축키 등록 연결.
- `ui_theme.py`: 새 포스트잇과 분리 폼에 필요한 최소 스타일.
- 기존 `schedule_popover.py`의 빠른 입력, `schedule_items`와 알림 미러 일정 계약,
  메모 D-Day·반복·휴지통·백업 데이터는 보존한다.
- 공통 라이브러리, 기존 EXE와 실제 사용자 DB는 변경하지 않는다.

### 단계별 진행 계획

#### 1단계 — 정보 구조·설정 범위와 시안 확정

- 일정 포스트잇, 오늘 요약과 지난 D-Day 묶음에 각각 무엇을 표시할지 중복 없는 역할표를 만든다.
- 포스트잇의 당일·주간·임박·우선도 보기와 D2·D3 항목을 한 시안에서 비교한다.
- 설정 5개가 스크롤 없는 설정창에 들어가는지 현재 크기와 100%·125%·150% 배율에서 먼저 잰다.
- 할 일의 `D-Day로 세기`, 지난 D-Day 위치와 반복 항목 완료 범위를 사용자에게 선택받는다.
- 시안이 승인되기 전에는 UI 코드를 수정하지 않는다.

1단계 확인 결과:

- 역할표와 A/B/C 배치 시안을 `7._일정_DDay_1단계_시안.md`와
  `output/schedule_dday_stage1_mockup.png`로 작성했다.
- 340px 좁은 포스트잇은 보기 단추를 2×2로 재배치하고, 각 항목의 목표 날짜·시각을
  제목 아래 보조 줄로 유지하는 별도 시안으로 확인했다. 가로 스크롤은 사용하지 않는다.
- `ui-ux-pro-max` 기준을 적용해 상태를 색만으로 표시하지 않고 글자 배지와 함께 표시했으며,
  좁은 폭에서는 보기 단추를 2×2로 바꾸고 가로 스크롤을 만들지 않는 안을 택했다.
- 현재 설정창에 요약 한 줄을 붙인 격리 렌더에서 100%·125%는 표시됐지만 150%는 기존 단축키
  2열 영역이 잘렸다. 단순 3열 전환도 가로폭 부족으로 부적합했다.
- 150%까지 스크롤 없이 맞추기 위한 범주 탭 안을 사용자 선택에 추가했다.
- 설정 범주 탭 A안은 1380×972 시안에서 추가 설정과 저장·취소가 내부 스크롤 없이 표시되고,
  선택 상태를 색·굵은 글자·밑줄로 함께 구분하는 것을 확인했다.
- 사용자 붙임 이미지를 기준으로 노란 포스트잇 표현을 흰 카드·파란 날짜 머리말·작은 배지·
  우측 시각·완료 삭선·하단 안내 스타일로 교체하고 일반·340px 시안을 다시 렌더했다.
- 사용자 권장안 전체 승인으로 시안을 확정했고, 2단계 구현과 검증까지 완료했다.

#### 2단계 — 조회·완료 규칙과 일정 표시 포스트잇

- 표시 대상과 정렬별 조회를 UI와 분리된 함수로 만들고 임시 DB에서 먼저 검증한다.
- 날짜 이동, 보기 전환, 체크 완료와 최대 행 수를 구현한다.
- 삭선은 원본을 즉시 삭제하지 않고 완료 상태로 표시하며, 휴지통 선택 때만 기존 안전 삭제 경로를 사용한다.
- 실제 행 크기로 창 높이를 다시 계산하고 최대 행 수를 넘을 때만 스크롤을 보인다.
- 창 위치·보기·최대 줄 수·표시 대상·완료 방식은 기존 `settings` 테이블에 저장한다.
- 단축키는 빈 값이면 등록하지 않고, 값이 있을 때 기존 충돌·위험 조합 검사를 그대로 거친다.

#### 3단계 — 전체 편집 폼을 일정과 할 일로 분리

- `＋ 새 일정`과 `＋ 새 할 일` 진입점을 분리하되 빠른 입력 팝오버의 종류 토글은 유지한다.
- 공통 필드는 재사용하고 일정에는 시작·종료, 할 일에는 마감 하나만 보이는 구조로 나눈다.
- 저장된 `kind`를 기준으로 폼을 열고, 불일치 데이터는 자동 수정하지 않고 사용자에게 종류 확인을 알린다.
- 승인되면 할 일 폼에 `D-Day로 세기`를 넣어 메모 D-Day와 충돌하지 않는 명시적 연결 규칙을 적용한다.
- 기존 일정 수정·반복·알림 미러 일정의 읽기 전용 규칙을 보존한다.

#### 4단계 — D-Day·캘린더·겹침 정보 연계

- 지난 D-Day를 별도 묶고 목표 날짜·시각을 같은 줄에 표시하며 선택한 위치에 일괄 완료를 둔다.
- 캘린더 필터에 D-Day 체크 항목을 추가해 일정·할 일과 독립적으로 켜고 끈다.
- 일정/할 일 전체 편집 아래에 같은 날의 다른 일정을 간결하게 표시하고 겹치는 시간은 강조한다.
- 일정 포스트잇과 오늘 요약이 같은 항목을 과도하게 반복하지 않는지 최종 조정한다.

#### 5단계 — 회귀·실제 화면·기록 정리

- 새 조회·완료·설정·폼 분기 테스트와 기존 캘린더·일정·D-Day 테스트를 파일별 독립 프로세스로 실행한다.
- 실제 마우스로 날짜 연속 이동, 체크, 보기 전환, 스크롤, 일정/할 일 작성과 저장 항목 재열기를 확인한다.
- 좁은 창과 100%·125%·150% 배율에서 포스트잇·설정창·편집 폼 잘림과 겹침을 렌더로 확인한다.
- 테스트에서는 프로그램적 setter가 아니라 실제 클릭 신호 경로를 사용하고,
  `set_deadlines()` 뒤 `set_month()` 순서를 지킨다.
- 완료 결과만 `2._WORK_LOG.md`에 쓰고 끝난 항목은 `3._TODO.md`에서 제거한다.

5단계 확인 결과:

- 자정 경계, 연속 날짜 이동·오늘 복귀, 단축키 등록 후 해제,
  포스트잇 설정·위치 재실행 복원 경계 검사를 추가했다.
- 전체 `test_*.py` 자동 검사 **660개가 전부 통과**했고 `compileall` 검사도 통과했다.
- Windows 플랫폼에서 100%·125%·150% 배율의 포스트잇·설정창·일정 폼·할 일 폼을
  임시 DB로 렌더했다. 9번째 행에서만 포스트잇 세로 스크롤이 나타났고,
  설정 탭 내부 스크롤과 680px 좁은 편집 화면의 가로 스크롤은 나타나지 않았다.
- 날짜·보기·체크박스·일정/할 일 진입은 `QTest.mouseClick`의 실제 위젯 마우스
  이벤트와 신호 경로로 검증했다. 현재 Computer Use 환경은 브라우저만 제공하고
  Windows 앱 제어는 비활성이어서 물리 OS 포인터 자동화는 중복 실행하지 않았다.

### 테스트 및 실제 동작 검증

- 당일 경계, 자정, 지난·오늘·미래 D-Day, 우선도 동률과 항목 없음 상태를 임시 DB로 검사한다.
- 이전날·다음날 연속 이동과 가운데 날짜를 눌러 오늘 복귀, 오늘이 아닐 때 색상 변화를 검사한다.
- 체크 후 삭선/휴지통, 반복 항목 처리 범위, 취소 시 무변경과 지난 D-Day 유지 여부를 검사한다.
- 포스트잇 0·1·8·9건에서 실제 높이와 스크롤 표시를 확인한다.
- 단축키 빈 값 미등록, 사용자 지정 등록·충돌·해제와 재실행 후 설정 복원을 검사한다.
- 일정/할 일 신규 저장과 기존 항목 재열기, 할 일 종료 입력 부재, 빠른 팝오버 보존을 검사한다.
- D-Day 필터 조합과 같은 날 다른 일정·겹침 표시를 검사한다.
- 사용자 데이터 대신 복사본 또는 임시 DB만 사용한다.

### 제외 범위

- 빠른 일정 입력 팝오버 재구현 또는 제거.
- 지난 D-Day 자동 완료·자동 삭제와 사용자가 승인하지 않은 일정 자동 생성.
- 전체 캘린더·알림·D-Day 스키마 통합과 기존 데이터의 자동 종류 변환.
- 단축키 화면을 3단으로 바꾸는 전체 재설계.
- 설정창에 스크롤 추가.
- 공통 라이브러리 조회·변경과 사용자가 요청하지 않은 EXE 생성·배포.

### 현재 진행 상태

- `5._인수인계_2.md` 확인과 완료 기능의 실제 코드 대조: **완료**
- 전체 계획·중앙 TODO·기본 인수인계 반영: **완료**
- 관련 코드·문서 확인과 중복 없는 표시 역할표 작성: **완료**
- UI 시안 제작과 100%·125%·150% 설정창 격리 렌더: **완료**
- 사용자 선택과 1단계 시안 확정: **완료**
- 2단계 일정 표시 포스트잇: **완료**
- 3단계 전체 편집 폼 분리: **완료**
- 4단계 D-Day·캘린더 연계: **완료**
- 5단계 회귀·실제 화면·기록: **완료**
- EXE 생성: **제외 범위**

## 요약 정리

현재 추가 계획은 두 묶음이다. 24번은 제목1~4, 기능 항목 순서 끌기와 줄앞 입력 설정을 다루고,
25번은 일정 표시 포스트잇, 일정/할 일 전체 편집 분리와 D-Day·캘린더 연계를 다룬다.
두 계획 모두 기존 메모·일정·빠른 팝오버와 사용자 데이터를 보존한다.
25번 일정·D-Day 추가 업데이트는 1~5단계를 모두 완료했고, 임시 DB 기반
전체 자동 검사는 **660개 통과·실패 0개**다. Windows 100%·125%·150%와 680px 좁은 화면
렌더에서 요청한 포스트잇 스타일, 폼 분리, 스크롤 조건과 겹침 표시를 확인했다.

## 추천 모델

- `gpt-5.6-sol + high`: 문서 서식·동적 입력 규칙과 일정 포스트잇·폼 분리·D-Day 연계를
  여러 모듈에 걸쳐 구현하고 Qt·데이터 회귀를 추적해야 하는 고난도 작업에 적합

## 추천 SKILL

- `ui-ux-pro-max` — 추천도: ★★★★☆ (4/5): 기능·설정 팝업의 정보 구조, 길게 누르기와
  placeholder 피드백, 일정 포스트잇·분리 폼과 좁은 창 반응형 배치 검토에 적용. 사용자 승인 시에만 사용
- `computer-use:computer-use` — 추천도: ★★★★☆ (4/5): 실제 Windows 창에서 길게 누르기,
  포스트잇 날짜 이동·체크·스크롤과 일정/할 일 폼 전환을 사람 입력 경로로 검증할 때 적용.
  사용자 승인 시에만 사용
