# TomaDesk 메모 편집 기능 대규모 개선계획

작성일: 2026-09-11
상태: 전체 계획 유지·단계별 승인 진행. 잔류 링크 보완 및 Phase 0~3 완료, Phase 4 승인 대기

## 1. 추진 배경과 목표

현재 메모 편집기는 `QTextEdit/QTextDocument`의 HTML 저장 구조 위에 토글·체크리스트·제목·페이지·메모 링크·표·그림·블록 이동 기능을 단계적으로 추가해 왔다. 개별 기능은 동작하지만, 복사·다중 선택·공통 명령·지속 가능한 블록 식별자 없이 기능별 처리가 편집기 한 클래스에 모여 있어 대규모 확장 전에 공통 경계를 정리할 필요가 있다.

이번 개선의 목표는 기존 메모·페이지·링크·백업 호환성을 보존하면서 다음을 단계적으로 제공하는 것이다.

- 토글 붙여넣기 빈 줄, 잔류 내부 링크, Undo/Redo 같은 현재 결함을 먼저 안정화한다.
- 복제와 참조 링크의 의미를 분리하고, 블록 명령을 한 Undo 단위로 실행한다.
- 선택 범위에 적용된 동작을 우클릭의 `연동 기능 삭제`로 안전하게 해제한다.
- 이후 목차·블록 링크·백링크·주석·템플릿·버전 기록이 같은 기반을 재사용하게 한다.

## 2. 현재 코드베이스 분석

### 2.1 주요 파일과 역할

| 영역 | 현재 파일·클래스 | 현재 역할 |
| --- | --- | --- |
| 본문 편집 | `alert_notes/rich_memo_edit.py`의 `RichMemoTextEdit` | 리치 텍스트, 토글, 체크리스트, 제목, 표, 그림, 페이지/메모 링크, 붙여넣기, 줄 이동, 마우스·키보드 이벤트 |
| 저장 변환 | `alert_notes/rich_text.py` | 일반 텍스트/HTML 판별, `QTextEdit.toHtml()` 저장, 위험 HTML 정리 |
| 구조 가져오기 | `alert_notes/structured_import.py` | Markdown/HTML/TXT를 정리된 HTML로 변환 |
| 삽입 명령 | `alert_notes/insert_menu.py`, `insert_panel.py`, `insert_preferences.py` | 기능 목록, `/` 메뉴, 단축키, 줄 앞 입력, 사용자 순서·트리거 설정 |
| 줄 손잡이 | `alert_notes/line_gutter.py` | 한 블록 또는 토글 가족의 드래그 시작·위치 표시·놓기 |
| 편집 화면 | `alert_notes/editor.py`, `standalone_editor.py`, `postit.py` | 공용 편집기 배치, 자동 저장, 메인/독립/포스트잇 연결 |
| 메모 목록 | `alert_notes/memo_list.py`, `panel.py` | 체크 선택, 트리, 고정, 하위 메모, 드래그 이동, 삭제·복원 |
| 데이터 | `alert_notes/sqlite_store.py` | `notes`, 첨부, 알림, 계층, 휴지통, 설정 저장 |
| 백업·복원 | `alert_notes/memo_archive.py`, `database_bundle.py` | 전체 메모 아카이브, ID 재매핑, 병합·교체 복원 |
| 서식 | `alert_notes/text_format_toolbar.py`, `format_presets.py` | 문자 서식과 서식 프리셋 |
| 회귀 검사 | `test_memo_toggle.py`, `test_memo_pages_stage4.py`, `test_memo_blocks_stage5.py`, `test_memo_drag_stage6.py`, `test_memo_stage7.py`, `test_memo_stage8.py`, `test_memo_management.py`, `test_memo_import_archive.py` | 토글·페이지·블록·붙여넣기·링크·목록·백업 검사 |

### 2.2 내부 표현과 데이터 흐름

- 메모 본문은 `notes.content` 한 칸에 일반 텍스트 또는 Qt HTML 전체를 저장한다. 별도 블록 테이블은 없다.
- 문단은 `QTextBlock`이며 제목 단계는 `QTextBlockFormat.UserProperty`, 그림 크기는 `QTextCharFormat.UserProperty`에 저장된다.
- 토글·체크리스트·강조 상자·인용문·코드·구분선은 줄 앞 표식과 문단 들여쓰기를 사용한다. 토글 접힘은 하위 블록의 `visible` 상태를 다시 계산한다.
- 페이지와 메모 링크는 모두 `toma-note://<note_id>`를 사용한다. `📄`는 본문에 포함된 하위 메모(`embedded=1`), `🔗`는 기존 메모 참조라는 표시문자로 의미를 구분한다.
- 페이지 줄이 문서에서 사라지면 `sync_page_titles()`가 해당 하위 페이지를 휴지통으로 보낸다. 메모 링크 삭제는 대상을 삭제하지 않는다.
- 복사·붙여넣기는 현재 Qt 표준 MIME/HTML을 사용하며 TomaDesk 전용 구조 직렬화 형식은 없다. HTML 안의 페이지 ID를 그대로 붙이면 원본과 같은 페이지를 참조할 위험이 있다.
- 토글의 빈 안내 줄 붙여넣기는 `_paste_into_lone_empty_toggle()`과 `_keep_pasted_blocks_inside_toggle()`의 별도 경로를 사용한다.
- 문서 내부 변경은 `QTextCursor.beginEditBlock()/endEditBlock()`으로 일부 묶이며 Qt 문서 Undo 스택을 사용한다. DB 생성·페이지 휴지통·다른 메모로 이동 같은 문서 밖 변경은 같은 Undo 스택에 완전히 포함되지 않는다.
- 줄 이동은 문단 위치를 임시 식별자로 사용해 HTML 조각을 잘라 재삽입한다. 현재 지속 가능한 `block_id`는 없다.
- 메모 목록은 체크된 ID와 현재 행을 알지만 Ctrl+C/Ctrl+V 복제 의미나 클립보드 상태는 없다.

### 2.3 재사용할 기존 기반

- `_selected_blocks()`, `_block_family()`, 들여쓰기·접두 표식 판정, `beginEditBlock()`을 범위 명령의 기초로 재사용한다.
- `INSERT_ITEMS`의 명령 메타데이터를 `/` 메뉴·설정·우클릭·부동 도구막대가 공유하도록 확장한다.
- `memo_archive.py`의 메모·첨부·내부 ID 재매핑 방식을 페이지/메모 복제 서비스에 축소 재사용한다.
- `NoteReminderStore.can_reparent()`, 휴지통·복원, 첨부 정리, 문서 레지스트리를 유지한다.
- `TextFormatToolbar.format_data()/apply_data()`를 Alt+C 문자 서식 복사의 기준으로 사용한다.

### 2.4 새 공통 기반이 필요한 부분

- `BlockSelectionManager`: 연속 텍스트 선택과 Alt 기반 비연속 블록 선택을 분리하고, 선택 블록·가족 범위·화면 강조를 제공한다.
- `BlockCommandDispatcher`: 이동·복제·변환·묶기·들여쓰기·삭제·`연동 기능 삭제`를 동일한 대상 해석과 Undo 정책으로 실행한다.
- `ClipboardPayload`: 버전이 붙은 TomaDesk 전용 MIME과 HTML/텍스트 대체값을 함께 제공하고 `deep-copy`와 `reference-copy`를 명시한다.
- `NoteCloneService`: 메모·페이지·첨부·하위 페이지를 트랜잭션으로 복제하고 새 ID를 재매핑한다.
- 영구 `block_id`: Phase 4부터 제목·블록 링크·고정·주석·목차가 공유한다. Phase 2의 화면 내 선택은 문서 위치 기반으로 시작하되 외부에 위치값을 저장하지 않는다.
- `DocumentAnchorResolver`: `memo_id + block_id`와 선택 원문·앞뒤 문맥으로 블록 링크와 주석 범위를 복구한다.
- 새 추상화는 두 기능 이상이 실제로 공유할 때만 도입하고 `RichMemoTextEdit`의 기존 작은 변환 함수는 유지한다.

## 3. 요구사항과 현재 구현의 충돌·중복

| 요구사항 | 현재 상태 | 계획상 처리 |
| --- | --- | --- |
| 토글 붙여넣기 빈 줄 | 빈 토글 전용 보정은 있으나 모든 혼합 구조 조합을 보장하지 않음 | Phase 0에서 원문 문단 경계 기준으로 집중 회귀 |
| 잔류 내부 링크 | 일반 본문에도 `toma-note://53`이 남아 클릭되는 실제 데이터 확인 | Phase 0에서 유효 표식 기반 판정과 발생 차단 |
| Redo | Qt Undo 기반은 있으나 두 Redo 단축키와 DB 연계 명령 보장이 없음 | Phase 0에서 문서 명령 확인, 이후 공통 명령으로 확대 |
| 블록 선택 | `_selected_blocks()`는 연속 선택만 지원 | Phase 2에서 비연속 선택 관리자 추가 |
| 블록 이동 | 한 줄/토글 가족 드래그는 구현됨 | 다중 이동·Alt+위/아래가 기존 이동 코어 재사용 |
| 블록 유형 변환 | 개별 함수가 흩어져 있음 | 공통 명령 메타데이터로 통합 |
| 페이지 복제 | HTML의 기존 note ID가 참조로 남을 수 있음 | 전용 MIME과 깊은 복제 ID 재매핑 |
| 메모 목록 복제 | 미구현 | 안전한 속성 정책의 `NoteCloneService` 사용 |
| 붙여넣기 방식 | Ctrl+V와 Ctrl+Shift+V 일부 구현 | 세 가지 정책과 메뉴 이름을 명시적으로 분리 |
| 제목 접기/목차 | 제목 스타일은 있으나 구조 인덱스 없음 | Phase 3 접기, Phase 4 영구 ID·목차 |
| `연동 기능 삭제` | 표준 우클릭 메뉴 외 별도 범위 명령 없음 | Phase 2의 공통 선택·명령 엔진에 포함 |
| 백링크·주석·템플릿·버전 | 미구현 | 영구 ID 이후 Phase 5~7에서 구현 |

## 4. 공통 명령: `연동 기능 삭제`

### 4.1 사용자 동작

1. 사용자가 삭제할 범위를 마우스로 드래그하거나 비연속 블록을 선택한다.
2. 선택 영역에서 마우스 오른쪽 버튼을 누른다.
3. 메뉴에서 `연동 기능 삭제`를 선택한다.
4. 선택 범위에 적용된 동작 속성을 한 번에 해제하고, Ctrl+Z 한 번으로 전체를 복원한다.

### 4.2 안전한 기본 규칙

- 보존: 사용자가 보는 글자, 일반 굵게·기울임·글자색·글꼴·크기, 이미지와 표 안의 내용.
- 해제: 토글과 접힘, 체크 상태, 글머리표, 제목 단계와 제목 접힘, 강조 상자, 인용문, 코드 블록, 구분선 기능, 내부/외부 링크, 블록 고정, 이후 추가되는 주석 연결.
- 선택이 블록의 일부만 걸쳐도 문자 링크·주석은 정확한 선택 범위만 해제한다. 토글·제목·체크리스트처럼 블록 단위인 기능은 해당 블록 전체가 선택 대상임을 메뉴 설명에 표시한다.
- 토글을 해제할 때 숨은 자식부터 펼치고 선택된 토글의 표식과 구조 들여쓰기를 제거한다. 선택 밖의 부모·자식 구조는 유지한다.
- `📄 페이지`는 표시문자를 일반 제목으로 바꾸되 하위 페이지 데이터를 삭제하지 않는다. 해당 페이지는 `embedded=0`인 일반 하위 메모로 전환하여 목록에서 계속 접근할 수 있게 한다.
- `🔗 메모 링크`와 외부 URL은 대상 메모·웹 대상을 건드리지 않고 표시문자만 일반 글로 바꾼다.
- 표 자체와 이미지는 콘텐츠이므로 이 명령으로 삭제하거나 평면화하지 않는다. 별도 `삭제` 명령만 내용을 지운다.
- 실행 전 `토글 2, 페이지 1, 링크 3…`처럼 해제 대상을 계산한다. 페이지가 포함되면 “페이지 내용은 보존되고 일반 하위 메모로 전환됩니다”를 명확히 알린다.

### 4.3 구현상 주의

- 현재 페이지 줄 제거는 자동으로 페이지를 휴지통으로 보내므로, 연결 해제 중에는 먼저 저장소에서 `embedded=0` 전환과 `_known_pages` 동기화를 끝낸 뒤 문서의 링크 속성을 제거해야 한다.
- 문서 변경과 페이지 상태 변경은 하나의 작업 객체로 기록하여 Undo 시 `embedded`, 링크 HTML, 선택·커서·스크롤을 함께 복원한다.
- `연동 기능 삭제`와 `내용 삭제`, `서식 지우기`, 페이지 메뉴의 `링크만 삭제`는 서로 다른 명령으로 유지한다.

## 5. 기능 의존성 및 최종 권장 Phase

```text
Phase 0 안정화
  └─ Phase 1 클립보드·복제 계약
       └─ Phase 2 선택·공통 명령·우클릭·연동 기능 삭제
            ├─ Phase 3 접기·편집·키보드 사용성
            └─ Phase 4 영구 block_id·목차·블록 링크·고정
                 └─ Phase 5 백링크·주석
                      ├─ Phase 6 템플릿
                      └─ Phase 7 버전 기록
```

사용자 제안 순서를 대체로 유지하되, `block_id`는 Phase 2 선택의 선행 조건으로 강제하지 않는다. 화면 안에서만 쓰는 선택에는 문서 위치를 쓰고, 저장·외부 참조가 필요한 Phase 4부터 영구 ID를 도입해 초기 위험을 줄인다.

## 6. 단계별 상세 계획

### Phase 0. 현재 결함 안정화와 Redo

진행 상태(2026-09-11): 토글 내부 붙여넣기 빈 줄과 Ctrl+Y/Ctrl+Shift+Z Redo, 일반 본문의 잔류 내부 링크 수정까지 구현·검증 완료했다.

1. 목표: 토글 붙여넣기 개행, 일반 본문 잔류 내부 링크, Ctrl+Y/Ctrl+Shift+Z를 먼저 안정화한다.
2. 선행 조건: 현재 713개 전체 테스트와 사용자 메모 읽기 전용 기준을 보존한다.
3. 예상 파일: `rich_memo_edit.py`, `rich_text.py`, `insert_menu.py`, 관련 `test_memo_*.py`.
4. 공통 엔진: 신규 대형 엔진 없이 링크 구간 판정과 붙여넣기 경계 도우미만 분리한다.
5. DB: 변경 없음. 사용자 DB 직접 정리 없음.
6. Undo/Redo: 붙여넣기와 잔류 링크 정리는 각 1회 Undo. Qt Redo 단축키를 명시적으로 검사한다.
7. 복사/붙여넣기: 원본 줄바꿈과 원래 빈 줄만 유지하며 토글 안내 줄을 중복 삽입하지 않는다.
8. 링크 호환: `📄`·`🔗`로 시작하는 실제 링크만 클릭 가능하고 일반 글의 잔류 URL 속성은 제거한다.
9. UI: 새 UI 없음. 손가락 커서는 유효 링크에서만 표시한다.
10. 조작: Ctrl+Z, Ctrl+Y, Ctrl+Shift+Z 모두 문서 편집에 적용한다.
11. 자동 테스트: 첨부 요구의 토글 붙여넣기 10종, 정상/잔류 링크, Redo 왕복.
12. Windows 수동 테스트: 실제 Qt 백엔드에서 링크 hover/click, 토글 안 붙여넣기, Undo/Redo.
13. 회귀: 페이지 생성·삭제·복원, 표 뒤 입력, 토글 접힘·스크롤, 구조 가져오기.
14. 위험: 보이지 않는 링크 문자 서식과 Qt HTML 문단 경계.
15. 난이도: 중.
16. 완료 기준: 원문 줄바꿈 일치, 잔류 링크 클릭 불가, 정상 링크 유지, 두 Redo 단축키와 전체 회귀 통과.

### Phase 1. 클립보드 계약·독립 복제·서식 복사

확정 사항(2026-09-11): 페이지 내부의 모든 하위 페이지를 새 note ID로 깊게 복제한다. 본문·서식·카테고리는 복제하고 알림·D-Day·반복·단축키는 초기화한다. 알림 동시 복제는 후속 선택 옵션으로 남긴다. 앱 내부 복사는 전용 MIME으로 구조를 완전 보존하고 외부 앱에는 HTML·텍스트 대체값을 함께 제공한다. 문서와 DB 변경은 하나의 Undo/Redo 작업 단위로 묶는다.

진행 상태(2026-09-11): 확정 범위를 구현·검증 완료했다. 기존 계획의 Phase 순서와 후속 범위 변경은 필요 없다. Phase 2는 착수하지 않았다.

1. 목표: 참조 복사와 독립 복제를 명시적으로 분리한다.
2. 선행 조건: Phase 0의 붙여넣기 경계와 Undo 안정화.
3. 예상 파일: `rich_memo_edit.py`, 신규 `memo_clipboard.py`, `note_clone_service.py`, `memo_list.py`, `panel.py`, `text_format_toolbar.py`, `memo_archive.py` 일부 재사용.
4. 공통 엔진: 버전형 `ClipboardPayload`, `NoteCloneService` 필요.
5. DB: 기본 schema 변경 없음. 복제 시 기존 테이블에 새 note/attachment ID를 생성한다.
6. Undo/Redo: 문서 블록 복제는 1회 Undo. DB 메모/페이지 복제는 보상 삭제/복원 명령을 함께 기록한다.
7. 복사/붙여넣기: 전용 MIME `application/x-tomadesk-blocks+json`에 구조·서식·첨부 참조·복사 정책을 기록하고 HTML/텍스트 대체값을 함께 제공한다.
8. 링크 호환: `🔗`는 기존 대상 ID 유지, `📄` 페이지는 새 ID와 하위 페이지 ID를 재귀 재매핑한다. 순환·누락 ID는 중단하고 알린다.
9. UI: 붙여넣기 옵션과 메모 복제 결과 이름(`원본 제목 복사본`, 중복 시 번호)을 표시한다.
10. 조작: 본문 Ctrl+C/V는 블록 payload, 목록 Ctrl+C/V는 선택 메모 복제, Alt+C는 선택이 없으면 문자 서식 저장·선택이 있으면 적용.
11. 자동 테스트: 한/다중 블록, 토글·페이지·첨부·표 혼합, 메모 목록 단건/다건 복제, ID 독립성, Alt+C 충돌.
12. Windows 수동 테스트: 본문/목록 포커스에 따라 Ctrl+C/V가 정확히 분리되는지 확인.
13. 회귀: 기존 OS 클립보드 상호운용, 외부 HTML 붙여넣기, 메모 링크 참조, 전체 백업.
14. 위험: 페이지와 첨부 ID 재매핑, 문서 Undo와 DB 트랜잭션의 경계.
15. 난이도: 상.
16. 완료 기준: 복제본을 수정해도 원본 무변경, 링크는 같은 대상 유지, 실패 시 중간 복제 데이터 없음.

복제 속성 기본안:

- 복사: 제목·본문·서식·첨부·토글·페이지 내부의 모든 하위 페이지·색상/투명도·카테고리(기능이 먼저 구현된 경우). 모든 복제 note와 첨부는 새 ID를 받는다.
- 초기화: 알림·알림 이력·D-Day·월간 반복·단축키·포스트잇 표시/시작 상태·고정·휴지통 상태·수동 정렬 위치. 알림 복제는 이번에 넣지 않고 후속 선택 옵션으로 남긴다.
- 부모 관계: 목록에서 복제하면 원본과 같은 부모의 다음 위치에 새 독립 메모로 둔다. 선택한 하위 트리는 내부 상대 계층을 유지한다.

### Phase 2. 다중 선택·공통 명령·우클릭 메뉴

진행 상태(2026-09-12): 승인 범위를 구현·검증 완료했다. 비연속 선택과 공통 명령 경로, 부동 도구막대, 대상별 우클릭, 마우스·키보드·버튼 이동, 구조 MIME 복사·독립 복제, 변환·묶기·들여쓰기·삭제·연동 기능 삭제를 반영했다. 페이지 연결 해제는 메모를 보존하고 기존 `embedded` 값만 복합 Undo/Redo로 전환한다. Phase 3은 착수하지 않았다.

1. 목표: 비연속 블록 선택과 모든 블록 조작의 단일 실행 경로를 만든다.
2. 선행 조건: Phase 1의 구조 payload와 복제 서비스.
3. 예상 파일: `rich_memo_edit.py`, `line_gutter.py`, `insert_menu.py`, 신규 `block_selection.py`, `block_commands.py`, `block_action_bar.py`.
4. 공통 엔진: `BlockSelectionManager`, `BlockCommandDispatcher` 필요.
5. DB: 페이지 연결 해제를 위한 `embedded` 상태 변경만 사용하며 schema 변경 없음.
6. Undo/Redo: 이동·복제·변환·묶기·들여쓰기·삭제·연동 기능 삭제 각각 1회 Undo.
7. 복사/붙여넣기: 다중 선택을 문서 순서대로 payload에 담고 붙여넣을 때 상대 순서·들여쓰기 유지.
8. 링크 호환: 페이지 깊은 복제와 링크 참조를 Phase 1 규칙으로 호출. 연결 해제는 대상 데이터를 보존.
9. UI: 선택 개수 부동 도구막대, 대상별 우클릭 메뉴, 페이지/링크 전용 하위 메뉴.
10. 조작: Alt+클릭/드래그로 추가 선택, Alt+위/아래 이동, 드래그와 위/아래 버튼을 함께 제공. 일반 텍스트 선택 중 Alt를 누르기 전에는 기존 선택을 유지한다.
11. 자동 테스트: 떨어진 선택, 겹침·역방향 선택, 토글 가족, 이동 순서, 모든 변환, 묶기, `연동 기능 삭제` 부분/전체 범위.
12. Windows 수동 테스트: 부동 막대 위치, 우클릭 대상 판정, 좁은 창과 100/125/150% 배율.
13. 회귀: 글자 드래그 선택, 표 셀 선택, 줄 손잡이, `/` 메뉴, 자동 저장.
14. 위험: Qt 기본 선택과 커스텀 선택 충돌, 숨은 토글 자식, 페이지 연결 해제의 데이터 보존.
15. 난이도: 매우 상.
16. 완료 기준: 마우스/키보드 양쪽으로 동일 명령 실행, 선택 순서 보존, 한 번의 Undo, 페이지·메모 데이터 손실 없음.

### Phase 3. 접기·편집·키보드 사용성

진행 상태(2026-09-14): 사용자 승인 범위를 구현·검증 완료했다. Qt 사용자 block property가 HTML에 직렬화되지 않아 제목 접힘 표시 `▶ `를 HTML에 보존하고 문서 안에서는 property를 병행했다. Phase 4는 착수하지 않았다.

1. 목표: 범위 토글, 전체 접기/펴기, 제목 접기, 줄 간격, 세 가지 붙여넣기, 키보드 탐색을 제공한다.
2. 선행 조건: Phase 2 선택·명령 체계.
3. 예상 파일: `rich_memo_edit.py`, `editor.py`, `text_format_toolbar.py`, `insert_menu.py`, `editor_shortcut_settings.py`.
4. 공통 엔진: 기존 명령 디스패처와 구조 범위 계산 재사용.
5. DB: 제목 접힘을 HTML block property로 저장하면 schema 변경 없음.
6. Undo/Redo: 범위 묶기·제목 변환·줄 간격·붙여넣기 각각 1회 Undo. 단순 접힘 UI 상태는 문서 내용 Undo와 분리.
7. 복사/붙여넣기: Ctrl+V 원본 서식, Ctrl+Shift+V 텍스트만, 메뉴에서 현재 서식에 맞추기를 제공한다.
8. 링크 호환: 링크 위 키보드 열기는 유효 링크 판정만 사용한다.
9. UI: 줄 간격 입력, 모두 접기/펴기, 제목 접힘 표시, 붙여넣기 방식 메뉴.
10. 조작: 페이지/메모 링크 열기와 토글 접기 단축키는 Enter 편집 동작과 겹치지 않는 조합을 설정 화면에서 검증한다.
11. 자동 테스트: 제목 계층 범위, 중첩 토글, 줄 간격 혼합 선택, 세 붙여넣기 방식, 키보드 진입/나오기.
12. Windows 수동 테스트: 접힌 범위 표시와 키보드만으로 이동 가능한지 확인.
13. 회귀: 기존 Ctrl+Enter 알림 저장, Ctrl+Shift+E 전체 토글, 표 Tab, 입력 트리거.
14. 위험: 제목/토글 접힘 범위 중첩과 단축키 충돌.
15. 난이도: 상.
16. 완료 기준: 모든 기능에 마우스와 키보드 경로가 있고 접힘·줄 간격·붙여넣기가 재실행 후 유지된다.

### Phase 4. 영구 block_id·목차·블록 링크·고정

1. 목표: 제목 변경과 문단 이동에도 유지되는 탐색·직접 링크 기반을 만든다.
2. 선행 조건: Phase 2 명령이 새 블록 생성/복제 시 ID를 갱신할 수 있어야 한다.
3. 예상 파일: 신규 `block_identity.py`, `outline_model.py`, `outline_panel.py`; `rich_memo_edit.py`, `editor.py`, `memo_archive.py`.
4. 공통 엔진: UUID 기반 block ID와 `memo_id + block_id` resolver 필요.
5. DB: 우선 block ID는 HTML block property에 저장한다. 검색 성능상 필요할 때만 별도 `memo_block_index` 파생 테이블을 추가하며 원본은 HTML이다.
6. Undo/Redo: 새/복제 블록 ID 부여와 구조 변경을 같은 명령에 포함한다.
7. 복사/붙여넣기: 독립 복제는 새 block ID, 참조 링크는 기존 대상 block ID 유지.
8. 링크 호환: 기존 `toma-note://id`를 계속 읽고 새 블록 링크는 버전형 주소를 추가한다.
9. UI: 우측 상단 구조 패널, 제목 계층·페이지·토글·고정 블록 목록과 현재 섹션 강조.
10. 조작: 목차 클릭·키보드 이동, 블록 링크 복사, 고정 토글.
11. 자동 테스트: ID 생성·중복 방지·복제 재매핑·제목 변경·이동·삭제 후 링크 상태.
12. Windows 수동 테스트: 긴 문서에서 현재 섹션 강조와 스크롤 이동.
13. 회귀: 구형 HTML을 열 때 보이는 내용과 수정 상태가 불필요하게 바뀌지 않는지 확인.
14. 위험: Qt가 임의 UserProperty를 HTML 왕복에서 보존하는지 버전별 확인 필요.
15. 난이도: 매우 상.
16. 완료 기준: 재실행·이동·제목 변경 후 같은 블록으로 이동하고 구형 메모/백업을 그대로 읽는다.

### Phase 5. 백링크와 주석

1. 목표: 메모/블록 참조 역방향 탐색과 텍스트·문단 주석을 제공한다.
2. 선행 조건: Phase 4 block ID와 anchor resolver.
3. 예상 파일: 신규 `backlink_index.py`, `annotation_store.py`, `annotation_popover.py`; `sqlite_store.py`, `editor.py`, `rich_memo_edit.py`, `memo_archive.py`.
4. 공통 엔진: 링크 인덱서와 문맥 기반 anchor 복구 필요.
5. DB: `memo_annotations` 테이블과 필요 시 재생성 가능한 `memo_backlinks` 인덱스. 마이그레이션 전 자동 DB 백업.
6. Undo/Redo: 주석 추가·수정·삭제는 보상 명령으로 복원. 백링크는 링크 변경에서 파생 재계산.
7. 복사/붙여넣기: 독립 복제 시 주석은 새 block ID로 복제, 외부 참조 백링크는 새 링크에 따라 재색인.
8. 링크 호환: 기존 메모 링크를 메모 단위 백링크에 포함하고 새 블록 링크는 세부 위치까지 표시.
9. UI: 백링크 접이식 목록, 파란 점선 주석 범위, 오른쪽 여백 💬, hover/고정 팝오버.
10. 조작: 선택 우클릭 주석 추가, 빈 문단 우클릭 주석 추가, Esc/바깥 클릭 닫기.
11. 자동 테스트: 편집 전후 anchor 복구, 중복 문장, 블록 삭제, 백링크 증가·감소·복원.
12. Windows 수동 테스트: 팝오버 위치·hover·클릭 고정·좁은 창.
13. 회귀: 하이퍼링크와 주석 표시·클릭 판정이 겹치지 않는지 확인.
14. 위험: 텍스트 대량 수정 후 범위 재부착의 모호성.
15. 난이도: 매우 상.
16. 완료 기준: 일반 편집 후 주석이 합리적으로 따라가고 모호하면 잘못 붙이지 않고 `위치 확인 필요`로 표시한다.

### Phase 6. 블록 템플릿

1. 목표: 선택 블록/메모를 재사용 가능한 구조 템플릿으로 저장·삽입한다.
2. 선행 조건: Phase 1 payload, Phase 2 명령, Phase 4 ID 재생성 규칙.
3. 예상 파일: 신규 `memo_templates.py`, `template_dialog.py`; `insert_menu.py`, `rich_memo_edit.py`, `sqlite_store.py`, `memo_archive.py`.
4. 공통 엔진: 기존 clipboard payload 재사용.
5. DB: 템플릿 이름·호출어·버전형 payload 테이블. 백업 형식 확장.
6. Undo/Redo: 템플릿 삽입 전체 1회 Undo.
7. 복사/붙여넣기: 삽입은 항상 독립 복제 규칙이며 내부 ID를 새로 만든다.
8. 링크 호환: 참조 링크만 기존 대상 유지.
9. UI: 템플릿 등록/관리와 `/template`·`/템플릿` 호출. `@`는 일반 입력과 충돌 가능성이 있어 기본 호출로 쓰지 않는다.
10. 조작: 키보드 검색·선택과 관리창 위/아래 순서 버튼.
11. 자동 테스트: 호출어 중복, 구조·서식·첨부·페이지 삽입, 삭제·복원.
12. Windows 수동 테스트: 팝업 위치와 키보드 완결성.
13. 회귀: 기존 `/` 메뉴와 사용자 지정 입력 트리거.
14. 위험: 오래된 payload 버전과 페이지 깊은 복제.
15. 난이도: 상.
16. 완료 기준: 템플릿 삽입본이 원본과 독립이고 구버전 템플릿을 안전하게 거부/변환한다.

### Phase 7. 버전 기록

1. 목표: 자동 저장과 별개인 제한된 메모 스냅샷·비교·복원을 제공한다.
2. 선행 조건: 앞 단계의 문서/페이지/첨부 직렬화 규칙 확정.
3. 예상 파일: 신규 `memo_versions.py`, `version_history_dialog.py`; `sqlite_store.py`, `memo_archive.py`, `editor.py`.
4. 공통 엔진: 버전 payload와 복원 트랜잭션 필요.
5. DB: `memo_versions`와 필요 시 버전 첨부 참조 테이블. 마이그레이션 전 자동 백업.
6. Undo/Redo: 복원 직전 현재 상태를 새 버전으로 저장하여 재복원 가능하게 한다.
7. 복사/붙여넣기: 버전에는 저장 당시 구조 payload를 보존하되 OS 클립보드와 분리한다.
8. 링크 호환: 복원 후 존재하지 않는 내부 ID는 사전 검증하고 사용자에게 영향 범위를 보여 준다.
9. UI: 버전 목록·미리보기·차이·복원 확인.
10. 조작: 수동 중요 버전과 정책 기반 자동 버전. 매 키 입력마다 스냅샷하지 않는다.
11. 자동 테스트: 보존 주기·용량 제한·복원 전 스냅샷·첨부·페이지 ID 무결성.
12. Windows 수동 테스트: 긴 문서 비교와 복원 확인 흐름.
13. 회귀: 자동 저장 지연, DB 종료, 백업·복원.
14. 위험: 저장 용량 증가와 부분 복원 실패.
15. 난이도: 매우 상.
16. 완료 기준: 중단/실패 시 현재 문서가 유지되고 복원 전 상태로 다시 돌아갈 수 있다.

## 7. DB/schema와 마이그레이션 예상

- Phase 0~3: schema 변경 없이 진행한다. 페이지 연결 해제는 기존 `embedded` 값을 사용한다.
- Phase 4: block ID는 우선 HTML 속성으로 저장한다. 기존 문서를 열 때 ID가 없으면 메모리에서 부여하고 실제 사용자 편집·저장 시 함께 저장하는 지연 마이그레이션을 우선한다.
- Phase 5: `memo_annotations` 추가가 필요하다. 백링크는 원본 링크에서 재생성 가능한 인덱스로 설계한다.
- Phase 6: 템플릿 테이블 추가와 `.tomamemo` 형식 버전 상향이 필요하다.
- Phase 7: 버전·버전 첨부 테이블 추가와 보존 한도 설정이 필요하다.
- 모든 schema 변경 단계는 기존 DB의 다음 번호 백업을 먼저 만들고, 구형 전체 백업 읽기를 유지하며, 병합 복원에서 note/block/attachment ID를 재매핑한다.

## 8. 테스트 전략

- 단위 검사: 블록 범위 계산, 링크 유효성, ID 생성·재매핑, 명령별 전후 상태.
- 명령 검사: 각 복합 작업이 Ctrl+Z 한 번으로 되돌고 Redo로 동일하게 복원되는지 확인한다.
- 데이터 검사: 임시 DB로 복제·휴지통·복원·마이그레이션·실패 rollback·백업 왕복을 확인한다.
- 문서 검사: plain/HTML, 표·그림·토글·페이지·링크·혼합 서식, 빈 문단·긴 문서·한글 경로를 포함한다.
- 포커스 검사: 본문과 메모 목록의 Ctrl+C/V가 서로 침범하지 않는지 확인한다.
- 실제 사용자 자료: 읽기 전용 연결과 복제 문서에서만 확인하며 원본 사용자 DB를 테스트 입력으로 수정하지 않는다.
- Windows 검사: `QT_QPA_PLATFORM=windows`, 100/125/150% 배율, 넓은/좁은 창, 메인/독립/포스트잇 편집기를 구분한다.
- 전체 회귀: 각 Phase 완료 때 기존 전체 pytest를 통과하고, 실제 창 검증과 소스 테스트·EXE 검증을 구분해 기록한다.

## 9. 고위험 항목

1. 페이지 복제·연결 해제: 현재 페이지 줄 삭제가 하위 페이지 휴지통과 연결되어 있어 순서가 틀리면 데이터가 사라져 보일 수 있다.
2. 문서 Undo와 DB 작업 결합: Qt 문서 스택만으로는 새 페이지·첨부·메모 행을 되돌릴 수 없어 보상 트랜잭션이 필요하다.
3. 영구 block ID: Qt HTML 직렬화에서 사용자 속성이 유실되면 링크·주석이 끊길 수 있다.
4. 비연속 선택: Qt 기본 텍스트 선택·표 셀 선택·IME 입력과 충돌할 수 있다.
5. 깊은 복제: 페이지 하위 트리, 첨부, 내부 링크의 새 ID/기존 ID 구분이 틀리면 원본과 복제본이 다시 연결된다.
6. 주석 anchor: 문장 편집 후 동일 문구가 여러 곳에 있으면 잘못 재부착할 위험이 있다.
7. 버전 기록: 첨부 중복으로 DB가 빠르게 커지고 복원 도중 실패하면 여러 테이블 정합성이 깨질 수 있다.

## 10. 승인 전에 확인할 결정

별도 답이 없으면 아래 권장 기본안으로 진행한다.

- `연동 기능 삭제`는 글자와 일반 문자 서식을 보존하고 구조·동작·연결만 해제한다.
- 페이지 연결 해제 시 페이지를 삭제하지 않고 일반 하위 메모로 전환한다.
- 표와 이미지는 기능 해제 대상이 아니라 콘텐츠로 간주하여 유지한다.
- 메모 복제 시 알림·D-Day·반복·단축키·포스트잇 표시·고정은 초기화한다.
- Alt+C는 문자 서식만 복사하며 문단 유형·들여쓰기·줄 간격은 복사하지 않는다.
- block ID는 UUID 계열을 쓰고, 기존 문서는 지연 마이그레이션한다.
- 템플릿 기본 호출은 `@`가 아니라 `/template`과 `/템플릿`을 사용한다.

## 11. Phase 0 구현 전 체크리스트

- [x] 전체 상세 계획을 최종 목표로 유지하고 단계별로 승인받기로 확인했다.
- [ ] `연동 기능 삭제`의 보존/해제 범위와 페이지 데이터 보존 규칙을 확인한다.
- [x] 현재 소스 변경분을 보존하고 Phase 0 제한 범위 테스트 결과를 기록했다.
- [x] 사용자 DB·기존 EXE·백업 파일을 수정하지 않았다.
- [x] Phase 0 제한 범위를 DB/schema 변경 없이 구현했다.
- [x] 토글 붙여넣기 요구 10종을 기존 검사와 새 검사로 구성해 확인했다. 잔류 링크는 승인 범위에서 제외해 기존 읽기 전용 원인 근거만 유지한다.
- [x] Ctrl+Y와 Ctrl+Shift+Z를 Qt 문서 Redo에 연결하고 Undo/Redo 왕복을 검사했다.
- [x] Phase 0 제한 범위를 검증·기록하고 Phase 1로 넘어가지 않았다.
- [x] EXE를 생성하지 않았다.

## 12. 제외 범위와 상태

- 메모 카테고리·정렬·모아보기는 별도 승인 대기 계획이며, 본 계획에서는 복제 호환 지점만 고려한다.
- 일정 카테고리 통합, AI 자동 분류, 외부 협업 동기화, EXE 생성·배포는 포함하지 않는다.
- 현재는 분석과 계획 문서만 작성했다. 소스 코드, DB, 사용자 메모, 기존 EXE는 이 계획으로 변경하지 않았다.
