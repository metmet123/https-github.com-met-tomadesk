# 2026-08-04 작업 일지

- 반복작업 설정을 녹화 제어·시간 설정·도움말 3개 섹션으로 재구성했다.
- 반복 횟수 입력을 녹화 제어에 배치하고 좁은 편집 폭에서는 중지 단축키 아래로 재배치했다.
- 시간 방식 라디오와 동작 순서 안내를 제거하고 속도 슬라이더를 항상 활성화했다.
- 현재 속도 표시를 1.0배 복원 버튼으로 변경했다.
- `repeat_count` 저장·불러오기·JSON·Excel·실행 취소·다시 실행을 연결했다.
- 매크로 전체 반복, 반복 시작 전 중지 확인, 구형 시간 데이터 호환을 구현했다.
- 기능·UI 테스트 64개를 독립 프로세스로 실행해 모두 통과했다.
- `output/macro_repeat_sections_wide.png`, `output/macro_repeat_sections_narrow.png`로 실제 소스 화면을 확인했다.
- EXE는 요청되지 않아 생성하지 않았다.

## 알림 메모 통합

- 알림노트 원본을 수정하지 않고 메모·단일 알림·포스트잇 핵심을 `alert_notes/` 패키지로 경량화했다.
- 메인창에 `단축키·반복작업`과 `알림 메모` 전환을 추가했다.
- 포스트잇 고정·항상 위·색상·투명도·입력 잠금·위치 복원과 20초 알림 검사·순차 알림·10분 미루기를 구현했다.
- `alert_notes.db`를 분리하고 두 DB 통합 JSON 백업과 기존 백업 보존 호환을 구현했다.
- 신규 테스트 10건, 기존 기능 56건, 기존 반응형 UI 8건을 통과했다.
- 실제 Python 소스가 5초간 실행 상태를 유지하는 것을 확인했다.
- 넓은·좁은 알림 메모 화면과 독립 포스트잇을 캡처하고 UI 검토 판정 `Approve`를 기록했다.
- 범용 모듈 6개와 테스트 3개를 공통 라이브러리에 반영하고 실제 위치에서 재검증했다.
- EXE는 요청되지 않아 생성하지 않았다.
