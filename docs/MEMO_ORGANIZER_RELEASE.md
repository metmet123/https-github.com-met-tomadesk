# 메모 정리 실행 파일 검증 · 2026-09-19

## 전달 파일

- `release/TomaDesk.exe` · 47,501,033 bytes
- SHA-256: `E88E6E2AB3EB746223C278FF3ADFD87E44BC5D806723DF5053D3525FC2743E1A`
- 기준: Git `1437c1f` 위의 현재 로컬 변경 사항. 메모 정리, 파일 이름 변경, 로컬 OCR 소스를 포함한다.
- 실행 후 메모·알림 창의 **메모 정리** 탭을 사용한다. **설정 → 외부 AI**에서 허용/차단을 저장할 수 있다. 기본은 차단이며 실제 AI API 연결은 아직 구현하지 않았다.

## 이번 배포 작업

독립 빌드 환경을 사용하고, DLL 탐색 경로를 선택한 Python·Qt와 Windows 경로로 제한했다. 다른 개발 도구의 ICU/CRT가 섞여 실행이 멈추던 문제를 해결했다. 토마펫 메타데이터와 WebP 이미지를 필수 배포 자료로 포함했다. 최신 OCR용 WinRT 설정도 유지했다.

빌드 실패 시 기존 최종 EXE를 교체하지 않는다. 임시 빌드 폴더 삭제 시 대상 경로와 링크 여부를 확인한다. `requirements-build.txt`와 `DISTRIBUTION.md`에 재빌드 방법을 기록했다.

`--package-check <새 JSON 경로>`로 사용자 데이터와 별개인 임시 DB에서 패키지를 검사할 수 있다. 전역 단축키나 실제 메인 창을 실행하지 않으며 외부 AI를 호출하지 않는다. 검사 후 임시 DB는 지우고 JSON과 화면 PNG 두 개를 남긴다.

## 확인 결과

- `test_package_check.py`, `test_memo_organizer.py`, `test_screen_ocr.py`: **65 passed, 1 skipped**. 건너뛴 항목은 실제 Windows OCR 통합 검사이며 이번 작업에서 OCR 실인식 성공을 주장하지 않는다.
- 진단 화면의 지연 삭제 처리 보완 후 패키지 진단 테스트 재검사: **1 passed**.
- 최종 EXE 직접 실행: 종료 코드 **0**, `frozen: true`, `ok: true`.
- EXE 진단 **14개 통과**: 아이콘·토마펫, 로컬 메모 검토, 상대 날짜, 일정 등록, 중복 방지, 외부 AI 기본 차단과 미연결 상태, 허용·차단 저장, DB 재열기, 검토·요약 화면 렌더링.
- 최종 검토 화면을 직접 확인했으며 입력칸 겹침이 없다. 날짜 해석 예제는 9월 15일 기준이고, 요약 D-day는 실제 실행일 9월 19일 기준이다.

Python이 없는 별도 PC에서의 실행, 실제 트레이 알림과 전역 입력 후크는 이번 검사의 범위에 포함되지 않았다. 사용자 데이터로 메인 창을 열어 시험하지 않았다.
