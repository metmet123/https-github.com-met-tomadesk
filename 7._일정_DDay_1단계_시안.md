# 일정·D-Day 추가 업데이트 — 1단계 표시 구조와 시안

작성일: 2026-09-08  
상태: **사용자 승인 완료 · 1단계 확정 · 2단계 구현 준비**

## 결론

세 영역은 다음처럼 역할을 나눈다.

| 영역 | 주 역할 | 보여 줄 것 | 보여 주지 않을 것 |
|---|---|---|---|
| 일정 표시 포스트잇 | 실행·완료 | 사용자가 선택한 일정·할 일, 선택 시 메모 D-Day, 당일/이번 주/임박순/우선도순, 체크 완료 | 알림 전체 내역, 지난 D-Day 일괄 관리 |
| 오늘 요약 | 오늘 전체 상황·이동 | 오늘 D-Day·다음 일정·예정 알림의 대표 항목과 총 개수, 원본 화면으로 이동 | 긴 체크리스트, 정렬과 대량 완료 |
| 지난 D-Day 묶음 | 밀린 마감 정리 | D+일수, 목표 날짜·시각, 선택 완료, 접기/펼치기 | 오늘 일정과 미래 일정 |

추천안은 **지난 D-Day 묶음을 오늘 요약 맨 위에 접이식으로 배치하는 A안**이다. 앱을 열었을 때
밀린 마감을 놓치지 않으면서도, 접으면 오늘 일정이 먼저 보인다.

## 기본 제안

- 일정 포스트잇 시각 스타일: **사용자 붙임 이미지 기준으로 확정**
  - 흰 카드, 얇은 회색 테두리, 약한 그림자와 작은 둥근 모서리
  - 이전날·다음날은 흰색 테두리 버튼, 가운데 날짜는 파란색 강조 버튼
  - 항목은 작은 문자 배지·제목·오른쪽 목표 시각을 한 줄에 배치
  - 완료 항목은 체크 표시와 함께 제목을 흐리게 하고 삭선 표시
  - 하단에는 `체크하면 삭선 · 설정에서 휴지통으로 바꿀 수 있음` 안내 표시
- 붙임 이미지에 없던 네 가지 보기는 기능 요구를 유지하기 위해 날짜 줄 아래에 얇은 선택 탭으로 둔다.
  선택 상태는 파란색만 쓰지 않고 굵은 글자와 아래 선을 함께 표시한다.
- 일정 포스트잇 기본 표시 대상: **일정 + 할 일**
- 메모 D-Day: 기본 끔, 사용자가 설정에서 켤 때만 포스트잇에 포함
- 지난 D-Day: 포스트잇에는 넣지 않고 오늘 요약의 별도 묶음에만 표시
- 기본 보기: **당일 목록**
- 기본 최대 높이: **8줄**, 실제 행 `sizeHint()`로 높이를 계산하고 9건부터 스크롤
- 완료 처리: **삭선으로 남기기**, 설정에서 휴지통을 선택했을 때만 안전 삭제 경로 사용
- 단축키: **기본값 비움**
- 목표 시각: D-Day 줄 오른쪽에 `09/08 16:00`, 당일에는 `오늘 16:00`으로 표시
- 상태는 색만 쓰지 않고 `일정`, `할 일`, `D-Day`, `D+3`처럼 글자를 함께 표시

## 반응형 원칙

- 일반 폭: 네 가지 보기 단추를 한 줄에 배치한다.
- 좁은 폭: 보기 단추를 2×2로 바꾸고 목표 시각은 툴팁/상세에 보존한다.
- 125%·150% 배율: 고정 높이를 늘리는 대신 텍스트 `sizeHint()`와 레이아웃 계산값을 다시 사용한다.
- 가로 스크롤은 만들지 않는다.
- 설정 5개는 별도 `일정 포스트잇 설정` 창에 배치하고, 기존 `일정 · D-Day` 카드에는
  현재값 요약과 `설정…` 버튼 한 줄만 둔다.
- 체크·날짜 이동·보기 전환은 키보드 포커스와 접근성 이름을 제공한다.

## 실제 설정창 배율 확인

현재 `SettingsDialog`를 임시 데이터 폴더로 열고 `일정 포스트잇 · 일정+할 일 / 당일 / 8줄`
요약 한 줄만 런타임에 붙여 렌더했다. 앱 소스는 바꾸지 않았다.

| 배율 | 실제 렌더 | 결과 |
|---|---:|---|
| 100% | 1150×783 | 요약 한 줄과 저장·취소까지 표시됨 |
| 125% | 1265×919 | 요약 한 줄과 저장·취소까지 표시됨 |
| 150% | 1380×972 | 기존 단축키 2열 내용이 세로로 눌려 잘림 |

150%의 잘림은 새 설정 다섯 줄 때문이 아니라 현재 설정창의 기존 문제다. 단축키를 3열로 바꾸는
시험도 각 `HotkeyBuilder`의 최소 너비 때문에 가로로 잘려 채택할 수 없다.

최종 목표의 150% 검증을 통과하려면 설정창을 `단축키 / 일정·D-Day / 프로그램 / 데이터` 범주 탭으로
나누는 **설정 A안**을 권장한다. 기존 색상·카드 스타일은 유지하고 한 번에 보이는 범위만 나눈다.
변경 폭을 최소화해 현재 한 화면 구성을 유지하는 **설정 B안**은 100%·125%까지만 안전하며
150% 완료 기준을 충족하지 못하므로 최종안으로는 권장하지 않는다.

설정 A안은 150% 배율 크기인 1380×972 시안으로도 별도 확인했다. 선택된 탭은 파란색뿐 아니라
굵은 글자와 밑줄을 함께 사용하고, `일정·D-Day`의 추가 설정과 저장·취소가 페이지 내부 스크롤 없이
한 화면에 들어간다. 키보드 순서는 범주 탭 → 설정 필드 → 저장·취소로 고정한다.

## 1단계 요구사항 대조

| 확인 항목 | 시안 또는 검증 증거 | 상태 |
|---|---|---|
| 당일 / 이번 주 / 임박순 / 우선도순 | 일반·좁은 포스트잇의 네 보기 버튼 | 확인 |
| 이전날 / 오늘 / 다음날 | 포스트잇 머리말의 세 날짜 이동 버튼 | 확인 |
| 일정 / 할 일 / 메모 D-Day 범위 | 표시 대상 설정과 문자 배지 | 선택 대기 |
| 완료 체크 | 체크박스, 삭선 완료와 취소 안내 | 확인 |
| 최대 8줄, 초과 스크롤 | 바닥 상태 문구와 실제 행 `sizeHint()` 적용 원칙 | 구현 전 규칙 확정 |
| 지난 D-Day 위치·표시 | A/B/C 위치안, D+일수와 목표 날짜·시각 | 선택 대기 |
| 설정창 무스크롤 배치 | 100%·125% 통과, 150% 기존 잘림 발견 | 범주 탭 선택 대기 |
| 좁은 창 | 340px 별도 렌더, 보기 2×2·가로 스크롤 없음 | 확인 |
| 붙임 스타일 | 흰 카드·파란 날짜·작은 배지·우측 시각·완료 삭선·하단 안내 | 확정 |

따라서 시안 증거는 갖춰졌고, 아래 다섯 선택을 사용자가 확정하면 1단계를 완료 처리할 수 있다.
선택 전에는 앱 UI 및 데이터 코드를 변경하지 않는다.

## 시안에서 선택할 사항

2026-09-08 사용자 승인으로 다음 권장안을 확정했다.

1. 지난 D-Day 위치: **오늘 요약 맨 위 접이식 묶음**
2. 할 일 폼에 `D-Day로 세기`: **넣기**
3. 반복 항목 완료: **이번 회차만**
4. 일정 포스트잇 기본 표시 대상: **일정+할 일**
5. 설정창 150% 대응: **범주 탭으로 분리**

## 시안 파일

- `output/schedule_dday_stage1_mockup.html`
- `output/schedule_dday_stage1_mockup.svg`
- `output/schedule_dday_stage1_mockup.png`
- `output/schedule_dday_stage1_narrow.svg`
- `output/schedule_dday_stage1_narrow.png`
- `output/schedule_dday_settings_tabs_150.svg`
- `output/schedule_dday_settings_tabs_150.png`
- `output/schedule_postit_settings_1_0.png`
- `output/schedule_postit_settings_1_25.png`
- `output/schedule_postit_settings_1_5.png`

이 파일은 승인된 1단계 시안이며 앱 소스가 아니다. 2단계 구현은 이 문서의 확정값과
사용자 붙임 스타일을 기준으로 진행한다.
