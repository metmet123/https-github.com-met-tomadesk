# 단축키 프로그램 UI 개선 추진계획

## 추진 방향

- Figma 대신 실제 실행 화면을 `screenshot`으로 반복 비교한다.
- 정적 아이디어 탐색이 필요할 때만 `imagegen`을 참고용으로 사용한다.
- 단축키 등록과 매크로 녹화·재생 동작을 우선 보존한다.
- EXE는 별도 요청이 있을 때만 빌드한다.

## 구현 항목

- `ui_theme.py`에서 화면 스타일 규칙을 관리한다.
- 목록에 전체·활성 작업 수와 이름·단축키·작업 검색을 제공한다.
- 편집 패널에 현재 편집 중인 작업을 표시한다.
- 매크로 설정 패널에 녹화 대기·녹화 중 상태를 표시한다.

## 검증

- `py_compile`로 문법을 확인한다.
- Qt offscreen 점검으로 검색, 편집 대상 표시, 녹화 상태 표시를 확인한다.
- 실행 화면을 캡처해 레이아웃을 점검한다.

## 진행 상태

- 완료: 테마 분리, 목록 검색·요약, 편집 대상 안내, 녹화 상태 배지
- 완료: 문법 및 Qt offscreen 점검, 기본 창 크기 실행 화면 확인
- 제외: Figma 연동, EXE 빌드

## 2단계 개선 결과

- 완료: 체크 선택 수를 표시하고, 선택이 없을 때 활성·비활성 버튼을 비활성화
- 완료: 새 작업 생성 시 이름 입력칸으로 포커스 이동
- 완료: 매크로 JSON을 기본 화면에서 접고, 고급 설정 토글로 표시
- 완료: 매크로 동작 수·대기 수·재생 타이밍 요약 표시
- 검증: Qt offscreen에서 선택 상태, JSON 접기/펼치기, 매크로 요약을 확인

## 3단계 개선 결과

- 완료: 저장·테스트·가져오기·단축키 등록 결과를 성공·주의·오류 색상과 다음 행동 안내로 구분
- 완료: 활성·비활성 및 작업 유형 빠른 필터, 검색 결과 없음 안내, 선택 수가 포함된 삭제 버튼 추가
- 완료: 1,050px 미만 창에서 목록과 편집 영역을 위아래로 전환하고 분할 위치를 저장
- 완료: 최소 창 크기를 820×560으로 조정하고 네 모서리 인식 범위를 확대
- 완료: 네이티브 경계 처리와 직접 드래그 처리의 충돌을 제거하고 드래그 중 마우스 캡처 적용
- 검증: UI 회귀 테스트 3건과 문법 검사 통과
- 실창 검증: 우하단 모서리 드래그로 900×650에서 1,020×730으로 변경 확인
- 화면 검증: 작은 창의 필터, 목록, 위아래 분할, 편집 폼 배치를 캡처로 확인

## 4단계 개선 결과

- 완료: 종료 시 마지막 창 위치·크기·최대화 상태를 `main_window_geometry` 설정값으로 저장
- 완료: 다음 실행 시 저장된 창 상태를 복원하고, 모니터 변경으로 화면 밖에 남은 창은 현재 화면 안으로 이동
- 검증: 창 상태 저장·복원·화면 범위 보정 UI 테스트를 추가해 총 4건 통과

## 5단계 인터페이스 폴리싱 결과

- 적용 기준: `make-interfaces-feel-better`의 타이포그래피·표면·모션 절제·아이콘·성능 원칙을 PyQt6에 맞게 적용
- 완료: `ui_theme.py`에 의미 기반 색상 토큰을 도입하고 작업 공간·카드·입력·표·상태 메시지의 시각 위계를 통일
- 완료: 버튼과 목록 행의 데스크톱 클릭 영역을 40px 이상으로 확보하고, 검색·필터·녹화·활성 스위치에 접근성 이름과 설명 추가
- 완료: 목록의 이름·단축키·작업 유형에 정렬과 글꼴 위계를 적용하고 활성 스위치 뒤 숫자 겹침 제거
- 완료: 녹화 시작·종료·대기 상태를 별도 제어 카드로 구성하고 녹화 중 종료 단축키를 배지에 표시
- 완료: 저장 전 반복작업 테스트를 검증 색상으로 분리하고 실행 중지 단축키를 긴급 중지 안내 영역에 상시 표시
- 완료: 매크로 JSON 오류, 선택 없음·편집 중·다중 선택 상태를 색상과 문구로 함께 구분
- 구조 개선: 중복된 구형 스타일 정의를 `main_window.py`에서 제거하고 `ui_polish.py`에 작은 공용 폴리싱 도우미 분리
- 검증: `py_compile`, `compileall`, UI 회귀 테스트 총 5건 통과
- 실창 검증: Python 소스 프로그램을 5초간 실행해 프로세스 유지 확인 후 종료
- 화면 검증: `output/ui_polish_preview.png`, `output/ui_polish_preview_narrow.png`에서 넓은·좁은 화면 배치와 한글 렌더링 확인
- 제외: 기능 변경, 외부 UI 패키지 추가, EXE 재빌드, 공통 라이브러리 변경

## 배포 산출물

- 완료: 최신 소스를 `build_release.ps1 -Clean`으로 단일 EXE 패키징
- 결과: `release/toma_shortcut_program.exe` (약 54.4 MB)
- 검증: EXE를 5초간 실행해 실행 프로세스 유지 확인 후 종료

## 디자인 자산

- 완료: 단축키 키캡·번개 모티프를 넣은 토마 캐릭터 아이콘 제작
- 산출물: `assets/toma_icon_v2.png`(투명 배경) 및 `assets/toma_icon_v2.ico`(16~256px)
- 참고: 기존 실행 파일 아이콘은 변경하지 않고 새 자산으로 추가

## 6단계 녹화·창·설정 개선 결과

- 완료: 세로·가로 마우스 휠의 방향, 회전량, 화면 좌표와 타이밍을 매크로 단계로 저장하고 `SendInput`으로 재생
- 안전성: 기존 클릭·키보드 폴링은 유지하고 휠 메시지만 전용 저수준 후크 스레드에서 큐로 전달해 입력 콜백의 작업량을 최소화
- 완료: 전체화면 전환·숨김·마우스 버튼 해제 누락 시 남아 있던 크기 조절 상태와 마우스 캡처를 강제로 정리
- 완료: ID·활성·이름·단축키·작업 열을 드래그로 조절하고 너비를 설정 DB에 저장·복원
- 완료: 메인창 열기, 트레이로 숨기기, 녹화 종료, 실행 긴급 중지 단축키를 한 설정창에서 관리하고 전체 충돌 검사
- 완료: 시작 시 메인창/트레이 선택, 데이터 폴더 열기, 백업·복원 위치 확인 기능 추가
- 기본값: 메인창 열기 `Ctrl+Alt+F10`, 트레이로 숨기기 `Ctrl+Alt+F11`
- 구조 개선: 설정 UI를 `settings_dialog.py`로 분리하고 기존 테마와 40px 이상 조작 영역 적용
- 검증: 문법 검사, 기존 UI 테스트 5건, 신규 기능 테스트 7건 통과
- 실창 검증: Python 소스 프로그램을 5초간 실행해 프로세스 유지 확인 후 종료
- 참고: 자동 입력 환경에서는 실제 물리 휠 회전이 전달되지 않아 전역 후크 설치와 저장·재생 경로를 분리 검증
- 제외: 사용자가 요청하지 않은 EXE 재빌드 및 공통 라이브러리 변경

## 7단계 편집 화면 밀도 개선 결과

- 완료: 필터 결과가 0건일 때 `선택 없음` 라벨이 남은 세로 공간 전체로 늘어나던 현상을 고정 높이 도구 행으로 수정
- 완료: 반복작업 저장 버튼을 녹화 제어 카드의 우측 상단으로 이동하고 일반 작업에서는 기존 하단 저장 버튼 유지
- 완료: JSON 백업·복원과 Excel 내보내기·가져오기 버튼을 소형 크기의 한 줄 도구 모음으로 재배치
- 완료: 이름·상태·작업 유형·단축키 입력부를 4행에서 2행으로 재구성
- 완료: 녹화 제어, 시간 설정, 안내 카드의 여백과 행 구성을 압축해 1,420×720 화면에서 세로 스크롤 없이 전체 표시
- 적용 기준: 기존 PyQt 테마를 유지하고 `make-interfaces-feel-better`의 조작 영역·표면 위계·모션 절제 원칙 적용
- 검증: 기존 및 신규 자동 테스트 총 13건 통과, 문법 검사 통과
- 화면 검증: `output/ui_compact_preview.png`, `output/ui_empty_filter_preview.png`에서 저장 위치·빈 결과·한 줄 버튼·스크롤 0 확인
- 실창 검증: Python 소스 프로그램을 5초간 실행해 프로세스 유지 확인 후 종료
- 제외: 사용자가 요청하지 않은 EXE 재빌드

## 8단계 단일 EXE 배포 결과

- 완료: 최신 녹화·설정·편집 화면 소스를 `build_release.ps1 -Clean`으로 다시 패키징
- 산출물: `release/toma_shortcut_program.exe` 단일 파일
- 크기: 57,085,674바이트(54.44MB)
- 패키징: PyInstaller 6.20.0, Python 3.12, `onefile`, 최적화 레벨 1, 토마 아이콘 포함
- 콘솔 확인: PE Subsystem 값 2(`Windows GUI`)로 콘솔 창 없는 실행 파일 확인
- 정리 확인: `release` 폴더 파일 1개, `.package-build` 임시 폴더 제거 확인
- 실행 검증: 완성된 EXE를 8초간 실행해 프로세스 유지 확인 후 정상 종료
- SHA-256: `E658CE6E75E1979A0040C1DE7CB5C255046B7B4A94D4ABF080E96B54F8B1C060`

## 9단계 녹화 안내 문구 개선 결과

- 완료: 녹화 안내의 현재 종료 단축키와 `녹화 종료` 버튼명을 빨간색·굵은 글씨로 강조
- 완료: 불필요한 복사·붙여넣기 및 키 입력 안내 3번 항목 제거
- 유지: 종료 단축키를 설정에서 변경해도 안내 문구의 강조 대상이 새 값으로 자동 반영
- 검증: 문법 검사, 기능 테스트 8건, UI 테스트 6건 통과
- 참고: 기존 EXE는 이 소스 변경 이전 빌드이므로, 요청 시 최신 소스로 재빌드 필요

## 10단계 반복작업 복사·붙여넣기 안정화 결과

- 원인 확인: 녹화된 공백 `text` 단계가 내부 클립보드를 바꾸고 `Ctrl+V`를 추가 실행해 실제 `Ctrl+V`와 중복·충돌
- 완료: 반복작업의 `text` 단계는 클립보드를 사용하지 않고 Windows 유니코드 키 입력으로 직접 재생
- 완료: 일반 작업의 긴 문구 입력은 기존 클립보드 붙여넣기 방식을 유지
- 완료: 녹화된 `Ctrl+C` 뒤에는 클립보드 변경을 최대 0.75초 확인해 복사 지연에 대응
- 검증: `Ctrl+C → 클릭 → Ctrl+V → 공백` 순서에서 Ctrl+V 1회, 공백 직접 입력, 클립보드 대기 테스트 통과
- 검증: 신규 입력 테스트 3건과 기존 UI 테스트 6건 통과
- 제한: 실제 대상 프로그램의 좌표 기반 매크로는 대상 창의 반응 시간·화면 배율 영향을 받으므로 사용자 환경에서 1회 재생 확인 필요
- 참고: 기존 EXE는 이 소스 변경 이전 빌드이므로, 요청 시 최신 소스로 재빌드 필요

## 11단계 데이터·백업 경로 사용자 지정 결과

- 완료: 설정창에서 데이터 폴더와 백업·복원 폴더를 각각 선택하고 열 수 있도록 개선
- 기본값: 첫 실행 시 EXE가 있는 폴더 아래 `data`, `backup` 사용
- 안전성: 데이터 폴더 변경 시 현재 SQLite DB를 새 빈 폴더에 복사하고 재시작 후 적용
- 덮어쓰기 방지: 대상 폴더에 `hotkeys.db`가 있으면 변경을 중단하고 JSON 복원을 안내
- 호환성: 기존 `%LOCALAPPDATA%\CodexShortcutLauncher` 데이터와 백업은 새 기본 위치가 비어 있을 때 자동 복사하며 원본 유지
- 구성: 시작 전 경로 확인용 `storage_paths.json`만 `%LOCALAPPDATA%\CodexShortcutLauncher`에 저장
- 연동: JSON 복원과 Excel 가져오기·내보내기의 초기 위치도 사용자가 선택한 백업·복원 폴더로 변경
- 검증: 기능 테스트 35건과 반응형 UI 테스트 6건, 총 41건 통과
- 참고: 기존 EXE는 이 소스 변경 이전 빌드이므로, 요청 시 최신 소스로 재빌드 필요

## 2026-08-03 제외 프로그램 단축키 전달 개선

- 완료: 제외 프로그램이 포그라운드일 때 해당 작업의 `RegisterHotKey` 등록을 해제하도록 변경했다.
- 완료: 다른 프로그램으로 전환하면 해당 작업 단축키를 다시 전역 등록한다.
- 완료: 런처 제어 단축키와 제외되지 않은 작업 단축키는 계속 등록 유지한다.
- 완료: 제외 앱에서 해당 작업 단축키만 해제되고 다른 작업 단축키는 유지되는 단위 테스트를 추가했다.
- 완료: 단일 EXE 패키징 및 6초 실제 실행 점검.

## 2026-08-04 반복작업 UI·속도·반복 횟수 개선

- 완료: 반복작업 설정을 `녹화 제어`, `시간 설정`, `도움말` 3개 섹션으로 분리
- 완료: 도움말을 최하단으로 이동하고 기본 접힘 상태 유지
- 완료: 녹화 제어 구역에 1~999회 반복 횟수 입력을 추가하고 좁은 폭에서 하단 재배치
- 완료: 시간 방식 라디오 버튼과 동작 순서 안내를 제거하고 속도 슬라이더를 항상 활성화
- 완료: 현재 속도 표시를 클릭 가능한 기본 속도 1.0배 복원 버튼으로 변경
- 완료: `repeat_count`를 저장·불러오기·JSON·Excel·실행 취소·다시 실행에 연동
- 완료: 전체 매크로 반복 실행, 반복 전 중지 확인, 기존 `recorded`·`fixed` 데이터 호환 처리
- 검증: 문법 검사 통과, 기능·UI 테스트 64개를 독립 프로세스로 실행해 전부 통과
- 검증: 실제 소스의 넓은·좁은 화면 캡처에서 가로 잘림 제거와 하단 도움말 접근 확인
- 제외: 사용자가 별도로 요청하지 않아 EXE 빌드 미실행

## 요약 정리

반복작업 화면을 세 섹션으로 재구성하고 속도 설정을 단일 방식으로 단순화했다. 매크로별 1~999회 반복 실행과 중지·저장·입출력 호환을 구현했으며 자동 테스트 64개와 실제 소스 화면 검증을 완료했다.

## 추천 모델

`gpt-5.6-terra + medium` — PyQt6 반응형 UI와 매크로 실행·저장 호환성을 함께 수정하고 검증하는 작업에 적합하다.

## 추천 SKILL

`make-interfaces-feel-better`: 세 섹션의 시각적 위계, 숫자 입력과 좁은 화면 재배치를 다듬을 때 활용

`imagegen`: 구현 전 UI 배치 목업을 생성하고 실제 화면과 비교할 때 활용

## 2026-08-04 알림 메모 통합 결과

- 완료: 메인창에 `단축키·반복작업`과 `알림 메모` 최상위 전환 추가
- 완료: 검색 가능한 메모 목록, 제목·내용 편집, 메모별 단일 알림과 완료·10분 미루기 구현
- 완료: 포스트잇 고정·항상 위·색상·투명도·입력 잠금과 화면 밖 위치 복원 구현
- 완료: 트레이 상태에서도 20초 알림 검사와 독립 포스트잇 유지
- 완료: `alert_notes.db` 분리 저장과 두 DB 통합 JSON 백업·기존 백업 보존 호환
- 구조: 알림 기능을 `alert_notes/` 패키지로 분리하고 알림노트 원본의 트레이·캐릭터·반복 일정·전역 단축키는 제외
- 검증: 신규 테스트 10건, 기존 기능 56건, 기존 반응형 UI 8건 통과
- 화면 검증: 넓은·좁은 패널과 포스트잇 캡처, 인터페이스 전체 검토 `Approve`
- 실창 검증: Python 소스 프로그램 5초 실행 상태 유지 확인
- 공통화: `reminder_tools`, `qt_tools`, `sqlite_tools/database_bundle.py`와 독립 테스트를 공통 라이브러리에 반영
- 제외: 사용자가 요청하지 않은 EXE 빌드

## 요약 정리

기존 단축키·반복작업 기능을 보존하면서 메모별 단일 알림과 지속형 포스트잇을 독립 패널로 통합했다. 분리 DB와 통합 백업을 적용하고 자동 테스트 74건, 실제 실행, UI 캡처를 검증했으며 범용 모듈은 공통 라이브러리에도 반영했다.

## 추천 모델

`gpt-5.6-terra + medium` — PyQt6 화면·SQLite 저장소·알림 생명주기와 공통 모듈 추출을 함께 구현하고 회귀 검증하기에 적합하다.

## 추천 SKILL

reuse-codex-library: 알림노트 원본을 보존하며 범용 저장소·백업·창 복원 모듈을 공통 라이브러리로 분리할 때 활용

make-interfaces-feel-better: 알림 메모 패널의 표면 위계·40px 조작 영역·좁은 화면 배치와 포스트잇 가독성을 검토할 때 활용

imagegen: 구현 전 승인 목업을 생성하고 실제 넓은·좁은 화면과 비교할 때 활용

## 2026-08-05 알림 날짜·시간 입력 및 캘린더 통합 결과

- 완료: 알림 메모 내부를 `메모 편집`, `캘린더` 두 화면으로 구분
- 완료: 날짜·시간 분리 입력, 달력 팝업, 시간 스핀, 숫자 입력·붙여넣기와 빠른 시간 버튼 구현
- 완료: 잘못된 값과 지난 시간 검증, 상대 시간 요약, `Ctrl+Enter` 설정과 `Esc` 복원 구현
- 완료: 08:00~23:00 일간·주간 일정표, 기간 이동, 오늘 이동, 현재 시각선과 전체 화면 구현
- 완료: 알림 카드 색상, 같은 시간의 복수 알림 선택, 빈 시간 새 메모·알림 생성과 기존 알림 변경 구현
- 완료: 좁은 화면 첫 일간 보기와 일정표 위·빠른 설정 아래 반응형 배치 구현
- 데이터: 기존 DB 구조를 유지하고 기간 조회와 새 메모·알림 동시 생성 API 추가
- 공통화: 순수 파서를 `datetime_tools`로 공통 라이브러리에 반영하고 인덱스 갱신
- 검증: 신규 알림 테스트 14건, 기존 기능 56건, 기존 UI 8건, 공통 모듈 2건 통과
- 화면 검증: `output/alert_calendar_preview_wide.png`, `output/alert_calendar_preview_narrow.png` 확인
- 제외: 사용자가 요청하지 않아 EXE 빌드 미실행

## 2026-08-05 Qt 폰트 크기 경고 수정

- 완료: 고정폭 숫자 폰트 적용 시 기본 폰트의 `pointSize()=-1`을 Qt에 전달하지 않도록 보정
- 완료: 포인트 크기가 없을 때는 유효한 픽셀 크기만 적용하도록 공통 보조 함수를 추가
- 검증: 관련 UI·알림 메모 테스트 15건 통과, 실제 Python 실행의 표준 오류 비어 있음

## 요약 정리

알림 날짜·시간 입력과 캘린더 통합을 유지하면서 Qt 폰트 크기 경고를 제거했다. 포인트 크기가 미설정된 환경에서도 숫자 폰트가 안전하게 적용된다.

## 추천 모델

`gpt-5.6-terra + low` — 제한된 Qt 경고 원인을 특정 모듈에서 보정하고 관련 화면을 빠르게 회귀 검증하는 작업에 적합하다.

## 추천 SKILL

make-interfaces-feel-better: 숫자 표기와 고정폭 폰트 적용처럼 UI 안정성에 영향을 주는 세부 요소를 점검할 때 활용
