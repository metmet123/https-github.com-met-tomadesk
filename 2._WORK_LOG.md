# 작업 기록

2026-08-07 이전 작업 기록은 [docs/WORK_LOG.md](docs/WORK_LOG.md)에 보존한다.

### 2026-09-11 메모 편집 대규모 개선 Phase 0 제한 범위

#### 승인 범위

- 토글 내부 붙여넣기에서 원문에 없는 빈 줄이 추가되는 문제를 수정한다.
- Ctrl+Y와 Ctrl+Shift+Z를 Redo로 명시적으로 지원한다.
- 잔류 내부 링크를 포함한 그 밖의 Phase 0 항목과 Phase 1 이후 기능, DB/schema 변경, EXE 생성은 하지 않는다.

#### 원인과 처리

- HTML 클립보드에는 원문의 `text/plain`에는 없는 `<p><br></p>`나 연속 `<br>`가 시각적 간격용으로 포함될 수 있었다. 기존 코드는 HTML을 우선 삽입한 뒤 첫 빈 블록만 제거하여 중간의 장식용 빈 줄이 실제 토글 자식으로 저장됐다.
- HTML과 일반 텍스트의 글자가 같고 HTML 쪽 줄바꿈만 더 많은 경우에만 초과 줄바꿈을 제거한다. 일반 텍스트에 실제 빈 줄이 있으면 그대로 유지하고, 남은 문자·문단·체크리스트·코드·글자 서식은 보존한다.
- 빈 안내 줄뿐 아니라 내용이 있는 토글 자식과 중첩 토글 안에 붙인 새 문단도 같은 토글 최소 들여쓰기를 유지하도록 기존 붙여넣기 래퍼를 좁게 확장했다.
- 첫 빈 안내 블록을 재사용할 때 HTML 첫 문단의 블록 서식을 적용하도록 보완했다.
- `RichMemoTextEdit.keyPressEvent()`에서 Ctrl+Y와 Ctrl+Shift+Z를 Qt 문서의 `redo()`로 연결했다. 붙여넣기와 들여쓰기 보정은 기존 편집 블록 하나로 유지된다.

#### 추가·수정 검사

- 새 검사 9개: 한 줄, HTML 장식 빈 문단, 연속 `<br>`, 실제 빈 줄 보존, 내용 있는 토글 중간, 중첩 토글, 제목형 글자 서식·체크리스트·코드 혼합, Ctrl+Y Redo, Ctrl+Shift+Z Redo.
- 기존 빈 안내 줄, 여러 줄 plain text, HTML 굵게, 선행 빈 줄, Ctrl+Z 검사와 함께 집중 검사 26개를 수행했다.

#### 검증과 보존

- 토글 집중 검사: **26 passed**.
- 인접 메모 편집 회귀: **272 passed in 47.379s**.
- 전체 회귀: **722 passed in 287.08s**, 실패 0개.
- Windows 실제 Qt 백엔드 집중 검사: **26 passed in 2.835s**.
- 사용자 DB·기존 EXE는 변경하지 않았고 새 EXE를 생성하지 않았다.

### 2026-09-11 메모 커서·스크롤 구조 경계 안정화

#### 요청과 목표

- 빈 토글 아래의 빈 문단에서 Backspace를 누르면 본문 스크롤과 커서가 문서 위로 이동하는 문제를 해결한다.
- 원본 서식 HTML을 붙여넣은 뒤 표의 구조용 빈 문단에 커서가 우측 하단으로 표시되는 문제를 해결한다.
- 토글 구조, 실제 표, 원본 서식, 자동 저장과 사용자 데이터를 보존한다.

#### 처리

- 토글 밖 빈 문단 삭제 시 숨은 안내용 자식 줄이 아니라 화면에 보이는 토글 제목을 커서 복원 대상으로 사용하고, 삭제 전 `RichMemoTextEdit` 내부 스크롤 값을 새 최대 범위 안에서 복원하도록 했다.
- 표 안팎의 `QTextBlock`이 서로 다른 `QTextTable` 경계에 있는지 판별해, 원본 서식 붙여넣기가 표 경계에서 끝나면 왼쪽의 깨끗한 일반 문단을 함께 열도록 했다.
- 붙여넣기와 새 일반 문단 생성을 한 편집 단계에 묶어 Ctrl+Z 한 번으로 함께 되돌아가게 했다.
- 기존 HTML은 메모를 열기만 해서는 변경하지 않는다. 기존 표 경계에서 글자·붙여넣기·Backspace/Delete·Enter 입력을 시작할 때만 안전한 일반 문단으로 이동한다.
- 방향키와 단축키 탐색은 문단을 새로 만들지 않으며, 사용자가 만든 `1×1`·다행·다열 표와 중첩 표는 평문화하거나 삭제하지 않는다.

#### 검증과 배포

- 토글·표 경계 집중 검사 24개와 인접 메모 편집 회귀 222개가 통과했다.
- 최종 전체 회귀 검사: **713 passed in 267.33s**, 실패 0개.
- Windows 실제 Qt 백엔드에서 토글 스크롤 2개와 표 경계 7개, 총 9개 집중 검사가 통과했다.
- 실제 사용자 메모는 SQLite 읽기 전용 연결과 화면 밖 Windows Qt 위젯으로만 검사했다. 우측 경계 커서 x좌표는 `534 → 11`로 바뀌었고, 기존 비어 있지 않은 문단과 표 2개는 그대로 유지됐으며 메모를 연 직후 수정 상태는 `False`였다.
- `python -m compileall -q alert_notes test_memo_toggle.py`와 `git diff --check`를 통과했다.
- Windows UI 자동화 연결은 앱 표면을 반환하지 않아 실제 포인터 입력은 수행하지 못했다. 검증용 임시 창 프로세스는 종료하고 임시 도우미 파일도 제거했다.
- 사용자 DB·백업·기존 EXE는 변경·삭제하지 않았고 새 EXE를 생성하지 않았다.

### 2026-08-10 메모 입력 중 Qt 네이티브 충돌 재발 방지

#### 요청과 목표

- 메모 작성 중 프로그램이 멈춘 뒤 종료된 문제를 줄이고, 자동 저장된 사용자 메모가 안전한지 확인한다.
- `Qt6Core.dll` 접근 위반은 Python 예외가 아니므로, 메모 편집 중 고빈도로 실행되는 서식 동기화·자동 저장의 지연 작업을 안전하게 정리한다.

#### 처리

- `TextFormatToolbar`의 커서·서식 변경 연결을 익명 지연 콜백에서 수명 관리 가능한 메서드 연결로 바꿨다.
- 0ms 서식 동기화 타이머를 40ms 단일 지연으로 합쳐 연속 입력 중 중복 갱신을 줄이고, 편집기 종료 시 타이머를 멈추고 신호를 해제하게 했다.
- 메모 편집기 종료 시 자동 저장 타이머도 먼저 멈추게 하고, 패널 종료가 SQLite 저장소 종료보다 앞서 이 정리를 수행하도록 했다.
- 6천 자 이상 본문 입력, 반복 입력, 자동 저장, 종료 전 보류 작업 취소를 확인하는 집중 테스트를 추가했다.

#### 검증과 배포

- `python -m compileall -q alert_notes test_alert_note_phase1.py`를 통과했다.
- 새 안정성 테스트, 기존 자동 저장 테스트, 메모 편집 UI 회귀 테스트를 각각 독립 프로세스로 실행해 모두 통과했다.
- 실제 `A_shortcut_launcher.py`를 8초간 실행했고 프로세스가 유지됨을 확인한 뒤 검증 프로세스만 종료했다.
- `data/alert_notes.db`를 읽기 전용으로 검사해 무결성 `ok`와 마지막 메모의 6,323자 저장 상태를 확인했다. EXE를 만들거나 사용자 메모를 변경·삭제하지 않았다.
- Qt 네이티브 접근 위반은 환경·Qt 런타임 문제도 포함할 수 있어 절대적 재발 불가를 보장하지는 않으며, 같은 충돌이 다시 발생하면 해당 시각의 Windows 오류 기록으로 추가 원인을 좁힌다.

### 2026-08-10 메모 편집 색상·반복·입력 조작 UI 보완

#### 요청과 목표

- 승인된 시안에 한정해 글자 색상 선택, 글꼴 크기·알림 시간·반복 횟수의 증감 버튼, 반복 선택, 빠른 알림 버튼의 표시를 개선한다.
- 메모·서식·알림·반복·단축키·저장과 SQLite 데이터 처리 계약은 변경하지 않는다.

#### 처리

- `■색상` 문자 버튼을 무지개 원형+기호 아이콘으로 교체하고, 클릭 시 20개 원형 색상 팔레트를 표시하도록 했다. 검정·빨강·파랑 빠른 색상은 기존 단축키 연결을 유지한 채 텍스트 없는 원형 스와치로 바꿨다.
- 글꼴 크기·알림 시간·반복 횟수 증감 영역의 오른쪽 서브컨트롤 폭과 입력 여백을 분리했다. 시간 증감은 항상 10분 단위로 처리한다.
- 빠른 알림 9개 버튼의 전용 높이·글자 크기·여백을 적용해 두 줄 표기가 잘리지 않도록 했다.
- 기존 반복 콤보의 내부 값과 저장 경로는 유지하고, 화면에는 상호 배타적인 `반복 X/매일/평일/매주/매월` 체크 버튼을 표시하도록 했다.
- 기존 메모 편집기의 휠 값 변경 차단과 날짜·시간 직접 입력 동작은 유지했다.

#### 검증과 배포

- `python -m compileall -q alert_notes ui_theme.py test_alert_notes.py`를 통과했다.
- `python -m unittest test_alert_notes.py -q`에서 17개 테스트가 모두 통과했다. 새 Qt 마우스 좌표 검증으로 20색 팔레트, 빠른 색상, 글꼴 크기·시간 증감 영역의 화살표 커서와 클릭, 시간 +10분, 반복 횟수 +1, 반복 선택, 빠른 알림 버튼 크기를 확인했다.
- 실제 `MainWindow`의 메모 작업 공간을 1920×1080으로 렌더링해 스크롤 없이 배치되는지 확인하고 [alert_notes_color_recurrence_1920x1080.png](output/alert_notes_color_recurrence_1920x1080.png)를 남겼다.
- 실제 `A_shortcut_launcher.py`를 6초간 실행했고 프로세스가 유지됨을 확인한 뒤 검증 프로세스만 종료했다. EXE 생성과 사용자 데이터 변경은 하지 않았다.

### 2026-08-07 알림노트 비UI 기능 통합 1차

#### 요청과 목표

- 현재 메모 편집 UI와 포스트잇·캘린더·기존 사용자 데이터를 보존하면서 원본 `15_알림노트`의 메모 편집 관련 비UI 기능을 모듈 단위로 이식한다.
- DB 백업 성공 후에만 다중·반복 알림과 처리 이력 스키마로 마이그레이션한다.
- 메모 편집, 리치 텍스트, 다중·반복 알림, 캘린더 연동, 알림 처리 이력과 `알림내역` 탭을 자동 테스트와 실제 소스 실행으로 검증한다.

#### 처리

- 원본의 반복 계산, 리치 텍스트 변환, 서식 프리셋과 시간·서식 단축키 로직을 프로젝트 모듈로 복사하고 현재 import·저장소 구조에 맞게 수정했다.
- 글꼴·크기·색상, 굵게·기울임·밑줄·취소선, 서식 프리셋 4개, 단축키 설정, 400ms 자동저장과 보조 수동 저장을 메모 편집기에 연결했다.
- 일반 텍스트 메모 호환을 유지하면서 서식 메모는 HTML로 저장하고 목록·포스트잇·알림에서는 일반 텍스트로 변환하도록 했다.
- 메모당 다중 알림, 단발·매일·평일·매주·매월 반복, 종료일·횟수 제한, 빠른 예약, 기존 알림 추가·변경 선택과 반복 알림의 이번만·전체 수정을 구현했다.
- 완료·미루기·건너뛰기·삭제와 다음 반복 발생, 처리 이력·재예약을 구현하고 단발 알림의 기존 ID 기반 미루기 호환성을 보존했다.
- `메모 편집 → 캘린더 → 알림내역` 순서의 세 번째 탭과 `예정 알림/처리 내역` 하위 탭을 추가했다.
- 예정 알림의 검색·메모 열기·수정·완료·미루기·건너뛰기·삭제와 처리 내역의 검색·재예약·선택 삭제·전체 삭제를 연결했다.
- 알림별 `source_reminder_id` 캘린더 투영을 유지하고 알림 수정·처리·다음 반복 생성 때 캘린더 항목도 동기화했다.
- 포스트잇 메모에도 다중·반복 알림을 계속 허용하고 알림 저장 후 메모 본문을 유지했다.
- 전체 JSON 백업·복원에 반복 시리즈와 처리 이력을 포함하고 외래키 순서에 맞춰 복원하도록 수정했다.
- 원본 `15_알림노트` 파일은 수정하지 않았다.

#### 검증과 배포

- 실제 `data/alert_notes.db`는 `data/alert_notes_before_upgrade_20260807113929.db` 백업 생성 성공 후 마이그레이션했다.
- 마이그레이션 전후 메모 0건·알림 0건이 동일했고 `UNIQUE(note_id)` 제거, 신규 반복·이력 테이블, 외래키 검사 통과를 확인했다.
- `python -m compileall`로 변경 소스 컴파일을 확인했다.
- 신규 기능 15개, 기존 알림 14개, 일정 12개를 모듈별 독립 프로세스로 통과했다.
- 전체 Qt 묶음의 네이티브 종료 문제를 피하기 위해 기능 테스트 57개와 반응형 테스트 8개도 각각 독립 프로세스로 전수 통과했다. 총 검증 테스트는 106개다.
- 임시 DB를 사용하는 실제 소스 UI에서 리치 텍스트 본문 유지, 다중·반복 알림, 캘린더 연결 2건, 처리 이력 2건과 세 탭 표시를 확인했다.
- 실제 `A_shortcut_launcher.py`를 실행해 5초 동안 프로세스가 유지되는 것을 확인한 뒤 검증 프로세스만 종료했다.
- 원본 프로젝트, 기존 배포물과 EXE는 변경하지 않았고 새 EXE를 생성하지 않았다.
- 2차 메모 바로 입력·목록 관리·Excel 내보내기는 이번 작업에서 구현하지 않았다.

### 2026-08-07 메모 편집 화면 UI 압축 및 크기 조절

#### 요청과 목표

- 첨부 이미지의 분홍색 범위를 기준으로 메모 편집 영역의 기본 너비를 축소하고 본문 입력 높이를 늘린다.
- 초록색 범위의 버튼 높이·내부 여백·항목 간격을 줄이되 표시된 서식·알림·반복·단축키·저장 기능을 모두 유지한다.
- 기존 목록–편집 분할선은 유지하고 편집 영역 오른쪽에 새 세로 분할선을 추가해 좌우 드래그로 편집 너비를 조절한다.

#### 처리

- 기존 메모 화면 `QSplitter`에 편집 영역 뒤의 여백 패널과 두 번째 손잡이를 추가했다.
- 1920px 폭에서 목록 700px, 편집 640px, 우측 여백 566px가 되도록 초기 크기를 설정하고 오른쪽 손잡이에 7px 세로 경계와 가로 조절 커서를 적용했다.
- 900px 미만에서는 우측 여백을 숨기고 기존 목록–편집 상하 배치를 유지하도록 했다.
- 메모 편집 바깥 여백, 카드 여백, 행·열 간격과 버튼·입력 높이를 메모 화면 안에서만 압축했다.
- 알림 날짜와 시간을 한 행으로 배치하고 반복 상태 문구의 여백을 줄였으며, 메모 본문은 약 100px 이상의 작성 높이를 우선 확보했다.
- 메모 작성·서식·프리셋·알림·반복·항상 위·포스트잇·단축키·색상·투명도·저장·삭제 위젯과 기존 신호 연결을 모두 유지했다.
- 데이터 구조, SQLite 저장 방식, 이벤트·단축키·알림 업무 로직, 좌측 목록과 상단 메뉴 기능은 변경하지 않았다.

#### 검증과 배포

- 변경 Python 파일과 관련 테스트 파일의 `py_compile`·`compileall`을 통과했다.
- 메모·알림 테스트 15개, 1차 통합 테스트 15개, 일정 테스트 12개, 반응형 테스트 8개와 메모 단축키·설정 집중 테스트 2개 등 관련 테스트 52개를 통과했다.
- 기존 Qt 전체 묶음은 한 프로세스에서 네이티브 종료되는 기존 특성이 있어 반응형 테스트를 각각 독립 프로세스로 실행했고 8개 모두 통과했다. 이번 UI와 직접 관련된 기능 테스트도 독립 실행으로 확인했다.
- 1920×1080 실제 `MainWindow` 렌더에서 초기 크기 `[700, 640, 566]`, 본문 높이 103px, 편집 영역 가로·세로 스크롤 없음과 전체 입력 항목 표시를 확인했다.
- 새 오른쪽 손잡이를 실제 Qt 마우스 이벤트로 80px 드래그했고, 별도 렌더 검증에서는 140px 이동해 편집 폭이 640px에서 780px로 변경되는 것을 확인했다.
- 검증 화면은 `output/memo_editor_ui_1920x1080.png`와 `output/memo_editor_ui_resized_1920x1080.png`로 남겼다.
- 실제 `A_shortcut_launcher.py`를 실행해 `토마 단축키 런처` 창이 8초 동안 유지되는 것을 확인한 뒤 검증 프로세스만 종료했다.
- 현재 작업의 미완료 항목은 없으며 기존 별도 후속 항목은 `3._TODO.md`에 그대로 보존했다.
- 기존 EXE와 사용자 데이터는 변경하지 않았고 새 EXE를 생성하지 않았다.

### 2026-08-07 메모 편집 화면 최종 시안 반영

#### 요청과 목표

- 승인된 좁은 폭 시안을 유지하면서 메모 본문을 충분히 넓히고, 알림·반복·단축키·하단 제어 영역을 스크롤 없이 보이도록 압축한다.
- 날짜 보조 버튼과 중복 안내·상태 영역을 제거하고 날짜·시간 직접 편집, 전체 값 선택 UI의 휠 잠금과 우측 분할선 조절을 검증한다.
- 기존 메모·서식·알림·반복·단축키·저장 기능과 데이터·업무 로직은 보존한다.

#### 처리

- 편집 영역 기본 폭 640px과 목록 700px, 오른쪽 드래그 분할선 구조를 유지하고 본문 최소 높이를 코드와 스타일 모두 180px로 통일했다.
- 날짜 아래 `오늘`, `내일`, `+10분`, `+30분`, `+1시간` 행과 숫자 입력 안내를 메모 편집 화면에서 제거했다.
- 날짜·시간 요약을 `알림 예약` 제목 오른쪽으로 이동하고 반복 유형·반복 종료·조건 입력을 기본 한 행에 배치했다. 반복 전용 요일·날짜 조건은 해당 유형에서만 추가로 표시한다.
- 빠른 알림 9개를 기존 순서와 단축키 그대로 4개+5개의 2행으로 재배치했다.
- 메모 전역 단축키를 기본 접힘 카드로 변경하고 펼쳤을 때 기존 사용 여부·단축키·동작 입력을 모두 유지했다.
- 입력 잠금, 색상 5개, 투명도, 저장과 삭제를 한 행에 배치했다.
- 글씨체 선택 폭을 제한하고 글씨 크기 입력 폭을 76px로 넓혀 값과 증감 버튼이 함께 보이게 했다.
- 날짜·시간 입력의 내부 텍스트를 드래그 또는 전체 선택해 표준 형식과 압축 형식으로 덮어쓰기·붙여넣기할 수 있게 했고 Enter 확정, Esc 복원, 달력 선택을 유지했다.
- 메모 편집기 안의 글씨체·크기·날짜·시간·반복·투명도·단축키 동작 등 콤보와 스핀 계열 입력은 포커스 여부와 무관하게 마우스 휠로 값이 바뀌지 않도록 했다.
- 저장 신호, 단축키 연결, 리치 텍스트, 알림·반복 규칙과 SQLite 구조는 변경하지 않았다.

#### 검증과 배포

- `python -m compileall -q alert_notes ui_polish.py ui_theme.py test_alert_notes.py`를 통과했다.
- 메모·알림 UI 테스트를 16개로 보완해 모두 통과했다. 본문 높이, 4+5 버튼 행, 보조 버튼 제거, 반복 상태줄 비표시, 단축키 접기·펼치기, 하단 한 행, 우측 분할선 드래그, 날짜·시간 직접 입력과 편집기 값 위젯 휠 잠금을 검증했다.
- 메모 단축키 등록과 기존 공통 단축키 등록 관련 회귀 테스트를 개별 통과했다. 전체 Qt 묶음은 기존과 같이 메모 작업과 무관한 반응형 테스트 도중 네이티브 종료되어 관련 테스트를 분리 실행했다.
- 실제 `MainWindow`를 1920×1080으로 렌더해 분할 크기 `[700, 640, 566]`, 본문 높이 410px, 가로·세로 스크롤 최대값 0, 빠른 알림 2행, 단축키 기본 접힘과 글씨체 210px·글씨 크기 76px를 확인했다.
- 검증 화면은 `output/alert_notes_final_1920x1080.png`에 남겼다.
- 실제 `A_shortcut_launcher.py`를 실행해 6초 동안 프로세스가 정상 유지되는 것을 확인한 뒤 검증 프로세스만 종료했다.
- 기존 사용자 데이터와 EXE는 변경하지 않았고 새 EXE를 생성하지 않았다. 이번 작업에 따른 미완료 항목은 없다.

### 2026-08-10 작업공간 전환 안내 및 작업 목록 확장

#### 요청과 목표

- 승인된 시안대로 왼쪽 일괄 작업 행을 제거하고 필터·검색과 작업 표를 위로 확장한다.
- 상단에는 장식 없는 검정색 `Shift+Tab 전환` 안내만 표시하고 실제 창 범위 전환 동작을 제공한다.
- 나머지 UI, 작업 처리 함수, 저장 데이터와 단축키 기능은 보존한다.

#### 처리

- 상단 두 작업공간 버튼 오른쪽에 일반 굵기의 `Shift+Tab 전환` 라벨을 추가했다. 좁은 화면이나 높은 UI 배율에서는 버튼과 겹치지 않도록 안내만 숨긴다.
- 창 범위 `Shift+Tab` 단축키를 연결해 `단축키·반복작업`과 `메모·일정` 화면, 선택 버튼 상태가 함께 전환되게 했다.
- `활성`, `비활성`, `실행 테스트`, 선택 상태 요약 행은 화면과 레이아웃에서 숨기고 필터·검색과 작업 표가 그 자리를 사용하게 했다.
- 기존 일괄 활성화·비활성화·실행 테스트 함수, 선택 체크박스·전체 선택·삭제와 데이터 처리 로직은 삭제하거나 변경하지 않았다.
- 메인 창과 하위 화면의 콤보·스핀 계열 값 선택 위젯을 공통 휠 잠금 대상으로 수집해 포커스 상태에서도 일반 휠로 값이 바뀌지 않게 했다.

#### 검증과 배포

- `main_window.py`, `ui_theme.py` 컴파일 검사를 통과했다.
- 반응형 UI 테스트 9개를 네이티브 Qt 종료 영향을 피하도록 각각 독립 프로세스로 실행해 모두 통과했다.
- 메모·알림 테스트 16개와 단축키 등록·메모 단축키·표 열 너비 회귀 테스트 3개를 통과했다.
- 키 입력 테스트에서 `Shift+Tab`을 두 번 눌러 작업공간 인덱스와 두 버튼의 선택 상태가 양방향으로 동기화되는 것을 확인했다.
- 1247×849 실제 소스 렌더에서 일괄 작업 행 숨김, 필터 Y=74, 표 Y=128, 표 높이 603px와 가로·세로 스크롤 최대값 0을 확인했다.
- 검증 화면은 `output/workspace_shift_tab_compact_1247x849.png`에 남겼다.
- 실제 `A_shortcut_launcher.py`를 실행해 6초 동안 프로세스가 유지되는 것을 확인한 뒤 검증 프로세스만 종료했다.
- 사용자 데이터와 기존 EXE는 변경하지 않았고 새 EXE를 생성하지 않았다. 이번 작업에 따른 미완료 항목은 없다.

### 2026-08-10 토마펫 알림노트 연계

#### 요청과 목표
- 알림노트의 예약·일정 알림을 토마펫이 알려 주도록 변경한다.
- 마우스 근접 반응과 우클릭 액션 선택을 제공하면서 기존 알림 처리 기능을 유지한다.

#### 처리
- `toma_pet_assets`에 검증된 토마펫 v2 자산을 프로젝트 복사본으로 추가했다.
- 투명 토마펫 말풍선, 16방향 근접 시선 반응, 우클릭 액션 메뉴, 드래그 이동을 구현했다.
- `AlertService`가 토마펫 말풍선을 우선 사용하고, 자산 또는 표시 오류에는 기존 알림 팝업으로 대체하도록 연결했다.
- 기존 SQLite 알림 큐, 완료·미루기·건너뛰기, 메모·일정 열기 동작을 보존했다.

#### 검증과 배포
- `python -m compileall -q alert_notes test_alert_notes.py` 통과.
- `python -m unittest test_alert_notes.py -q` 결과: 19개 테스트 통과.
- 실제 토마펫 말풍선 렌더링 이미지와 단축키프로그램 6초 실행 상태를 확인했다.
- EXE는 사용자 요청이 없어 생성하지 않았다.

### 2026-08-10 메모 편집·독립창·목록 관리 개선

#### 요청과 목표

- 메모 선택이나 `+ 새 메모` 없이 편집 구역에서 바로 작성해 자동저장하고 수동 저장은 즉시 저장·실패 재시도용으로 유지한다.
- 메모별 `메모 열기` 전역 단축키는 메인창 대신 편집 영역만 담은 독립창을 열게 한다.
- 메모·예정 알림·처리 내역에 선택 관리, 삭제와 Excel 내보내기를 적용하고 설정 저장 성공 안내를 추가한다.

#### 처리

- 제목 또는 본문이 생긴 경우에만 400ms 자동저장 경로에서 메모 한 건을 만들고 새 ID를 편집기에 연결했다. 본문만 입력하면 제목은 `새 메모`를 사용하며 빈 입력·옵션 변경·입력 후 전부 삭제는 레코드를 만들지 않는다.
- 자동저장과 수동 저장을 같은 저장 경로로 통합하고 저장 실패 시 입력·메모 ID를 유지해 재시도할 수 있게 했다. 검색 결과가 없는 상태의 첫 저장은 검색어를 해제하고 생성 메모를 선택한다.
- `MemoEditor` 전체를 재사용하는 Windows 네이티브 독립창을 추가했다. 기본 680×880px, 최소 560×640px, 위치·크기·최대화 상태 복원, 모든 가장자리 크기 조절, 닫기 전 보류 저장 후 창만 숨기기를 적용했다.
- 메모별 `open` 전역 단축키는 독립창만 열고 `postit` 동작은 기존 경로를 유지했다. 독립창 저장 뒤 목록·캘린더·알림내역과 단축키 등록을 갱신한다.
- 메모 목록을 `선택, 번호, 표시, 제목, 수정 시간, 내용 미리보기` 표로 바꾸고 수정 시간 내림차순, 검색·스크롤, 전체 선택/해제, 선택 삭제와 Excel 내보내기를 추가했다.
- 예정 알림과 처리 내역에도 전체 선택/해제, 선택 삭제와 Excel 내보내기를 추가하면서 완료·미루기·건너뛰기·수정·재예약·전체 이력 삭제를 보존했다.
- `메모목록`, `예정알림`, `처리내역`별 xlsx 내보내기 모듈을 추가해 체크 항목 우선, 체크가 없으면 현재 검색 결과 전체, 전체 일반 텍스트 본문, `YYYY-MM-DD HH:mm`, 헤더 고정·필터·자동 열 너비를 적용했다.
- 메인 설정과 노트 단축키 설정 저장 성공 시 `설정이 저장되었습니다.`를 한 번 표시한다. 기본 `Alt+\`` 시간 단축키도 저장할 수 있도록 Windows 백틱 키 파싱을 보완했다.
- SQLite 테이블·사용자 데이터, 새 메모 버튼, 리치 텍스트·알림·반복·포스트잇·토마펫 기능은 변경하지 않았다.

#### 검증과 배포

- 변경 모듈과 테스트 파일의 `compileall`을 통과했다.
- 새 집중 테스트 11개를 네이티브 Qt 종료 간섭을 피하도록 기능 묶음과 독립 프로세스로 실행해 모두 통과했다. 빈 초안, 제목·본문 저장, 한 건 생성, 검색 해제, 저장 실패 재시도, 목록·삭제, 세 Excel 유형, 독립창 재사용·닫기 저장, 설정 성공·실패 안내를 확인했다.
- 기존 메모 패널·리치 텍스트·400ms 자동저장·휠 변경 차단·단축키 등록 관련 회귀 테스트를 독립 실행해 통과했다.
- 실제 Windows 창에서 크기 조절 프레임, 최소화·최대화·시스템 메뉴, 최대화·복구, 닫기 숨김, 닫기 전 저장을 확인해 `NATIVE_STANDALONE_OK` 결과를 얻었다.
- 1400×900 메모 화면과 680×880 독립창을 렌더해 표·편집 레이아웃을 확인했고 검증 화면을 `output/memo_management_preview.png`, `output/standalone_memo_editor_preview.png`에 남겼다. 최소 560×640에서는 필요한 가로 스크롤로 모든 편집 제어에 접근 가능함을 확인했다.
- 실제 `A_shortcut_launcher.py`를 실행해 5초 동안 프로세스가 유지되는 것을 확인한 뒤 검증 프로세스만 종료했다.
- 기존 EXE와 사용자 데이터는 변경하지 않았고 새 EXE를 생성하지 않았다. 이번 작업의 완료 항목은 `3._TODO.md`에서 제거했다.

### 2026-08-11 단일 EXE 재빌드

#### 요청과 목표

- 현재 단축키 프로그램을 배포 가능한 단일 EXE로 생성하고 실제 실행을 확인한다.

#### 처리

- 기존 `build_release.ps1 -Clean`과 `shortcut_launcher.spec`로 `release/toma_shortcut_program.exe`를 재생성했다.
- `release/data`의 기존 3개 항목과 프로젝트 원본 소스, 사용자 데이터는 보존했다.

#### 검증과 배포

- PyInstaller 6.20.0 빌드가 성공했고, 결과 파일 `release/toma_shortcut_program.exe`(64,708,067 bytes)를 확인했다.
- 새 EXE를 숨김 상태로 5초 실행해 프로세스가 유지됨(`RUNNING`)을 확인한 뒤 검증용 프로세스만 종료했다.
- EXE 생성 완료. 이번 요청에 따른 미완료 항목은 없다.

### 2026-08-15 포스트잇 1·2단계 개선

#### 1단계: 포스트잇 편집 경험

- 네 방향 가장자리와 네 모서리의 시스템 크기 조절, 호버·포커스 때만 나타나는 상·하단 도구, 숨기기와 포스트잇 해제를 분리한 창 수명 주기를 구현했다.
- B/I/U/S, 글머리표, 체크리스트, 데이터베이스 첨부 이미지, 링크 자동 인식, 리치/일반 텍스트 붙여넣기와 공통 리치 문서를 메인·단독·포스트잇 편집기에 적용했다.
- Sticky Notes형 보조 패널에 색상, 배경 투명도 0/30/50/70/100, 항상 위, 입력 잠금, 시작 표시, 빠른 알림과 주요 작업을 배치했다.
- 포스트잇에만 적용되는 Ctrl+N/W/D/B/I/U/T/Shift+L/Tab/Shift+Tab/Esc 단축키와 삭제 확인을 구현했다.

#### 2단계: D-Day·접기·체크리스트

- D-Day 이름·시각·토마 알림을 메인 편집기, 단독 편집창과 포스트잇에서 공통 설정하고 D-/D-DAY/D+ 배지를 표시한다. 지정 시각에는 기존 알림 큐를 통해 토마 캐릭터 말풍선이 나타난다.
- 제목 표시줄 더블클릭 또는 메뉴로 제목만 접기, 색상 배지로 최소화, 배지 클릭으로 이전 확장 크기 복원을 구현했다. 여러 배지는 바탕화면 우하단에 순서대로 정렬한다.
- 선택한 줄만 체크리스트 변환, 버튼 온/오프, Enter 다음 체크박스 생성, 빈 항목 Enter 및 Backspace/Delete로 체크박스 제거, 체크 클릭 시 해당 줄 취소선과 해제 복원을 구현했다.
- 스키마에 `postit_display_mode`, `d_day_at`, `d_day_label`, `d_day_alert`를 추가하고 구형 데이터 기본값·업그레이드 전 자동 백업·JSON 백업 호환성을 적용했다.

#### 검증

- 변경 모듈 전체 컴파일을 통과했다.
- 기존 및 신규 테스트 143개를 각각 독립 프로세스로 실행했다. 신규 기능 반영으로 드러난 구형 토마 알림 필드 호환성을 보완해 모두 통과시켰고, 공통 체크리스트 도구와 빠른 알림 전용 검증 1개도 추가 통과했다(총 144개 동작).
- 실제 사용자 DB의 읽기 전용 복사본을 마이그레이션해 무결성 `ok`, 기존 메모 3건 보존, 2단계 열 생성과 업그레이드 전 백업 생성을 확인했다. 원본 DB는 변경하지 않았다.
- 일반·옵션·제목 접기·색상 배지를 렌더링해 레이아웃을 확인했고, 보조 패널은 2열 작업 그룹으로 줄였다.
- 격리된 임시 데이터로 실제 프로그램을 실행해 D-Day 포스트잇을 포함한 프로세스가 8초 이상 유지됨을 확인했다. 기존 EXE는 재빌드하지 않았다.

### 2026-08-25 메모 화면 비례 크기 조절과 버튼 최적화

#### 요청과 목표

- 기존 A·B 스플리터를 유지하면서 메모 목록·편집 영역의 최소 폭, 목록 열 비율, 편집 내부 반응형 배치와 크기 기억을 보강한다.
- C의 빠른 알림 버튼에서 균등 가로 확장을 제거하고 내용에 맞는 클릭 크기를 적용한다.

#### 처리

- A·B 핸들에 각각 접근성 이름을 부여하고 목록 480px, 편집 560px의 최소 폭을 적용했다. 세 구역의 가로 비율은 SQLite 설정에 저장해 다시 표시하거나 가로 배치로 복귀할 때 복원한다.
- 메모 목록 표의 여섯 열을 기존 너비 기준 비율로 정의하고, 실제 viewport 크기 변경 때 전체 폭을 채우도록 재계산했다. 최소 열 폭 이하에서는 더 줄이지 않아 헤더와 데이터 정렬을 유지한다.
- 편집기 컨테이너가 부모 viewport 폭을 따르게 하고, 620px 미만에서는 서식 프리셋, 노트 단축키 설정, 하단 저장·삭제 조작부를 다음 줄로 이동시켰다. 제목·본문·알림·반복·포스트잇 기능과 글자·아이콘 크기는 유지했다.
- 빠른 알림 버튼은 기존 2행 구성을 유지하면서 `sizeHint()` 기반 고정 size policy로 바꿔 텍스트 주변의 가로 빈 공간만 제거했다. 34px 이상의 클릭 높이와 버튼 간 간격은 보존했다.
- 기존 콤보·스핀 계열 값 선택 UI의 휠 변경 차단을 유지했다.

#### 검증과 배포

- 변경 모듈과 테스트 파일의 `compileall`을 통과했다.
- A·B 핸들 드래그, 여섯 열 비율, 스플리터 비율 저장·복원, 560px 편집기 재배치, 빠른 알림 내용 맞춤 폭, 휠 변경 차단을 포함한 집중 테스트 4개가 모두 통과했다.
- 1478×907 렌더를 `output/alert_notes_responsive_splitters_1478x907.png`에 남기고 목록 열, 560px 편집 폭, 서식 프리셋과 하단 작업의 줄바꿈, 가로 스크롤 최대값 0을 확인했다.
- 격리된 임시 데이터로 실제 Windows 소스 창을 실행해 같은 폭의 편집 화면과 버튼 잘림이 없음을 확인했다. 자동 UI 드래그는 사용자 입력 감지로 중단했지만 동일 좌표 드래그는 Qt 마우스 테스트로 통과했다.
- 사용자 데이터와 기존 EXE는 변경하지 않았고 새 EXE를 생성하지 않았다. 이번 요청에 따른 미완료 항목은 없다.

### 2026-08-25 메모 서식·설정 공간·탭·빠른 알림 개선과 EXE 재빌드

#### 요청과 목표

- B/I/U/S 서식 선택 버튼에 체크 표시를 추가하고 이미지 삽입 버튼을 직관적인 사진 아이콘으로 바꾼다.
- 설정 창의 빈 공간을 줄여 단축키 설정을 효율적으로 배치하고 메모 하위 탭을 상단 작업공간 버튼과 정렬한다.
- 빠른 알림시간 버튼 9개를 한 줄로 배치하고 최신 소스로 단일 EXE를 생성한다.

#### 처리

- B/I/U/S 버튼의 checked 상태와 커서 서식 동기화에 맞춰 `✓` 표시와 `선택됨/선택 안 됨` 접근성 이름이 갱신되게 했다.
- 모호한 `▧` 문자를 제거하고 테두리·태양·산으로 구성된 20px 벡터 사진 아이콘을 이미지 삽입 버튼에 적용했다.
- 프로그램 설정의 9개 단축키 항목을 기본 2열, 좁은 폭 1열의 반응형 grid로 바꾸고 하단 stretch를 제거했다. 전체 콘텐츠의 `sizeHint` 높이를 확보해 안내문과 저장 버튼 잘림을 방지했다.
- 메모 전용 탭에 별도 스타일을 적용해 상단 작업공간 버튼 묶음과 좌우 열을 맞췄다.
- 5분·30분·1~6시간·1일 빠른 알림 버튼 9개를 내용 맞춤 폭으로 한 행에 배치했다.
- 메모·알림·서식·설정 저장·백업·복원·폴더 선택과 SQLite 데이터 계약은 변경하지 않았다.

#### 검증과 배포

- 변경 모듈 `py_compile`과 체크 표시·이미지 아이콘·빠른 알림 동작·탭 정렬·설정 2열/1열·백업/복원을 다루는 집중 테스트 7개가 모두 통과했다.
- 전체 Qt 파일 단위 실행은 기존 네이티브 종료 코드 `-1073740791`로 끝나 이번 변경은 관련 테스트를 독립 프로세스로 실행해 확인했다.
- 실제 Windows 1177×887 메인창에서 B 체크 표시, 사진 아이콘, 탭 열 정렬, 빠른 알림 9개 한 줄과 텍스트 잘림 없음을 확인했다. 실제 설정 창에서는 2열 단축키, 하단 안내문과 저장 버튼 전체 표시를 확인했다.
- PyInstaller 6.20.0으로 `release/toma_shortcut_program.exe`를 단일 파일로 생성했다. 결과는 64,768,443 bytes, SHA256 `E713A777C17D305558966525E9C1556DC6FA1E2C392212B5446EE787C7E9BB05`다.
- 새 EXE와 `LOCALAPPDATA`를 별도 임시 경로로 격리해 6초 이상 프로세스 유지 여부를 확인한 뒤 검증 프로세스와 임시 폴더만 제거했다.
- `release/data`의 기존 백업 3개는 빌드 전후 파일명·크기·SHA256이 모두 같아 보존됐고 사용자 DB는 실행 검증에 사용하지 않았다. 미완료 항목은 없다.

### 2026-08-25 GPT펫 원본 기반 토마펫 알림·상시 표시 개선

#### 요청과 목표

- 프로젝트의 기존 펫 동작을 기준으로 삼지 않고 현재 Codex GPT펫 Toma의 이미지·프레임·속도·마우스 반응을 그대로 이식한다.
- 토마펫 알림과 상시 표시를 독립 설정으로 제공하고, 완료 반응·말풍선·9개 빠른 재알림·메모/일정 편집을 결합한다.
- 설정창 값 선택 UI의 휠·드래그 오변경을 막고, Computer Use와 EXE 생성은 이번 범위에서 제외한다.

#### 처리

- 현재 GPT펫 원본 `pet.json`·`spritesheet.webp`와 프로젝트 자산의 SHA-256이 각각 일치하는 것을 확인해 이미지 재생성이나 프레임 편집 없이 동일 자산을 사용했다.
- GPT펫의 상태별 프레임 시간, 완료 동작 3회 후 6배 느린 기본 동작, 전용 neutral 프레임, 16방향 시선, hover 점프와 4px 드래그 임계값을 독립 상태 머신으로 구현했다.
- 알림용·상시 표시용 설정을 독립 저장하고, 상시 펫 단일 인스턴스·마지막 위치 복원·화면 안쪽 보정·일시정지·행동 재생·캘린더·빠른 메모·위치 초기화·숨기기 메뉴를 추가했다.
- 아이보리 말풍선에 제목·본문·5+4 배열의 재알림 버튼·확인·메모편집/일정편집을 구성했다. 상시 펫이 있으면 같은 펫을 재사용하고 표시 실패 시 기존 알림창으로 돌아가도록 했다.
- 설정창에 두 토마펫 옵션과 위치 초기화를 추가하고, 콤보박스·숫자·슬라이더 계열의 휠 및 누른 채 이동 입력을 공통 차단했다. 기존 SQLite 구조·사용자 데이터·메모·포스트잇·단축키 계약은 보존했다.

#### 검증과 배포

- 변경 모듈 전체 `py_compile`을 통과했다.
- GPT펫 원본 해시, 정확한 프레임 시간, 3회 완료 반응, 일시정지 중 알림 우선, 9개 재알림, 편집 연결, 독립 설정 저장, 단일 펫 재사용, 휠·드래그 보호와 4px 임계값을 다룬 집중 테스트 9개가 모두 통과했다.
- 기존 기능 테스트 153개를 포함해 총 162개 동작을 독립 실행 방식으로 확인했으며 assertion 실패는 없었다.
- 알림과 설정 화면을 100%·150% 조건으로 렌더링하고, 버튼 높이·두 줄 배열·스크롤·겹침·잘림을 독립 시각 검토해 통과했다.
- 실제 소스 프로그램을 숨김 상태로 5초 실행해 프로세스 유지 여부를 확인한 뒤 검증용 프로세스만 종료했다. Computer Use는 사용하지 않았고 EXE도 생성하지 않았다.

### 2026-08-25 프로그램 설정창 무스크롤 2열 재배치

#### 요청과 목표

- 승인된 설정창 시안처럼 모든 설정을 내부 스크롤 없이 한 화면에서 확인할 수 있게 한다.
- 여백과 카드·버튼 크기를 줄이되 단축키·트레이·토마펫·데이터 관리 기능과 클릭 영역은 유지한다.

#### 처리

- 내부 `QScrollArea`를 제거하고 본문을 `왼쪽 단축키 / 오른쪽 창·트레이와 토마펫`의 2열 구조로 재배치했다.
- 단축키 9개는 기존 순서를 보존해 왼쪽 5개·오른쪽 4개로 표시하고 입력 컨트롤 높이를 36px 이상으로 유지했다.
- 데이터 폴더·폴더 선택·폴더 열기를 한 행에, 전체 백업·전체 복원·안내문을 다음 행에 배치해 `데이터 및 안전` 카드를 압축했다.
- 저장·취소 버튼은 고정 하단에 두고 `Cancel`을 `취소`로 통일했다. 경로는 한 줄로 표시하고 전체 값은 선택과 툴팁으로 확인할 수 있게 했다.
- UI 크기가 125%·150%이면 설정창을 각각 1165×800, 1280×900으로 확장해 확대된 글자와 컨트롤이 겹치지 않게 했다.
- 설정 저장값·단축키 중복 검사·시작 모드·토마펫 위치 초기화·폴더 선택·백업/복원·휠/드래그 입력 보호는 변경하지 않았다.

#### 검증과 배포

- 변경 파일의 `py_compile`을 통과했다.
- 무스크롤 구조·단축키 5+4 배치·카드 순서·취소 한글화·설정 저장·폴더 선택·백업/복원·데이터 보호·토마펫 입력 보호를 다룬 집중 테스트 6개가 모두 통과했다.
- 100% 1050×700, 125% 1165×800, 150% 1280×900으로 렌더링해 스크롤바 0개와 카드·입력·버튼의 겹침 및 잘림 없음을 확인했다.
- 실제 소스 프로그램이 5초 동안 정상 실행 상태를 유지하는 것을 확인한 뒤 검증용 프로세스만 종료했다.
- 기존 사용자 데이터와 EXE는 변경하지 않았으며 새 EXE도 생성하지 않았다. 이번 요청에 따른 미완료 항목은 없다.

### 2026-08-25 메모 편집 화면 시안 구현

#### 요청과 목표

- 승인된 메모 화면 시안처럼 본문 입력 영역을 우선하고, 서식·상태·알림 예약을 압축 배치한다.
- 기존 메모·알림·반복·포스트잇·단축키·토마펫 동작과 SQLite 사용자 데이터를 보존한다.

#### 처리

- 편집기 바깥 여백을 줄이고, 본문 아래에 `서식 1~3`만 별도 행으로 옮겼다. 기존 슬롯 4의 저장값은 삭제하지 않았다.
- 글자 색상 조작부 바로 아래에 `단축키 설정`을 배치하고, 항상 위·포스트잇 모드·저장 상태를 본문 아래 오른쪽으로 정리했다.
- 알림 예약의 기존 날짜·시간·반복·빠른 시간·저장/해제 조작을 접이식 상세 영역에 넣고, 기본 접힘 상태와 마지막 선택 상태를 기존 settings 테이블에 저장했다.
- 화면 빠른 알림 버튼은 첫 클릭을 현재 시각 기준으로 잡고 이후 클릭 시간을 누적하게 했다. 키보드 빠른 알림의 즉시 저장 흐름은 유지했다.
- 상태 배지와 알림 접기 버튼의 여백을 압축하고, 키보드 포커스·접기 상태의 접근성 이름을 제공했다.

#### 검증과 배포

- 변경 모듈 `py_compile`을 통과했다.
- 접힌 알림 상태, 상태 복원, 화면 빠른 알림 누적, 수동 시간 변경 후 기준 재설정, 프리셋 1~3, 스플리터 재배치, 서식·색상·휠 차단을 포함한 집중 UI 테스트 6개가 모두 통과했다.
- 소스 프로그램을 5초간 실행해 프로세스가 유지되는 것을 확인했다. Computer Use와 EXE 생성은 요청 범위에 따라 수행하지 않았다.
- 이번 요청과 직접 관련된 미완료 항목은 없어 `3._TODO.md`는 변경하지 않았다.

### 2026-08-25 상단 내비게이션 한 줄 통합과 EXE 재빌드

#### 요청과 목표

- 두 행을 차지하던 작업공간 전환과 메모 하위 탭을 한 줄로 합쳐 본문 공간을 확보한다.
- 기존 전환 기능과 사용자 데이터를 보존하고 단일 EXE를 생성한다.

#### 처리

- 작업공간 버튼을 내용 맞춤 폭·36px 높이로 줄이고 상단 바의 위아래 여백을 6px로 축소했다.
- 메모·일정 작업공간에서만 `메모 편집·캘린더·알림내역` 버튼을 같은 행에 표시하고 기존 QTabWidget 페이지와 양방향 동기화했다.
- 기존 탭 막대는 숨겼지만 페이지, currentChanged 신호, 메모·일정 직접 열기와 Shift+Tab 전환은 그대로 유지했다.
- Shift+Tab 안내를 오른쪽 끝으로 옮기고 기존 좁은 창 숨김 규칙을 유지해 가로 잘림을 방지했다.

#### 검증과 배포

- 변경 모듈 `py_compile`을 통과했다.
- 상단 높이 50px 이하, 한 줄 정렬, Shift+Tab, 하위 탭 양방향 동기화와 좁은 화면 회귀를 다룬 반응형 UI 테스트 3개가 모두 통과했다.
- 기존 메모 편집·스플리터·알림 접기 관련 집중 테스트 3개가 모두 통과했고, 소스 프로그램이 5초간 정상 실행됐다.
- PyInstaller 6.20.0으로 `release/toma_shortcut_program.exe`를 생성했다. 크기는 64,789,927 bytes, SHA256은 `4247E5264E114F273A1DAD6B4206EB521F7EE74653B0E30633554AE44EB6E55C`다.
- EXE를 격리된 임시 LOCALAPPDATA에서 6초 실행해 프로세스 유지를 확인한 뒤 검증 프로세스와 임시 폴더만 제거했다. 빌드 전후 `release/data`는 존재하지 않았으며 사용자 데이터는 실행 검증에 사용하지 않았다.
- 이번 요청에 따른 미완료 항목은 없어 `3._TODO.md`는 변경하지 않았다.

### 2026-08-27 프로그램명 토마 데스크 통일 및 단일 EXE 생성

#### 요청과 목표
- 좌측 상단을 포함한 사용자 노출 프로그램명을 `토마 데스크`로 통일한다.
- 콘솔 없는 단일 실행 파일을 새 제품명으로 생성하고 실제 실행을 확인한다.

#### 처리
- 공통 앱 이름, 중복 실행 안내, 도움말 제목, 트레이 표시와 현재 배포 안내를 `토마 데스크`로 바꿨다.
- 사용설명서 생성기의 제목·메타데이터·파일명을 `토마 데스크`로 갱신해 `토마_데스크_사용설명서.pdf`를 생성했다.
- 이전 제목의 현재 사용설명서 `토마_단축키_런처_사용설명서.pdf`는 새 PDF 생성과 텍스트 검증 후 제거했다.
- PyInstaller 스테이징 이름은 ASCII로 유지하고, 빌드 스크립트가 최종 파일을 `release/토마 데스크.exe`로 이름 변경하게 했다. Windows PowerShell의 UTF-8 무서명 스크립트 한글 해석 문제는 유니코드 코드값으로 파일명을 조합해 해결했다.
- `%LOCALAPPDATA%/CodexShortcutLauncher`, SQLite 데이터 위치와 단일 인스턴스 mutex는 변경하지 않았다. 빌드 전후 `release/data`는 존재하지 않아 사용자 데이터는 건드리지 않았다.

#### 검증과 배포
- 변경 Python 파일의 `compileall`을 통과했고, offscreen Qt에서 `APP_NAME`과 도움말 창 제목이 모두 `토마 데스크` 기준인지 확인했다.
- 실제 소스 프로그램을 5초 실행해 프로세스 유지 여부를 확인했다.
- PDF는 5페이지, 제목 메타데이터와 첫 페이지의 `토마 데스크` 텍스트를 확인하고 페이지 PNG를 렌더링했다. 현재 작업 경로 연결 오류로 이 대화에서 렌더 PNG 미리보기는 열 수 없었다.
- PyInstaller 6.20.0으로 `release/토마 데스크.exe`를 생성했다. 크기는 79,532,142 bytes, SHA256은 `E6370617303C71BCD95691C8E8F9AE4EFF2E605D0C6FF9E5E8BC067ACB628F37`이다.
- 새 EXE를 5초 실행해 프로세스 유지 여부를 확인한 뒤 검증 프로세스만 종료했다. 이번 요청과 직접 관련된 미완료 항목은 없어 `3._TODO.md`는 변경하지 않았다.

### 2026-08-27 현재 소스 기준 단일 EXE 재생성

#### 요청과 목표

- 기존 EXE 생성 이력과 관계없이 현재 작업 폴더의 소스·PyInstaller 설정을 기준으로 단일 EXE를 처음부터 다시 생성한다.
- 콘솔 없는 실행과 실제 프로세스 유지 여부를 확인한다.

#### 처리

- `build_release.ps1 -Clean`으로 PyInstaller 6.20.0 단일 파일 빌드를 새로 실행했다.
- 빌드 과정에서 이전 EXE와 `.package-build`만 교체·정리했으며, 원본 소스·SQLite 사용자 데이터·백업·문서는 변경하지 않았다.
- 빌드 전후 `release/data`는 존재하지 않았으므로 사용자 데이터는 빌드나 검증에 사용되지 않았다.

#### 검증과 배포

- 결과물 `release/TomaDesk.exe`의 존재·크기·SHA-256을 확인했다: 100,267,702 bytes, `0E78DAFB9D51A161BFF10EA4C98D0DF7089444F22604257E8DCE5F14D6759850`.
- 임시 `LOCALAPPDATA`에서 EXE를 6초 실행해 프로세스 유지 상태를 확인한 뒤 검증용 프로세스만 종료하고 임시 폴더를 제거했다.
- 이번 요청에 직접 관련된 미완료 항목이 없어 `3._TODO.md`는 변경하지 않았다.

### 2026-08-28 요청 기준 단일 EXE 재빌드

#### 요청과 목표

- 현재 작업 폴더의 소스와 설정을 기준으로 콘솔 없는 단일 EXE를 다시 생성하고 실제 실행 상태를 확인한다.

#### 처리

- 실행 중이던 `toma_shortcut_program` 인스턴스가 모두 종료된 것을 확인한 뒤 `build_release.ps1 -Clean`으로 새 빌드를 실행했다.
- 기존 EXE와 `.package-build`만 교체·정리했으며, 원본 소스·SQLite 사용자 데이터·백업·문서는 변경하지 않았다.
- 빌드 전후 `release/data`는 존재하지 않아 사용자 데이터는 빌드와 검증에 사용되지 않았다.

#### 검증과 배포

- PyInstaller 6.20.0으로 `release/TomaDesk.exe`를 생성했다. 크기는 100,267,137 bytes, SHA-256은 `B1663B5100E125A27164E22572F65C1C4DF9ECD118457EB6353761A060EA3906`이다.
- 임시 `LOCALAPPDATA`에서 EXE를 5초 실행해 프로세스 유지 상태를 확인한 뒤 검증용 프로세스만 종료했고, 임시 폴더 제거도 확인했다.
- 기존 미완료 항목은 이번 요청과 직접 관련되지 않아 `3._TODO.md`를 변경하지 않았다.

### 2026-08-28 메모창 선택 상태 및 공통 상단 액션 UI 개선

#### 요청과 목표

- 메모 서식·프리셋은 실제 활성 상태에서만 파란색으로 표시하고 버튼 폭과 배치를 콘텐츠에 맞춘다.
- 빠른 알림을 한 행으로 만들고 반복 유형·반복 종료·메모 전역 단축키 동작을 배타적 선택 컨트롤로 바꾼다.
- `시작/휴지통/설정/도움말`을 모든 작업공간에서 보이는 공통 상단 우측에 배치한다.

#### 처리

- B/I/U/S·글머리표·체크리스트에서 고정 폭과 문자 `✓`를 제거하고 공통 neutral/checked 스타일을 적용했다. 글머리표도 현재 블록 상태와 선택 표시가 동기화된다.
- `서식 1~3`을 같은 크기로 맞추고 직접 적용된 프리셋만 현재 서식이 유지되는 동안 선택 표시하도록 했다. `기본값` 옆에는 이름을 줄인 `단축키` 버튼을 배치했다.
- 빠른 알림 9개를 intrinsic width의 한 행으로 바꿨다.
- 반복 유형은 `반복 안 함`을 포함한 5개 segmented control, 반복 종료와 메모 단축키 동작은 각각 라디오 그룹으로 바꿨다. 기존 combo는 숨은 호환 어댑터로 유지해 저장 문자열과 로드·저장 경로를 보존했다.
- 공통 액션 4개를 workspace mode bar 우측으로 옮겨 단축키·반복작업과 메모·일정의 모든 하위 탭에서 표시했다. 기존 목록 제목 행에는 `＋ 새 작업/삭제`만 유지했다.
- SQLite 스키마·사용자 데이터·자동저장·알림·반복 계산·전역 단축키 등록·포스트잇 기능은 변경하지 않았다.

#### 검증과 배포

- `compileall`을 통과했다. `test_alert_notes`, `test_ui_responsive`, `test_alert_note_phase1`, `test_confirmed_memo_ux`, `test_feature_updates`, `test_memo_management`, `test_ux_safety_updates`의 총 129개 테스트를 통과했다.
- 임시 DB와 가짜 전역 입력 연결을 사용한 `A_shortcut_launcher.main()` 진입점을 6초 실행해 종료 코드 0을 확인했다. 실제 실행 중인 TomaDesk와 사용자 DB는 건드리지 않았다.
- 1247×849 공통 상단, 1420×720 메모 화면, 680×880 독립 메모창을 렌더해 한국어, 선택 상태, segmented/radio 배치와 빠른 알림 한 행을 확인했다. 메모·독립창의 가로 스크롤 최대값은 모두 0이었다.
- 검증 이미지는 `output/ui_selection_top_actions_1247x849.png`, `output/ui_selection_memo_1420x720.png`, `output/ui_selection_standalone_680x880.png`에 남겼다.
- 이번 작업의 미완료 항목은 없어 기존 `3._TODO.md`를 변경하지 않았고, 사용자 요청이 없어 EXE를 생성하지 않았다.

### 2026-08-28 PyQt6 concrt140.dll EXE 실행 오류 수정

#### 요청과 목표

- `TomaDesk.exe` 실행 시 `concrt140.dll`을 찾지 못한다는 PyInstaller 오류를 수정하고, EXE가 오류 대화상자 없이 실제로 시작되는지 확인한다.

#### 처리

- EXE 아카이브를 확인해 `concrt140.dll`이 `PyQt6\\concrt140.dll`에 잘못 배치된 것을 확인했다. `shortcut_launcher.spec`의 Qt DLL 대상 경로를 `PyQt6/Qt6/bin`으로 수정했다.
- `pyqt6_runtime_hook.py`에서 Qt DLL 폴더를 Windows DLL 검색 경로에 등록한 뒤 런타임 DLL을 미리 로드하도록 보완했다. DLL의 추가 의존성이 같은 Qt DLL 폴더에서 해석되도록 한 조치다.
- 프로그램 기능·UI·SQLite 사용자 데이터·백업은 변경하지 않았다.

#### 검증과 배포

- 런타임 훅의 `py_compile`을 통과했다.
- PyInstaller 6.20.0으로 `release/TomaDesk.exe`를 다시 생성했다. 아카이브에서 `PyQt6\\Qt6\\bin\\concrt140.dll`과 Qt 핵심·Visual C++ 런타임 DLL의 배치를 확인했다.
- 임시 `LOCALAPPDATA`에서 EXE를 6초 실행했다. PyInstaller 부모·실제 앱 프로세스 2개가 유지됐고, 이전의 `Unhandled exception in script` 오류 대화상자는 감지되지 않았다. 검증용 프로세스와 임시 폴더는 모두 제거했다.
- 이번 오류 수정과 직접 관련된 미완료 항목은 없어 `3._TODO.md`를 변경하지 않았다.

### 2026-08-28 요청 기준 단일 EXE 생성

#### 요청과 목표

- 현재 소스와 설정을 기준으로 콘솔 없는 단일 `TomaDesk.exe`를 생성하고 실제 실행을 확인한다.

#### 처리

- 실행 중인 `TomaDesk`·`toma_shortcut_program` 프로세스가 없는 것을 확인한 뒤 `build_release.ps1 -Clean`으로 빌드했다.
- 새 `release/TomaDesk.exe`만 생성했으며 원본 소스, `data`, `backup`, SQLite 사용자 데이터, 문서와 프로그램 기능은 변경하지 않았다.

#### 검증과 배포

- PyInstaller 6.20.0 빌드를 완료했다. 결과물은 66,698,822 bytes, SHA-256 `67F22B7A20F7A4A481D40087B739CE341E51380E8D75F427FDDAE7BAE655339C`이다.
- EXE 아카이브에서 `pyqt6_runtime_hook`과 `PyQt6\\Qt6\\bin\\concrt140.dll` 포함을 확인했다.
- 임시 `LOCALAPPDATA`에서 6초 실행해 프로세스가 유지됨을 확인한 뒤 검증용 프로세스와 임시 폴더만 정리했다.
- 기존 미완료 항목은 이번 요청과 직접 관련되지 않아 `3._TODO.md`를 변경하지 않았다.

### 2026-08-28 메모목록·편집창·휴지통 레이아웃 개선

#### 요청과 목표
- 메모목록과 휴지통의 첫 헤더를 전체선택 체크박스로 바꾸고 휴지통 체크 항목을 일괄 복원한다.
- 서식 버튼 크기, 글씨체 콤보박스 빈 폭, 빠른 알림 배치와 메모 편집창 너비를 정돈한다.

#### 처리
- 메모목록의 `선택` 헤더와 하단 중복 전체선택 버튼을 제거하고 전체·부분·미선택 상태를 행 체크와 동기화하는 공용 헤더를 적용했다.
- 휴지통을 `선택/종류/이름/삭제 시각` 구조로 바꾸고 ID·복원 콜백은 행 데이터에 보존했다. 체크 항목은 일괄 복원하며 체크가 없으면 현재 행 하나를 복원한다.
- B/I/U/S/글머리표/체크리스트와 이미지 삽입 버튼을 42×38px로 통일하고 두 번째 툴바 간격을 줄였다.
- 글씨체 콤보박스를 130px 내용 폭으로 줄이고 뒤쪽 크기·글자색 컨트롤을 왼쪽으로 이동했다. 글자색 선택과 빠른 색상 기능은 유지했다.
- 빠른 알림 9개를 첫·마지막 버튼 양끝 정렬과 균등한 중간 간격으로 배치했다.
- 독립 메모창을 기본 600×880px, 최소 480×640px로 줄이고 메인 편집 최소 폭도 480px로 조정했다. 기존 저장 창 폭은 위치·높이를 유지하며 한 번만 80px 축소하도록 기존 settings 저장소에 호환 상태를 기록한다.
- 메모·서식·알림·포스트잇·자동저장·단축키·SQLite 스키마와 사용자 데이터는 변경하지 않았다.

#### 검증과 배포
- `compileall`을 통과했다. `test_memo_management` 12개, `test_alert_note_phase1` 16개, `test_confirmed_memo_ux` 6개, `test_ux_safety_updates` 4개와 편집기 집중 테스트 3개 등 총 41개를 통과했다.
- Qt 창 누적 실행에서 발생하는 테스트 프로세스 종료를 피하기 위해 편집기 집중 테스트는 각각 독립 프로세스로 실행했으며 모두 통과했다.
- 임시 데이터베이스와 가짜 전역 입력 연결로 실제 PyQt 창을 렌더했다. 서식·이미지 버튼은 모두 42×38px, 글씨체 폭은 130px, 독립창 가로 스크롤 최대값은 0이었다.
- 빠른 알림은 600px 독립창에서 첫·마지막이 콘텐츠 양끝에 맞고 중간 간격이 26~27px였다. 휴지통 열 너비 합은 viewport 폭과 일치했다.
- 검증 이미지는 `output/ui_memo_layout_main_1247x849.png`, `output/ui_memo_layout_main_1420x720.png`, `output/ui_memo_layout_standalone_600x880.png`, `output/ui_memo_layout_trash_680x420.png`에 남겼다. 두 메인 화면과 독립창의 가로 스크롤 최대값은 0이었다.
- 기존 실행 중인 Python 프로그램과 사용자 DB는 건드리지 않았고, 이번 작업의 미완료 항목은 없어 `3._TODO.md`는 변경하지 않았다. 사용자 요청이 없어 EXE는 생성하지 않았다.
### 2026-08-31 장기 업데이트 1단계 안정성·데이터 보호

#### 요청과 목표

- 승인된 장기 업데이트 계획의 1단계부터 구현한다.
- 기본 단축키·설정 저장·D-Day·Excel 가져오기·미러 일정·알림 중지·백업 복원에서 재현된 오류와 데이터 소실 위험을 해소한다.

#### 처리

- 단축키 파서와 입력 빌더를 수식키 3개까지 지원하도록 맞추고 `Ctrl+Alt+Shift+N` 기본값을 손실 없이 표시·저장하게 했다.
- 잘못되거나 중복된 단축키 변경은 기존 값을 유지하면서 다른 프로그램 설정은 저장할 수 있게 했다.
- deadline 알림을 완료·삭제·전체 해제하면 D-Day 날짜는 유지하고 `d_day_alert`를 함께 끄도록 정합성을 맞췄다.
- Excel 가져오기 전에 가져올 수, 교체되는 현재 작업 수, 영구 삭제되는 휴지통 수와 백업 경로를 보여 주고 확인 후에만 백업·교체한다.
- 알림 미러 일정을 삭제·재생성하던 동기화를 같은 ID 부분 갱신으로 바꿔 분류·우선순위·미리 알림을 보존했다. 미러 일정은 읽기 전용 안내와 함께 저장·삭제·드래그 이동을 차단했다.
- 예정 알림과 처리 이력의 삭제는 체크된 행만 대상으로 삼아 강조된 현재 행이 자동 삭제되지 않게 했다.
- 매크로 녹화·재생·저장 전 테스트 동안 알림 폴링을 중첩 안전하게 일시 중지하고 종료 후 다시 확인하도록 했다.
- 전체 복원 문구를 실제 동작과 맞추고 수동·자동·복원 직전 백업이 모두 회전 대상이 되도록 파일 패턴을 정정했다.
- SQLite 스키마, 설정 키, 백업 JSON, 매크로 payload, 포스트잇 상태와 사용자 데이터를 변경하지 않았다.

#### 검증과 배포

- `test_update_plan_stage1.py` 전용 회귀 테스트 8개를 통과했다.
- `test_schedule_management.py` 12개, `test_postit_stage2.py` 10개, `test_memo_management.py` 12개와 `test_alert_notes.py`의 알림 서비스 집중 테스트 4개를 각각 독립 프로세스로 통과했다.
- `test_feature_updates.py`의 공통 단축키 등록과 녹화 중 종료 단축키 테스트를 각각 통과했다. 기존 전체 Qt 파일은 창 누적 종료 시 네이티브 종료 코드가 있어 관련 테스트를 독립 실행했다.
- 격리된 `tmp/update-stage1-smoke/data`에서 실제 소스 창을 6초 실행해 종료 코드 0과 두 임시 DB 생성을 확인했다.
- Windows `RegisterHotKey` 메시지 큐에 `Ctrl+Alt+Shift+N`을 등록하고 합성 키 입력으로 콜백 수신과 종료 코드 0을 확인했다.
- 실행 중인 사용자 TomaDesk와 실제 DB, 기존 EXE는 변경하지 않았으며 EXE를 새로 생성하지 않았다.

### 2026-09-04 메모 토글 강화 1단계 — 마우스 모양·빈 토글 안내

#### 요청과 목표

- 승인된 메모 토글 개편안의 1단계를 구현한다.
- 토글 표시 위에서 글자 입력용 커서가 그대로 떠 눌리는 자리인지 알 수 없던 문제를 없앤다.
- 펼쳐 두었는데 안이 빈 토글에 아무 표시도 남지 않던 문제를 없앤다.

#### 처리

- 토글 표시와 체크리스트 네모 위에 마우스가 오면 손가락 커서로 바꾸고, 벗어나면 글자 입력용 커서로 되돌린다. 창을 벗어날 때도 되돌린다.
- 표준 손가락 커서를 그대로 쓰므로 125%·150% 배율에서도 흐려지지 않는다. 그림을 커서로 등록하지 않았다.
- 마우스가 올라온 표시 뒤에 옅은 사각형을 그려 누를 자리를 보이게 했다. 누르는 자리(22px)는 그대로 두고 그리는 사각형만 좁혀 제목 글자를 덮지 않게 했다.
- 펼친 토글에는 늘 안쪽 빈 줄이 하나 있도록 맞추고, 그 줄이 비어 있는 동안 "빈 토글입니다. 클릭하거나 블록을 내부로 드래그하세요."를 회색으로 그린다.
- 안내는 화면에만 그리므로 저장한 메모 내용에는 이 문장이 들어가지 않는다. 편집기 폭이 좁으면 한 줄로 줄여 그려 다음 줄을 덮지 않는다.
- 토글에서 Enter 를 누르면 안내가 떠 있던 빈 줄을 그대로 첫 내용 줄로 쓴다. 줄을 새로 만들지 않으므로 빈 줄이 겹치지 않는다.
- 토글을 되돌리면 남아 있던 빈 줄도 함께 걷어낸다. 접은 토글에는 안내를 그리지 않는다.
- 이 기능이 생기기 전에 만든 빈 토글은 메모를 열 때 안쪽 줄을 만들어 주되, 그것만으로 수정 상태가 되지 않게 했다.
- SQLite 스키마, 설정 키, 자동저장 규칙과 사용자 데이터는 변경하지 않았다.

#### 검증과 배포

- `test_memo_toggle.py` 25개를 통과했다. 빈 토글 안내 8개와 커서 모양 4개를 새로 추가했다.
- `test_memo_management` 12개, `test_confirmed_memo_ux` 6개, `test_ux_safety_updates` 4개, `test_postit_stage1` 13개, `test_postit_stage2` 10개, `test_alert_note_phase1` 16개, `test_ui_responsive` 9개를 각각 독립 프로세스로 통과했다. 모두 95개다.
- 실제 PyQt 창을 렌더해 빈 토글 안내와 마우스 올림 사각형을 확인했고, 올린 상태의 커서가 `PointingHandCursor` 임을 확인했다.
- 실행 중인 사용자 TomaDesk 와 실제 DB, 기존 EXE 는 건드리지 않았고 EXE 를 새로 만들지 않았다.

### 2026-09-04 토글 사용성 보완 4건과 메모 층 나누기 2단계

#### 요청과 목표

- 2단계에 들어가기 전에 토글 밖 입력, 목록 체크 정렬, 작업공간 단추 폭, 포스트잇 반영 네 가지를 먼저 고친다.
- 이어서 메모 표에 부모·순서 칸을 추가한다. 화면은 그대로 둔다.

#### 처리 — 보완 4건

- 마지막 줄 아래 빈 곳을 누르면 토글 밖 맨 위층에 새 줄을 만들고 커서를 놓는다. 토글에 늘 안쪽 줄이 있어 문서 끝을 누르면 커서가 토글 안으로 들어가던 문제를 없앴다. 토글이 없는 메모에서는 지금과 똑같이 동작하며 빈 줄이 늘지 않는다.
- 접힌 토글 뒤에 새 줄을 만들 때도 그 줄은 숨지 않는다.
- 메모 목록 체크 상자를 칸 한가운데에 그리는 전용 그리개를 넣었다. 누르는 자리도 그 네모로 맞췄고, 두 번 눌러 제자리로 돌아오던 것도 막았다.
- 머리글 전체선택 체크는 위젯이 아니라 네모 그림을 기준으로 가운데 맞춘다. 빈 QCheckBox 가 글자 자리를 남겨 두어 왼쪽으로 치우쳐 보이던 원인을 없앴다.
- `단축키·반복작업`·`메모·일정` 단추의 `min-width: 142px` 를 걷어내고 글자 폭에 맞췄다. 132px 과 93px 로 갈렸다. 남는 자리를 받아 늘어나지 않도록 크기 정책도 Maximum 으로 바꿨다.
- 포스트잇 서식 막대에 토글 단추를 더하고 메모 편집창과 같은 순서로 두었다. 포스트잇도 `> ` 입력, 빈 토글 안내, 손가락 커서가 모두 같이 동작한다.

#### 처리 — 2단계

- `notes` 표에 `parent_id`(부모 메모, 0 은 맨 위층)와 `sort_order`(형제 사이 차례, 0 은 직접 옮긴 적 없음)를 더하고 `idx_notes_parent` 를 만들었다.
- 기존 메모는 모두 `parent_id=0` 으로 남는다. 칸이 없던 파일을 열면 바꾸기 전에 `alert_notes_before_upgrade_*.db` 백업을 먼저 뜬다.
- `child_notes`·`note_descendants`·`can_reparent`·`set_note_parent`·`reorder_notes` 를 더했다. 자기 자신이나 자기 안의 메모 밑으로 옮기는 고리는 막는다.
- 부모를 지우면 하위 메모도 한 덩어리로 휴지통에 들어가고, 되살리면 함께 버려진 것만 함께 돌아온다.
- 하위 메모만 따로 되살리면 부모가 아직 휴지통이므로 맨 위층으로 올린다. 부모가 영구 삭제돼 번호만 남은 메모도 맨 위층으로 올린다.
- `notes()` 는 그대로 평평한 목록을 돌려준다. 화면과 자동저장, 설정 키는 바뀌지 않았다.

#### 검증과 배포

- `test_memo_tree_stage2.py` 16개를 새로 만들어 통과했다. 칸 추가·기존 파일 승급·백업·옮기기·고리 차단·차례 고정·휴지통 묶음 처리를 덮는다.
- `test_memo_toggle` 33개(토글 밖 입력 4개와 포스트잇 4개 추가), `test_memo_management` 14개(체크 정렬 2개 추가), `test_ui_responsive` 10개(단추 폭 1개 추가)를 통과했다.
- `test_alert_notes` 24개, `test_feature_updates` 59개, `test_schedule_management` 30개, `test_alert_note_phase1` 16개, `test_postit_stage1` 13개, `test_postit_stage2` 10개, `test_update_plan_stage1` 8개, `test_update_plan_stage2` 6개, `test_confirmed_memo_ux` 6개, `test_ux_safety_updates` 4개를 각각 독립 프로세스로 통과했다. 모두 249개다.
- 실제 PyQt 창을 렌더해 목록 체크 정렬, 작업공간 단추 폭, 포스트잇 토글 단추와 빈 토글 안내를 확인했다.
- 실행 중인 사용자 TomaDesk 와 실제 DB, 기존 EXE 는 건드리지 않았고 EXE 를 새로 만들지 않았다.

### 2026-09-04 기능 펼침 단추와 메모 층 나누기 3단계

#### 요청과 목표

- 토글 단추를 앞으로 늘어날 기능까지 담는 펼침 단추로 바꾸고, 눌린 상태가 남지 않게 한다. 포스트잇도 똑같이 맞춘다.
- 이어서 메모 목록을 접었다 펼 수 있는 표로 바꾼다.

#### 처리 — 기능 펼침 단추

- `alert_notes/insert_menu.py` 를 새로 두고 넣을 수 있는 기능 목록을 한 곳에 모았다. 메모 편집창과 포스트잇이 같은 목록을 쓴다.
- 편집기에 그 기능이 아직 없으면 항목을 "(준비 중)" 으로 흐리게 보여 준다. 4단계에서 `insert_page_link` 가 생기면 목록만으로 켜진다.
- 메모 서식 막대의 `▾` 토글 단추를 `＋` 펼침 단추로 바꿨다. 체크 가능 속성을 없애 켜짐/꺼짐 표시가 남지 않는다.
- 포스트잇 서식 막대도 같은 자리에 `＋` 단추를 두고 같은 목록을 붙였다. 쓰지 않게 된 `toggle_requested` 신호와 토글 아이콘은 걷어냈다.
- 두 단추 모두 목록 표시 화살표를 감춰 42×38·36×36 크기 안에서 글자가 눌리지 않게 했다.

#### 처리 — 3단계

- 메모 목록을 `QTableWidget` 에서 열이 있는 트리(`MemoTree`)로 바꿨다. 다섯 열, 선택 체크, 열 너비 저장, 검색, Excel 내보내기, 선택 삭제는 그대로다.
- 펼침 화살표는 `setTreePosition(1)` 로 제목 열에 두었다. 0 번 열은 선택 체크 자리로 남는다.
- 접어 둔 부모 오른쪽 끝에 안에 든 개수를 숫자만 그린다. 펼치면 숫자를 지운다. "하위" 같은 낱말을 붙이지 않아 제목 자리를 잡아먹지 않는다.
- 펼침 상태는 `memo_list_expanded_ids` 설정에 담아 다시 열어도 그대로다. 검색 중에는 모두 펼쳐 찾은 메모가 접힌 부모에 가려지지 않게 하고, 그 상태는 저장하지 않는다.
- 검색으로 부모가 걸러졌을 때는 찾은 메모를 맨 위층으로 올려 보여 준다.
- 줄을 끌어 다른 메모 안으로 넣거나 형제 사이 자리를 바꿀 수 있다. 놓은 자리는 저장소에 바로 적어 다시 그린다. 고리가 되는 이동은 막고 상태줄로 알린다.
- 부모를 지울 때 안의 메모가 몇 건 함께 가는지 물음창에 적었다.
- 끌어 놓은 차례가 곧 목록 차례이므로 머리글 눌러 정렬하기는 껐다. 손대지 않은 메모는 지금까지처럼 수정 시간 최신순이다.

#### 검증과 배포

- `test_memo_tree_stage3.py` 22개를 새로 만들어 통과했다. 층 모양·화살표 자리·개수 표시·펼침 저장·검색·선택·차례·이동·휴지통 안내와 놓은 자리 계산을 덮는다.
- `test_memo_toggle` 34개(펼침 단추 확인 포함), `test_memo_management` 14개를 트리에 맞춰 고쳐 통과했다.
- `test_alert_notes` 24개, `test_feature_updates` 59개, `test_schedule_management` 30개, `test_memo_tree_stage2` 16개, `test_alert_note_phase1` 16개, `test_postit_stage1` 13개, `test_postit_stage2` 10개, `test_ui_responsive` 10개, `test_confirmed_memo_ux` 6개, `test_update_plan_stage1` 8개, `test_update_plan_stage2` 6개, `test_ux_safety_updates` 4개를 각각 독립 프로세스로 통과했다. 모두 272개다.
- 실제 PyQt 창을 렌더해 접힌 부모의 숫자, 펼친 부모의 하위 줄, 선택 체크 정렬과 펼침 단추 목록을 확인했다.
- SQLite 스키마와 자동저장 규칙, 사용자 데이터는 이번에 바꾸지 않았다. 실행 중인 사용자 TomaDesk 와 실제 DB, 기존 EXE 는 건드리지 않았고 EXE 를 새로 만들지 않았다.

### 2026-09-04 좁은 메모 목록 읽기 개선 3건과 줄 높이 축소

#### 요청과 목표

- 좁은 목록에서 제목이 잘리는 문제, 목록 화살표의 마우스 표시, 선택한 줄이 너무 어두운 문제를 고친다.
- 목록의 줄 높이가 불필요하게 큰 것도 함께 줄인다.

#### 처리

- 선택색: 3단계에서 목록을 표에서 트리로 바꿨는데 화면 서식에는 `QTableWidget` 규칙만 있어 Qt 기본 선택색이 그대로 나왔다. `QTreeWidget#memoListTable` 에 같은 옅은 파랑(`#dfeaff` 바탕, `#173b6c` 글자)과 겹침색을 주었다.
- 펼침 화살표 자리(`::branch`)에는 색을 주지 않았다. 배경을 주는 순간 Qt 가 화살표 그림을 통째로 지우는 것을 실제 렌더로 확인했다.
- 화살표 위에 마우스가 오면 손가락 커서로 바꾸고 화살표 뒤에 옅은 사각형을 그린다. 벗어나거나 목록을 떠나면 되돌린다. 하위가 없는 메모에는 뜨지 않는다.
- 머리글 이름을 `내용`·`일정`·`수정시간` 으로 줄였다. 머리글 글자가 곧 그 열의 최소 폭이라, 좁은 목록에서 제목이 잘리던 가장 큰 까닭이었다. 넓은 창에서도 같은 이름을 쓴다.
- 목록 폭이 520px 밑으로 내려가면 `수정시간` 열을 접는다. 접힌 값은 제목 말풍선에 남기고, 넓히면 바로 돌아온다.
- 제목이 잘릴 상황이면 `수정시간 → 일정 → 내용` 차례로 폭을 빌려 제목에 준다. 잘리지 않으면 아무 열도 움직이지 않는다. 펼치고 접을 때마다 다시 계산한다.
- 접은 열이 있는 동안에는 열 너비 비율을 저장하지 않는다. 좁을 때의 폭이 저장되면 다음에 열었을 때 열 하나가 사라진 채로 남기 때문이다.
- 들여쓰기를 16px 에서 12px 로, 줄 높이를 48px 에서 32px 로 줄였다.

#### 검증과 배포

- 430px 폭에서 제목 열이 72px → 191px 가 됐다. 이 목록이 제목을 다 보여 주는 데 필요한 값이 191px 다. 760px 에서는 다섯 열 모두 보이며 제목이 220px 다.
- `test_memo_tree_stage3.py` 를 35개로 늘려 통과했다. 머리글 이름, 좁을 때 열 접기·복귀, 말풍선, 제목 폭, 빈 띠 없음, 폭 저장 차단, 줄 높이와 화살표 손가락 커서 4개를 새로 덮는다.
- `test_memo_management` 14개, `test_alert_notes` 24개, `test_feature_updates` 59개, `test_memo_toggle` 34개, `test_alert_note_phase1` 16개, `test_ui_responsive` 10개, `test_postit_stage1` 13개, `test_confirmed_memo_ux` 6개를 각각 독립 프로세스로 통과했다.
- 실제 PyQt 창을 430px·760px 두 폭으로 렌더해 열 접힘, 제목 표시, 선택색과 줄 높이 32px 를 확인했다.
- SQLite 스키마와 사용자 데이터는 바꾸지 않았다. 실행 중인 사용자 TomaDesk 와 실제 DB, 기존 EXE 는 건드리지 않았고 EXE 를 새로 만들지 않았다.

### 2026-09-04 메모 층 나누기 4단계 — 페이지 추가

#### 요청과 목표

- 메모 안에 메모를 넣는 "페이지"를 만든다. 목록의 + 단추, 본문의 페이지 줄, 위치 표시줄, 고리 방지까지.

#### 처리

- 저장소에 `note_path`(맨 위층부터 이 메모까지)와 `create_child_note` 를 더했다. `note_path` 는 어쩌다 고리가 생겨도 지나온 번호를 기억해 멈춘다.
- 본문에 `insert_page_link` 를 더했다. 그 메모의 하위로 새 메모를 만들고, 본문에는 `toma-note://<번호>` 를 가리키는 `📄 제목` 한 줄을 남긴다. 아직 저장하지 않은 메모에서는 안내만 띄우고 만들지 않는다.
- 페이지 줄은 그냥 눌러도 열린다. 바깥 주소와 달리 Ctrl 이 필요 없고, 마우스를 올리면 손가락 커서가 뜬다.
- 페이지 줄의 글자는 열 때마다 그 메모의 지금 제목으로 맞춘다. 하위 메모의 제목을 고치고 돌아와도 옛 이름이 남지 않고, 지운 페이지는 "지운 페이지" 로 보인다.
- 페이지 줄 다음 줄은 맨 서식으로 시작하게 했다. 그러지 않으면 이어서 친 글이 페이지 줄의 서식을 물려받아 페이지로 잘못 읽히고, 다시 열 때 그 글이 페이지 줄로 덮여 사라졌다. 실제 렌더로 확인하고 고쳤다.
- 페이지 줄인지는 줄 첫 글자의 서식과 `📄` 로 시작하는지를 함께 보고 가린다.
- 편집창 맨 위에 위치 표시줄을 더했다. `취미 › 드라마` 처럼 보이고 앞쪽을 누르면 그 메모로 올라간다. 맨 위층 메모에는 뜨지 않는다.
- 목록에서 줄에 마우스를 올리면 제목 열 오른쪽 끝에 `+` 가 뜬다. 누르면 그 메모의 하위로 새 메모가 생기고, 부모를 펼친 뒤 제목 칸에 바로 커서가 간다. 마우스를 올린 줄에는 하위 개수 대신 `+` 를 그린다.
- 기능 펼침 메뉴의 "페이지 추가" 는 `insert_page_link` 가 생기면서 목록만으로 저절로 켜졌다. 메뉴 코드는 손대지 않았다.

#### 검증과 배포

- `test_memo_pages_stage4.py` 17개를 새로 만들어 통과했다. 하위 메모 생성, 본문 줄, 제목 따라가기, 지운 페이지, 이어 친 글 보존, 저장 전 차단, 줄 눌러 열기, 위치 표시줄과 고리 멈춤, 목록 + 단추를 덮는다.
- `test_memo_toggle` 34개, `test_memo_management` 14개, `test_memo_tree_stage3` 35개, `test_alert_notes` 24개, `test_feature_updates` 59개, `test_alert_note_phase1` 16개, `test_postit_stage1` 13개, `test_postit_stage2` 10개, `test_confirmed_memo_ux` 6개를 각각 독립 프로세스로 통과했다.
- 실제 PyQt 창을 렌더해 위치 표시줄, 페이지 두 줄과 그 사이의 평범한 글, 목록의 + 단추를 확인했다.
- SQLite 스키마는 3단계 이후 그대로다. 실행 중인 사용자 TomaDesk 와 실제 DB, 기존 EXE 는 건드리지 않았고 EXE 를 새로 만들지 않았다.

### 2026-09-04 페이지 후속 — + 단추 종료 오류, Esc 되돌아가기, 본문 페이지 숨김

#### 요청과 목표

- 목록의 + 를 누르면 프로그램이 꺼지던 오류를 잡는다.
- 페이지에서 왔던 메모로 돌아가는 Esc 를 메모 편집 중에만 듣도록 넣는다.
- 본문에 넣은 페이지는 메모 목록에 내놓지 않는다.

#### 처리

- + 를 누르면 목록을 다시 그리는데, 마우스가 잡고 있던 줄(`_hover_row`)이 지워진 항목을 그대로 가리켰다. 다음 그리기에서 그 항목을 만지는 순간 `RuntimeError: wrapped C/C++ object ... has been deleted` 가 나고, 그리기 중의 예외라 프로그램이 통째로 꺼졌다.
- 목록을 비우기 전에 잡고 있던 줄을 놓도록 `forget_hover` 를 넣고 `set_rows` 에서 먼저 부른다. 그리는 쪽에서도 이미 지워진 줄이면 없는 셈 치고 넘어가도록 막아 두었다.
- 패널에 얕은 방문 기록을 두고 `go_back` 을 더했다. Esc 는 왔던 메모로 돌아가고, 온 길이 없으면 한 층 위 메모로 올라간다. 둘 다 없으면 아무 일도 하지 않는다. 그 사이 지워진 메모는 건너뛴다.
- Esc 단축키는 메모 편집 구역(`MemoEditor`)에 `WidgetWithChildrenShortcut` 으로 걸어, 캘린더·알림내역이나 다른 창에서는 듣지 않는다.
- `notes` 표에 `embedded` 칸을 더했다. 본문에서 만든 페이지만 1 이고, 목록의 + 로 만든 하위 메모는 0 이라 지금처럼 목록에 보인다.
- 메모 목록은 `embedded` 인 메모를 걸러 낸다. 다만 검색 중에는 보여 준다 — 그러지 않으면 본문 페이지를 영영 찾을 수 없다.
- 부모를 지우면 본문 페이지도 함께 휴지통으로 간다. 하위 메모와 같은 규칙이다.

#### 검증과 배포

- `test_memo_pages_stage4.py` 를 31개로 늘려 통과했다. 종료 오류 3개(줄 놓기·눌러도 안 죽음·지워진 줄 무시), Esc 6개, 본문 페이지 숨김 5개를 새로 덮는다.
- 실제 창에서 + 를 눌러 하위 메모가 생기고, 그 뒤 세 번 다시 그리고 마우스를 다시 올려도 종료 코드 0 으로 살아 있는 것을 확인했다.
- `test_memo_toggle` 34개, `test_memo_management` 14개, `test_memo_tree_stage2` 16개, `test_memo_tree_stage3` 35개, `test_alert_notes` 24개, `test_feature_updates` 59개, `test_schedule_management` 30개, `test_alert_note_phase1` 16개, `test_postit_stage1` 13개, `test_postit_stage2` 10개, `test_ui_responsive` 10개, `test_confirmed_memo_ux` 6개, `test_update_plan_stage1` 8개, `test_update_plan_stage2` 6개, `test_ux_safety_updates` 4개를 각각 독립 프로세스로 통과했다. 모두 303개다.
- 칸이 없던 파일은 열 때 백업을 먼저 뜬 뒤 `embedded` 를 더한다. 기존 메모는 모두 0 이라 지금과 똑같이 보인다.

### 2026-09-04 페이지 제목을 본문 줄에서 바로 입력

#### 요청과 목표

- 페이지 제목을 본문 줄에서 바로 치는 방식으로 만든다.
- 본문에서 페이지 줄을 지우면 그 페이지도 함께 휴지통으로 보낸다.

#### 처리

- 페이지를 넣으면 줄에 `📄` 표시만 남기고 커서를 그 뒤에 둔다. 이어서 치는 글이 곧 그 페이지의 제목이 된다. 비워 두면 `제목 없음` 으로 남는다.
- 본문 줄을 참으로 삼아 저장소를 맞추는 `sync_page_titles` 를 넣었다. 글을 치면 350ms 뒤에, 그리고 저장하거나 다른 메모로 넘어갈 때 한 번 더 맞춘다.
- 페이지 줄에서 Enter 를 누르면 제목을 갈무리하고 아래에 맨 서식의 새 줄을 편다.
- 본문에서 페이지 줄을 지우면 그 페이지도 휴지통으로 간다. 되돌리기로 줄이 살아나면 페이지도 휴지통에서 꺼낸다.
- 문서를 바꿀 때 "직전에 있던 페이지" 기준을 새 문서로 다시 잡는다. 그러지 않으면 다른 메모로 넘어갈 때 앞 메모의 페이지가 사라진 줄 알고 휴지통으로 보낸다. 검사로 막아 두었다.
- 저장소 → 본문(제목 따라가기)은 메모를 열 때만, 본문 → 저장소(친 이름 반영)는 치는 동안에만 돈다. 서로 덮어쓰지 않는다.

#### 검증과 배포

- `test_memo_pages_stage4.py` 를 37개로 늘려 통과했다. 표시만 넣고 커서 두기, 친 글이 제목이 되는 것, 빈 채로 두면 제목 없음, Enter 로 줄 마치기, 줄을 지우면 휴지통, 되돌리면 복구, 메모를 바꿔도 지워지지 않음을 새로 덮는다.
- `test_memo_toggle` 34개, `test_memo_management` 14개, `test_memo_tree_stage2` 16개, `test_memo_tree_stage3` 35개, `test_alert_notes` 24개, `test_feature_updates` 59개, `test_schedule_management` 30개, `test_alert_note_phase1` 16개, `test_postit_stage1` 13개, `test_postit_stage2` 10개, `test_ui_responsive` 10개, `test_confirmed_memo_ux` 6개, `test_update_plan_stage1` 8개, `test_update_plan_stage2` 6개, `test_ux_safety_updates` 4개를 각각 독립 프로세스로 통과했다. 모두 309개다.
- 실제 창에서 페이지 두 개를 넣고 이름을 친 뒤, 줄을 지워 휴지통으로 가는 것과 되돌려 살아나는 것을 확인했다.

### 2026-09-04 페이지 밑줄 오류와 5단계 — / 삽입 메뉴·강조 상자·구분선·고정·전부 접기

#### 요청과 목표

- 페이지를 연 뒤 글을 치면 밑줄이 붙던 것을 고친다.
- 5단계를 만든다. `/` 삽입 메뉴, 강조 상자, 구분선, 목록 고정, 전부 접기·펼치기.

#### 처리 — 밑줄 오류

- 페이지 줄 끝에 커서를 둔 채 그 페이지를 열면, 위젯이 들고 있던 입력 서식(밑줄·링크색·앵커)이 새 문서까지 따라와 첫 글자가 링크처럼 보였다. 실제로 재현해 확인했다.
- 문서를 바꾸거나 내용을 새로 불러올 때 입력 서식을 맨 서식으로 되돌린다. 커서가 실제로 링크 안에 있을 때만 그 서식을 잇는다.

#### 처리 — 5단계

- 넣을 수 있는 것들의 목록을 `insert_menu.py` 한곳으로 모았다. 메모의 ＋ 단추, 포스트잇의 ＋ 단추, 본문의 `/` 메뉴가 모두 이 목록 하나를 쓴다. 검사로 세 곳이 같은 것을 내놓는지 확인한다.
- 본문에서 `/` 를 치면 삽입 메뉴가 뜬다. 이어서 친 글로 목록이 좁혀지고, 위아래로 고르고 Enter 로 넣는다. Esc 로 닫으면 친 `/` 는 글자로 남는다. 낱말 한가운데의 `/`(주소 등)에서는 뜨지 않는다.
- Esc 는 메뉴가 떠 있으면 메뉴만 닫는다. 그 뒤에야 왔던 메모로 돌아간다.
- 강조 상자(`💡`)를 더했다. 줄 앞 글자로 표시를 들고 다녀 저장한 HTML 에 그대로 남고, 배경과 여백은 그 줄에만 준다. Enter 로 나오면 다음 줄은 맨 서식으로 시작한다 — 그러지 않으면 배경이 아래 줄까지 번졌다. 실제 렌더로 보고 고쳤다.
- 구분선을 더했다. 글자는 화면에서 감추고 그 자리에 가로줄을 그린다.
- `Ctrl+Shift+E` 로 한 메모의 토글을 한 번에 접거나 편다. 하나라도 펼쳐져 있으면 모두 접는다.
- `notes` 표에 `pinned` 를 더했다. 고정한 메모는 형제들 가운데 늘 먼저 놓이고 목록에서 `⭐` 로 표시된다. 편집창의 `목록 고정` 스위치로 켜고 끈다.
- 고정만 켜고 끄는 것은 손댄 시각을 올리지 않는다. 올리면 메모를 고치지도 않았는데 목록에서 맨 위로 튀어 순서가 흐트러진다.

#### 검증과 배포

- `test_memo_blocks_stage5.py` 28개를 새로 만들어 통과했다. `/` 메뉴 10개, 강조 상자·구분선 7개, 전부 접기 4개, Esc 2개, 고정 5개를 덮는다.
- `test_memo_pages_stage4.py` 를 39개로 늘려 통과했다. 페이지를 연 뒤 친 글에 밑줄이 붙지 않는지, 페이지 줄 자체는 그대로 링크로 보이는지 새로 덮는다.
- `test_memo_toggle` 34개, `test_memo_management` 14개, `test_memo_tree_stage2` 16개, `test_memo_tree_stage3` 35개, `test_alert_notes` 24개, `test_feature_updates` 59개, `test_schedule_management` 30개, `test_alert_note_phase1` 16개, `test_postit_stage1` 13개, `test_postit_stage2` 10개, `test_ui_responsive` 10개, `test_confirmed_memo_ux` 6개, `test_update_plan_stage1` 8개, `test_update_plan_stage2` 6개, `test_ux_safety_updates` 4개를 각각 독립 프로세스로 통과했다. 모두 352개다.
- 실제 창을 렌더해 구분선·강조 상자·토글이 한 화면에서 서로 번지지 않는 것과 `/` 메뉴의 여섯 항목, 고정한 메모가 맨 위로 오는 것을 확인했다.

### 2026-09-04 목록 공간 넓히기, 전부 접기 단추, / 메뉴 엔터, 고정 우클릭

#### 요청과 목표

- 메모 목록에 모든 토글을 한 번에 접고 펴는 단추를 넣는다.
- 목록이 세로로 더 많이 보이도록 위아래 여백을 줄인다.
- `/` 삽입 메뉴에서 방향키로 고른 뒤 Enter 가 먹히지 않던 것을 고친다.
- 목록 고정을 목록의 오른쪽 단추로 옮기고 편집창에서는 걷어낸다.

#### 처리

- `/` 메뉴는 창 하나라서 키를 먼저 가져간다. 편집기까지 오지 않아 Enter 가 먹히지 않았다. 메뉴에 이벤트 거르개를 달아 Enter·Tab 은 고른 것을 넣고, Esc 는 닫고, 위아래는 목록을 옮기고, 나머지 글자는 편집기로 넘겨 치는 대로 좁혀지게 했다.
- 검사도 실제 앱처럼 메뉴 쪽으로 키를 보내도록 고쳤다. 편집기에 직접 보내는 검사만 있으면 이 오류를 놓친다.
- 목록 머리글 줄에 `모두 접기`·`모두 펼치기` 단추를 넣었다. 글자는 지금 누르면 무슨 일이 나는지를 적고, 접을 것이 없으면 꺼진다.
- 목록 위아래 여백을 줄였다. 바깥 여백 18/16 → 12/8, 사이 간격 12 → 6, 단추와 검색칸 높이 → 28px, 표 머리글 → 29px, 제목 글자 20px → 15px.
- 서식의 `min-height` 가 `setFixedHeight` 보다 세서 높이가 줄지 않았다. 메모 목록 칸 안에서만 `min-height` 를 풀어 주는 규칙을 더했다.
- 420px 짜리 목록에서 글이 시작되는 자리가 104px → 68px, 보이는 줄이 7줄 → 10줄이 됐다. `Excel 내보내기` 아래 남던 자리도 9px 로 줄었다.
- 목록 고정을 줄에서 오른쪽 단추로 켜고 끈다. 편집창의 `목록 고정` 스위치는 걷어냈다.

#### 검증과 배포

- `test_memo_blocks_stage5.py` 를 43개로 늘려 통과했다. 메뉴 쪽 키 처리 5개, 접기 단추 4개, 목록 여백 4개, 고정 우클릭 3개를 새로 덮는다.
- `test_memo_pages_stage4` 39개, `test_memo_tree_stage3` 35개, `test_memo_toggle` 34개, `test_memo_management` 14개, `test_alert_notes` 24개, `test_feature_updates` 59개, `test_alert_note_phase1` 16개, `test_postit_stage1` 13개, `test_ui_responsive` 10개, `test_confirmed_memo_ux` 6개를 각각 독립 프로세스로 통과했다.
- 실제 창을 렌더해 10줄이 보이는 목록과 `모두 접기` 단추, 머리글 글자가 잘리지 않는 것을 확인했다.
- 서식 도구막대 재구성은 아직 손대지 않았다. 실제 Qt 위젯으로 A·B·C 세 안을 그려 승인을 기다린다.

### 2026-09-04 서식 두 번째 줄을 A안으로 (승인 범위만)

#### 요청과 목표

- 서식 도구막대를 A안(테두리 없는 한 줄)으로 바꾸되, 승인하신 대로 **두 번째 줄만** 적용한다.
- 첫 줄(글씨체·크기·색상)과 아래쪽 서식 1~3 은 그대로 둔다.

#### 처리

- `B I U S`, 글머리표, 체크리스트, 기능(＋), 이미지, 기본값, 단축키의 테두리를 걷어냈다. 마우스를 올리면 옅은 회색, 누르면 진한 회색이 깔린다.
- 켜져 있는 서식은 옅은 파랑 바탕에 진한 파랑 글자로 보인다. 단추를 통째로 파랗게 칠하던 방식은 걷어냈다 — 그러면 테두리를 없앤 뜻이 사라진다.
- 묶음은 가는 세로선으로만 나눴다. `B I U S` / `글머리표·체크리스트·기능·이미지` / `기본값·단축키` 세 묶음이다.
- 단추 크기를 42×38 에서 30×28 로 줄이고 줄 간격을 4 에서 2 로 좁혔다. 두 번째 줄이 42px 에서 28px 이 됐다.
- 크기는 서식(QSS)에서 한 번에 정한다. 서식의 `min-*` 은 `setFixedSize` 로 잡은 최소 크기를 지워, 단추가 글자 폭(20px)까지 쪼그라들었다. 실제 크기를 재 보고 잡았다.
- 첫 줄과 서식 1~3 은 손대지 않았다.

#### 검증과 배포

- `test_memo_blocks_stage5` 43개, `test_memo_pages_stage4` 39개, `test_memo_toggle` 34개, `test_memo_management` 14개, `test_confirmed_memo_ux` 6개, `test_ui_responsive` 10개를 각각 독립 프로세스로 통과했다.
- 실제 창을 렌더해 두 번째 줄이 한 줄로 낮아지고, 켜진 서식(U)이 옅은 파랑으로 보이며, 첫 줄과 서식 1~3 이 그대로인 것을 확인했다.

### 2026-09-04 메모·일정 화면 아래쪽 빈 자리 정리

#### 요청과 목표

- 화면 맨 아래 상태 한 줄 자리에 남던 빈 공간을 줄인다.

#### 처리

- 상태줄 높이를 38px 에서 22px 로 줄이고 글자를 세로 가운데에 맞췄다. 글자는 한 줄인데 위아래로 21px 이 비어 있었다.
- 탭에 늘어남 몫을 주고 패널 세로 간격을 6 에서 2 로 좁혔다. 창을 키우면 남는 높이가 모두 탭 안 내용으로 간다.
- 1400×800 기준으로 탭 안 높이가 672px 에서 692px 이 됐다.

#### 검증과 배포

- `test_memo_blocks_stage5.py` 를 46개로 늘려 통과했다. 상태줄 높이, 탭과 상태줄 사이 간격, 창을 키웠을 때 늘어난 높이가 내용으로 가는지를 새로 덮는다.
- `test_memo_pages_stage4` 39개, `test_memo_management` 14개, `test_memo_tree_stage3` 35개, `test_alert_notes` 24개, `test_alert_note_phase1` 16개, `test_ui_responsive` 10개, `test_confirmed_memo_ux` 6개를 각각 독립 프로세스로 통과했다.
- 실제 창을 렌더해 상태줄이 한 줄로 붙고 아래에 남는 자리가 없는 것을 확인했다.

### 2026-09-04 메모 층 나누기 6단계 — 줄 끌어 옮기기

#### 요청과 목표

- 본문 줄을 손잡이로 잡아 순서를 바꾼다.
- 줄을 토글이나 페이지 안으로 끌어 넣는다.

#### 처리

- `alert_notes/line_gutter.py` 를 새로 두어 본문 왼쪽에 손잡이 칸(20px)을 만들었다. 줄 앞에 마우스를 가져가면 점 여섯 개 손잡이가 뜨고 커서가 손 모양이 된다.
- 본문은 `setViewportMargins` 로 그만큼 밀었다. 문서 여백이 아니라 보기 쪽 여백이라, 같은 메모를 띄운 포스트잇에는 영향이 없다.
- 손잡이를 잡고 4px 이상 움직이면 끌기로 본다. 끄는 동안 놓을 자리를 파란 줄로, 토글·페이지 위라면 그 줄 전체를 옅은 파랑으로 보여 준다.
- 놓으면 그 줄을 옮긴다. 토글을 잡으면 그 안의 줄까지 한 덩어리로 따라간다. 자기 자신이나 자기 안으로는 옮기지 않는다.
- 줄 한가운데에 놓으면 그 토글 안으로 들어간다. 토글에 안내용 빈 줄만 있었다면 그 줄을 대신 차지한다.
- 페이지 줄 한가운데에 놓으면 그 페이지 메모의 본문 끝으로 옮겨 간다. 페이지에 이미 있던 내용은 그대로 남는다.
- 옮긴 글은 HTML 조각으로 오가므로 굵게·색·링크 같은 서식이 그대로 따라간다.
- 지운 자리에 빈 줄이 남지 않도록 줄 구분자까지 함께 지운다. 문서 맨 끝 줄은 뒤에 지울 구분자가 없어 앞의 것을 지운다 — 이것 때문에 안내용 빈 줄이 안 지워지던 것을 재현해 잡았다.

#### 검증과 배포

- `test_memo_drag_stage6.py` 16개를 새로 만들어 통과했다. 순서 바꾸기 5개, 토글 4개, 페이지 2개, 손잡이 칸 5개를 덮는다.
- `test_memo_toggle` 34개, `test_memo_blocks_stage5` 46개, `test_memo_pages_stage4` 39개, `test_memo_management` 14개, `test_memo_tree_stage3` 35개, `test_alert_notes` 24개, `test_feature_updates` 59개, `test_alert_note_phase1` 16개, `test_postit_stage1` 13개, `test_postit_stage2` 10개, `test_ui_responsive` 10개, `test_confirmed_memo_ux` 6개를 각각 독립 프로세스로 통과했다.
- 실제 창을 렌더해 손잡이, 놓을 자리 파란 줄, 토글 위 옅은 파랑, 그리고 옮긴 뒤 토글 안으로 들어간 모습을 확인했다.

### 2026-09-04 메모 링크·그림 크기·전체 화면·오른쪽 손잡이와 전체 점검

#### 요청과 목표

- 줄 손잡이를 오른쪽 끝으로 옮긴다.
- 노션식 메모 링크를 넣는다. 링크를 지워도 그 메모는 지워지지 않아야 한다.
- 붙여넣은 그림을 끌어서 크기를 바꾼다.
- 메모 편집 전체 화면 모드를 넣는다.
- 메모 편집 탭 전체를 실제로 눌러 보고 다음에 강화할 것을 갈래별로 정리한다.

#### 처리

- 손잡이 칸을 오른쪽 끝으로 옮겼다. 보기 쪽 여백이라 같은 메모를 띄운 포스트잇에는 영향이 없다.
- `alert_notes/note_link_dialog.py` 로 메모 고르는 창을 만들고, 본문에 `🔗 제목` 링크를 넣는다. 페이지(📄)와 표시 글자를 달리해, 링크를 지워도 `sync_page_titles` 가 그 메모를 휴지통으로 보내지 않는다. 지금 자기 메모는 고를 목록에서 뺐다.
- 링크 글자는 메모를 열 때 그 메모의 지금 제목으로 맞춘다. 가리키던 메모가 사라졌으면 "지운 메모" 로 적는다.
- 링크 뒤에는 맨 서식의 빈 칸을 하나 넣어, 이어서 치는 글까지 링크가 되지 않게 했다.
- 그림 오른쪽 아래 모서리를 잡아 끌면 가로세로 비율을 지키며 크기가 바뀐다. 최소 60px, 편집기 폭까지다. 직접 정한 폭은 그림에 적어 두어 창 크기가 바뀌어도 지킨다.
- `F11` 과 제목 줄의 단추로 편집 구역만 크게 본다. 목록·오늘 요약·탭 막대를 접고, 돌아오면 나눔선 자리도 되돌린다.
- 실제로 눌러 보다 결함을 하나 찾았다. 강조 상자 다음에 구분선·페이지 줄·그림을 넣으면 상자의 노란 배경이 그 줄까지 번졌다. 키보드 Enter 만 막고 프로그램이 직접 줄을 만드는 길은 막지 않았기 때문이다. 새 줄을 늘 맨 서식으로 여는 `_open_clean_block` 하나로 모으고 검사를 넣었다.

#### 검증과 배포

- `test_memo_stage7.py` 21개를 새로 만들어 통과했다. 메모 링크 9개, 그림 크기 6개, 전체 화면 6개를 덮는다.
- 메모 편집 탭을 처음부터 끝까지 실제로 눌러 보는 점검 스크립트를 돌려 스물네 가지가 모두 제대로 도는 것을 확인했다.
- 빠르기를 쟀다. 본문 200줄 2.9ms, 1000줄 10.7ms, 3000줄 29.0ms 로 글자 한 자마다 문서 전체를 세 번 훑는 값이 그대로 쌓인다. 목록은 1000개 다시 그리는 데 72ms 로 넉넉하다.
- `test_memo_drag_stage6` 16개, `test_memo_blocks_stage5` 48개, `test_memo_pages_stage4` 39개, `test_memo_tree_stage3` 35개, `test_memo_tree_stage2` 16개, `test_memo_toggle` 34개, `test_memo_management` 14개, `test_alert_notes` 24개, `test_feature_updates` 59개, `test_schedule_management` 30개, `test_alert_note_phase1` 16개, `test_postit_stage1` 13개, `test_postit_stage2` 10개, `test_ui_responsive` 10개, `test_confirmed_memo_ux` 6개, `test_ux_safety_updates` 4개를 각각 독립 프로세스로 통과했다. 모두 395개다.

### 2026-09-04 / 메뉴 전부 보이기, 탭 크기, 메모 목록 접기

#### 요청과 목표

- `/` 삽입 메뉴가 스크롤 없이 다 보이게 한다.
- 메모 편집·캘린더·알림내역 탭 단추를 위쪽 작업공간 단추와 비슷한 비율로 줄인다.
- 메모 목록을 접었다 펴고, 접어서 넓어진 자리를 어떻게 쓸지 정한다.

#### 처리

- `/` 메뉴를 항목 수만큼 편다. 줄 높이는 글꼴에 따라 달라지므로 짐작하지 않고 위젯에 물어본다(`sizeHintForRow`). 일곱 항목이 223px 에 모두 들어가 스크롤이 사라졌다. 두 칸으로 나누는 방법도 있었지만, 높이를 푸는 것만으로 해결돼 화살표로 오르내리는 조작을 그대로 두었다.
- 탭 단추의 `min-width: 120px; min-height: 40px` 을 `0px / 32px` 로 바꾸고 여백을 18px 로 맞췄다. 위쪽 작업공간 단추(36px, 글자에 맞춘 폭)와 같은 말투가 됐다.
- 제목 줄에 `◀` 단추를 넣어 메모 목록을 접었다 편다. `Ctrl+백슬래시` 로도 된다. 오늘 요약은 그대로 둔다.
- 나눔선은 감춘 칸의 폭을 스스로 나눠 주지 않는다. 비운 자리를 편집 구역에 그대로 얹어 1300px 창에서 편집 폭이 480 → 960px 이 됐다.
- 넓어졌을 때 자리 나눔은 요청하신 모양 그대로다. 본문만 남은 자리를 다 쓰고, 서식·옵션 줄과 알림 예약·전역 단축키 카드, 맨 아래 색상·휴지통 줄은 720px 에서 멈춰 왼쪽에 붙는다. 가로로 늘어나 봐야 빈 자리만 생기는 줄들이라 한 덩어리로 묶었다.

#### 검증과 배포

- `test_memo_stage7.py` 를 29개로 늘려 통과했다. `/` 메뉴 2개, 목록 접기 6개를 새로 덮는다.
- `test_memo_blocks_stage5` 48개, `test_memo_pages_stage4` 39개, `test_memo_drag_stage6` 16개, `test_memo_toggle` 34개, `test_memo_management` 14개, `test_alert_notes` 24개, `test_feature_updates` 59개, `test_ui_responsive` 10개, `test_postit_stage1` 13개, `test_confirmed_memo_ux` 6개를 각각 독립 프로세스로 통과했다.
- 실제 창을 렌더해 일곱 항목이 스크롤 없이 보이는 것, 탭 단추가 위쪽 단추와 비슷해진 것, 접었을 때 본문만 넓어지고 보조 줄은 왼쪽에 붙는 것을 확인했다.

### 2026-09-04 / 제목 아래 서식 도구를 오른쪽으로 옮겨 본문 넓히기

#### 요청과 목표

- 서식 도구 줄(붙임2)을 반으로 줄여, 보조 줄(붙임1) 오른쪽 빈 자리로 옮긴다.
- 비워진 위쪽 자리는 메모 입력창이 가져간다.
- 시안을 먼저 드리고, 고르신 뒤에만 손댄다.

#### 시안

- 프로그램의 진짜 위젯을 자리만 옮겨 세 장을 찍어 드렸다. 지금 286px, 제목까지 내리는 A안 381px, 제목은 위에 두고 서식만 내리는 B안 339px.
- B안을 고르시면서 "붙임1을 붙임2 아래로 보내 공간을 더 확보하라"고 하셨다. 왼쪽은 알림 예약·전역 단축키만 남고, 오른쪽은 서식 도구 아래로 서식 1~3 줄과 색상·휴지통 줄이 붙는다.

#### 처리

- 본문 아래 보조 줄들을 `bottom_host` 한 그릇에 담고, 그 안을 왼쪽·오른쪽 두 칸으로 나눴다. 폭이 좁아지면 예전처럼 한 칸으로 쌓는다(`_apply_bottom_layout`).
- 칸을 옮길 때 위젯의 부모는 건드리지 않고 `takeAt` 으로 자리만 비운다. 곧바로 다른 칸에 담기므로 깜빡임이 없다. 접었다 폈다를 두 번 반복해도 모든 줄이 그대로 남는 것을 확인했다.
- 폭을 세 번 재서 기준값을 정했다. 색상·휴지통 줄만 해도 615px, 서식 도구가 413px, 알림 카드가 433px 이다. 세로로 쌓으면 다 들어가지만 나란히 놓으면 폭이 모자란다.
- 처음에 기준값을 1000px 으로 잡았더니 1400px 창에서 목록을 접었을 때 편집 폭이 991px 이라 9px 차이로 두 칸이 되지 않았다. 억지로 낮췄더니 왼쪽 칸이 354px 까지 눌려 알림 요약이 네 줄로 접혔다.
- 그래서 왼쪽 칸에 440px 바닥을 깔고, 오른쪽 칸이 620px 아래로 좁아지면 이미 있던 색상 줄 두 줄 접기를 쓰도록 했다. 기준값은 900px 이 됐다. 목록을 접으면(991px) 두 칸이 되고, 잘리는 곳도 눌린 곳도 없다.
- 좁을 때 모습은 손대지 않았다. 서식 도구는 제목 아래 본문 위 제자리로 돌아간다.

#### 결과

- 목록을 펼친 기본 화면(480px)은 본문 317px 로 그대로다.
- 1400px 창에서 목록을 접으면 359 → **447px**.
- F11 전체 화면은 **523px**.
- 편집창만 놓고 재면 1080px 에서 286 → **407px** 로 121px 늘었다.

#### 검증과 배포

- `test_memo_stage7.py` 를 35개로 늘려 통과했다. 좁을 때 예전 모양 유지, 넓을 때 두 칸 분리, 본문이 실제로 넓어지는지, 오갔을 때 줄이 사라지지 않는지, 오른쪽 칸이 좁으면 색상 줄이 접히는지, 900~1400px 어디서도 휴지통 단추가 넘치지 않는지를 덮는다.
- `test_ui_responsive` 10개, `test_feature_updates` 59개, `test_alert_notes` 24개, `test_memo_management` 14개, `test_memo_blocks_stage5` 48개, `test_memo_pages_stage4` 39개, `test_memo_tree_stage3` 35개, `test_memo_tree_stage2` 16개, `test_memo_toggle` 34개, `test_memo_drag_stage6` 16개, `test_postit_stage1` 13개, `test_postit_stage2` 10개, `test_confirmed_memo_ux` 6개, `test_ux_safety_updates` 4개, `test_alert_note_phase1` 16개, `test_update_plan_stage1` 8개, `test_update_plan_stage2` 6개를 각각 독립 프로세스로 통과했다.
- 실제 창을 900·960·991·1080·1200·1400px 으로 찍어 눈으로 확인했다. 900px 에서 휴지통이 잘리던 것을 렌더에서 발견해 기준값과 두 줄 접기로 고쳤다.


### 2026-09-07 요약 한 번 클릭, D-Day 칩, 설정창 비켜서기와 자리 정리

#### 요청

1. 오늘 요약 목록을 누르면 그 메모가 바로 뜨게
2. D-Day 칩(붙임1)도 같이
3. 설정창을 띄운 채로 다른 창을 쓸 수 있게
4. 설정 화면 공간 정리 — ‘단축키’ 글자 둘레 빈자리 제거, 아래 단축키 지정칸 세로 줄이기

#### 처리

- 오늘 요약의 세 목록(D-Day·오늘 일정·예정 알림)을 `SummaryList` 로 바꿔 한 번 누르면 열리게 했다. 마우스는 손 모양으로 미리 알린다.
- D-Day 줄 왼쪽 체크칸만은 예외다. 그 자리는 남은 날짜 세기를 멈추는 자리다. 체크칸 자리는 짐작하지 않고 스타일에게 물어보고(`SE_ItemViewItemCheckIndicator`), 답이 없을 때만 왼쪽 24px 을 체크칸으로 본다.
- 체크칸을 누르면 목록을 통째로 다시 그리므로, 누른 줄의 번호를 **먼저** 챙겨 두고 나서 기본 동작을 부른다. 지워진 항목을 붙잡고 있다가 프로그램이 꺼지던 예전 `+` 단추 오류와 같은 자리다.
- 칩은 지금까지 오늘 요약으로 데려다 주기만 했다. 이제 가장 임박한 메모를 바로 연다(`_nearest_deadline_id`). 나머지 D-Day 는 옆 요약에 그대로 남아 있어 놓치지 않는다.
- 설정창이 `exec()` 로 프로그램 전체를 막고 있었다. 검색창은 `show()` 로 뜨는 창이라 설정창을 닫기 전에는 손댈 수 없었다. 설정창을 비모달로 바꾸고, 저장을 누른 뒤 할 일을 `accepted` 신호로 옮겼다(`show_settings` → `_apply_settings`). 두 번 열면 새로 만들지 않고 이미 뜬 창을 앞으로 올린다.
- ‘단축키’ 글자 둘레가 빈 이유는 칸이 옆 칸 높이에 맞춰 늘어나면서 남는 자리를 머리글이 나눠 가졌기 때문이다(머리글 라벨 높이 **220px**). 칸 아래에 여백을 두어 글과 줄이 위에 붙게 했다.
- 단축키 한 줄을 낮췄다. 입력칸 42 → **32px**, 한 줄 62 → **52px**. 열 줄이 늘어선 칸이라 낮춘 만큼이 그대로 창 높이로 돌아온다. QSS 는 설정창 것만 골라 바꿨다(`settingsHotkeyBuilder`) — 같은 위젯을 쓰는 메모·일정 쪽은 건드리지 않았다.
- 그래도 왼쪽 338px / 오른쪽 590px 로 어긋나 왼쪽이 200px 남았다. ‘일정 · D-Day’ 칸을 왼쪽 아래로 옮기고 그 안 체크상자를 세 줄로 벌려 두 칸을 맞췄다. 토마펫 칸은 억지로 늘리지 않고 남는 자리를 칸 밖에 둔다.

#### 결과

- 설정창 높이 891 → **783px**(-108). 단축키 칸 590 → 338px 로 내용만큼만 차지한다.
- 머리글 라벨 220 → **18px**. 머리글과 첫 줄 사이 9px.
- 두 칸 높이 510 : 510 으로 맞았다.
- 설정창을 띄운 채로 메모·일정 검색창에 바로 글자를 넣을 수 있다(`activeModalWidget` 없음).

#### 검증

- `test_settings_dday_ux.py` 20개를 새로 썼다. 한 번 클릭·체크칸 예외·안내 줄 무시, 칩이 가장 가까운 메모를 여는지, 설정창이 다른 창을 막지 않는지·두 번 열어도 하나인지·저장이 기다리지 않고 반영되는지, 빈자리 정리 결과를 덮는다.
- `test_feature_updates` 는 59개 그대로 통과한다. 가짜 설정창이 `exec()` 를 기다리던 세 곳을 `accept_settings` 로 바꾸고, 줄 높이 바닥값을 36 → 30 으로 내렸다.
- `test_alert_notes` 24개, `test_memo_stage7` 35개, `test_memo_blocks_stage5` 48개, `test_memo_pages_stage4` 39개, `test_memo_tree_stage3` 35개, `test_memo_tree_stage2` 16개, `test_memo_toggle` 34개, `test_memo_drag_stage6` 16개, `test_memo_management` 14개, `test_ui_responsive` 10개, `test_confirmed_memo_ux` 6개, `test_schedule_management` 30개, `test_postit_stage1` 13개, `test_postit_stage2` 10개, `test_toma_pet_gpt_integration` 9개, `test_update_plan_stage1` 8개, `test_ux_safety_updates` 4개를 각각 독립 프로세스로 통과했다.
- 설정창을 실제로 찍어 눈으로 확인했다.

### 2026-09-07 단축키 작업 화면 자리 정리 — ID·상태·단추 줄

#### 요청

1. ID 머리글 제거 2. ‘등록 상태’를 ‘상태’로 줄이고 ON/OFF 표시로 3. 단추 줄과 등록 줄을 걷어 목록을 넓히고, 삭제는 새 작업 왼쪽으로 4. ‘편집 중’ 칩 제거, 등록 실패 알림만 살려 작업 편집 오른쪽으로

#### 처리

- ID 열은 지우지 않고 감췄다. 줄을 찾는 열쇠로 코드 곳곳이 쓰고 있어서, 없애면 손댈 곳이 열 군데가 넘는다. 감추기만 하면 사용자 화면에서는 똑같이 사라진다. 머리글을 새로 끼우면 감춘 상태가 풀려, `setHorizontalHeader` 뒤에 감춰야 했다.
- 검색은 감춘 ID 를 더 이상 훑지 않는다. 보이지 않는 값으로 걸리면 왜 걸렸는지 알 수 없다.
- 상태는 점 하나가 됐다(`RegistrationStateItem`). 켜진 것은 찬 점 ●, 꺼진 것은 빈 점 ○, 색으로 등록됨·실패·비활성을 나눈다. 말은 도움말과 읽어 주기(AccessibleText)에 그대로 남기고, 정렬도 글자가 아니라 상태 차례로 한다.
- 줄을 고르면 스타일시트가 글자색을 선택색으로 덮어써 점 색이 사라졌다. 이 칸만 배경은 스타일에 맡기고 점은 직접 찍는 대리자를 뒀다(`RegistrationDotDelegate`). 처음에 팔레트만 바꿔 봤지만 스타일시트가 이겼다.
- 늘 떠 있던 두 줄(활성·비활성·실행 테스트·편집 작업 칩·삭제 / 등록 개수·실패 상세·다시 등록)을 걷었다. 삭제만 제목 줄로 올리고, 나머지는 목록 줄에서 오른쪽 단추로 부른다(`row_menu`). 없애는 대신 옮긴 이유는 실행 테스트와 다시 등록이 실제로 필요한 기능이기 때문이다.
- ‘편집 중 · ID 14 · 비번1’ 칩은 없앴다. 어느 작업을 고쳤는지는 바로 아래 이름칸이 이미 말하고 있다.
- 노란 띠에 있던 ‘등록 완료 32개 / 실패 2개…’는 실패한 것이 있을 때만 뜨는 한 줄로 줄여 ‘작업 편집’ 제목 오른쪽에 붙였다. ‘실패 상세’는 녹색 글자이고 누르면 실패 목록이 뜬다. 같은 말을 두 번 하지 않도록 실패 시에는 노란 띠를 띄우지 않는다.

#### 결과

- 두 줄이 사라진 만큼 목록이 길어졌다. 같은 창에 옛 두 줄을 다시 끼워 재 보니 **144px**, 760px 창에서 **8줄 → 11줄**이다.
- 열 폭은 `[38, 0(ID), 66, 263, 122, 134, 45]`. 상태 열 104 → **45px**, 남은 폭은 이름 열이 받았다.
- 머리글을 직접 끌어 본 적이 없는 사람만 새 폭으로 옮긴다(비율 판 번호 2 → 3).

#### 검증

- `test_task_list_room.py` 19개를 새로 썼다. ID 감춤과 검색, 점·도움말·정렬·대리자, 걷어 낸 줄, 삭제 자리, 오른쪽 단추 메뉴, 실패 알림의 자리·녹색·누름·사라짐을 덮는다.
- `test_update_plan_stage2`·`test_ui_responsive` 의 옛 단추 줄 확인 다섯 자리를 새 모양(메뉴·알림 줄)으로 바꿨다.
- 실제 창을 1420×760 으로 찍어 눈으로 확인했다. 고른 줄에서 점 색이 죽는 것을 렌더에서 발견해 대리자로 고쳤다.

### 2026-09-07 목록 접기 단축키와 1순위 전역 단축키

#### 요청

1. 메모 목록 모두 접기/펼치기 단축키 (그 창이 앞에 있을 때만)
2. 프로그램 설정의 단축키를 1순위로 — 다른 프로그램이 같은 조합을 쓰고 있어도 우리가 먼저. 치명적인 것과 바꿀 수 없는 것만 빼고 일괄 적용

#### 처리

- 목록 접기는 **Ctrl+Shift+A**. 본문 토글 접기가 이미 Ctrl+Shift+E 를 쓰고 있어 글자를 겹치지 않게 골랐다. 창이 앞에 있을 때만 듣는다(`WindowShortcut`). 다른 작업공간에 있거나 목록을 접어 둔 상태면 못 들은 척한다. 단추 도움말에 조합을 적었다.
- 1순위 문제의 뿌리는 `RegisterHotKey` 다. 먼저 잡은 프로그램이 임자라, 남이 쓰고 있으면 우리 것은 등록조차 되지 않는다. 순서를 바꿀 방법이 없다.
- 그래서 낮은 수준 키보드 갈고리(`hotkey_hook.py`)를 뒀다. Windows 가 단축키를 나눠 주기 전에 키를 보므로 우리가 먼저 가져가고, 가져간 키는 뒤로 넘기지 않는다. 누름뿐 아니라 뗌까지 삼켜, 누름 없는 뗌만 받는 프로그램이 없게 했다.
- 갈고리 안에서 오래 머물면 컴퓨터 전체의 입력이 멈춘다. 그래서 갈고리는 메시지만 도는 제 스레드에 걸고, 그 안에서는 표를 한 번 찾고 창에 메시지 하나를 보내는 일만 한다. 실제 처리는 예전 그대로 창이 자기 차례에 한다(WM_HOTKEY 길을 그대로 씀). 무슨 일이 있어도 예외가 새어 나가지 않게 감쌌다.
- 반복작업이 스스로 눌러 만든 키(LLKHF_INJECTED)는 그냥 흘려보낸다. 잡으면 제 꼬리를 문다.
- 수식키가 하나라도 더 눌려 있으면 다른 단축키로 본다. Ctrl+Alt+N 을 맡아 뒀다고 Ctrl+Alt+Shift+N 까지 가져가면 남의 것을 먹는다.
- 가져가면 안 되는 조합은 `UNSAFE_TO_INTERCEPT` 가 막는다. Ctrl+C·V·X·Z·Y·A·S·P·F·N·O·W·T 는 삼키면 컴퓨터 전체에서 복사·붙여넣기가 멈춘다. Ctrl+Shift+Esc·Ctrl+Alt+Del·Win+L 등은 Windows 가 우리보다 아래에서 처리해 애초에 가져올 수 없다. 이것들만 예전처럼 RegisterHotKey 에 맡긴다. 수식키를 하나 더 붙인 Ctrl+Shift+C 는 복사가 아니므로 다시 우리 것이다.
- 갈고리를 걸지 못하는 자리(권한·환경)에서는 조용히 예전 길로 돌아간다. 한 번 실패하면 단축키마다 다시 시도하지 않는다.
- 프로그램을 닫을 때 갈고리를 반드시 푼다(`HotkeyManager.shutdown`, `closeEvent`).

#### 결과

- 다른 프로그램이 이미 쓰는 조합이어도 등록에 실패하지 않는다. ‘상태’ 점이 빨강으로 남던 자리가 사라진다.
- 갈고리 하나로 모든 단축키를 받는다. 스레드는 하나뿐이다.

#### 검증

- `test_shortcut_updates.py` 30개. 접기 단축키(글자·범위·본문 것과 겹치지 않음·접고 펴기·감춘 목록 무시), 가져가도 되는지 판정, 표 맞추기(정확히 일치·수식키 하나 더면 무시), 갈고리 판단(맡은 키·남의 키·주입된 키·수식키 자체·뗌 삼키기), 관리자(갈고리 우선·Ctrl+C 는 Windows·갈고리 하나·놓기·실패 시 되돌아감·실패 재시도 안 함·닫을 때 풀기·메시지로 실행)를 덮는다.
- 실제 갈고리가 이 컴퓨터에서 걸리고 풀리는 것을 따로 확인했다.
- **직접 못 해 본 것**: 다른 프로그램이 이미 잡은 조합을 실제로 빼앗아 오는지는 사람이 실제 키를 눌러 봐야 한다. 시험은 주입한 키를 일부러 무시하므로 여기서 대신할 수 없다. 실제로 쓰시는 조합으로 한 번 확인해 주시면 좋겠다.

### 2026-09-07 / 명령어 붙이기, 그리고 빠르기·최근 메모·찾기·인용문·표

#### 요청과 목표

- 기능마다 `> ` 같은 명령어를 붙인다.  없는 것은 일반적으로 쓰는 것으로 정한다.
- ＋ 메뉴에서 마우스를 올리면 무엇인지와 명령어가 함께 뜨게 한다.
- 그다음 1 → 4 → 2 → 5 → 8 순으로 만든다.

#### 명령어와 설명

- 명령어를 `INSERT_ITEMS` 한곳에 적고, 단축키를 거는 일과 설명을 띄우는 일이 같은 줄을 읽게 했다.  어긋날 수가 없다.
- 노션에 있는 것은 노션과 같은 번호로 맞췄다.  체크리스트 Ctrl+Shift+4, 글머리표 5, 토글 7, 코드 8, 페이지 9.  노션에 없는 강조 상자·구분선·인용문·표·메모 링크는 이름 첫 글자를 땄다(C·D·Q·T·K).
- 줄 앞에서 치는 입력도 늘렸다.  `> ` 토글(전부터 있던 것), `- ` `* ` 글머리표, `[] ` 체크리스트, `! ` 강조 상자, `---` 구분선, `" ` `| ` 인용문, ``` 코드 줄, `[[` 메모 링크.
- Qt 는 메뉴 설명을 기본으로 감춘다.  `setToolTipsVisible(True)` 를 켜 주지 않으면 아무리 적어도 뜨지 않는다.  이걸 켜고, `/` 메뉴에도 같은 설명을 달았다.

#### 1. 바뀐 자리만 다시 보기

- 글자 한 자를 칠 때마다 문서 전체를 세 번 훑고 있었다.  체크리스트 칠하기, 토글 접힘 계산, 빈 토글 줄 채우기다.  그리기까지 하면 네 번이다.
- 화면 밖 줄은 그려지지도 않는다.  칠하기와 그리기를 보이는 자리 앞뒤 40줄로 좁혔다(`_visible_blocks`).  스크롤하면 그때 다시 칠한다.
- 접힘 계산과 빈 토글 줄은 구조가 바뀌었을 때만 돈다.  `contentsChange` 로 한 줄 안에서 글자만 바뀐 것인지 보고, 줄이 나뉘거나 합쳐졌거나 토글 줄을 건드렸을 때만 다시 훑는다.
- **3000줄에서 한 자 치는 데 21.7ms → 0.6ms.  36배다.**  200줄 1.6 → 0.5ms, 1000줄 7.2 → 0.5ms 로, 줄 수와 상관없이 일정해졌다.  첫 그리기도 19.4 → 2.1ms.

#### 4. 최근 본 메모 칩

- 검색칸 아래에 최근 본 메모 세 개를 칩으로 놓았다.  눌러 바로 옮겨 간다.
- 되돌아가기(Esc)에 쓰는 자취와는 다른 목록이다.  그쪽은 왔던 길을 되짚는 스택이고, 이쪽은 최근 순서다.  설정에 남겨 껐다 켜도 그대로다.
- 지금 보고 있는 메모와 본문에 넣은 페이지는 칩에 넣지 않는다.  지운 메모는 저절로 빠진다.

#### 2. 본문 찾기 (Ctrl+F)

- 본문 아래에 얇은 찾기 줄을 두었다.  치는 동안 찾은 자리가 노랗게, 지금 자리가 주황으로 칠해지고 `3 / 12` 처럼 센다.
- Enter 다음, Shift+Enter 이전, F3·Shift+F3 도 같다.  Esc 로 닫으면 칠도 지워진다.
- 접힌 토글 안에 있는 자리로 갈 때는 그 토글을 펴 준다.  못 찾는 곳이 없어야 찾기다.
- 긴 메모에서 한 글자마다 문서를 뒤지지 않도록, 손을 멈추면(120ms) 찾는다.

#### 5. 인용문 · 코드 줄

- 인용문은 왼쪽에 세로줄이 서고 글자가 기울며 흐려진다.  코드 줄은 회색 바탕에 고정폭 글씨다.
- 표시 글자(`❝ `, `⌗ `)는 문서에 남기되 화면에서는 감춘다.  체크리스트 네모와 같은 방법이다.  이렇게 해야 저장했다 열어도 남는다.
- 코드는 여러 줄인 일이 많아 Enter 로 이어지고, 빈 코드 줄에서 Enter 를 누르면 빠져나온다.  인용문은 강조 상자처럼 한 줄이라 Enter 로 나온다.
- 여러 줄 코드가 한 덩어리로 보이도록 위아래 여백을 0 으로 두었다.

#### 8. 표

- 머리줄 한 줄에 세 칸으로 시작한다.  Tab 으로 다음 칸, 마지막 칸에서 Tab 이면 줄이 늘어난다.  Shift+Tab 은 되돌아간다.  표 밖에서는 Tab 이 예전처럼 들여쓰기다.
- 칸 바탕은 `QTextTableCellFormat` 으로만 붙는다.  보통 `QTextCharFormat` 으로 넣으면 조용히 사라진다.
- 그리고 칸 안쪽 글자 서식을 나중에 건드리면 그 바탕까지 함께 지워진다.  칸 서식은 반드시 맨 마지막에 걸어야 한다.  검사가 잡아낸 것으로, 화면만 봐서는 다른 이유로 회색이 보여 놓칠 뻔했다.

#### 검증과 배포

- `test_memo_stage8.py` 54개를 새로 만들었다.  빠르기 6개, 찾기 15개, 최근 메모 7개, 인용문·코드 13개, 표 11개, 전부 부를 수 있는지 2개다.
- `test_memo_blocks_stage5.py` 를 60개로 늘렸다.  줄 앞 입력 8개와 설명·단축키 5개를 새로 덮는다.
- 스물두 벌을 각각 독립 프로세스로 돌려 **552개 모두 통과**했다.
- 화면으로도 확인했다.  300줄 메모를 가운데로 스크롤해 체크박스·구분선·토글 안내가 제대로 그려지는 것, 칩이 왼쪽에 붙는 것, 찾기 줄과 노란 칠, 인용문 세로줄과 코드 회색 바탕, 표 머리줄이 굵고 회색인 것을 눈으로 봤다.

### 2026-09-08 일정·D-Day 추가 업데이트 1단계 — 표시 구조와 시안 확정

#### 요청과 목표
- 일정 포스트잇, 오늘 요약, 지난 D-Day의 역할을 중복 없이 나누고 구현 전 시안을 확정한다.
- 일반·좁은 창과 Windows 100%·125%·150% 배율에서 설정 배치를 검토한다.

#### 처리
- 일정 포스트잇은 실행·완료, 오늘 요약은 오늘 전체 상황·이동, 지난 D-Day는 밀린 마감 정리로 역할을 분리했다.
- 사용자 붙임을 기준으로 흰 카드·파란 날짜·작은 문자 배지·우측 시각·완료 삭선 스타일을 확정했다.
- 지난 D-Day 상단 접이식, 할 일의 D-Day로 세기, 반복 이번 회차만 완료, 일정+할 일 기본 표시와 설정 범주 탭을 확정했다.

#### 검증과 배포
- 전체 시안과 340px 좁은 포스트잇을 렌더해 가로 스크롤과 주요 정보 잘림이 없음을 확인했다.
- 기존 설정창 격리 렌더는 100%·125%에서 표시됐고 150% 잘림을 확인해, 1380×972 범주 탭 시안으로 무스크롤 배치를 확인했다.
- 앱 소스·실제 사용자 DB·기존 EXE는 변경하지 않았다. EXE는 생성하지 않았다.

### 2026-09-08 일정·D-Day 추가 업데이트 2단계 — 일정 포스트잇

#### 요청과 목표
- 승인된 붙임 스타일로 일정 포스트잇을 만들고 날짜 이동, 네 보기, 완료, 최대 8줄과 설정 복원을 연결한다.
- 단축키는 기본값을 비우고 사용자가 지정한 때만 기존 충돌·예약 조합 검사를 거쳐 등록한다.

#### 처리
- 조회·정렬·완료 규칙을 `schedule_postit_model.py`, 설정 저장을
  `schedule_postit_settings.py`, 실제 창을 `schedule_postit.py`로 분리했다.
- 흰 카드·파란 날짜·문자 배지·우측 시각·완료 삭선·옅은 그림자를 적용하고,
  이전날·오늘·다음날과 당일·이번 주·임박순·우선도순을 실제 단추로 연결했다.
- 실제 행 `sizeHint()`로 높이를 계산하고 9번째 줄부터만 세로 스크롤을 보이게 했다.
- 반복 항목은 선택 회차만 완료하며, 단발 항목의 휴지통 완료는 기존 안전 삭제 경로를 사용한다.
- 위치·보기·최대 줄 수·표시 대상·완료 방식·선택 단축키를 기존 `settings` 테이블에 저장한다.
- 설정창은 네 범주 탭으로 나누고 일정·D-Day 탭에 포스트잇 설정을 배치했다.
  콤보와 최대 줄 수는 포커스 중에도 휠로 값이 바뀌지 않는다.
- 테스트의 저장 경로 쓰기는 가짜 함수로 격리해 실제 LOCALAPPDATA와 사용자 DB를 건드리지 않았다.

#### 검증과 배포
- 포스트잇 모델·창·설정·단축키·트레이·일정 관리·빠른 입력·반응형 UI 회귀
  122개가 한 묶음에서 전부 통과했다.
- 별도 기능 회귀 60개 중 59개가 즉시 통과했고, 승인된 범주 탭과 충돌한 옛 2열 위치 검사를
  새 탭 부모 관계로 갱신한 뒤 나머지 1개도 통과했다.
- 100%·125%·150% 실제 위젯 렌더에서 9건 스크롤, 가로 스크롤 없음,
  설정 탭의 내부 스크롤 없음과 저장 단추 표시를 확인했다.
- 실제 사용자 DB와 기존 EXE는 변경하지 않았고 EXE를 생성하지 않았다.

### 2026-09-08 일정·D-Day 추가 업데이트 3단계 — 일정·할 일 전체 편집 분리

#### 요청과 목표
- 전체 편집 진입점을 새 일정과 새 할 일로 나누고, 할 일에는 마감 하나와 `D-Day로 세기`만 제공한다.
- 빠른 입력 팝오버와 저장된 종류, 반복·알림 미러·백업 계약을 유지한다.

#### 처리
- 캘린더 머리말에 `＋ 새 일정`, `＋ 새 할 일`을 두고 각각 고정 종류의 전체 편집기를 열게 했다.
- 일정 폼은 시작·종료를, 할 일 폼은 마감 하나를 표시한다. 전체 편집기의 종류 콤보는 숨기고
  진입점 또는 저장된 `item_type`만 따르며, 비정상 값은 자동 변환하지 않고 안내한다.
- 기존 빠른 팝오버는 그대로 두고, 팝오버에서 전체 편집으로 넘어올 때 저장된 종류가 유지되도록 보완했다.
- 할 일의 `D-Day로 세기`를 `count_as_dday` 추가 열에 저장한다. 기존 DB는 변경 직전 자동 백업하고,
  구형 전체 백업 복원은 기본값 0으로 안전하게 채운다.

#### 검증과 배포
- 새 집중 검사 7개와 일정 관리·일정 포스트잇·전체 백업·빠른 입력 회귀를 합쳐
  83개가 모두 통과했다.
- 실제 Windows 글꼴로 일정·할 일 400px 폼, 두 진입점 1200px 화면과 680px 좁은 할 일 폼을
  렌더해 종료 줄 분리, D-Day 선택, 잘림 없음과 가로 스크롤 최대값 0을 확인했다.
- 테스트와 렌더는 임시 DB만 사용했다. 실제 사용자 DB와 기존 EXE는 변경하지 않았고 EXE를 만들지 않았다.

### 2026-09-08 일정·D-Day 추가 업데이트 4단계 — D-Day·캘린더·겹침 연계

#### 요청과 목표
- 지난 D-Day를 별도 묶음으로 유지하고 목표 날짜·시각, 일괄 완료와 캘린더 D-Day 필터를 제공한다.
- 일정·할 일 편집 중 같은 날의 다른 항목과 실제 시간 겹침을 확인하고 포스트잇·오늘 요약 중복을 줄인다.

#### 처리
- 오늘 요약 맨 위에 지난 D-Day 접이식 묶음과 `전체 완료`를 추가했다. 지난 날짜만으로
  자동 완료·삭제하지 않으며, 각 줄에 D+일수·제목·목표 날짜·시각을 함께 표시한다.
- 캘린더에 일정·할 일·D-Day 독립 필터와 `D-Day만` 단추를 추가해 설정값을 저장한다.
  메모 D-Day는 현재 범위 띠·목록·월간에, D-Day 할 일은 기존 일정 블록 계약으로 표시한다.
- `schedule_day_context.py`에 같은 날 조회와 겹침 계산을 분리하고, 전체 편집기 아래에
  `겹침` 글자와 붉은 배경으로 해당 줄만 강조했다.
- 오늘 요약은 일정 대표 3건과 나머지 개수만 남겨, 실행·완료용 긴 목록은 일정 포스트잇이 맡게 했다.

#### 검증과 배포
- 4단계 집중 검사 5개와 일정 편집·일정 관리·캘린더 캔버스·설정·포스트잇 회귀를
  합쳐 106개가 모두 통과했다.
- 실제 Windows 글꼴 렌더에서 지난 D-Day 2건 접기·목표 시각·전체 완료 자리,
  D-Day만 필터와 같은 날 3건 중 겹치는 2건 강조를 확인했다. 세 목록의 가로 스크롤은 0이었다.
- 임시 DB만 사용했고 실제 사용자 DB·기존 EXE는 변경하지 않았다. EXE는 생성하지 않았다.

### 2026-09-08 일정·D-Day 추가 업데이트 5단계 — 전체 회귀·Windows 배율 검증

#### 요청과 목표
- 1~4단계의 새 기능과 기존 메모·일정·알림·반복·재실행 기능을 함께 회귀 검증한다.
- 자정 경계, 날짜 연속 이동, 단축키 해제와 설정 복원을 보강하고
  Windows 100%·125%·150%와 680px 좁은 화면을 확인한다.

#### 처리
- `test_schedule_postit_stage2.py`에 자정을 걸치는 일정·23:59 항목·다음 날 00:00 제외,
  다중 이전/다음 이동 후 오늘 복귀, 새 창의 표시 설정·위치 복원 검사를 추가했다.
- `test_settings_dday_ux.py`에서 일정 포스트잇 단축키를 등록한 뒤 빈 값으로 저장하면
  저장값과 Windows 등록 목록에서 함께 해제되는 경로를 추가했다.
- Windows 렌더 도우미를 배율 인자와 실제 플랫폼, `WA_DontShowOnScreen`을 쓰도록 보강해
  사용자 화면을 가리지 않고 한글·레이아웃·DPI를 검사했다.
- `ui-ux-pro-max`의 접근성·상태를 색만으로 구분하지 않기·가로 스크롤 방지·
  레이아웃 변동 없는 상태 표시 기준으로 최종 UI를 점검했다.

#### 검증과 배포
- 전체 `test_*.py`: **660 passed in 229.55s**, 실패 0개.
- `python -m compileall -q .`: 통과.
- 실제 클릭은 `QTest.mouseClick`으로 날짜·보기·체크박스·폼 진입의 위젯 이벤트와
  연결 신호까지 검사했다. Computer Use 표면은 브라우저만 반환해 Windows 앱의
  OS 포인터 자동화는 사용할 수 없었다.
- Windows 100%·125%·150% 실제 플랫폼 렌더에서 포스트잇 9건은 8줄까지 보이고
  9번째부터만 세로 스크롤이 나타났다. 설정창은 내부 스크롤 없이 저장·취소가 보였다.
- 일정 폼에는 시작·종료, 할 일 폼에는 마감 하나와 `D-Day로 세기`가 보였고,
  680px 좁은 화면에서 편집 서랍 열림·가로 스크롤 최대값 0을 확인했다.
- 검사와 렌더는 전부 임시 DB만 사용했다. 실제 사용자 DB·LOCALAPPDATA·기존 EXE는
  변경하지 않았고 EXE를 생성하지 않았다.

### 2026-09-09 기존 DB 보존 폴더 변경·단일 EXE 생성

#### 요청과 목표
- 대상 폴더에 기존 DB가 있어도 다음 빈 번호 파일로 보존한 뒤 현재 DB를 복사하도록 한다.
- 변경을 포함한 콘솔 없는 단일 `TomaDesk.exe`를 만들고 실제 GUI 실행을 확인한다.

#### 처리
- `storage_config.py`에 다음 빈 번호 결정, 기존 표준 DB 이동, 현재 DB 복사와 실패 복구 경로를 반영했다.
- `main_window.py`의 데이터 폴더 변경 흐름에 새 이동 함수와 보존된 파일명 안내를 연결했다.
- 기존 메모 편집 변경, 실제 사용자 DB, 백업과 공통 라이브러리는 변경하지 않았다.

#### 검증과 배포
- `test_feature_updates.py`: **62 passed**, 전체 `test_*.py`: **676 passed in 243.13s**, `compileall`: 통과.
- `build_release.ps1 -Clean`으로 `release/TomaDesk.exe`를 생성했다. 크기는 **66,989,472 bytes**,
  SHA-256는 `4868393D2A5503CB8DA760C5B288274F3BAE0E6E92F855CBDCDD9C5BEF6A8751`이다.
- PE subsystem 2로 콘솔 없는 GUI 파일임을 확인했고, 아카이브의 `pyqt6_runtime_hook`과
  `PyQt6\\Qt6\\bin\\concrt140.dll` 포함을 확인했다.
- 단순 프로세스 유지만으로 판정하지 않고, 빈 임시 폴더의 EXE 복사본을 18초 실행해
  PyInstaller 부모·GUI 프로세스 2개, `TomaDesk` 메인 창 1개, 오류 창 0개를 확인했다.
- 첫 실행 검증이 `release/data`의 구버전 알림 DB를 자동 업그레이드하여, 즉시 자동 생성된
  업그레이드 직전 DB로 복원하고 `PRAGMA integrity_check=ok`와 복원 해시를 확인했다.
  이후 검증은 EXE를 빈 폴더로 복사해 `release/data`를 열지 않는 방식으로 수행했다.
- 검증용 프로세스와 임시 폴더는 모두 정리했고, `release/data`에는 기존 4개 파일만 남겼다.
- EXE 생성·실행 검증 완료. 미완료 항목은 없다.

### 2026-09-09 빈 토글 붙여넣기 범위·안내 줄 삭제 수정

#### 요청과 목표
- 빈 토글에 여러 문단을 붙여넣으면 토글 밖에 문단이 생기는 문제를 수정한다.
- 유일한 빈 안내 줄에서 더 이상 삭제할 수 없는 상태를 없앤다.

#### 처리
- `RichMemoTextEdit`이 빈 안내 줄에서 일반 텍스트·HTML·일반 텍스트 붙여넣기를 시작했는지 확인하고,
  새 문단의 최소 들여쓰기를 부모 토글보다 한 단계 깊게 보정하도록 했다.
- 빈 안내 줄의 Backspace/Delete는 안내 줄을 다시 만들지 않고 해당 토글을 일반 문단으로 되돌린다.
- 내용이 있는 토글, 중첩 토글, 저장 HTML, 접기/펼치기와 기존 EXE는 변경하지 않았다.

#### 검증과 배포
- `python -m py_compile alert_notes\\rich_memo_edit.py test_memo_toggle.py`: 통과.
- `test_memo_toggle.py` 38개, `test_memo_blocks_stage5.py` 60개, `test_memo_stage7.py` 35개,
  `test_memo_stage8.py` 54개, `test_memo_heading_preferences.py` 13개가 모두 통과했다.
- 실제 Windows Qt 640×360 창에서 빈 토글에 3문단을 붙여넣어 내부 자식 3개와 올바른 들여쓰기를 확인했다.
- EXE는 요청 범위에 없으므로 새로 생성하지 않았다.
- 이후 사용자 실제 재확인에서 붙여넣기 앞 빈 줄, 빈 줄 Backspace 시 내용 이탈,
  Ctrl+Z 단계 분리와 토글 밖 빈 줄 삭제 문제가 남은 것을 확인해 아래 후속 수정으로 보완했다.

### 2026-09-09 빈 토글 붙여넣기·삭제 후속 보완

#### 요청과 목표
- 토글에 붙여넣은 내용 앞의 불필요한 빈 줄을 제거하고, 빈 줄 삭제 뒤 내용이 토글 밖으로 나가지 않게 한다.
- Ctrl+Z 한 번으로 붙여넣기와 내부 들여쓰기 보정이 함께 되돌아가게 한다.
- 토글 밖에서 커서가 있는 빈 줄도 Backspace로 실제 삭제되게 한다.

#### 처리
- HTML 붙여넣기가 안내 줄 자리에 만든 첫 빈 문단만 제거하고 뒤의 문단·목록 서식은 유지했다.
- 붙여넣기와 토글 내부 들여쓰기 보정을 하나의 편집 트랜잭션으로 묶어 실행 취소 단계를 합쳤다.
- 내용 앞의 빈 토글 자식 줄은 그 줄만 삭제하고 나머지 내용을 토글 안에 유지한다.
- 빈 토글 안내 줄 직후의 바깥 빈 문단은 안내 줄과 합치지 않고 해당 바깥 문단만 삭제한다.

#### 검증과 배포
- 새 재현 검사 4개를 포함한 `test_memo_toggle.py`: **42 passed**.
- 인접 메모 회귀: `test_memo_blocks_stage5.py` 60개, `test_memo_stage7.py` 35개,
  `test_memo_stage8.py` 54개, `test_memo_heading_preferences.py` 13개가 모두 통과했다.
- 실제 Windows Qt 플랫폼에서도 `test_memo_toggle.py`: **42 passed**.
- `py_compile`과 `git diff --check`를 통과했다. 사용자 데이터·기존 EXE는 변경하지 않았고
  EXE는 새로 생성하지 않았다.

### 2026-09-09 구조 가져오기·전체 메모 백업복원

#### 요청과 목표
- GPT·Claude 응답과 Markdown·HTML·TXT를 제목·토글·목록·표·서식을 유지해 메모로 가져온다.
- 모든 메모·휴지통·계층·첨부·알림을 한 파일로 백업하고 기존 메모 병합 또는 전체 교체로 복원한다.

#### 처리
- 클립보드는 HTML→Markdown→일반 텍스트 순으로 해석하고, 단일 파일은 현재 커서에,
  복수 파일은 확인 후 각각 새 메모로 가져오도록 했다. 파일 끌어놓기도 같은 경로를 사용한다.
- 클립보드는 실제 제목 단계 중 상위 2단계를 토글로, 파일은 제목 유지로 가져오며
  `＋` 메뉴의 가져오기 탭에서 두 기본값을 각각 변경할 수 있게 했다.
- 외부 HTML의 실행 요소·이벤트·위험 URL·원격 이미지를 제거하고 10MB/파일·50개 제한과
  UTF-8 및 TXT의 CP949 대체 해석을 적용했다.
- ZIP 기반 `.tomamemo`에 일반·휴지통 메모, 계층·페이지 링크, 첨부, 메모 속성,
  D-Day·월간 규칙·일반/반복 알림과 이력을 기록하고 첨부 해시를 검증하도록 했다.
- 병합 복원은 새 ID를 발급해 부모·페이지·그림·알림 관계를 치환하고 제목을 `(2)`로 구분한다.
  충돌하는 복원 메모 단축키만 해제한다. 전체 교체는 사용자 일정·단축키 작업·앱 설정을 보존한다.
- 두 복원 방식 모두 직전 `.tomamemo` 안전 백업과 단일 트랜잭션 롤백을 적용하고,
  알림 일정 미러는 복원 트랜잭션에서 다시 생성한다.
- 기존 설정의 버튼은 `앱 전체 백업·복원`으로 이름만 명확히 하고 기존 JSON/구형 복원 동작은 유지했다.

#### 검증과 배포
- 신규 `test_memo_import_archive.py`: **12 passed**. 제목 단계, HTML 정리, details 토글,
  빈 토글 붙여넣기·한 단계 실행 취소, 파일 드롭, 백업 왕복, ID 치환, 손상 차단,
  단축키 충돌과 투영 실패 롤백을 확인했다.
- 전체 회귀 검사: **696 passed in 251.43s**, 실패 0개. `compileall`과 `git diff --check`도 통과했다.
- 실제 Windows Qt 플랫폼 집중 검사: **63 passed**. 1500×880 임시 DB 렌더에서
  `가져오기·백업` 메뉴 4개 항목과 가져오기 설정 3번째 탭이 잘림 없이 표시됨을 확인했다.
- Computer Use는 `Trusted RPC service is not configured: sky`로 연결되지 않아 실제 Windows Qt
  백엔드와 `WA_DontShowOnScreen` 렌더로 대체 검증했다.
- 실제 사용자 DB·기존 앱 전체 백업·기존 EXE는 변경하지 않았고 새 EXE도 생성하지 않았다.

### 2026-09-11 프로그램 안에서 읽는 사용 설명서

#### 요청과 목표
- 개발자 문서가 아니라 처음 쓰는 일반 사용자가 바로 이해할 수 있는 설명서를 만든다.
- 첨부 예시처럼 화면 캡처 위에 번호 마커를 얹고 아래에 번호별 설명을 둔다.
- 기능 나열이 아니라 "사용자가 실제로 어떻게 쓰는지"를 적는다.
- 설명서를 프로그램 안에 넣어 단추로 열고, 좌우로 넘기거나 상단 섹션으로 바로 간다.

#### 처리
- `tools/capture_manual_shots.py`를 추가해 임시 데이터 폴더에 예시 메모·일정·단축키를 채운 뒤
  실제 화면 9장(`assets/manual/*.png`)을 캡처한다. 사용자 DB는 건드리지 않는다.
- `user_manual_content.py`에 6개 섹션·11장 본문과 마커 좌표를 0~1 비율로 두었다.
  `{main_open}` 같은 자리는 설정에 저장된 실제 단축키로 치환한다.
- `user_manual.py`의 `UserManualDialog`는 섹션 단추, 이전/다음, ←→ 방향키, 장 번호를 제공하고
  `AnnotatedShot`이 그림 위에 번호 동그라미를, `MarkerNote`가 아래에 같은 번호의 설명을 그린다.
  포스트잇·빠른 메모처럼 작은 그림은 두 배까지 키워 동그라미가 단추를 덮지 않게 했다.
- 기존 `HelpDialog`(카드 7장)를 걷어내고 `도움말` 단추를 `설명서`로 바꿔 새 창을 연다.
- `tools/preview_manual.py`로 각 장을 PNG로 뽑아 마커 위치를 눈으로 맞췄다.

#### 검증과 배포
- 신규 `test_user_manual.py`: **9 passed**. 그림 파일 존재, 마커 좌표 범위, 번호와 설명 개수 일치,
  처음/끝 넘기기, 섹션 이동, 실제 단축키 치환과 자리표시자 잔존 없음을 확인했다.
- 전체 회귀 검사: **705 passed in 258.98s**, 실패 0개.
- `tools/preview_manual.py`로 11장 전부를 실제 렌더로 확인하고 마커가 글자를 가리던 자리를 조정했다.
- 사용자 DB·기존 앱 전체 백업·기존 EXE는 변경하지 않았고 새 EXE도 생성하지 않았다.

### 2026-09-11 메모 편집 Phase 1 클립보드·독립 복제

#### 계획 재검토
- Phase 0 결과는 기존 단계 순서나 데이터 원칙을 바꿀 사유가 없었다. 사용자가 확정한 깊은 페이지 복제,
  위험 기능 초기화, 앱 전용 클립보드, 문서·DB 복합 Undo/Redo를 Phase 1 상세 계약으로 확정했다.
- 잔류 내부 링크 결함을 Phase 1보다 먼저 처리했고 Phase 2 이후 기능은 구현하지 않았다.

#### 처리
- `toma-note://` 주소만으로 링크를 판정하지 않고, 연속 앵커의 표시 문자가 `📄` 또는 `🔗`로
  시작하는 경우에만 손가락 커서와 클릭 이동을 허용한다. 문서를 열 때 일반 본문에 남은 내부 앵커
  속성은 다른 문자 서식을 보존하면서 제거한다.
- `application/x-tomadesk-blocks+json`과 `application/x-tomadesk-notes+json` 버전형 MIME을 추가했다.
  앱 내부에서는 블록 메타데이터·페이지 트리·첨부를 보존하고 외부 앱에는 HTML과 일반 텍스트를 함께 제공한다.
- 페이지는 모든 하위 페이지와 첨부를 새 ID로 깊게 복제한다. `📄` 페이지 링크와 그림 ID는 새 ID로
  바꾸고 `🔗` 메모 링크는 기존 대상을 유지한다. 현재 또는 향후 존재하는 카테고리 열은 그대로 복제한다.
- 알림·D-Day·월간 반복·단축키·포스트잇 표시/시작·고정·휴지통·수동 정렬 상태는 초기화한다.
  알림 동시 복제 옵션은 후속 범위로 남겼다.
- 메모 목록 Ctrl+C/V는 체크된 여러 메모 또는 현재 메모를 같은 부모 아래 독립 복제하며,
  하위 계층을 유지하고 제목에 `- 복사본`과 중복 번호를 붙인다.
- 페이지/첨부를 포함한 본문 붙여넣기는 Qt 문서 변경과 DB 복제 배치를 한 번의 Ctrl+Z로 함께 제거하고,
  Ctrl+Y 또는 Ctrl+Shift+Z로 같은 ID와 내용을 복원한다. 목록 다중 복제도 한 배치로 Undo/Redo한다.
- Alt+C는 선택이 없을 때 문자 서식만 저장하고 선택이 있을 때 적용한다. 문단 유형·들여쓰기·링크 주소는 복사하지 않는다.

#### 검증과 배포
- 신규 `test_memo_phase1_clipboard.py`: **6 passed**. 잔류/정상 링크, 전용 MIME과 외부 대체값,
  블록 메타데이터, 페이지 2단계 깊은 복제, 참조 링크 유지, 첨부 ID 재발급, 위험 속성 초기화,
  본문 문서·DB Undo/Redo, 목록 다중 복제·Undo/Redo, Alt+C를 확인했다.
- 집중·인접 검사: **104 passed in 13.85s**, Undo 분기·우클릭 직접 검사 **6 passed in 1.34s**.
  최종 전체 회귀 검사: **728 passed in 253.71s**.
- 실제 Windows Qt 플랫폼 최종 집중 검사: **6 passed in 1.83s**. `compileall`과 `git diff --check`도 통과했다.
- 테스트는 임시 DB만 사용했다. 사용자 DB와 기존 EXE는 변경하지 않았고 새 EXE도 생성하지 않았다.

### 2026-09-12 메모 편집 Phase 2 다중 블록 선택·공통 명령

#### 승인 범위
- Phase 1의 구조 MIME과 복제 서비스를 재사용해 비연속 블록 선택, 공통 명령, 부동 도구막대,
  대상별 우클릭과 페이지·링크 하위 메뉴를 구현한다.
- Alt+클릭/드래그 추가 선택, Alt+위/아래와 부동 막대 버튼 이동, 기존 줄 손잡이 드래그를
  같은 명령 실행 경로로 연결한다. 일반 글자·표 셀 선택은 Alt를 누르기 전까지 유지한다.
- 이동·복제·변환·묶기·들여쓰기·삭제·연동 기능 삭제를 한 번의 Undo/Redo로 처리하고,
  Phase 3 이후 기능과 새 schema, EXE 생성은 하지 않는다.

#### 처리
- `BlockSelectionManager`, `BlockCommandDispatcher`, `BlockActionBar`를 별도 모듈로 나눴다.
  토글을 선택하면 하위 블록을 가족으로 포함하고, 떨어진 선택은 문서 순서로 정렬한다.
- 선택 개수와 위/아래·내어쓰기/들여쓰기·복사·해제·삭제를 담은 부동 막대를 추가했다.
  좁은 창에서도 막대가 편집 영역 안에 머물며 Esc로 선택과 막대를 함께 닫는다.
- 블록 우클릭에 이동·복사·독립 복제·묶기·삭제, 본문·제목1~4·토글·체크리스트·
  글머리표·강조 상자·인용문·코드 변환, `연동 기능 삭제`를 연결했다.
- 비연속 복사는 문서 순서·상대 들여쓰기·제목 메타데이터와 외부 HTML/텍스트를 함께 담는다.
  페이지·첨부 독립 복제는 Phase 1의 깊은 복제와 ID 재매핑을 그대로 사용한다.
- `연동 기능 삭제`는 토글·체크·목록·제목·상자·인용·코드·구분선·링크 동작을 걷어내되
  일반 글자와 색·굵기 등 문자 서식, 표·이미지 내용을 보존한다. 부분 링크 선택은 선택한 구간만 해제한다.
- 페이지 연결 해제는 페이지 메모와 부모 관계·내용을 보존하고 `embedded=0`으로 바꾼다.
  Ctrl+Z/Y에서 링크 줄과 `embedded` 상태를 함께 원복·재적용한다.
- 토글 변환 시 안내 자식 생성과 커서 복원을 같은 편집 단계에 묶어 기존 토글 도구 동작과
  한 번의 Undo를 동시에 유지했다.

#### 검증과 보존
- 신규 `test_memo_phase2_blocks.py`: **15 passed**. 비연속·역방향 선택, 토글 가족,
  일반 글자 선택 보존, 구조 MIME 순서, 마우스·키보드·버튼 이동, 모든 변환·묶기·들여쓰기·삭제,
  부분/전체 연동 해제, 페이지 DB Undo/Redo, 부동 막대와 대상별 메뉴를 확인했다.
- 최종 전체 회귀 검사: **743 passed in 258.48s**, 실패 0개.
- Windows 실제 Qt 백엔드에서 Phase 2와 기존 줄 끌기 **31 passed in 5.27s**.
- Windows 100%·125%·150%와 480px 좁은 창을 실제 렌더해 선택 강조, 부동 막대 위치,
  7개 버튼의 잘림이 없음을 확인했다. 결과는 `output/phase2_blocks_*.png`에 남겼다.
- Computer Use로 실제 검증 창을 찾았으나 연결이 네이티브 앱 표면을 반환하지 않아 물리 포인터 입력은
  수행하지 못했다. Qt Windows 마우스·키보드 이벤트와 실제 렌더 근거를 구분해 기록한다.
- `python -m compileall -q alert_notes test_memo_phase2_blocks.py`와 `git diff --check`를 통과했다.
  테스트는 임시 DB만 사용했고 사용자 DB·기존 백업·기존 EXE는 변경하지 않았으며 새 EXE도 생성하지 않았다.

### 2026-09-14 메모 편집 Phase 3 접기·편집·키보드 사용성

#### 처리
- 제목 단계가 같은 다음 제목 전까지 제목 하위 범위를 접는다. 중첩 제목과 들여쓰기 토글의 표시 상태를 함께 계산하며, 제목 화살표·우클릭·키보드로 접고 펼친다. 기존 `Ctrl+Shift+E`는 제목과 토글의 모두 접기/펴기에 연결했다.
- Qt 사용자 block property는 HTML에 직렬화되지 않는 것을 확인했다. 접힌 제목의 `▶ ` 표시 마커를 문서 내용에 보존해 재열기 후 복원하고, 일반 본문 전환 시 표시를 제거한다. 페이지 링크 줄은 해당 마커가 `📄` 판정을 깨지 않도록 제목 접기 대상에서 제외했다.
- 줄 간격 1.0/1.15/1.5/2.0을 일반 범위 또는 Phase 2의 비연속 선택 문단에 한 번의 Undo로 적용한다. 서로 다른 줄 간격을 선택하면 도구막대에 `혼합`을 표시한다. 값 선택 UI는 포커스와 무관하게 휠로 바뀌지 않도록 입력 보호기를 적용했다.
- 우클릭에 원본 서식(Ctrl+V), 현재 서식에 맞추기, 텍스트만(Ctrl+Shift+V)을 분리했다. 텍스트만·현재 서식 붙여넣기는 내부 링크 속성을 옮기지 않고 한 번의 Undo로 되돌린다.
- 설정 가능한 `Ctrl+Alt+Enter`로 유효한 페이지·메모 링크만 열고, `Ctrl+Alt+Space`로 현재 제목·토글을 접는다. `Alt+Right/Left`는 접힌 구조 진입·부모 구조 복귀를 제공한다. 설정 저장 시 알림·기존 편집 단축키와의 충돌을 검사한다.
- Windows Qt 넓은 1100px/좁은 760px 창을 `tools/preview_phase3_editing.py`로 렌더했다. 결과는 `output/phase3_preview_*.png`에 남겼다.

#### 검증과 보존
- 신규 `test_memo_phase3_editing.py`: **10 passed**. 제목 계층·토글 내 같은 단계 제목·HTML 재열기, 화살표 마우스 입력, 모두 접기 한 번 Undo, 줄 간격 혼합·비연속 선택·Undo·재열기, 세 붙여넣기 방식·Undo, 키보드 진입·복귀와 유효 링크 판정, 휠 차단과 단축키 충돌을 검사했다.
- Phase 3·인접 테스트: **97 passed**. 최종 전체 회귀: **753 passed in 280.99s**. 실제 Windows Qt 백엔드의 집중 검사도 **97 passed in 10.13s**. `compileall`과 `git diff --check`를 통과했다.
- Qt 이벤트 입력과 Windows 렌더는 확인했지만 물리 포인터를 이용한 실사용 검증은 미완료로 `3._TODO.md`에 남겼다. 테스트는 임시 DB만 사용했고 사용자 DB·기존 EXE는 변경하지 않았다. 새 EXE는 생성하지 않았다.
