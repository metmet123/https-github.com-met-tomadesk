# TomaDesk 프로그램 설명서

> 이 문서는 **파일 접근이 불가능한 대화형 AI에게 프로그램을 설명하기 위한 자료**입니다.
> 코드를 보지 않고도 구조·기능·상호작용을 파악할 수 있도록 작성했습니다.

---

## 1. 한 줄 요약

**Windows 데스크톱 상주 프로그램.** 전역 단축키로 자주 하는 작업을 즉시 실행하고, 메모·일정·알림·D-Day를 한 창에서 관리한다. Python + PyQt6로 만들었고, 데이터는 SQLite에 저장한다.

핵심 설계 의도는 **"손을 떼지 않는 것"** 이다. 다른 프로그램을 쓰는 중에도 단축키 하나로 작업이 실행되고, 알림은 화면 구석에서 스스로 말을 건다.

---

## 2. 전체 구조

프로그램은 **두 개의 큰 축**으로 나뉘고, 최상단 탭으로 전환한다. (`Ctrl+Tab`)

```
TomaDesk
├── [1] 단축키·반복작업   ← 자동화 축
└── [2] 메모·일정         ← 기록/일정 축
     ├── 메모 편집
     ├── 캘린더            (Ctrl+Shift+Tab 으로 순환)
     └── 알림내역
```

두 축은 **전역 단축키**와 **알림**이라는 두 지점에서 서로 연결된다. (→ 6장)

---

## 3. 축 1 — 단축키·반복작업

### 3.1 하는 일

작업 하나에 단축키 하나를 붙여두고, 어느 프로그램에 있든 그 키를 누르면 실행된다.

| 작업 유형 | 동작 |
|---|---|
| **문구 입력** | 자주 쓰는 문장을 현재 커서 위치에 타이핑. '입력 후 Enter'를 켜면 전송·검색까지 이어짐 |
| **사이트 열기** | 지정한 URL을 브라우저로 염 |
| **프로그램/폴더 열기** | 실행 파일이나 폴더 경로를 염 |
| **반복작업(매크로)** | 마우스 클릭·드래그·휠·키 입력·대기 시간을 **녹화해서 재생** |

### 3.2 화면

좌우 2단 구성이다.

- **왼쪽 — 작업 목록:** 표 형태. 열은 `선택 / ID / 활성 / 이름 / 단축키 / 작업 / 등록 상태`. 각 행에 ON·OFF 토글 스위치가 있어 단축키를 지우지 않고 잠시 끌 수 있다. 상단에 상태·유형 필터와 검색창.
- **오른쪽 — 작업 편집:** 이름, 활성 체크, 작업 유형 선택, 단축키 지정, 유형별 입력 영역(문구 / URL / 경로 / 매크로 단계).

### 3.3 UX 상 중요한 판단들

- **단축키는 "누르는 대로" 잡는다.** 조합 키 드롭다운도 있지만, 입력란에 원하는 조합을 실제로 누르면 그대로 등록된다.
- **등록 실패를 숨기지 않는다.** 다른 프로그램이 이미 점유한 조합은 등록에 실패하는데, 헤더에 `등록 30개 · 실패 3개`로 표시하고 **실패가 있을 때만** '실패 상세'·'다시 등록' 버튼 줄이 나타난다. 실패가 0이면 그 줄 자체가 사라진다.
- **여러 개를 고르면 그때 도구줄이 나타난다.** 체크박스로 행을 선택해야 '선택 삭제' 같은 버튼이 보인다. 평소에는 없는 버튼이라 오작동 여지가 줄어든다.
- **반복작업에는 항상 탈출구가 있다.** 매크로는 실제 클릭·입력을 수행하므로 위험하다. 그래서 기본 `Ctrl+Alt+Esc`로 **실행 즉시 중지**가 가능하고, 저장 전에 테스트 실행을 권한다. 녹화 종료 키(`Ctrl+Alt+F12`)와 중지 키는 설정에서 바꿀 수 있다.
- **제외 프로그램 지정:** 특정 앱에서는 단축키가 동작하지 않도록 제외 목록을 둔다. (게임·원격 접속 등에서 키를 뺏지 않기 위함)

---

## 4. 축 2 — 메모·일정

### 4.1 메모 편집 (3단 구성)

```
┌──────────────┬────────────────────┬──────────────┐
│  메모 목록    │     메모 편집       │  오늘 요약    │
│  (표 + 검색)  │  (서식 있는 편집기)  │  (D-Day/일정) │
└──────────────┴────────────────────┴──────────────┘
```

**왼쪽 — 메모 목록.** 열은 `선택 / 번호 / 표시 / 제목 / 알림·D-Day / 수정 시간 / 내용 미리보기`. 각 헤더 너비는 사용자가 직접 끌어 조절할 수 있고, 구분선을 최소까지 좁혀도 헤더 글자가 잘리지 않는다(글꼴 폭을 재서 열별 최소 너비를 계산한다).

**가운데 — 메모 편집.** 서식 있는 텍스트 편집기다.
- 글꼴·크기·색상, **B / I / U / S**(굵게·기울임·밑줄·취소선), 글머리 기호, 표, 이미지 삽입
- 서식 프리셋 3개 저장(`서식 1·2·3`) — 자주 쓰는 서식을 한 번에 적용
- 메모별 배경색 5종, 투명도 지정
- 자동 저장 옵션
- 하단에 **알림 예약 / D-Day / 매달 반복** 버튼

**오른쪽 — 오늘 요약.** 원래 빈 여백이던 자리다. 창을 넓혀도 회색 공간만 늘어나서, 지금은 세 가지를 보여준다.
1. **D-Day** — 체크박스 목록. 급한 정도를 색으로 구분(오늘·지남=빨강, 이번 주=주황, 그 외=회색)
2. **오늘 일정**
3. **예정 알림**

### 4.2 포스트잇

메모를 **화면에 항상 떠 있는 쪽지**로 띄울 수 있다. 항상 위 고정, 투명도, 시작 시 자동 표시를 각각 지정한다. 본문에 `☐`로 체크리스트를 쓰면 클릭으로 `☑`가 된다.

### 4.3 캘린더

**일간 / 주간 / 월간 / 목록** 4개 보기. 상단 세그먼트 버튼으로 전환하고, 보기마다 조작 방법 안내 문구가 바뀐다.

- **월간:** 날짜 칸에 일정 칩과 D-Day 칩이 뜬다(D-Day는 테두리형이라 구분됨). **빈 날짜를 더블클릭하면 그 날짜로 새 일정 편집기가 열린다.** 토요일 파랑·일요일 빨강.
- **일간/주간:** 시간축 타임라인.
- **목록:** 다가오는 일정을 순서대로.
- 사이드바에 **미니 달력**이 있어 먼 날짜로 바로 이동한다. 카테고리 범례(업무·개인·중요·학습·기타)가 실제 색과 함께 표시된다.

**일정 편집기**의 설계 원칙:
- **날짜·시간을 키보드로 직접 친다.** `20260910`, `2026-09-11`, `930`, `1500` 모두 인식하고 붙여넣기도 된다.
- **시작을 옮기면 종료가 간격을 유지하며 따라온다.** 종료가 시작보다 앞서면 자동 교정한다. (예전에는 종료를 반드시 지정해야만 저장돼서 막혔다)
- **종일** 체크 시 시간 입력이 사라지고 하루 전체로 저장된다.
- 반복 일정(매일·매주·매월 등)과 카테고리를 지정한다.

### 4.4 알림내역

지나간 알림과 예정된 알림의 기록. 여기서 다시 열거나 재알림할 수 있다.

---

## 5. D-Day — 알림과 다른 개념

원래 D-Day는 알림 기능과 구분이 안 됐다. 지금은 **성격이 다른 두 가지**로 분리했다.

| | 알림 | D-Day |
|---|---|---|
| 목적 | **그 시각에 한 번** 알려줌 | **남은 날짜를 계속 보여줌** |
| 끝나면 | 사라짐 | **사라지지 않음** — 사용자가 직접 끝낼 때까지 |
| 표시 | 알림 창 | 상단 칩 · 오늘 요약 · 목록 배지 · 트레이 |

### 5.1 지난 D-Day가 사라지지 않는 이유

업무 마감 관리가 주 용도라서다. 마감이 지났다는 사실이야말로 가장 중요한 정보인데, 날짜가 지났다고 목록에서 지워버리면 **처리 안 한 일이 조용히 없어진다.** 그래서 `D+2`처럼 지난 일수를 계속 세어 보여주고, 사용자가 체크박스로 **'카운트 끝내기'** 를 해야 비로소 완료 처리된다.

> **"체크박스를 달면 할 일 목록과 똑같아지지 않나?"**
> 다르다. 할 일은 **끝내는 게 목적**이라 체크하면 사라진다. D-Day는 **남은 날짜를 세는 게 목적**이고, 체크는 "이제 그만 세도 된다"는 뜻이다. 끝낸 뒤에도(설정에 따라) 취소선으로 남는다.

### 5.2 표시 위치 — 네 군데

1. **상단 바 칩** — 가장 임박한 것 하나. 클릭하면 전체 요약이 열린다.
2. **오늘 요약 패널** — 체크박스가 달린 목록. 급한 순.
3. **메모 목록 배지** — `▪ D-3`(D-Day) / `9/2 09:09 🔔`(알림) / `✓ D+1`(끝낸 것)으로 한눈에 구분된다.
4. **트레이 아이콘 툴팁** — 창을 닫아둬도 보인다.

### 5.3 당일에 잊지 않게 하는 방법

D-Day 당일 **그날 처음 프로그램을 켤 때 트레이 알림으로 한 번** 알린다. 하루에 한 번만이라 반복해서 성가시지 않다.

---

## 6. 알림 — 토마펫

알림이 오면 **화면 구석에 캐릭터('토마펫')가 말풍선과 함께 나타난다.** 모달 창으로 화면을 막지 않으므로 하던 일을 계속할 수 있다.

말풍선에서 바로 할 수 있는 것: **확인 / 빠른 재알림(스누즈) / 메모편집 / 일정편집**. 즉 알림이 **작업의 입구** 역할을 한다 — 알림을 받고 그 자리에서 해당 메모나 일정으로 들어간다.

토마펫은 알림 때만 뜨게 하거나 **상시 표시**할 수 있고, 알림에 펫 대신 일반 창을 쓰도록 끌 수도 있다.

---

## 7. 매달 반복 업무 — 포스트잇 체크리스트

매달 돌아오는 업무를 위한 기능이다.

**동작:** 지정한 날짜가 되면 그 메모가 자동으로 포스트잇으로 뜨고, **체크가 전부 초기화된다.** 그 달에 한 번만 뜬다. 며칠 늦게 프로그램을 켜도 놓치지 않는다(기준일이 이미 지났으면 그때 뜬다).

**반복 기준일 3가지:**
| 방식 | 설명 |
|---|---|
| **매달 n일** | 달력에서 날짜를 고른다. 31일로 지정하면 30일까지인 달은 말일로 처리 |
| **매달 말일** | 달의 길이와 윤년을 정확히 따른다 |
| **n째 주 n요일** | 첫째~넷째 + **마지막** 주 지원 (예: 셋째 주 화요일) |

대화상자에 **"다음 표시 2026-09-15"** 미리보기가 실시간으로 나온다.

### 7.1 프로그램의 추천

같은 제목의 일정을 **같은 날짜에 3개월 이상** 반복해서 만들면, 요약 패널 위에 한 줄 제안이 뜬다.

> ‘월간 보고서’를(를) 4개월째 매달 10일에 하고 계세요.
> 매달 체크리스트 포스트잇으로 띄울까요?  `괜찮아요` `포스트잇 만들기`

**만들기**를 누르면 그 제목의 메모와 반복 규칙이 만들어진다. **거절하면 그 제목은 다시 묻지 않는다**(껐다 켜도 기억한다).

> **자동으로 만들지 않는 이유:** 프로그램이 멋대로 만든 포스트잇은 사용자가 "이게 왜 생겼지"를 알 수 없다. 항상 물어보고, 거절하면 물러난다.

---

## 8. 기능 간 연결

이 프로그램의 특징은 **기능이 서로 물려 있다**는 점이다.

```
        전역 단축키
             │
   ┌─────────┼──────────┐
   │         │          │
 작업 실행  빠른 메모   메모/일정 열기
             │
             ▼
           메모 ─────┬──→ 포스트잇 (화면 상주)
             │       ├──→ 알림 예약 ──→ 토마펫 말풍선 ──→ 다시 메모/일정으로
             │       ├──→ D-Day ──→ 상단 칩 · 요약 · 목록 배지 · 트레이
             │       └──→ 매달 반복 ──→ 매달 포스트잇 자동 표시
             │
           일정 ─────┬──→ 캘린더 4개 보기
                     ├──→ 알림 ──→ 토마펫
                     └──→ 반복 패턴 감지 ──→ 매달 반복 추천
```

구체적인 연결 지점:

- **메모 하나에 단축키를 붙일 수 있다.** 축 1의 단축키 체계를 메모가 그대로 쓴다. 누르면 그 메모가 열리거나 포스트잇으로 뜬다.
- **전역 단축키로 곧장 들어간다.** `Ctrl+Alt+N` 빠른 메모(작은 입력창만 뜸), `Ctrl+Alt+Shift+N` 새 메모, `Ctrl+Alt+C` 오늘 일정, `Ctrl+Alt+M` 메모 검색.
- **알림이 편집 화면의 입구다.** 말풍선의 '메모편집'·'일정편집' 버튼이 해당 항목으로 데려간다.
- **일정 사용 패턴이 메모 기능을 제안한다.** 캘린더에 반복해서 만든 일정을 읽어 매달 반복 포스트잇을 추천한다(7.1).
- **D-Day는 메모에 붙는 속성**이다. 별도 항목이 아니라 메모가 D-Day를 갖는 구조라, 마감과 그 마감에 관한 메모 내용이 한 덩어리로 움직인다.
- **하루 전 알림은 D-Day와 한 몸이다.** '하루 전에 미리 알림'을 켜면 알림이 하나 더 생기지만, D-Day를 지우면 둘 다 함께 사라진다.

---

## 9. UI·UX 디자인 원칙

실제로 프로그램을 조작하며 문제를 찾아 고친 결과 정리된 원칙들이다.

**1. 빈 것은 보여주지 않는다.**
실패가 0이면 실패 안내줄이 사라지고, 선택된 행이 없으면 삭제 버튼이 없고, 알림이 없으면 배너가 없다. 화면에 남아 있는 요소는 전부 지금 의미가 있는 것이다.

**2. 창 크기에 따라 배치가 바뀐다.**
좁아지면 가로 배치가 세로로 접히고, 표는 최소 6행을 확보하되 화면의 62%를 넘지 않는다. 좁은 창에서 표가 화면을 다 먹거나 입력란이 겹치는 문제를 막는다.

**3. 거리를 색으로 읽는다.**
D-Day는 멀면 회색, 이번 주면 주황, 오늘이거나 지났으면 빨강. 글자를 읽기 전에 색으로 먼저 파악된다.

**4. 단축키를 화면에 적어둔다.**
`Ctrl+Tab 전환`, `Ctrl+Shift+Tab 전환`이 탭 옆에 그대로 표시된다. 단축키가 있어도 모르면 없는 것과 같다.

**5. 애매한 것은 설정으로 넘긴다.**
사람마다 갈릴 판단은 프로그램이 정하지 않는다. 예: *끝낸 D-Day를 숨길지 / 하루 전 알림을 받을지 / 당일 아침에 알릴지 / 상단 바에 칩을 띄울지* — 각각 설정에서 켜고 끈다.

**6. 되돌릴 수 있게 한다.**
삭제는 휴지통으로 가고, 매크로는 언제든 중지되고, 전체 백업·복원이 있다.

**7. 파괴적인 동작은 확인을 받는다.**
편집 중 다른 항목으로 넘어가면 저장 여부를 묻는다.

---

## 10. 데이터와 저장

- **SQLite 2개** — 단축키 작업용, 메모/일정/알림용.
- **저장 위치:** 프로그램과 같은 경로의 `data/` 폴더. 사용자가 직접 열어볼 수 있는 자리다. 설정에서 '폴더 열기'로 바로 갈 수 있다.
- **쓰기 권한이 없으면**(예: Program Files에 설치) `%LOCALAPPDATA%`로 자동 대피한다. 단, **권한 문제일 때만** 그렇게 하고 DB가 깨진 경우에는 하지 않는다 — 조용히 빈 DB로 갈아타면 데이터가 사라진 것처럼 보이기 때문이다.
- **백업:** 전체 백업/복원을 JSON 묶음으로 한다. 예전 버전 백업도 복원된다(없는 항목은 기본값으로 채운다).
- **Excel 내보내기/가져오기** 지원.

---

## 11. 창 동작

- **트레이 상주.** 닫아도 종료되지 않고 트레이로 들어간다. 시작 시 트레이에서 시작하도록 설정할 수 있다.
- **중복 실행 방지.** 이미 실행 중이면 안내하고 새로 뜨지 않는다.
- **전역 단축키:** 메인창 열기 `Ctrl+Alt+F10`, 트레이로 숨기기 `Ctrl+Alt+F11`, 프로그램 종료 `Ctrl+Alt+F9`, 매크로 긴급 중지 `Ctrl+Alt+Esc`. 전부 설정에서 변경 가능하며, 충돌하면 그 자리에서 경고한다.

---

## 12. 기술 스택

| 항목 | 내용 |
|---|---|
| 언어·UI | Python 3.12 / PyQt6 |
| 저장소 | SQLite (ALTER TABLE 기반 스키마 마이그레이션) |
| 스타일 | 단일 QSS 스타일시트 + 동적 속성(급한 정도 등)으로 상태 표현 |
| 플랫폼 | Windows (전역 단축키·입력 자동화에 Win32 API 사용) |
| 테스트 | pytest (오프스크린 Qt) — 약 190건 |

