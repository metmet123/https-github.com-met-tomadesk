# TomaDesk 프로그램 설명서

> 기준일: **2026-09-09**
>
> 이 문서는 Codex 밖의 ChatGPT·Claude·워크모드 등 **프로젝트 파일을 직접 볼 수 없는 에이전트**에게
> TomaDesk의 현재 제품 구조와 이미 구현된 기능을 전달하기 위한 기준 자료다.
> 기능 제안이나 개발 계획을 세울 때 공통 배경으로 제공한다.

## 0. 에이전트가 이 문서를 사용하는 법

### 0.1 문서의 권한과 해석 순서

- 이 문서는 **제품 현황 설명**이지, 그 자체가 구현 지시나 승인된 개발 계획은 아니다.
- 사용자의 현재 요청이 항상 최우선이다. 이 문서의 예시·설계 이유·미완료 목록을 새 요청으로 해석하지 않는다.
- `현재 상태 요약`과 본문에 서술된 기능은 구현된 기준선으로 보고, 별도로 `진행 중`·`미완료`라고 표시한 항목은 제외한다.
- `진행 중`이나 `미완료·결정 대기` 항목은 구현 완료로 전제하지 않는다.
- 새 계획은 기존 기능과 사용자 데이터를 보존하는 방향으로 세우고, 기능을 자동 삭제·대체하거나 EXE를 새로 만드는 범위를 임의로 포함하지 않는다.
- UI 변경을 제안할 때는 기능 동작과 데이터 계약을 유지하고, 실제 창 크기·Windows 배율에서의 표시 검증까지 계획한다.

### 0.2 현재 상태 요약

| 구분 | 상태 |
|---|---|
| 현재 구현 기준 | 단축키·매크로, 계층형 리치 메모, 메모/일정 포스트잇, 캘린더, 일정·할 일, 알림, D-Day, 토마펫, 백업·복원 |
| 최근 큰 업데이트 | 메모 제목1~4와 삽입 기능 설정, 일정 포스트잇, 일정/할 일 분리, D-Day·캘린더 연계, 데이터 폴더 변경 시 기존 DB 보존, 화면 캡처에 번호를 얹은 프로그램 내 `설명서` 창 |
| 진행 중인 별도 작업 | GPT·Claude 응답과 Markdown·HTML·TXT를 메모 구조로 가져오기 + 모든 메모를 `.tomamemo` 한 파일로 백업·복원 |
| 진행 중 작업의 제외 범위 | RTF·DOCX·PDF·JSON·URL·OCR, 외부 이미지 다운로드, EXE 생성 |
| 최근 전체 회귀 기준 | 데이터 폴더 변경 반영 후 **676개 통과**. 이후 토글 붙여넣기·삭제 보완은 관련 42개와 인접 메모 162개를 통과했으나 전체 676개를 다시 돌렸다는 기록은 없음 |
| 배포 상태 | 2026-09-09 단일 `TomaDesk.exe`의 실제 메인 창·오류 창 없음까지 확인. 그 뒤 반영된 토글 붙여넣기·삭제 보완은 EXE로 다시 만들지 않았으므로 **현재 소스가 마지막 EXE보다 새로움** |

진행 중 작업이나 배포물의 최신 상태는 대화 시점에 달라질 수 있다. 외부 에이전트가 실제 개발 계획을 확정할 때는 사용자에게 현재 상태를 다시 확인하거나 최신 프로젝트 기록을 제공받아야 한다.

---

## 1. 한 줄 요약

**Windows 데스크톱 상주 업무 도구.** 전역 단축키로 자주 하는 작업을 즉시 실행하고, 계층형 메모·일정·할 일·알림·D-Day를 한 창에서 관리한다. Python + PyQt6로 만들었고 데이터는 SQLite에 저장한다.

핵심 설계 의도는 **“하던 작업에서 손을 떼지 않는 것”**이다. 다른 프로그램을 쓰는 중에도 단축키 하나로 작업을 실행하고, 메모를 빠르게 남기며, 알림은 화면을 막지 않고 다음 행동으로 연결한다.

---

## 2. 전체 구조

프로그램은 두 개의 큰 축으로 나뉘고 최상단 작업공간 탭으로 전환한다. 기본 전환 키는 `Ctrl+Tab`이다.

```text
TomaDesk
├── [1] 단축키·반복작업          ← 실행·자동화 축
└── [2] 메모·일정                ← 기록·일정 축
     ├── 메모 편집
     ├── 캘린더                  ← 하위 화면은 Ctrl+Shift+Tab으로 순환
     └── 알림내역

별도 상주 창
├── 메모 포스트잇
├── 일정 포스트잇
└── 토마펫 알림
```

두 축은 전역 단축키와 알림을 통해 연결된다. 메모·일정에서 만든 정보를 다시 포스트잇, D-Day, 알림, 토마펫으로 꺼내 쓴다.

---

## 3. 축 1 — 단축키·반복작업

### 3.1 작업 유형

| 작업 유형 | 동작 |
|---|---|
| **문구 입력** | 자주 쓰는 문장을 현재 커서 위치에 입력. `입력 후 Enter`를 켜면 전송·검색까지 이어짐 |
| **사이트 열기** | 지정한 URL을 브라우저로 엶 |
| **프로그램/폴더 열기** | 실행 파일이나 폴더 경로를 엶 |
| **반복작업(매크로)** | 마우스 클릭·드래그·휠·키 입력·대기 시간을 녹화해 재생 |

### 3.2 화면과 사용 흐름

화면은 좌우 2단 구성이다.

- **왼쪽 — 작업 목록:** 선택, 상태 점, 이름, 단축키, 작업 내용, 등록 상태를 보여준다. 내부 ID 열은 공간 절약을 위해 숨겨져 있다. 상태·유형 필터와 검색을 제공한다.
- **오른쪽 — 작업 편집:** 이름, 활성 여부, 작업 유형, 단축키와 유형별 세부값을 편집한다.
- 여러 행을 선택했을 때만 일괄 동작이 나타난다. 삭제는 새 작업 옆의 명확한 위치에서 수행한다.
- 등록 실패가 있을 때만 실패 안내와 다시 등록 동작을 보여준다.

### 3.3 단축키와 안전장치

- 입력란에서 원하는 조합을 실제로 누르면 그 조합을 잡아 등록한다.
- 일반 단축키는 낮은 수준 키보드 갈고리를 우선 사용해 다른 프로그램이 잡은 조합도 받을 수 있게 했다.
- `Ctrl+C`, `Ctrl+V`처럼 Windows 전체에서 막으면 위험한 조합과 운영체제 전용 조합은 가로채지 않고 기존 Windows 등록 방식을 사용한다.
- 갈고리를 사용할 수 없는 환경에서는 기존 등록 방식으로 되돌아간다.
- 실제 사용자 키보드에서 다른 프로그램과 충돌하는 조합을 먼저 받는지는 최종 수동 확인 항목으로 남아 있다.
- 반복작업은 기본 `Ctrl+Alt+Esc`로 즉시 중지할 수 있다. 녹화 종료 키와 실행 중지 키는 설정에서 바꿀 수 있다.
- 특정 프로그램에서는 작업 단축키가 동작하지 않도록 제외 앱을 지정할 수 있다.
- 매크로는 저장 전에 시험 실행할 수 있고 편집 내용의 실행 취소·다시 실행·원본 복원을 제공한다.

---

## 4. 축 2 — 메모 편집

### 4.1 기본 화면

```text
┌──────────────┬────────────────────────┬──────────────┐
│  메모 목록    │       메모 편집         │  오늘 요약    │
│ 계층·검색·최근 │ 제목·본문·서식·삽입 기능 │ D-Day·일정·알림│
└──────────────┴────────────────────────┴──────────────┘
```

- **왼쪽 — 메모 목록:** 부모·하위 메모가 접히는 트리다. 선택, 표시, 제목, 알림·D-Day, 수정 시간, 미리보기를 보여주며 검색할 때는 접힌 부모를 자동으로 펼친다.
- 최근에 본 메모 3개를 검색창 아래 칩으로 보여주며, 현재 메모·본문 속 페이지·삭제된 메모는 제외한다.
- 목록은 `Ctrl+\`로 접고 펼칠 수 있고, 전체 트리는 `Ctrl+Shift+A`로 모두 접기·펼치기를 할 수 있다.
- **가운데 — 메모 편집:** 제목과 리치 텍스트 본문, 서식 도구, 삽입 기능, 알림·D-Day·월간 반복 설정을 제공한다.
- **오른쪽 — 오늘 요약:** 지난 D-Day, 오늘 일정의 대표 항목, 예정 알림을 보여주고 해당 항목으로 이동한다.

### 4.2 계층·페이지·링크

- 메모를 부모·하위 구조로 정리할 수 있다. 부모를 휴지통으로 보내면 하위 메모도 함께 안전 삭제 경로를 따른다.
- 본문에서 **페이지**를 만들면 하위 메모가 생긴다. 페이지 제목은 본문 줄에서 바로 바뀌고, 그 페이지 줄을 지우면 연결된 페이지 메모도 휴지통으로 간다.
- **메모 링크**는 이미 존재하는 메모를 가리킨다. 링크를 지워도 대상 메모는 남는다.
- 페이지와 메모 링크는 순환 참조를 막고, 본문 줄이나 목록에서 해당 메모로 이동할 수 있다.
- 줄 손잡이로 본문 줄을 옮기고 토글이나 페이지 안으로 넣을 수 있다. 그림은 손잡이로 크기를 조절할 수 있다.
- `F11`은 메모 편집 전체 화면이다.

### 4.3 서식과 구조 블록

제목 아래 칩 줄 맨 왼쪽의 **서식 ▾**를 누르면 글꼴·크기·줄 간격·색상과 **굵게 / 기울임 / 밑줄 / 취소선** 도구가 두 줄로 열린다. 알림·D-Day·단축키·그 밖의 속성과 서식 패널은 한 번에 하나만 열린다.

자주 쓰는 문자 서식은 `서식 1·2·3`으로 저장한다. 메인 서식 패널의 26px `가` 샘플은 저장된 글꼴·색·효과를 보여 주며 클릭하면 적용된다. 빈 점선 `+` 슬롯을 클릭하거나 샘플을 우클릭하면 **현재 서식 저장 / 이름 바꾸기 / 초기화** 메뉴가 열린다. 오른쪽 톱니에서는 세 슬롯 관리와 편집 단축키 설정을 연다.

본문에서 일반 글자 범위를 선택하면 B·I·U·S와 24px 프리셋 3개가 가까이에 나타난다. 오른쪽 공간이 부족하면 도구가 본문 아래 고정 줄로 이동하며, 선택 창과 메인 서식 패널의 프리셋 적용 상태는 함께 바뀐다. 다중 글자 구간이나 블록을 선택했을 때는 해당 선택 전용 도구를 사용한다.

문서 구조용 항목은 다음과 같다.

| 항목 | 핵심 동작 |
|---|---|
| **제목1~4 / 본문** | 22·18·15·13pt의 단계형 제목. 선택한 여러 줄에도 적용하며 제목에서 Enter를 누르면 일반 본문으로 돌아감 |
| **토글** | 하위 여러 줄을 접고 펼침. 빈 안내 줄에 여러 문단을 붙여도 토글 내부에 유지하고 한 번의 실행 취소로 되돌림 |
| **체크리스트** | 네모를 클릭하거나 해당 줄에서 Space로 완료 표시 |
| **글머리표** | 일반 목록 |
| **강조 상자** | 노란 배경의 강조 한 줄 |
| **구분선** | 문단 사이의 가로선 |
| **인용문** | 왼쪽 세로선과 기울임 표시 |
| **코드 줄** | 고정폭 글씨와 회색 배경. 여러 줄을 이어 쓸 수 있음 |
| **표** | 기본 머리줄 1개·3칸으로 시작. Tab으로 이동하고 마지막 칸에서 Tab을 누르면 줄 추가 |
| **페이지 / 메모 링크** | 하위 메모를 만들거나 기존 메모를 연결 |

표는 줄·칸 추가까지 구현되어 있다. **줄·칸 삭제와 칸 너비 조절은 아직 없다.**

### 4.4 삽입 기능을 부르는 세 가지 방법

메인 편집기와 포스트잇은 같은 삽입 기능 목록을 공유한다.

1. 편집기의 `＋` 단추
2. 본문에서 `/`를 입력해 여는 검색 메뉴
3. 기능별 단축키 또는 줄 맨 앞 입력

주요 줄 앞 입력은 `# `~`#### ` 제목, `> ` 토글, `[] ` 체크리스트, `- ` 글머리표, `! ` 강조 상자, `---` 구분선, `" ` 인용문, `` ``` `` 코드 줄, `[[` 메모 링크다.

`＋` 패널은 **기능 / 설정** 탭으로 구성된다. 사용자는 삽입 항목의 순서를 길게 눌러 끌거나 `위로`·`아래로` 단추로 바꾸고 기본 순서로 되돌릴 수 있다. 줄 앞 입력도 바꿀 수 있으며 빈 값·중복·접두 충돌은 즉시 막는다. 바뀐 설정은 열린 메인 편집기·독립 편집기·포스트잇에 바로 반영되고 다음 실행에도 유지된다.

### 4.5 찾기·자동 저장·성능

- 본문 찾기는 `Ctrl+F`, 다음/이전은 Enter·Shift+Enter 또는 F3·Shift+F3다. 접힌 토글 안의 결과를 찾으면 자동으로 펼친다.
- 제목이나 본문을 입력하면 약 400ms 뒤 자동 저장한다. 본문만 입력한 새 메모는 기본 제목 `새 메모`를 쓴다. 빈 입력이나 옵션만 바꾼 상태는 새 레코드를 만들지 않는다.
- 긴 메모는 보이는 영역 주변만 다시 그리도록 최적화했다. 기록된 기준으로 3000줄에서 한 글자 입력 처리가 21.7ms에서 0.6ms로 줄었다.

### 4.6 메모 포스트잇

메모를 화면 위에 항상 떠 있는 쪽지로 띄울 수 있다. 항상 위, 투명도, 시작 시 자동 표시를 각각 지정한다. 포스트잇에서도 같은 리치 편집기와 삽입 기능을 사용하며 체크리스트·토글·제목 등 주요 본문 동작을 공유한다.

---

## 5. 일정·할 일·캘린더

### 5.1 캘린더 보기

**일간 / 주간 / 월간 / 목록** 네 가지 보기를 제공한다.

- **월간:** 날짜 칸에 일정·할 일과 D-Day가 표시된다. 빈 날짜를 더블클릭하면 그 날짜로 새 일정 편집기가 열린다. 토요일은 파랑, 일요일은 빨강이다.
- **일간/주간:** 시간축에서 일정과 할 일을 본다.
- **목록:** 다가오는 항목을 순서대로 본다.
- 미니 달력으로 먼 날짜로 바로 이동한다.
- 업무·개인·중요·학습·기타 카테고리를 색과 문자로 구분한다.
- **일정 / 할 일 / D-Day** 필터를 각각 저장하며, `D-Day만` 단추로 D-Day에 집중할 수 있다.

### 5.2 일정과 할 일은 입력 구조가 다르다

캘린더 머리말에 `＋ 새 일정`과 `＋ 새 할 일`을 따로 둔다.

| 종류 | 입력 구조 |
|---|---|
| **일정** | 시작과 종료, 종일 여부, 반복, 카테고리, 알림 |
| **할 일** | 마감 하나, 반복, 카테고리, 알림, `D-Day로 세기` |

- 날짜·시간은 `20260910`, `2026-09-11`, `930`, `1500`처럼 직접 입력하거나 붙여넣을 수 있다.
- 일정 시작을 옮기면 종료가 기존 간격을 유지해 따라오고, 종료가 시작보다 앞서면 자동 교정한다.
- 종일을 켜면 시간 입력이 사라지고 하루 전체로 저장된다.
- 빠른 일정 입력 팝오버는 유지하며 전체 편집으로 넘어가도 일정/할 일 종류를 보존한다.
- 저장된 종류 값이 비정상이면 임의로 다른 종류로 바꾸지 않고 사용자에게 설명한다.
- 편집 중 같은 날의 다른 항목을 보여주고 실제 시간이 겹치는 줄만 `겹침` 문자와 붉은 배경으로 강조한다.

### 5.3 일정 포스트잇

일정 포스트잇은 **실행과 완료에 집중하는 별도 상주 창**이다.

- 이전날 / 오늘 / 다음날로 이동한다.
- 당일 / 이번 주 / 임박순 / 우선도순 보기를 제공한다.
- 일정과 할 일을 기본 표시하며 표시 대상, 보기, 위치, 최대 줄 수, 완료 방식, 선택 단축키를 설정에 저장한다.
- 기본 최대 표시 수는 8줄이며 9번째부터 세로 스크롤이 나타난다.
- 반복 항목은 선택한 회차만 완료한다. 단발 항목의 삭제형 완료는 기존 휴지통 경로를 사용한다.
- 단축키 기본값은 비어 있으며 사용자가 지정했을 때만 등록한다. 설정에서 다시 비우면 저장값과 Windows 등록을 함께 해제한다.

오늘 요약은 오늘의 전체 상황과 이동을 위한 짧은 요약이고, 일정 포스트잇은 긴 실행 목록이다. 같은 목록을 두 군데에 중복 표시하지 않도록 오늘 요약은 대표 3건과 나머지 개수만 보여준다.

---

## 6. D-Day — 알림과 다른 개념

| | 알림 | D-Day |
|---|---|---|
| 목적 | 지정한 시각에 알려 줌 | 목표일까지 남은 날이나 지난 날을 계속 셈 |
| 종료 | 확인·재알림 등으로 처리 | 사용자가 `카운트 끝내기` 또는 완료를 해야 종료 |
| 주요 표시 | 토마펫·일반 알림·알림내역 | 상단 칩·오늘 요약·메모 목록·캘린더·트레이 |

### 6.1 지난 D-Day를 유지하는 이유

업무 마감이 지났다는 사실을 자동으로 감추지 않기 위해서다. 지난 D-Day는 `D+2`처럼 계속 세고, 사용자가 끝낼 때까지 남는다. 오늘 요약 맨 위의 접이식 **지난 D-Day** 묶음은 목표 날짜·시각을 함께 표시하며 `전체 완료`도 제공한다.

할 일의 완료와 D-Day 카운트 종료는 목적이 다르다. 할 일은 실행 여부를 관리하고, D-Day는 날짜와의 거리를 관리한다. 할 일에서 `D-Day로 세기`를 선택하면 캘린더와 D-Day 표시 체계에도 함께 나타난다.

### 6.2 표시 위치

1. **상단 바 칩:** 가장 임박한 D-Day 하나. 클릭하면 해당 메모로 이동한다.
2. **오늘 요약:** 지난 D-Day와 급한 D-Day를 정리하고 완료할 수 있다.
3. **메모 목록 배지:** D-Day, 알림, 끝낸 D-Day를 서로 다른 표기로 구분한다.
4. **캘린더:** 메모 D-Day와 `D-Day로 세기`를 켠 할 일을 기간·월간·목록에 표시한다.
5. **트레이 툴팁과 당일 알림:** 창을 닫아도 가장 중요한 상태를 확인한다.

D-Day 당일에는 그날 처음 프로그램을 켤 때 트레이 알림으로 한 번 알릴 수 있다. 하루 전 알림, 당일 알림, 끝낸 D-Day 표시, 상단 칩 표시는 각각 설정에서 조정한다.

---

## 7. 알림과 토마펫

알림이 오면 화면 구석에 캐릭터 **토마펫**이 말풍선과 함께 나타난다. 모달 창으로 작업을 막지 않는다.

말풍선에서 **확인 / 빠른 재알림 / 메모 편집 / 일정 편집**으로 이어진다. 알림은 단순 통지가 아니라 관련 기록으로 들어가는 입구다.

토마펫은 알림 때만 띄우거나 상시 표시할 수 있고, 펫 대신 일반 알림창을 사용하도록 끌 수도 있다. 알림내역에서는 지난 알림과 예정 알림을 보고 다시 열거나 재알림한다.

---

## 8. 매달 반복 업무 — 포스트잇 체크리스트

지정한 기준일이 되면 메모를 포스트잇으로 띄우고 체크 상태를 초기화한다. 그 달에 한 번만 실행하며, 프로그램을 며칠 늦게 켜도 기준일이 지났다면 놓치지 않고 표시한다.

| 반복 방식 | 설명 |
|---|---|
| **매달 n일** | 없는 날짜는 그 달의 말일로 처리 |
| **매달 말일** | 월 길이와 윤년을 반영 |
| **n째 주 n요일** | 첫째~넷째와 마지막 주 지원 |

대화상자는 다음 표시 날짜를 미리 보여준다.

같은 제목의 일정을 같은 날짜에 3개월 이상 반복해서 만들면 매달 체크리스트 포스트잇으로 바꿀지 제안할 수 있다. 자동 생성하지 않고 먼저 묻고, 거절한 제목은 다시 묻지 않는다. 제품 자동화의 기본 방향은 **감지 → 제안 → 사용자 선택**이다.

---

## 9. 기능 간 연결

```text
전역 단축키
├── 저장 작업 실행
├── 빠른 메모·새 메모·메모 검색
├── 오늘 일정·빠른 일정
└── 일정 포스트잇 열기

메모
├── 부모·하위 메모 / 페이지 / 메모 링크
├── 메모 포스트잇
├── 알림 → 토마펫 → 해당 메모 편집
├── D-Day → 칩·요약·목록·캘린더·트레이
└── 매달 반복 → 체크 초기화된 포스트잇

일정·할 일
├── 캘린더 네 보기
├── 일정 포스트잇 → 실행·회차 완료
├── 알림 → 토마펫 → 해당 일정 편집
├── 할 일의 D-Day 연계
└── 반복 패턴 감지 → 매달 반복 메모 제안
```

기능 연결은 제목 문자열이 아니라 가능한 경우 안정적인 내부 ID로 유지한다. 새 계획에서 메모·일정·알림의 관계를 추가한다면 이름이 바뀌어도 끊어지지 않는 ID 기반 연결과 삭제·복원 규칙을 함께 설계해야 한다.

---

## 10. UI·UX 설계 원칙

1. **지금 의미가 없는 것은 감춘다.** 실패가 없으면 실패 안내를, 선택이 없으면 일괄 도구를 보여주지 않는다.
2. **좁은 창에서도 핵심 정보를 지킨다.** 가로 배치를 세로로 접고, 열과 패널의 최소 폭을 계산하며 가로 스크롤을 피한다.
3. **상태를 색만으로 구분하지 않는다.** D-Day 색과 함께 `D-3`, `D+2`, 문자 배지, 취소선 같은 표기를 사용한다.
4. **단축키를 화면에서 발견할 수 있게 한다.** 기능 옆에 키와 사용법을 표시한다.
5. **사람마다 다른 판단은 설정으로 둔다.** D-Day 표시, 알림 시점, 포스트잇 표시 대상을 사용자가 고른다.
6. **드래그에는 대체 조작을 제공한다.** 삽입 기능 순서처럼 끌어 옮기는 UI에는 `위로`·`아래로` 단추도 둔다.
7. **모든 값 선택 UI는 마우스 휠 오변경을 막는다.** 포커스 여부와 관계없이 콤보·숫자 선택값이 휠로 바뀌지 않게 한다.
8. **되돌리기와 안전 삭제를 우선한다.** 메모·일정은 휴지통과 백업을 거치고, 매크로는 중지·원본 복원이 가능하다.
9. **파괴적이거나 애매한 동작은 확인한다.** 편집 전환, 복원, 자동 제안은 사용자의 선택을 거친다.
10. **실제 Windows 표시를 검증한다.** UI 계획은 자동 테스트만이 아니라 100%·125%·150% 배율과 좁은 창 렌더를 포함한다.

---

## 11. 데이터·저장·복원

- 핵심 DB는 단축키용 `hotkeys.db`와 메모·일정·알림용 `alert_notes.db`다.
- 기본값은 프로그램 폴더의 `data/`이지만, 설정에서 데이터 폴더를 바꿀 수 있다. 선택한 경로는 `%LOCALAPPDATA%\CodexShortcutLauncher\storage_paths.json`에 기록한다. 제품명은 TomaDesk지만 이 저장 식별자는 기존 사용자 호환을 위해 유지한다.
- 기본 경로에 쓸 수 없을 때는 `%LOCALAPPDATA%`의 사용자 저장소를 사용한다. DB 손상을 쓰기 권한 문제로 오인해 빈 DB로 조용히 바꾸지 않는다.
- 데이터 폴더를 바꿀 때 대상 폴더에 같은 이름의 DB가 있으면 덮어쓰지 않는다. `hotkeys2.db`, `hotkeys3.db`처럼 다음 빈 번호로 먼저 보존한 뒤 현재 DB를 복사한다.
- 복사나 설정 저장이 실패하면 새 파일을 정리하고 보존한 DB를 원래 이름으로 돌리는 복구 경로가 있다.
- 앱 전체 JSON 백업·복원과 Excel 내보내기·가져오기를 제공하며, 구형 백업의 없는 필드는 안전한 기본값으로 채운다.
- 현재 진행 중인 `.tomamemo` 메모 전용 백업·복원은 완료 기록을 확인하기 전까지 현행 기능으로 간주하지 않는다.

---

## 12. 창·설정·실행

- 닫기 단추는 기본적으로 프로그램을 종료하지 않고 트레이로 보낸다.
- 시작 시 트레이에서 시작하도록 설정할 수 있다.
- 중복 실행을 막고 이미 실행 중이면 안내한다.
- 기본 공통 단축키는 메인 창 `Ctrl+Alt+F10`, 트레이 숨기기 `Ctrl+Alt+F11`, 종료 `Ctrl+Alt+F9`, 매크로 긴급 중지 `Ctrl+Alt+Esc`다. 사용자가 바꿀 수 있고 충돌이나 예약 조합은 즉시 알린다.
- 설정창은 비모달이라 띄워 둔 채 메모 검색이나 빠른 메모를 사용할 수 있다.
- 설정은 일반, 단축키·반복작업, 메모·알림, 일정·D-Day의 범주 중심으로 나뉜다.

---

## 13. 기술·배포 기준

| 항목 | 내용 |
|---|---|
| 언어·UI | Python 3.12 / PyQt6 |
| 저장소 | SQLite, 기존 DB를 보존하는 점진적 스키마 마이그레이션 |
| 플랫폼 | Windows, 전역 단축키와 입력 자동화에 Win32 API 사용 |
| 실행 진입점 | `A_shortcut_launcher.py` |
| 주요 UI | `main_window.py`, `alert_notes/` 모듈 |
| 패키징 | PyInstaller 단일 EXE, 콘솔 미표시 |
| 검증 원칙 | 임시 DB·격리된 LOCALAPPDATA, pytest, compileall, Windows 실제 플랫폼 렌더, 필요 시 실제 EXE 메인 창·오류창 검사 |

EXE는 사용자가 명시적으로 요청할 때만 만든다. 프로세스가 살아 있다는 사실만으로 성공으로 보지 않고 실제 `TomaDesk` 메인 창과 오류 대화상자 유무를 확인해야 한다. 실제 사용자 DB, 선택 저장 경로, 기존 EXE는 테스트 대상으로 사용하지 않는다.

---

## 14. 계획 수립 시 보존해야 할 경계

외부 에이전트가 TomaDesk 개선 계획을 제안할 때 다음을 기본 전제로 삼는다.

- 기존 정상 기능, 사용자 DB, `%LOCALAPPDATA%` 설정, 기존 EXE와 과거 기록을 임의로 변경·삭제하지 않는다.
- 메모·일정·알림·D-Day의 기존 의미와 구형 백업 호환을 유지한다.
- 예시 파일이나 한 화면만 보고 모든 사용 사례를 하드코딩하지 않는다. 일반 규칙을 만들고 예외는 사용자에게 확인한다.
- 일정과 할 일, 알림과 D-Day, 메모 페이지와 단순 메모 링크처럼 이미 분리된 개념을 다시 합치지 않는다.
- 기능 간 자동화는 멋대로 실행하기보다 **감지 → 제안 → 미리보기 또는 설명 → 사용자 승인** 흐름을 우선한다.
- UI 변경은 코딩 전에 시안과 범위를 합의하고, 구현 뒤에는 자동 검사와 실제 Windows 표시를 함께 확인한다.
- 요청 범위를 넘어 공통 라이브러리 추가, 데이터 이전, EXE 생성, 배포를 계획에 몰래 포함하지 않는다.

---

## 15. 미완료·결정 대기 항목

다음은 현재 기능으로 약속하면 안 되는 항목이다.

- 메모 목록 머리글 정렬과 사용자 지정 순서의 초기화 방식
- 첫째 줄 전체 서식 도구 배치 확대 여부, 900px 두 칸 전환 기준과 440px 왼쪽 칸 기준 조정
- 백링크, 그림 용량 관리, `Alt+↑/↓` 줄 이동, 추가 실행 취소 묶기
- 표의 줄·칸 삭제와 칸 너비 조절
- 자연어 일정 입력, 일정 템플릿, 주간 통계의 우선순위
- 기존 일정 가장자리 드래그 시간 조절
- 실제 키보드 충돌 환경, 실제 마우스 일정 조작감, 다중 모니터·배율 환경의 최종 수동 확인
- 진행 중인 구조 가져오기와 `.tomamemo` 메모 전용 백업·복원의 완료 확인

새 계획은 이 목록을 참고하되, 사용자가 요청하거나 우선순위를 승인한 항목만 범위에 넣는다.
